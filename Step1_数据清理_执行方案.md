# Step 1：S1 数据清理

> 来源文档：`硬编码清理_完整方案.md`
> 前置依赖：无
> 解锁：所有阶段

---

## 做什么

清掉所有硬编码的假数据，让 App 从零状态启动。

## 具体任务

| # | 任务 | 说明 |
|---|------|------|
| 1.1 | 芯片计数动态化 | L328 `3待处理` → `0待处理`（从实际数据统计） |
| 1.2 | 种子任务清理 | `resetAllData` 中 9 条种子任务 → 空 `{}` |
| 1.3 | 种子用户清理 | 删除 `_seedIfEmpty` 中的 4 用户种子数据 |
| 1.4 | `PUB_USERS` 动态化 | 从硬编码名单 → `Object.keys(getUsers())` |
| 1.5 | 空状态处理 | `renderLoginUserList` 无用户时显示空状态，不回退到 demo |

## 验证

首次打开 App，无任何预设数据，注册第一个用户后正常使用。
