# 南塘云村 · 三步修复方案 v2（审查修正版）

> 2026-07-21 · 修正了审查指出的所有问题

---

## 第一步：修复 NT 系统 Bug（6 处）

### 1.1 服务端 spend —— 去掉 scope 判断

**文件：** `server/routes/nt.py`，约 170-173 行  
**问题：** scope≠"personal" 时 NT 不返回池子，直接消失。  
**改法：**

找：
```python
    if req.scope == "personal":
        pool = await _get_pool(db)
        pool.balance += req.amount
```
改成：
```python
    pool = await _get_pool(db)
    pool.balance += req.amount
```

---

### 1.2 服务端 verify_task —— assignee/poster 不存在时保护

**文件：** `server/routes/nt.py`，约 306-312 行（approve），约 326-332 行（reject）

**approve 路径** —— 找：
```python
        if assignee:
            assignee.nt_balance += task.reward
            ...
        pool.task_escrow -= task.reward
```
改成：
```python
        if not assignee:
            raise HTTPException(status_code=400, detail="任务执行者不存在")
        assignee.nt_balance += task.reward
        ...
        pool.task_escrow -= task.reward
```

**reject 路径** —— 找：
```python
        if poster:
            poster.nt_balance += task.reward
            pool.task_escrow -= task.reward
```
改成：
```python
        if poster:
            poster.nt_balance += task.reward
        else:
            pool = await _get_pool(db)
            pool.balance += task.reward
        pool.task_escrow -= task.reward
```

---

### 1.3 服务端 tasks.py create_task —— 改用 _get_pool()

**文件：** `server/routes/tasks.py`  
**第 10 行**，已有 `from routes.nt import _ledger_id, _add_ledger, _adjust_trust`，追加 `_get_pool`：
```python
from routes.nt import _ledger_id, _add_ledger, _adjust_trust, _get_pool
```

**约 80-85 行**，找：
```python
    pool_result = await db.execute(select(CommunityPool).limit(1))
    pool = pool_result.scalar_one_or_none()
    if pool:
        pool.task_escrow += req.reward
```
改成：
```python
    pool = await _get_pool(db)
    pool.task_escrow += req.reward
```

---

### 1.4 服务端 tasks.py delete_task —— poster 不存在时保护 + 托管一致性

**文件：** `server/routes/tasks.py`，约 137-148 行

找：
```python
    if task.escrow_amount > 0 and task.status != "已结算":
        poster_result = await db.execute(select(User).where(User.id == task.poster))
        poster = poster_result.scalar_one_or_none()
        if poster:
            poster.nt_balance += task.escrow_amount
        pool_result = await db.execute(select(CommunityPool).limit(1))
        pool = pool_result.scalar_one_or_none()
        if pool:
            pool.task_escrow = max(0, pool.task_escrow - task.escrow_amount)
```
改成：
```python
    if task.escrow_amount > 0 and task.status != "已结算":
        poster_result = await db.execute(select(User).where(User.id == task.poster))
        poster = poster_result.scalar_one_or_none()
        pool = await _get_pool(db)
        if poster:
            poster.nt_balance += task.escrow_amount
        else:
            pool.balance += task.escrow_amount
        pool.task_escrow -= task.escrow_amount  # 不加 max(0,)，由调用方保证一致性
```
注意：去掉 `max(0, ...)`。如果 `task_escrow < escrow_amount`，让异常暴露出来（减法变负会被 Python 正常执行），同时在前端做日志记录。

---

### 1.5 客户端 NT —— 未知池子保护

**文件：** `nantang-mobile/js/nt-core.js`

**earnFromPool**（约 389 行后）加：
```javascript
  if (pool !== 'community' && (!pool || pool.indexOf('camp:') !== 0)) {
    console.error('[NT] earnFromPool: invalid pool', pool);
    return null;
  }
```

**spendToPool**（约 416 行后）加：
```javascript
  if (pool !== 'community' && (!pool || pool.indexOf('camp:') !== 0)) {
    console.error('[NT] spendToPool: invalid pool', pool);
    return null;
  }
```

---

### 1.6 客户端 NT —— 死代码标记

**文件：** `nantang-mobile/js/nt-core.js`

`deposit()` 函数体第一行加：
```javascript
  console.warn('[NT] deposit() deprecated, use topUp()');
  return null;
```

`withdraw()` 函数体第一行加：
```javascript
  console.warn('[NT] withdraw() deprecated, use cashOut()');
  return null;
```

---

## 第二步：删内联重复（300 个函数）

### 核心数据

```
内联函数总数：314
可删除（外部文件有同名的）：300
必须保留（仅内联有）：14
```

### 2.1 必须保留的 14 个内联函数

这些函数只在 `index.html` 内联脚本中存在，外部 JS 文件没有：

| 函数 | 原因 |
|------|------|
| `autoLogin` | 会话恢复，必须在内联脚本末尾执行 |
| `_closeInbox` / `closeInbox` | 内联 inbox 系统 |
| `_computeInboxMessages` | 同上 |
| `_filterArchiveCards` | 内联档案卡片 |
| `_isInboxUnread` / `_markInboxRead` | 内联 inbox |
| `_renderInbox` / `_renderMsg` | 内联 inbox 渲染 |
| `_setArchiveCardFilter` | 内联档案 |
| `_toggleInbox` | 内联 inbox 开关 |
| `_inboxJump` | 内联 inbox 跳转 |
| `renderArchiveCards` | 内联档案卡片渲染 |

> 注：`_inline_enterVillage` 是废弃的旧函数，一并删除。

### 2.2 可删除的 300 个函数（完整清单）

按行号排序。执行时从下往上删（避免行号偏移）：

```
NT_ICON _campMsgRow _canManage _cancelMeal _canteenDateLabel _canteenHasMenu 
_canteenShift _completeNewbieQuest _doSettle _ensureCampTaskClaimants _findInnBuilding 
_formatActivityItems _fundsIcon _fundsTypeLabel _genTxId _getCampMessages 
_getPendingReview _getPendingSubmissions _getUnpaidList _initNewbieQuests 
_innApplyId _innDaysBetween _innShiftDate _loadItems _markAllCampMsgRead 
_openCampInboxPanel _openDossierOverlay _openQuickMenu _openRejectSheet _orderMeal 
_parseActText _refreshFundsChips _renderActivityFeed _renderCanteenHistory 
_renderFundsChips _renderFundsSummary _renderFundsTransactions 
_renderInnDetailSheet _renderNTPoolStats _renderPersonalBlock 
_renderPersonalBlockBody _renderSection _saveCamp _saveItems _saveTxns _timeAgo 
_typeColor _unfreezeCV addBudgetItemMobile addCampTask addItem addJournal 
addMilestone addNewBuilder addSchedDay addSchedSlot applyQuickTemplate approveTx 
avatarImg avatarURL calcStep3Total campWizardNext campWizardPrev cancelCampWizard 
cancelInnApplication card catLedger changePwd claimTask clearPubForm closeAllExpands 
closeCanteen closeInn closeInnDetail closeInviteModal closeItemPopup closeMemberPicker 
closeMyPage closeOverlay closePeriod closeSchedEdit closeSub closeTaskEditSheet 
computeInboxMessages confirmReject confirmSettle confirmUnclaim confirmWithdraw 
copyPrevDaySched cycleAvatar deleteDraft doPublish doSubmit downloadMd editCampTask 
editDraft editItem editSchedCell editTask enterCamp esc expandCard exportCampReport 
fillSeedPool filterArchiveMembers filterPubTargets filterQuests filterReviewers 
findInnBed generateCampSchedule getCampData getCamps getFoodIcon getInboxLastOpened 
getInnApplications getMyBlocks getUserStay goWizardStep importTaskTemplate inboxMsgRow 
initActivityLog initArchiveBuilding initCarousel initDefaultBudgetItems initSpcCard 
isTaskOverdue itemAuction itemCancelAuction itemDelist itemRelist itemShelf 
keysSchedTemplates launchCamp loadAppData loadMoreArchiveLog loadSchedTemplate 
loadSchedTemplateData loadTaskTemplate logActivity logout mgmtApproveTask 
mgmtArchiveCamp mgmtConfirmSubmit mgmtRejectSubmit mgmtRejectTask mgmtRevealTask 
mgmtScrollToBlock mgmtSettleAll mgmtSettleOne myCard offsetDate onBuilderPicked 
onPubScopeChange onPubTypeChange openAdminConfig openArchive openCampHome openCampInbox 
openCampReport openCanteen openCommunityPage openCreateCamp openDossier openInboxPanel 
openInn openItemForm openItemPopup openLedger openMapPage openMyFromQuestHall 
openPublishTask openQuestHallPage openSub openSubmit openTaskEditSheet pickAvatar 
pickChip playCloseCeremony playCreateCeremony playLaunchCeremony profileSrc 
publishDraft publishTask questCard recordTransaction refreshLedgerCards refreshUserUI 
refreshVillageCards rejectTx removeBudgetItemMobile removeCampTask removeMilestone 
removeSchedDay removeSchedSlot renderArchiveLog renderArchiveMembers 
renderArchivePeriods renderCampFunds renderCampManage renderCampMembers 
renderCampMilestones renderCampOverview renderCampRanking renderCampReport 
renderCampSchedule renderCampSettle renderCampTab renderCampTimeline renderCanteen 
renderCommunityHub renderDrafts renderInbox renderInn renderItemsInTab 
renderMemberPicker renderMilestones renderMyTasks renderPage renderProfile 
renderSection renderSettleDetail renderStep1 renderStep2 renderStep3 renderStep4 
renderStep5 renderStep6 renderStep7 renderTaskCard renderTimeline renderWizardStep 
requestWithdraw resetAllData resolveMilestoneOffset reviewTask rm roleIcon roleName 
saveAdminConfig saveAppData saveDraft saveItemForm saveProfileEdits saveSchedEdit 
saveSchedTemplate saveTaskEdit saveTaskTemplate saveWizardStep scrollCampToToday 
scrollToSection selectBuilder selectMemberPicker selectPubTarget selectReviewer 
sendConfirmations sendEncouragement setCanteenMenu setCurrentUser settleTask 
showCampWindow showConfirm showInnDetail showInviteModal showMemberPicker showMy 
showRechargeForm showToast showVillage showWithdrawForm skipCeremony skipCloseCeremony 
submitInnApplication submitInviteCode submitRecharge submitWithdraw switchArchiveTab 
switchCampTab switchMyTab today toggleArchiveExpand toggleArchiveGroup 
toggleCampMemberDetail toggleCampMemberGroup toggleClaimantSub toggleInnBuilding 
toggleProfile toggleQuestCard toggleSection toggleSettleCalc toggleTaskStatus 
typedColor unclaimTask updateInboxBadge updateInboxLastOpened updateMyChips 
updateRegAvatar updateWizardButtons upgradeRole validateWizardStep withdrawTask
```

### 2.3 删除方法

**从 index.html 底部向上删**（从最后一个 `function` 开始往上删，避免行号偏移）。

每删除一个函数时：
1. 找到 `function xxx(` 开头
2. 找到对应的闭合 `}`（与 function 关键字同缩进级别）
3. 整个删除
4. **不留注释**——删干净

保留 autoLogin、hash 路由 wrapper、和 14 个 inbox/archive 函数。

### 2.4 需要从内联移到外部文件的内容

`window.Game.openCamp` 方法仅存在于内联脚本。加到 `core.js` 的 `window.Game = {` 对象中：
```javascript
openCamp: function(campId) { closeOverlay('overlayMap'); openCampHome(campId); },
```

### 2.5 删 body 顶部不必要的脚本

**文件：** `index.html` 第 14-25 行

最终保留的 body 顶部脚本：
```html
<script>
if (window.location.protocol !== 'file:') {
  document.documentElement.style.visibility = 'hidden';
  setTimeout(function(){
    var _lu = null; try { _lu = localStorage.getItem('nt_last_user'); } catch(e) {}
    if (_lu) { var _inp = document.getElementById('loginName'); if (_inp && !_inp.value) _inp.value = _lu; }
  }, 500);
}
</script>
```

去掉 `localStorage.removeItem(...)` 那一行（如存在）。

### 2.6 第二步验证

```bash
cd nantang-mobile

# 确认内联函数数量减少
grep -c "^function " index.html
# 预期：~15（14 保留 + hash wrapper 辅助函数）

# 确认所有外部 JS 语法正确
for f in js/*.js; do node -c "$f" 2>&1 || echo "FAIL: $f"; done
echo "ALL OK"
```

---

## 第三步：补全服务端连接

### 核心思路

不逐个文件添加 API 调用。在 `app-data.js` 的 `_saveShared()` 中加一个统一钩子。所有写操作最终都经过 `_saveShared()`，一处改动覆盖全部。

### 3.1 统一同步钩子

**文件：** `nantang-mobile/js/app-data.js`

在 `_saveShared` 函数末尾（`this._timerS = setTimeout(...)` 之前），添加：

```javascript
// 服务器同步：在 _saveShared 末尾统一推送
if (typeof API !== 'undefined' && API.token) {
  var self = this;
  var payload = {
    camps: this._data.camps,
    map_locations: this._data.map_locations,
    inventory: this._data.items,
    canteenMenu: this._data.canteenMenu
  };
  // 异步推送，不阻塞本地操作
  fetch('/api/data/sync_shared', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json', 'Authorization': 'Bearer ' + API.token },
    body: JSON.stringify(payload)
  }).catch(function(){});
}
```

### 3.2 服务端新增统一接收端点

**文件：** `server/routes/data.py`

```python
@router.post("/sync_shared")
async def sync_shared(req: dict, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    if not user: raise HTTPException(status_code=401)
    # 营地
    if req.get("camps"):
        for camp_id, camp_data in req["camps"].items():
            existing = (await db.execute(select(Camp).where(Camp.id == camp_id))).scalar_one_or_none()
            if not existing:
                db.add(Camp(id=camp_id, name=camp_data.get("name",""), created_by=user.id,
                           created_at=datetime.utcnow().isoformat()))
    # 地图
    if req.get("map_locations"):
        ml = (await db.execute(select(MapLocation).where(MapLocation.key == "shared"))).scalar_one_or_none()
        if not ml:
            ml = MapLocation(key="shared"); db.add(ml)
        ml.data = json.dumps(req["map_locations"], ensure_ascii=False)
    # 食堂菜单
    if req.get("canteenMenu"):
        for date, menu in req["canteenMenu"].items():
            existing = (await db.execute(select(CanteenMenu).where(CanteenMenu.date == date))).scalar_one_or_none()
            if not existing:
                db.add(CanteenMenu(date=date, lunch=json.dumps(menu.get("lunch",[]), ensure_ascii=False),
                                   dinner=json.dumps(menu.get("dinner",[]), ensure_ascii=False)))
    await db.commit()
    return {"ok": True}
```

### 3.3 删掉第三步未实现的内容

以下端点**不接**——没有实际需求、数据量太小、或通过 `sync_shared` 已覆盖：

- ❌ 公告独立端点（极少使用）
- ❌ 物品独立端点（数据量极小，通过 sync_shared 同步）
- ❌ 校核独立端点（校核流程是本地 -> 管理员验证 -> 本地，不需要服务器）
- ❌ 食堂菜单独立端点（sync_shared 已覆盖）
- ❌ 住宿订单端点（数据量极小）
- ❌ `/api/nt/tasks` 系列端点（已被 `/api/tasks` 覆盖，保留但不接前端）

### 3.4 第三步验证

```bash
cd server && source venv/Scripts/activate && python -c "from main import app; print('OK')"
cd nantang-mobile && node -c js/app-data.js && node -c js/api.js && echo "OK"
```

---

## 最终验证清单

- [ ] `python -c "from main import app"` 无报错
- [ ] 所有 JS `node -c` 通过
- [ ] `index.html` 内联函数 ≤ 20 个
- [ ] `start.bat` 启动无报错
- [ ] 手机注册 → 刷新 → 不弹回登录页
- [ ] 换手机登录同一账号 → 数据同步
- [ ] 退出 → 刷新 → 留在登录页
- [ ] `NT.verify()` 会计等式通过

---

## 文件变更汇总

| 文件 | 改动 | 行数变化 |
|------|------|:---:|
| `server/routes/nt.py` | 1.1, 1.2 修复 | +12 |
| `server/routes/tasks.py` | 1.3, 1.4 修复 | +8 |
| `server/routes/data.py` | 3.2 新增端点 | +25 |
| `nantang-mobile/js/nt-core.js` | 1.5, 1.6 保护 | +15 |
| `nantang-mobile/js/core.js` | 2.4 加 openCamp | +1 |
| `nantang-mobile/js/app-data.js` | 3.1 统一钩子 | +15 |
| `nantang-mobile/index.html` | 第二步：删 300 函数 | **-5500** |
| `nantang-mobile/index.html` | 2.5 精简 body 脚本 | 0 |

---

*方案 v2 完毕。所有步骤可独立验证，每个改动有明确文件和行号。*
