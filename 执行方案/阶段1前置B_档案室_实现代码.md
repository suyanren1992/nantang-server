# 阶段 1 前置B 实现代码：档案室

> 2026-07-19 · 按 `阶段1前置B_档案室_UI设计.md` 执行
> 涉及文件：`nantang-mobile.html`
> 前置：阶段 0（AppData + 身份系统）+ 阶段 1 前置A（信箱 + activity_log 写入）已完成

---

## 现状审计

| UI 清单项 | 描述 | 状态 |
|-----------|------|------|
| C1.1 | 大地书房建筑 + 档案室房间 | 不存在 |
| C1.2 | `#overlayArchive` overlay HTML | 不存在 |
| C1.3 | `renderMemberGroup()` 通用成员渲染函数 | 不存在（`renderCampMembers` 仅营地成员，不适用） |
| C1.4 | `renderArchiveLog()` 日志渲染函数 | 不存在 |
| C1.5 | `openArchive(tab)` 入口函数 | 不存在 |
| C1.6 | 大地书房 → 档案室点击处理 | 不存在（地图由外部 `app.js` 渲染，需通过 postMessage 桥接） |
| C1.7 | 社区主页底部「📚 社区档案室」链接 | 不存在 |
| C1.8 | 工作台 `toggleArchiveExpand()` 简化 + 跳转 | 部分存在（`toggleArchiveExpand` 有统计无跳转） |
| C1.9 | CSS — 档案室 overlay / Tab 栏 / 成员行 / 日志行 | 不存在 |

**数据源状态：**
- `getUsers()` — 外部函数（`js/auth.js` 或 `js/app-data.js`），全局可用，调用处均有 `typeof getUsers==='function'` 守卫
- `logActivity()` — L2953，写入 `AppData._data.activity_log`，`log.unshift()` 时间倒序
- `AppData._data.activity_log` — L2955，数组，元素 `{time:'ISO string', type:'task_post'|'tip'|..., text:'...'}`
- `AppData._data.map_locations.buildings` — 地图建筑数组，由外部 `app.js` 渲染
- `closeOverlay(id)` — L1303，移除 `.open` class
- 角色名称映射：`roleName(role)` — L752，`roleIcon(role)` — L751

---

## 改动 1：新增 overlay HTML（L529 之前插入）

**对应 UI 设计：** C1.2（§ 4.1 档案室 overlay）

**位置：** 在 `</div>` of `#overlayCreateCamp`（L528）之后、`<!-- 创营仪式动画层 -->`（L530）之前插入。

**新增：**

```html
<div id="overlayArchive" class="overlay">
  <div class="overlay-top">
    <button class="overlay-close" onclick="closeOverlay('overlayArchive')">←</button>
    <span class="overlay-title">📚 档案室</span>
  </div>
  <div class="archive-tabbar" id="archiveTabBar">
    <span class="archive-tab on" onclick="switchArchiveTab('members')">📂 成员</span>
    <span class="archive-tab" onclick="switchArchiveTab('log')">📜 日志</span>
  </div>
  <div class="overlay-body" id="archiveBody"></div>
</div>
```

> **注意：** 使用 `←` 返回（与营地主页一致），不遮盖标题。「个人案卷」「往期营期」Tab 为后续阶段预留，本阶段仅 2 个 Tab。

---

## 改动 2：新增 CSS（`</style>` 前插入）

**对应 UI 设计：** C1.9（§ 4.1-4.3 全部视觉样式）

**位置：** 在 `</style>` 标签之前（约 L308 附近）插入。

**新增：**

```css
/* ARCHIVE OVERLAY */
.archive-tabbar{display:flex;gap:4px;padding:8px 14px;overflow-x:auto;white-space:nowrap;-webkit-overflow-scrolling:touch;border-bottom:1px solid #d0d9ce;background:#f5f3ef}
.archive-tabbar::-webkit-scrollbar{display:none}
.archive-tab{padding:6px 14px;border-radius:16px;font-size:.7rem;cursor:pointer;color:#5a6e5c;flex-shrink:0;transition:.15s}
.archive-tab.on{background:#3d6b52;color:#fff;font-weight:600}
.archive-search{padding:10px 14px}
.archive-search input{display:block;width:100%;padding:10px 14px;border:1px solid var(--green-border);border-radius:10px;font-size:.78rem;background:#fff;color:#1d2e24;font-family:inherit}
.archive-search input:focus{outline:none;border-color:var(--green-primary)}
.archive-group-head{padding:8px 14px;font-size:.7rem;font-weight:700;color:#5a6e5c;cursor:pointer;display:flex;justify-content:space-between}
.archive-group-head:active{opacity:.7}
.archive-member-row{display:flex;align-items:center;gap:10px;padding:8px 14px;cursor:pointer;transition:.12s}
.archive-member-row:active{transform:scale(.98);background:#fafaf8}
.archive-member-avatar{width:32px;height:32px;border-radius:50%;object-fit:cover;flex-shrink:0;background:#e8ede6}
.archive-member-info{flex:1;min-width:0}
.archive-member-name{font-weight:600;font-size:.75rem;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}
.archive-member-sub{font-size:.6rem;color:#5a6e5c}
.archive-member-nt{font-weight:700;font-size:.7rem;color:var(--green-primary);white-space:nowrap}
.archive-member-dot{font-size:.55rem}
.archive-log-divider{text-align:center;font-size:.6rem;color:#8a8a8a;padding:10px 14px 4px}
.archive-log-row{display:flex;gap:8px;padding:8px 14px;font-size:.68rem;line-height:1.5;border-bottom:1px dotted #f0f0f0}
.archive-log-row:last-child{border-bottom:none}
.archive-log-icon{flex-shrink:0;width:22px;text-align:center;font-size:.85rem}
.archive-log-text{flex:1;color:#5a6e5c}
.archive-log-time{flex-shrink:0;color:#8a8a8a;font-size:.58rem;white-space:nowrap}
.archive-load-more{text-align:center;padding:12px;font-size:.65rem;color:var(--green-primary);cursor:pointer}
.archive-load-more:active{opacity:.7}
.archive-empty{text-align:center;padding:40px 20px;color:#5a6e5c;font-size:.7rem}
```

---

## 改动 3：新增 JS 函数（`toggleArchiveExpand` 之前插入）

**对应 UI 设计：** C1.3（renderMemberGroup）、C1.4（renderArchiveLog）、C1.5（openArchive）

**位置：** 在 `function toggleArchiveExpand(){`（L3117）之前插入。

**新增：**

```javascript
// ═══ 档案室（阶段 1 前置B）═══
var _archiveActiveTab = 'members';
var _archiveLogPage = 0;
var _archiveLogPageSize = 50;
var LOG_ICONS = {
  task_post:'📋', task_claim:'✋', review_approve:'✅', review_reject:'❌',
  tip:'💬', encourage:'💬', role_change:'🔄', camp_launch:'🏕️'
};

function openArchive(tab) {
  _archiveLogPage = 0;
  document.getElementById('overlayArchive').classList.add('open');
  switchArchiveTab(tab || 'members');
}

function switchArchiveTab(tab) {
  _archiveActiveTab = tab;
  var tabs = document.querySelectorAll('#archiveTabBar .archive-tab');
  for (var i = 0; i < tabs.length; i++) tabs[i].classList.remove('on');
  var target = document.querySelector('#archiveTabBar .archive-tab[onclick*="'+tab+'"]');
  if (target) target.classList.add('on');
  var body = document.getElementById('archiveBody');
  body.scrollTop = 0;
  if (tab === 'members') renderArchiveMembers(body);
  else if (tab === 'log') renderArchiveLog(body);
}

// ═══ C1.3 renderMemberGroup — 按角色分组 + 折叠展开 + 搜索过滤 ═══
function renderArchiveMembers(el) {
  var users = typeof getUsers === 'function' ? getUsers() : {};
  if (!Object.keys(users).length) { el.innerHTML = '<div class="archive-empty">📂 暂无注册成员</div>'; return; }

  var groups = [
    { label:'🧙 管理员', role:'admin', members:[] },
    { label:'🧱 共建者', role:'builder', members:[] },
    { label:'⚔️ 冒险者', role:'adventurer', members:[] },
    { label:'👥 在地伙伴', role:'npc', members:[] },
    { label:'🏕️ 云村民', role:'visitor', members:[] }
  ];

  Object.keys(users).forEach(function(name) {
    var u = users[name];
    var group = groups.find(function(g) { return g.role === u.role; }) || groups[4];
    // NT 余额从 NT 系统读取，fallback 0
    var nt = 0;
    try { var ntu = window.NT && NT.getUser(name); if (ntu) nt = ntu.ntBalance || 0; } catch(e) {}
    group.members.push({
      name: name,
      role: u.role,
      seed: u.avatar_seed || 0,
      nt: nt,
      online: u._online
    });
  });

  // 搜索框
  var h = '<div class="archive-search"><input type="text" placeholder="🔍 搜索姓名…" oninput="filterArchiveMembers()" id="archiveMemberSearch"></div>';

  groups.forEach(function(g, gi) {
    if (!g.members.length) return;
    // 默认展开第一个有人的分组
    var expanded = g.members.length > 0 && (gi === 0 || (gi === 1 && !groups[0].members.length));
    h += '<div class="archive-group" data-role="'+g.role+'">'+
      '<div class="archive-group-head" onclick="toggleArchiveGroup(this)">'+
        '<span>' + (expanded ? '▾' : '▸') + ' ' + g.label + ' · ' + g.members.length + '人</span>'+
      '</div>'+
      '<div class="archive-group-body" style="display:' + (expanded ? 'block' : 'none') + '">';

    g.members.forEach(function(m) {
      var avatarUrl = 'https://api.dicebear.com/7.x/avataaars/svg?seed=' + m.seed + '&size=56';
      var onlineDot = m.online ? '🟢' : '🔴';
      h += '<div class="archive-member-row" onclick="showToast(\'' + m.name + ' · ' + roleName(m.role) + ' · ' + (m.nt || 0) + ' NT\',\'\')">'+
        '<img src="' + avatarUrl + '" width="32" height="32" class="archive-member-avatar" alt="" onerror="this.outerHTML=\'<div style=width:32px;height:32px;border-radius:50%;background:#e8ede6;display:flex;align-items:center;justify-content:center;font-size:.65rem;color:#5a6e5c;flex-shrink:0>\'+(\'' + m.name + '\').charAt(0)+\'</div>\'">'+
        '<div class="archive-member-info"><div class="archive-member-name">' + m.name + '</div><div class="archive-member-sub">' + roleName(m.role) + '</div></div>'+
        '<span class="archive-member-nt">' + (m.nt ? m.nt + ' NT' : '') + '</span>'+
        '<span class="archive-member-dot">' + onlineDot + '</span>'+
      '</div>';
    });

    h += '</div></div>';
  });

  if (!h) h = '<div class="archive-empty">📂 暂无注册成员</div>';
  el.innerHTML = h;
}

function toggleArchiveGroup(el) {
  var body = el.nextElementSibling;
  var isVisible = body.style.display !== 'none';
  body.style.display = isVisible ? 'none' : 'block';
  el.innerHTML = '<span>' + (isVisible ? '▸' : '▾') + el.textContent.slice(2) + '</span>';
}

function filterArchiveMembers() {
  var kw = (document.getElementById('archiveMemberSearch').value || '').trim().toLowerCase();
  var groups = document.querySelectorAll('#archiveBody .archive-group');
  groups.forEach(function(g) {
    if (!kw) { g.style.display = ''; return; }
    var rows = g.querySelectorAll('.archive-member-row');
    var hasMatch = false;
    rows.forEach(function(r) {
      var name = (r.querySelector('.archive-member-name') || {}).textContent || '';
      var match = name.toLowerCase().indexOf(kw) !== -1;
      r.style.display = match ? '' : 'none';
      if (match) hasMatch = true;
    });
    g.style.display = hasMatch ? '' : 'none';
  });
}

// ═══ C1.4 renderArchiveLog — activity_log 时间倒序 + 图标映射 + 加载更多 ═══
function renderArchiveLog(el) {
  _archiveLogPage = 0;
  var log = (window.AppData && AppData._data.activity_log) ? AppData._data.activity_log : [];
  if (!log.length) { el.innerHTML = '<div class="archive-empty">📜 暂无运行日志<br><span style="font-size:.6rem;color:#aaa">社区活动将自动记录在这里</span></div>'; return; }

  var total = log.length;
  var pageSize = _archiveLogPageSize;
  var maxPage = Math.ceil(total / pageSize) - 1;
  var start = 0;
  var end = Math.min(pageSize, total);

  function renderPage() {
    var slice = log.slice(start, end);
    var h = '<div class="archive-log-divider">── 最近 ' + end + ' 条 ──</div>';

    slice.forEach(function(item) {
      var d = new Date(item.time);
      var dateStr = (d.getMonth()+1) + '/' + d.getDate();
      var timeStr = ('0'+d.getHours()).slice(-2) + ':' + ('0'+d.getMinutes()).slice(-2);
      var icon = LOG_ICONS[item.type] || '📝';
      h += '<div class="archive-log-row">'+
        '<span class="archive-log-icon">' + icon + '</span>'+
        '<span class="archive-log-text">' + item.text + '</span>'+
        '<span class="archive-log-time">' + dateStr + ' ' + timeStr + '</span>'+
      '</div>';
    });

    if (end < total) {
      h += '<div class="archive-load-more" onclick="loadMoreArchiveLog()">显示 ' + end + '/' + total + ' 条 · 加载更多</div>';
    }
    return h;
  }

  el.innerHTML = renderPage();

  // 闭包捕获当前状态，供 loadMoreArchiveLog 使用
  _archiveLogRenderState = { log: log, total: total, pageSize: pageSize, end: end, el: el };
}

var _archiveLogRenderState = null;

function loadMoreArchiveLog() {
  var s = _archiveLogRenderState; if (!s) return;
  s.end = Math.min(s.end + s.pageSize, s.total);
  var slice = s.log.slice(0, s.end);
  var h = '<div class="archive-log-divider">── 最近 ' + s.end + ' 条 ──</div>';
  slice.forEach(function(item) {
    var d = new Date(item.time);
    var dateStr = (d.getMonth()+1) + '/' + d.getDate();
    var timeStr = ('0'+d.getHours()).slice(-2) + ':' + ('0'+d.getMinutes()).slice(-2);
    var icon = LOG_ICONS[item.type] || '📝';
    h += '<div class="archive-log-row">'+
      '<span class="archive-log-icon">' + icon + '</span>'+
      '<span class="archive-log-text">' + item.text + '</span>'+
      '<span class="archive-log-time">' + dateStr + ' ' + timeStr + '</span>'+
    '</div>';
  });
  if (s.end < s.total) {
    h += '<div class="archive-load-more" onclick="loadMoreArchiveLog()">显示 ' + s.end + '/' + s.total + ' 条 · 加载更多</div>';
  }
  s.el.innerHTML = h;
}
```

---

## 改动 4：社区主页底部加档案室链接

**对应 UI 设计：** C1.7（§ 三 入口 2：社区主页底部 → 📚 社区档案室链接）

**位置：** `renderCommunityHub()` 函数（L1313），在 `el.innerHTML = h;`（L1348）之前插入。

**修改：** 在 `if (isAdmin) h += ...` 行之后，`el.innerHTML = h;` 之前加入：

```javascript
  // 阶段 1 前置B：社区档案室入口
  h += '<div style="text-align:center;padding:14px 0 4px;border-top:1px solid #e8ede6;margin-top:8px">'+
    '<span style="font-size:.72rem;color:var(--green-primary);cursor:pointer;font-weight:600" onclick="closeOverlay(\'overlayCommunity\');openArchive(\'members\')">📚 社区档案室 →</span>'+
    '<div style="font-size:.58rem;color:#8a8a8a;margin-top:2px">成员目录 · 运行日志</div>'+
  '</div>';
```

**完整上下文（L1339-L1348 修改后）：**

```javascript
  if (isAdmin) h += '<div class=world-terminal onclick="openCreateCamp()">🌍 世界终端 · 创建新的共创营队</div>';
  // 阶段 1 前置B：社区档案室入口
  h += '<div style="text-align:center;padding:14px 0 4px;border-top:1px solid #e8ede6;margin-top:8px">'+
    '<span style="font-size:.72rem;color:var(--green-primary);cursor:pointer;font-weight:600" onclick="closeOverlay(\'overlayCommunity\');openArchive(\'members\')">📚 社区档案室 →</span>'+
    '<div style="font-size:.58rem;color:#8a8a8a;margin-top:2px">成员目录 · 运行日志</div>'+
  '</div>';
  // FIX-24: 空状态
  if (!h) {
```

---

## 改动 5：工作台归档文件简化 + 跳转

**对应 UI 设计：** C1.8（§ 五 工作台「📚 归档文件」→ 点「打开档案室 →」进入档案室 overlay）

**位置：** `toggleArchiveExpand()` 函数（L3117-L3133），替换整个函数体。

**旧代码（L3117-L3133）：**

```javascript
function toggleArchiveExpand(){
  var ex=document.getElementById('archiveExpand');if(!ex)return;
  if(ex.style.display==='block'){ex.style.display='none';return}
  ex.style.display='block';
  var u=CURRENT_USER;var users=getUsers();var total=Object.keys(users).length;
  var taskCount=Object.keys(TASKS).length;var itemCount=MOCK_ITEMS.length;
  var ledgerRecords=window.NT?NT.getLedger({userId:CURRENT_USER}):[];
  var ledgerCount=ledgerRecords.length;
  var countEl=document.getElementById('archiveCountText');if(countEl)countEl.textContent='个人'+total+' · 社区0 · 共创营1 · 物品'+itemCount+' · 账目'+ledgerCount;
  ex.innerHTML='<div style="font-size:.72rem;color:#5a6e5c;line-height:1.8">'+
    '<div>👤 <b>个人档案</b> — '+total+' 位村民注册</div>'+
    '<div>🏘️ <b>社区记录</b> — 暂无归档</div>'+
    '<div>🏕️ <b>共创营</b> — 第四期进行中</div>'+
    '<div>📦 <b>物品</b> — '+itemCount+' 件登记</div>'+
    '<div>💰 <b>账目</b> — '+ledgerCount+' 笔流水</div>'+
    '</div>';
}
```

**改后：**

```javascript
function toggleArchiveExpand(){
  var ex=document.getElementById('archiveExpand');if(!ex)return;
  if(ex.style.display==='block'){ex.style.display='none';return}
  ex.style.display='block';
  var users=typeof getUsers==='function'?getUsers():{};
  var total=Object.keys(users).length;
  var taskCount=Object.keys(TASKS).length;
  var itemCount=MOCK_ITEMS.length;
  var logCount=(window.AppData&&AppData._data.activity_log)?AppData._data.activity_log.length:0;
  var countEl=document.getElementById('archiveCountText');if(countEl)countEl.textContent='登记'+total+'人 · 任务'+taskCount+' · 日志'+logCount;
  ex.innerHTML='<div style="font-size:.72rem;color:#5a6e5c;line-height:1.8;margin-bottom:10px">'+
    '<div>👤 <b>注册成员</b> — '+total+' 位</div>'+
    '<div>📋 <b>社区任务</b> — '+taskCount+' 个</div>'+
    '<div>📜 <b>运行日志</b> — '+logCount+' 条</div>'+
    '<div>📦 <b>物品</b> — '+itemCount+' 件登记</div>'+
    '</div>'+
    '<div style="text-align:center;padding:8px;background:var(--green-primary);color:#fff;border-radius:8px;font-size:.7rem;font-weight:600;cursor:pointer" onclick="closeMyPage();openArchive(\'log\')">📚 打开档案室 →</div>';
}
```

> **说明：** 点击「打开档案室 →」先关闭工作台（`closeMyPage()`），再打开档案室 overlay。保持单一顶层 overlay，不嵌套。

---

## 改动 6：地图 postMessage 桥接 — 档案室房间点击

**对应 UI 设计：** C1.1 + C1.6（§ 二 地图入口：大地书房 → 档案室房间 → 打开 overlay）

**背景：** 实景地图由外部文件 `../实景地图移动端代码/app.js` 渲染，建筑和房间数据由 `AppData._data.map_locations.buildings` 驱动。地图内部点击房间时通过 `postMessage` 发送事件给父页面。

**位置：** `window.addEventListener('message', ...)` 块（L4034 附近），在现有事件处理链中加入新分支。

**在 `else if(d.type==='closeMap')` 之后、`// NT 桥接` 之前插入：**

```javascript
  else if(d.type==='openArchive'){closeOverlay('overlayMap');openArchive(d.tab||'members')}
```

**完整上下文（修改后 L4047-L4050 附近）：**

```javascript
  else if(d.type==='closeMap'){closeOverlay('overlayMap')}
  // 阶段 1 前置B：地图建筑 → 档案室
  else if(d.type==='openArchive'){closeOverlay('overlayMap');openArchive(d.tab||'members')}
  // NT 桥接
  else if(d.type==='ntEarn'){...
```

### 6a. 初始化大地书房建筑（数据层）

**位置：** `AppData._seedIfEmpty()` 或在 `initActivityLog()` IIFE 附近（L3100）追加一个初始化块。或者更简单的方式——直接在 script 中追加一个自执行函数。

**在 `initActivityLog()` IIFE 之后（L3115 之后）插入：**

```javascript
// 阶段 1 前置B：确保大地书房（含档案室房间）存在于 map_locations
(function initArchiveBuilding() {
  if (!window.AppData || !AppData._data.map_locations) return;
  var ml = AppData._data.map_locations;
  ml.buildings = ml.buildings || [];
  var hasDadi = ml.buildings.some(function(b) { return b.id === 'dadi_study'; });
  if (!hasDadi) {
    ml.buildings.push({
      id: 'dadi_study',
      name: '大地书房',
      icon: '📖',
      floors: {
        '1F': [
          { id: 'archive', name: '档案室', icon: '📚', sub: '社区记录 · 成员目录 · 运行日志', action: 'openArchive' }
        ]
      }
    });
    // 触发 shared 保存，使地图刷新时可见
    if (typeof AppData._saveShared === 'function') AppData._saveShared();
  }
})();
```

> **说明：** 如果大地书房已存在（可能之前通过地图编辑器创建），此 IIFE 不重复添加。`action: 'openArchive'` 字段供外部 `app.js` 读取，地图渲染时如遇 `action` 不为空，则通过 postMessage 发送 `{type:'openArchive', tab:'members'}` 给父窗口。

> **外部 app.js 适配提示（不在本次实施范围内）：** 地图 app.js 需要在渲染房间卡片时检查 `room.action` 字段——若存在，点击时发送 `postMessage({type: room.action, ...}, '*')` 而非默认的房间面板行为。这是一行改动，可在阶段 1 执行时顺带完成。

---

## 改动 7：closeMyPage 辅助函数检测

**前置检查：** 改动 5 中 `closeMyPage()` 被调用。需确认此函数是否存在。

**搜索结果：** `closeMyPage` 未在目标文件中找到定义。工作台关闭使用 `document.getElementById('myPage').classList.add('hidden')` 模式。需要新增一个辅助函数或在调用处直接内联。

**在 `closeOverlay` 函数（L1303）之后新增：**

```javascript
function closeMyPage(){document.getElementById('myPage').classList.add('hidden');document.getElementById('villagePage').classList.remove('hidden')}
```

**同时，更新改动 5 中的 `closeMyPage()` 调用（如果已存在此函数的项目其他位置已定义，则跳过此改动）。**

---

## 自检清单

| # | 检查项 | 对应 UI § | 状态 |
|---|--------|-----------|------|
| 1 | `#overlayArchive` overlay 存在，header + Tab 栏 + body 正确 | § 4.1 | ☐ |
| 2 | Tab 栏 [📂 成员] [📜 日志] 两个 Tab，点击切换正常 | § 4.1 | ☐ |
| 3 | 成员 Tab：按 5 角色分组（管理员/共建者/冒险者/在地伙伴/云村民） | § 4.2 | ☐ |
| 4 | 成员 Tab：分组标题可折叠/展开，默认展开第一个有人的分组 | § 4.2 | ☐ |
| 5 | 成员 Tab：搜索框输入 → 实时过滤，跨分组显示匹配结果 | § 4.2 | ☐ |
| 6 | 成员 Tab：每行显示 DiceBear 头像 + 名字 + 角色 + NT + 在线状态 | § 4.2 | ☐ |
| 7 | 成员 Tab：点击成员行 → toast 显示名字+角色+NT | § 4.2 | ☐ |
| 8 | 日志 Tab：activity_log 时间倒序列表 | § 4.3 | ☐ |
| 9 | 日志 Tab：8 种 type 图标映射正确（📋✋✅❌💬💬🔄📝） | § 4.3 | ☐ |
| 10 | 日志 Tab：显示「最近 N 条」，底部「加载更多」 | § 4.3 | ☐ |
| 11 | 社区主页底部有「📚 社区档案室 →」链接，点击进入档案室（成员 Tab） | § 三 | ☐ |
| 12 | 工作台「📚 归档文件」→ 展开统计 +「打开档案室 →」按钮 | § 五 | ☐ |
| 13 | 工作台跳转按钮 → 关闭工作台 + 打开档案室 overlay | § 五 | ☐ |
| 14 | 大地书房建筑存在于 map_locations，含档案室房间 | § 二 | ☐ |
| 15 | 地图 postMessage 桥接：收到 `openArchive` → 关闭地图 + 打开档案室 | § 二 | ☐ |
| 16 | 送鼓励后 → 档案室日志 Tab 能看到对应记录（已有 logActivity 写入） | § 八 | ☐ |
| 17 | 邀请码升级后 → 档案室日志 Tab 能看到角色变更记录 | § 八 | ☐ |
| 18 | CSS 完整：overlay / Tab 栏 / 搜索 / 分组标题 / 成员行 / 日志行 / 加载更多 | § 4 | ☐ |
| 19 | `getUsers()` 调用处均有 `typeof getUsers==='function'` 守卫 | — | ☐ |
| 20 | 不嵌套弹出：档案室和工作台互斥（先关一个再开另一个） | § 五 | ☐ |

---

## 数据校验点

以下为代码正确性的自查点（非功能验收）：

- `LOG_ICONS` 的 key 必须与 `logActivity(type, text)` 调用处的 `type` 值完全一致（当前代码中 `logActivity` 调用处：`'tip'`、`'encourage'`、`'role_change'`、`'camp_launch'`、`'task_post'`、`'review_approve'`、`'review_reject'`、`'task_claim'`）
- `roleName(role)` 和 `roleIcon(role)` 已在 L751-L752 定义，不改动
- `AppData._data.activity_log` 元素无 `from`/`to`/`amount` 字段——日志仅为 `{time, type, text}`，`text` 字段已由 `logActivity()` 调用时拼好完整字符串（如 `'砚仁 发布了「厨房打扫」· 5 NT'`）
- 成员 NT 余额从 `NT.getUser(name).ntBalance` 读取，需要 try-catch 防 undefined

---

## 执行命令

```
文件位置：
  c:\Users\苏砚仁\thinknote\项目\实景游戏移动端代码\nantang-mobile.html

执行顺序：
  1. 改动 2（CSS）— </style> 前插入
  2. 改动 1（overlay HTML）— </div> of overlayCreateCamp 后插入
  3. 改动 7（closeMyPage）— closeOverlay 后插入
  4. 改动 3（JS 函数）— toggleArchiveExpand 前插入
  5. 改动 5（简化 toggleArchiveExpand）— 替换旧函数体
  6. 改动 4（社区主页链接）— renderCommunityHub 中插入
  7. 改动 6（postMessage 桥接）— 消息监听中添加分支
  8. 改动 6a（大地书房初始化）— initActivityLog 后插入

  改完后自检：
    1. 在 village 页面点击社区副本 → 底部有「📚 社区档案室」链接
    2. 点击链接 → 档案室 overlay 打开，Tab=📂成员
    3. 切换到 📜日志 Tab → 显示 activity_log 时间倒序列表
    4. 工作台 → 归档文件 → 展开看到统计 + 跳转按钮
    5. 送鼓励 → 切换到日志 Tab → 看到对应记录
```
