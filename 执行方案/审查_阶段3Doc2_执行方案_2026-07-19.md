# 阶段 3 Doc2 执行方案 · 五维度审查

> 2026-07-19 · 审查 `阶段3_Doc2_P1_日程财务升级.md` 全部 7 处改动
> 对照：UI 设计 `阶段3_营地骨架_UI设计.md` / 总体方案 `阶段3_实现代码.md` / 目标代码 `nantang-mobile.html`

---

## 结论：1 个数据 Bug 需修正，修正后通过

Doc2 定位为**创营向导增强**（日程编辑 / 财务自定义 / 模板扩充），与 UI 设计的营地 overlay（6 Tab + 底部栏）是不同的代码域。Doc2 不直接构建营地 overlay，而是增强创营阶段的数据输入能力——这些数据最终通过 `getCampData()` 流入 overlay 的日程 Tab 和资金 Tab。

---

## 逐条审查

### 1. 少即是多 ✅

| 检查点 | 结论 |
|--------|------|
| 7 处改动均服务于创营向导的实际痛点（日程复制、收支自定义、模板不足） | ✅ |
| 里程碑编辑用 `prompt()` 而非复杂表单，符合现有代码风格 | ✅ |
| 自定义收支项用 `prompt()` 双弹窗，ponytail 风格 | ✅ |
| Quick Start 卡片 3 套，未盲目堆量 | ✅ |
| 无新依赖、无新 CSS 类、无新抽象层 | ✅ |

### 2. 复用优先 ✅

| 检查点 | 结论 |
|--------|------|
| 所有新函数复用 `_campDraft` 数据结构 | ✅ |
| 所有渲染复用 `renderWizardStep()` 触发刷新 | ✅ |
| 复用 `esc()` / `showToast()` / `prompt()` / `confirm()` | ✅ |
| 复用现有 `.wiz-chip` CSS 类，无新样式 | ✅ |
| `copyPrevDaySched` 复用 `sch[0].cells` 数组结构 | ✅ |
| `addSchedSlot` / `removeSchedSlot` 复用 schedule 数组模式 | ✅ |
| `applyQuickTemplate` 复用 `_campDraft.step1/step2/step3` 字段 | ✅ |

### 3. 需求对齐 ⚠️

Doc2 与 UI 设计的对应关系（注意：Doc2 是创营向导增强，UI 设计是营地 overlay）：

| UI 设计需求 | Doc2 覆盖 | 说明 |
|-------------|-----------|------|
| Tab 1 概览 — 5 张导航卡片 (3.3) | 不覆盖 | 由总体方案的 `renderCampOverview` 实现 |
| Tab 2 日程 — 日期滚轮+时间线 (3.7) | **间接** | Doc2 增强日程编辑（复制前一天/+行/−行），丰富 `c.schedule` 数据源 |
| Tab 3 资金 — RMB/NT 双卡 (3.8) | **间接** | Doc2 增加自定义收支项，丰富 `c.budget` 数据源。但 overlay 的 `renderCampFunds` 当前未计入自定义项（阶段 4 补齐） |
| Tab 4 成员 (3.9) | 不覆盖 | - |
| Tab 5 排行 (3.10) | 不覆盖 | - |
| Tab 6 管理 (3.11) | 不覆盖 | - |
| 底部栏角色切换 (3.12) | 不覆盖 | - |
| 营地 overlay HTML+CSS (3.1/3.13) | 不覆盖 | - |
| 任务可见性 (3.5/3.6) | 不覆盖 | - |
| 任务模板不足（只有 2 套） | ✅ 改动 2 | 从 2 套扩充到 5 套 |
| 创营第①步无快速入口 | ✅ 改动 3 | Quick Start 卡片 3 套 |
| 创营无法自定义收支 | ✅ 改动 4 | `addBudgetItemMobile` |
| 营期类别缺「特别期」 | ✅ 改动 5 | 追加到 season 数组 |
| 里程碑不可编辑 | ✅ 改动 7 | input 可编辑 + 增删 |

**遗漏检查**：

| 潜在遗漏 | 状态 |
|----------|------|
| `extraIncome` 没有对应的 NT 字段 | ⚠️ **P2**：自定义收入项只有 `amount`（默认 ¥），无 NT 字段。如果资助金以 NT 形式发放，无法记录。当前影响小（阶段 3 资金 Tab 也不显示自定义项），但数据结构预留不足 |
| `saveSchedTemplate` 不会把 `extraIncome`/`extraExpense` 持久化到模板 | 🟢 合理——日程模板只存日程结构，不存财务数据 |
| Quick Start 卡片未传 `testMode` | 🟢 合理——快速模板默认非测试模式 |

### 4. 全局最优 ✅

| 检查点 | 结论 |
|--------|------|
| Doc2 全部改动在创营向导区域内（`renderStep1/2/3`、`CAMP_TASK_PRESETS`） | ✅ 不改动营地 overlay 或其他模块 |
| `addBudgetItemMobile` 命名带 `Mobile` 后缀，避免与未来桌面端函数冲突 | ✅ |
| `extraIncome`/`extraExpense` 数据结构 `{name, amount}` 简洁，与现有 `_campDraft.step3` 平级 | ✅ |
| Quick Start 卡片在 `renderStep1` 开头渲染，不干扰现有表单 | ✅ |
| `calcStep3Total` 包含 `extraExpense`，与 `renderStep3` 的合计计算一致 | ✅ |
| 所有新增函数在全局作用域，与现有代码风格一致 | ✅ |

### 5. 长期主义 ✅

| 检查点 | 结论 |
|--------|------|
| `extraIncome`/`extraExpense` 数组结构支持后续加字段（如 `ntAmount`、`category`） | ✅ |
| `CAMP_TASK_PRESETS` 新增模板与原数组无缝合并，后续可继续追加 | ✅ |
| `applyQuickTemplate` 使用 name→map 查找模式，添加新模板只需加条目 | ✅ |
| 里程碑 `locked` 字段保留，自定义的 `locked:false` 可删除 | ✅ |
| `copyPrevDaySched` 判断 `slot.cells[days-2]` 存在且目标为空才复制，不覆盖已编辑内容 | ✅ |

---

## 问题汇总

| # | 严重度 | 位置 | 问题 | 修正 |
|---|--------|------|------|------|
| **P1** | 高 | 改动 3a L83 | Quick Start 卡片「工笔画营」显示 `fp:1099`，但 `applyQuickTemplate`（改动 3b L101）实际写入 `fp:599`。卡片显示价格与点击后填充的价格不一致，用户看到 1099 但实际得到 599 | 将改动 3a 中工笔画营的 `fp:1099` 改为 `fp:599`（与 3b 一致）。或者将 3b 的 `fp:599` 改为 `fp:1099`（同时调整卡片显示）——建议前者，因为 `fp:599` 与现有目标代码一致 |
| **P2** | 低 | 改动 4a L128 | `addBudgetItemMobile` 的自定义收支项只有 `amount`（¥），无 NT 字段。若未来资助金以 NT 形式存在则无法记录 | 当前不影响功能（阶段 3 资金 Tab 不显示自定义项）。建议在数据结构注释中标明 `ponytail: amount 默认 ¥，阶段 5 加 ntAmount 字段` |
| **P3** | 低 | 改动 2 L58-66 | 新增 3 套模板的 JSON 代码块使用 `,` 连接原数组，依赖执行者精确找到 `];` 闭合位置。若原数组结构被其他改动修改，替换可能失败 | 用搜索锚点 `{ name:'轻量营·最小任务集'` 已足够。但建议在自检清单中增加「模板列表显示 5 套」的验证步骤（已有：自检 #2） |

---

## 额外发现

### F1: Doc2 已全部实现在目标代码中

通过搜索 `nantang-mobile.html` 确认：Doc2 的 7 处改动**已全部存在于目标代码**中（改动 1-7 的每个函数和 UI 元素均可找到）。这意味着 Doc2 可能是对已执行改动的**事后文档化**，而非待执行方案。

| 改动 | 函数/元素 | 目标代码行号 | 状态 |
|------|-----------|-------------|------|
| 1a | `copyPrevDaySched()` 按钮 | L2263 | 已存在 |
| 1b | `copyPrevDaySched()` 函数 | L2375 | 已存在 |
| 2 | `CAMP_TASK_PRESETS` 5 套 | L2550-2570 | 已存在 |
| 3a | Quick Start 卡片 | L2119-2128 | 已存在 |
| 3b | `applyQuickTemplate()` | L2158-2172 | 已存在 |
| 4a | `addBudgetItemMobile()` / `removeBudgetItemMobile()` | L2708-2719 | 已存在 |
| 4b-4d | `renderStep3` 自定义收支 | L2515-2533 | 已存在 |
| 4e | `calcStep3Total` 含 extraExpense | L2814-2822 | 已存在 |
| 5 | 期数含「特别期」 | L2134 | 已存在 |
| 6a | `addSchedSlot` / `removeSchedSlot` 按钮 | L2264-2265 | 已存在 |
| 6b | `addSchedSlot()` / `removeSchedSlot()` | L2389-2404 | 已存在 |
| 7a | `renderMilestones` 可编辑版 | L2450-2465 | 已存在 |
| 7b | `addMilestone()` / `removeMilestone()` | L2467-2476 | 已存在 |

**且目标代码中工笔画营卡片已修正为 `fp:599`（L2120），与 `applyQuickTemplate` 一致。** 说明 P1 在目标代码中不存在，但 Doc2 文档文本中仍有此 Bug——如果未来有人按照 Doc2 文档重新执行，会引入此 Bug。

### F2: Doc2 与总体方案的分工

| 域 | 负责文档 | 状态 |
|----|---------|------|
| 营地 overlay HTML + CSS | 总体方案 改动 0/7 | 独立 |
| 营地 6 Tab 渲染函数 | 总体方案 改动 1-5 | 独立 |
| `showMy({presetChip})` + 可见性 | 总体方案 改动 6 | 独立 |
| 创营向导增强（日程/财务/模板） | **Doc2** | 独立 |
| 营地信箱入口 | 总体方案 改动 7 | 独立 |

Doc2 和总体方案没有重复覆盖，分工清晰。

---

## 确认

修正 P1（工笔画营卡片 `fp:1099`→`fp:599`）后通过审查。P2/P3 为低优先级建议。

Doc2 整体质量良好：内部分工清晰、复用现有基础设施、数据结构简洁、与总体方案无冲突。唯一需要注意的是一旦 Doc2 文档与目标代码出现偏差（P1），应以目标代码为准更新文档。
