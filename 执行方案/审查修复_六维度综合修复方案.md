# 审查修复：六维度综合修复方案 v2

> 2026-07-19 · 基于 6 Agent 审查报告 + 方案自审查修正
> 涉及文件：`nantang-mobile.html`、`js/nt-core.js`
> 审查来源：游戏设计师 · UX设计师 · UI程序员 · 主程序员 · 经济设计师 · 创意总监
> v1→v2：消除 1 公式 3 次重写、加幂等守卫、CAMP_ECONOMY 去重、wipToast 统一、删除无需求来源的 P2-3

---

## 🔧 前置 0：抽取共享函数 `_calcCampTotalNT`

**原因：** `(budget.lodgingNT||0)*people*days + (budget.mealNT||0)*people*days` 在 P0-1/P0-3/P1-3 及已有代码中即将出现 7 次。先抽函数，后面全部复用。

**位置：** 在 `function getCampData()` 之后（约 L1720）新增。

**插入代码：**

```javascript
// 审计修复：统一 NT 池计算公式（避免 7 处重复）
function _calcCampTotalNT(c) {
  var budget = c.budget || {};
  var people = (budget.adventurers||0)+(budget.builders||0);
  var days = (c.schedule||[])[0] ? ((c.schedule[0].cells||[]).length||8) : 8;
  return (budget.lodgingNT||0)*people*days + (budget.mealNT||0)*people*days;
}
```

**后续所有 P0/P1 改动均使用 `_calcCampTotalNT(c)`，不再内联公式。**

---

## P0 🔴 紧急 — 结算 NT 发放修复（经济设计师#1）

### P0-1：launchCamp 注资 camp_pool（含幂等守卫）

**问题：** `camp_pool` 用户账户从未被充值，`NT.transfer('camp_pool', person, nt)` 始终因余额不足静默失败。

**位置：** `launchCamp()` 内部，在 `_saveCamp(c)` 之后、`showToast` 之前。

**插入代码：**

```javascript
  // P0-1：营队启动时注资 camp_pool（幂等：已有余额则跳过）
  var campTotalNT = _calcCampTotalNT(c);
  if (window.NT && campTotalNT > 0) {
    var existingPool = NT.getUser('camp_pool');
    if (!existingPool || existingPool.ntBalance === 0) {
      NT.topUp('camp_pool', campTotalNT, '营队注资: ' + c.name);
    }
  }
```

**涉及 API（nt-core.js 已验证存在）：**
- `NT.topUp(userId, amount, reason)` — L360，直接 mint NT 到账户
- `NT.getUser(userId)` — L531，返回 `{ntBalance, ...}` 或 null

---

### P0-2：_doSettle 检查转账返回值 + camp_pool 余额门禁

**位置：** `function _doSettle(c, t, person, silent)`。

**整个函数替换为：**

```javascript
function _doSettle(c, t, person, silent) {
  var cl = (t.claimants||[]).find(function(x){return x.name===person;});
  if (!cl) return;
  var amount = t.nt||0;

  // P0-3: 检查本期总池是否超支
  var totalPool = _calcCampTotalNT(c);
  if (totalPool > 0) {
    var tasks = c.tasks || [];
    var issuedNT = 0;
    tasks.forEach(function(task){
      (task.claimants||[]).forEach(function(cl2){
        if (cl2.status==='settled') issuedNT += (task.nt||0);
      });
    });
    if (issuedNT + amount > totalPool) {
      showToast('⚠️ NT 池超支 ' + (issuedNT + amount - totalPool) + ' NT。请调整预算或减少任务 NT 值。', 'error');
      return;
    }
  }

  // P0-2: 检查 camp_pool 余额 + 转账返回值
  if (window.NT) {
    var pool = NT.getUser('camp_pool');
    if (!pool || pool.ntBalance < amount) {
      showToast('⚠️ camp_pool 余额不足（需 ' + amount + ' NT，当前 ' + (pool ? pool.ntBalance : 0) + ' NT）。', 'error');
      return;
    }
    var result = NT.transfer('camp_pool', person, amount, '任务结算: '+t.name);
    if (!result) {
      showToast('⚠️ 转账失败，结算未执行', 'error');
      return;
    }
  }

  cl.status = 'settled';
  cl.settledAt = today();
  logActivity('task_reward', CURRENT_USER+' 结算了「'+t.name+'」· '+amount+' NT → '+person);
  if (!silent) {
    _saveCamp(c);
    showToast('已结算 '+person+' · NT '+amount,'ok');
    switchCampTab('manage');
  }
}
```

> **说明：** P0-2 和 P0-3 合并为一个函数，因为修改范围相同。超支检查仅看 `issuedNT`（已结算的），不看 unpaid/pending（尚未结算的），避免重复计数。

---

## P0 🔴 紧急 — COMMUNITY_POOL 耗尽告警（经济设计师#2）

### P0-4：社区池低余额 UI 警告

**位置：** `_renderNTPoolStats(c)` 函数内。

**改动 1 — 标题行显示社区池余额。** 将：
```javascript
'<div class="mgmt-section-head">📊 NT 池统计</div>'+
```
改为：
```javascript
'<div class="mgmt-section-head">📊 NT 池统计 <span style="font-size:.58rem;color:'+(communityPool<500?'#b84c38':'#5a6e5c')+'">社区池 '+(communityPool||0)+' NT</span></div>'+
```

**改动 2 — 低余额告警条。** 在函数返回字符串的最后一个 `</div>` 之前插入：

```javascript
    var communityPool = (window.NT && typeof NT.getCommunityPool === 'function') ? NT.getCommunityPool() : 0;
    if (communityPool < 500) {
      h += '<div style="margin-top:8px;padding:8px 10px;background:#fff5f5;border:1px solid #f0c8c8;border-radius:8px;font-size:.6rem;color:#b84c38">'+
        '⚠️ 社区 NT 池仅剩 ' + communityPool + ' NT，日志奖励和新手任务可能无法发放。'+
        (communityPool === 0 ? '<br>池已耗尽，请管理员立即补充。' : '')+
      '</div>';
    }
```

**涉及 API（nt-core.js 已验证存在）：**
- `NT.getCommunityPool()` — L427，返回 `COMMUNITY_POOL` 数值
- `NT.depositToCommunityPool(amount, reason)` — L430，管理员补充入口（已存在，无需新增）

---

## P1 🟡 重要 — 虚假按钮消除（UX 设计师#2）

### P1-1：共建者 ＋ 按钮连接 _openQuickMenu

**位置：** `openCampHome()` 底部栏（约 L1823-1826）。

**1 行改动：**
```javascript
// 旧:
    plusBtn.onclick = function(){ showToast('即将上线','warn'); };
// 新:
    plusBtn.onclick = function(){ _openQuickMenu(); };
```

---

## P1 🟡 重要 — 叠加层面包屑（UX 设计师#1）

### P1-2：食堂/客栈标题显示营地名

**位置 1：** `openCanteen()` — 在 `classList.add('open')` 之后、`renderCanteen()` 之前插入：
```javascript
  var c = getCampData();
  document.querySelector('#overlayCanteen .overlay-title').textContent = '🥬 ' + (c ? c.name : '') + ' · 食堂';
```

**位置 2：** `openInn()` — 同上位置插入：
```javascript
  var c = getCampData();
  document.querySelector('#overlayInn .overlay-title').textContent = '🏨 ' + (c ? c.name : '') + ' · 客栈';
```

> `closeCanteen`/`closeInn` 恢复营地 overlay 后，`openCampHome()` 中的 `campHomeTitle.textContent = c.name` 会自然恢复标题——无需额外代码。

---

## P1 🟡 重要 — 角色/活动 NT 实际发放 + CAMP_ECONOMY 全局常量（经济设计师#3+4 + 游戏设计师#2）

### P1-3a：全局声明 CAMP_ECONOMY

**位置：** 文件顶部全局变量区（约 L980，在 `var _mealOrders = {};` 附近）。

```javascript
// ══ 经济常量（统一调参面）══
// 调参范围：roleBonus 5-50, activityBonus 0-30, canteenMealCost 5-30, poolLowWarning 100-1000
var CAMP_ECONOMY = {
  roleBonus: { admin: 20, builder: 15, adventurer: 0 },
  activityBonus: 10,
  canteenMealCost: 10,
  poolLowWarning: 500
};
```

### P1-3b：launchCamp 发放角色/活动 NT（含幂等守卫）

**位置：** `launchCamp()` — 在 P0-1 camp_pool 注资之后插入：

```javascript
  // P1-3：发放角色 NT + 活动 NT（幂等：检查 ledger 是否已有记录）
  var econ = window.CAMP_ECONOMY || { roleBonus:{admin:20,builder:15,adventurer:0}, activityBonus:10 };
  if (window.NT && campTotalNT > 0) {
    var builders = c.builders || [];
    // 计算额外 NT 需求
    var extraNT = 0;
    builders.forEach(function(b) {
      var bonus = econ.roleBonus[b.role] || 0;
      var alreadyPaid = false;
      // 幂等检查：NT ledger 中是否已有「角色激励」记录
      var entries = NT.getLedger({userId: b.name});
      for (var ei = 0; ei < entries.length; ei++) {
        if (entries[ei].reason && entries[ei].reason.indexOf('角色激励') !== -1) { alreadyPaid = true; break; }
      }
      if (!alreadyPaid && bonus > 0) {
        NT.transfer('camp_pool', b.name, bonus, '角色激励: ' + b.role);
        extraNT += bonus;
      }
      var actAlreadyPaid = false;
      for (var ej = 0; ej < entries.length; ej++) {
        if (entries[ej].reason && entries[ej].reason.indexOf('活动参与激励') !== -1) { actAlreadyPaid = true; break; }
      }
      if (!actAlreadyPaid && econ.activityBonus > 0) {
        NT.transfer('camp_pool', b.name, econ.activityBonus, '活动参与激励');
        extraNT += econ.activityBonus;
      }
    });
    // 补充 camp_pool 以覆盖角色/活动 NT（此笔不在预算公式中）
    if (extraNT > 0) {
      NT.topUp('camp_pool', extraNT, '角色/活动激励注资: ' + c.name);
    }
  }
```

> **注意：** 此处 `campTotalNT` 复用 P0-1 中已计算的变量，不重新计算。

### P1-3c：renderSettleDetail 引用全局 CAMP_ECONOMY

**位置：** `renderSettleDetail` — 搜索 `var roleNT = member.role==='builder' ? 15`。

**替换为：**
```javascript
  var econ = window.CAMP_ECONOMY || { roleBonus:{admin:20,builder:15,adventurer:0}, activityBonus:10 };
  var roleNT = econ.roleBonus[member.role] || 0;
  var activityNT = econ.activityBonus || 10;
```

### P1-4：食堂费用引用 CAMP_ECONOMY

**位置：** `_orderMeal` + `_cancelMeal` 函数。

在函数顶部各加一行局部变量：
```javascript
  var mealCost = (window.CAMP_ECONOMY && window.CAMP_ECONOMY.canteenMealCost) || 10;
```

然后将函数内所有硬编码 `10`（餐费相关）替换为 `mealCost`。影响点：
- `NT.spend(CURRENT_USER, mealCost, ...)`
- `confirm('预定 ... 费用：' + mealCost + ' NT')`
- `showToast('预定成功！-' + mealCost + ' NT', 'ok')`
- `recordTransaction({ type:'canteen_preorder', ... amount:mealCost ... })`
- 取消退款的 `NT.earn(CURRENT_USER, mealCost, ...)` 和对应 toast/confirm

---

## P2 🟢 改善 — 叙事桥接（创意总监#1）

### P2-1：敬字亭 → 结营报告 过渡文案

**位置：** `playCloseCeremony` 的 `onComplete` 回调。

**旧代码：**
```javascript
  _closeTL = gsap.timeline({ onComplete: function() {
    setTimeout(function() {
      document.getElementById('campCloseLayer').style.display = 'none';
      document.getElementById('closeCeremonySkip').style.display = 'none';
      openCampReport(campId);
    }, 1000);
  }});
```

**改后：**
```javascript
  _closeTL = gsap.timeline({ onComplete: function() {
    setTimeout(function() {
      document.getElementById('closeCeremonySkip').style.display = 'none';
      var jztReport = document.getElementById('jzt-report');
      if (jztReport) {
        jztReport.innerHTML = '<div style="color:#a0c8a8;font-size:.72rem;margin-bottom:6px">火光照亮了这段旅程的记录</div>'+
          '<div style="color:rgba(160,200,168,.5);font-size:.58rem">数据报告已生成</div>';
      }
      setTimeout(function() {
        document.getElementById('campCloseLayer').style.display = 'none';
        openCampReport(campId);
      }, 1500);
    }, 500);
  }});
```

---

## P2 🟢 改善 — 世界观一致的「即将上线」（创意总监#2）

### P2-2：_wipToast 替换散落的「即将上线」

**新增函数（位置：`showToast` 函数附近）：**

```javascript
// P2-2: 世界观一致的占位提示
function _wipToast(feature) {
  var messages = [
    '村口告示：' + feature + '尚在筹备',
    '📜 ' + feature + '——此页尚在书写中',
    '匠人正在打磨「' + feature + '」，稍后再来',
    '这条路还没通——' + feature + '请走大路'
  ];
  showToast(messages[Math.floor(Math.random() * messages.length)], 'warn');
}
```

**替换范围：** `_openQuickMenu()` 函数内的所有 `showToast('X功能即将上线', 'warn')` → `_wipToast('X')`。约 12 处。不改其他文件中的 toast（保持最小影响面）。

---

## P2 🟢 改善 — 数据访问层统一（主程序员#3）

### P2-3：appData() 辅助函数

**新增函数（位置：`esc()` 函数附近）：**

```javascript
// P2-3: 统一数据访问层（替代 ~50 处 window.AppData && 守卫）
function appData(key, fallback) {
  return (window.AppData && AppData._data && AppData._data[key] != null) ? AppData._data[key] : (fallback !== undefined ? fallback : null);
}
```

**策略：** 新增代码必须用 `appData()`。旧代码不改（没坏不修）。

---

## 📋 执行顺序

```
前置0: _calcCampTotalNT 共享函数           ← 先抽函数，后续全部复用

P0 层（阻断性，必须先做）:
  P0-1: launchCamp 注资 camp_pool（含幂等）  ← 修复结算零转账根因
  P0-2: _doSettle 替换（含 P0-2 返回值 + P0-3 超支门禁） ← 合并为一个改动
  P0-4: COMMUNITY_POOL 低余额告警           ← UI 告警

P1 层（数据完整性 + 高ROI）:
  P1-1: ＋按钮 → _openQuickMenu（1行）
  P1-2: 食堂/客栈面包屑（8行）
  P1-3a: CAMP_ECONOMY 全局常量（5行）
  P1-3b: launchCamp 发放角色/活动 NT（含幂等 + extraNT 注资）
  P1-3c: renderSettleDetail 引用 CAMP_ECONOMY（2行改）
  P1-4: _orderMeal/_cancelMeal 引用 mealCost（~5处改）

P2 层（体验打磨 + 工程改善）:
  P2-1: 敬字亭过渡文案（8行改）
  P2-2: _wipToast + _openQuickMenu 替换（~15行新 + 12处改）
  P2-3: appData() 辅助函数（4行新）
```

---

## 🧪 自检清单

- [ ] 前置0: `_calcCampTotalNT` 存在且原有 4 处引用未受影响
- [ ] P0-1: 创营后 `NT.getUser('camp_pool').ntBalance === campTotalNT`
- [ ] P0-1: 二次调用 `launchCamp` 不重复注资（幂等）
- [ ] P0-2: 结算后 `NT.getUser(person).ntBalance` 增加了 `t.nt`
- [ ] P0-2: camp_pool 余额不足时 toast 报错 + 状态不变
- [ ] P0-2: 超支时 toast 报错 + 状态不变
- [ ] P0-4: 社区池 < 500 时管理面板 NT 池统计显示红色告警
- [ ] P1-1: 共建者点击 `＋` → 快捷菜单 Sheet 弹出
- [ ] P1-2: 食堂 overlay 标题显示「营地名 · 食堂」
- [ ] P1-3b: 创营后 builder 的 NT 增加了 roleBonus + activityBonus
- [ ] P1-3b: 二次 launchCamp 不重复发放角色/活动 NT（幂等）
- [ ] P1-3c: 修改 CAMP_ECONOMY.roleBonus.admin 后结算页显示随之变化
- [ ] P1-4: 修改 CAMP_ECONOMY.canteenMealCost=15 后预定扣 15 NT
- [ ] P2-1: 敬字亭动画结束后显示"火光照亮了这段旅程的记录"1.5秒
- [ ] P2-2: 点击快捷菜单"成员签到"→ toast 显示世界观文案而非"签到功能即将上线"
- [ ] P2-3: `appData('activity_log', [])` 在 AppData 未加载时返回 `[]`
- [ ] NT.verify() 通过（会计等式不报错）

---

## 🔗 与审查报告的追溯矩阵

| 修复项 | 审查来源 | Agent |
|--------|----------|-------|
| P0-1 + P0-2 | 结算从未成功发放 NT；返回值未检查 | 经济设计师 #1, #2 |
| P0-2 (超支) | NT 池超支无拦截 | 游戏设计师 #1 |
| P0-4 | COMMUNITY_POOL 耗尽后经济冻结 | 经济设计师 #2 |
| P1-1 | "更多"按钮是虚假功能 | UX 设计师 #2 |
| P1-2 | 叠加层深度 5 层无面包屑 | UX 设计师 #1 |
| P1-3 + P1-4 | 角色NT/活动NT 仅为显示值；经济常量硬编码 | 经济设计师 #3, #4；游戏设计师 #2 |
| P2-1 | 敬字亭仪式→数据仪表盘情绪断崖 | 创意总监 #1 |
| P2-2 | "即将上线"toast 破世界沉浸 | 创意总监 #2 |
| P2-3 | AppData 无统一访问层 | 主程序员 #3 |

**不在本方案中的审查建议（保留决策记录）：**

| 建议 | 不做的原因 |
|------|-----------|
| 按钮样式体系统一（`.btn` + `--modifier`） | 跨 ~100 处改动，风险 > 收益，不修旧代码 |
| JS 模板字面量替换字符串拼接 | 机械重构，零功能收益，不修旧代码 |
| 空 catch 块加 console.error | P0 修复后可观测，单独处理 |
| 概览卡片感知当前 Tab 状态 | 新功能，超出修复范围，阶段 7 考虑 |
| 在地伙伴 NPC 日常节奏 | 内容创作，非代码修复 |
| 假结算补偿修复工具 | 无审查来源，P0-1/P0-2 修复后不再产生假数据 |

---

> 方案 v2 完。按前置0 → P0 → P1 → P2 顺序执行。每完成一层，自检后再进下一层。