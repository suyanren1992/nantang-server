# 阶段 2 实现代码 · 五维度审查

> 2026-07-19 · 审查 `阶段2_实现代码.md` 全部 12 处改动

---

## 结论：基本通过，2 个需修正

---

## 逐条审查

### 1. 少即是多 ⚠️

| 检查点 | 结论 | 说明 |
|--------|------|------|
| 7 步复用同一 overlay | ✅ | 只切换 body 内容，不新建 7 个 overlay |
| 动画复用同一层 | ✅ | `#campCeremonyLayer` 复用于创营/启营 |
| 3 个 ponytail 简化 | ✅ | 日程/任务/选人用 `prompt()`，合计砍掉 ~500 行 |
| `esc()` 可能重复 | ⚠️ | **问题 1**：阶段 1 已定义 `esc()`（阶段1_实现代码 L193），阶段 2 再次定义。JS 重复声明不报错但冗余 |

### 2. 复用优先 ✅

| 检查点 | 结论 | 说明 |
|--------|------|------|
| AppData | ✅ | 复用 `shared('camps')` / `sharedSet` |
| showToast | ✅ | 复用已有 |
| getUsers / changeUserRole | ✅ | 复用已有 |
| logActivity | ✅ | 复用阶段 1 前置A |
| renderCommunityHub | ✅ | 复用刷新 |
| 旧 `#overlayCreateCamp` 删除 | ✅ | 完全替换，无残留 DOM |

### 3. 需求对齐 ⚠️

| 检查点 | 结论 | 说明 |
|--------|------|------|
| 7 步流程完整 | ✅ | ①→②→③→④→⑤→⑥→⑦ 全链路 |
| 表单字段和设计一致 | ✅ | 名称/期数/类型/主题/描述/测试模式 |
| 里程碑自动计算 | ✅ | 4 个节点按规则生成 |
| 财务实时计算 | ✅ | 收入/支出/预测损益 |
| 分包确认 MVP 模式 | ⚠️ | `sendConfirmations()` 自动确认所有人，设计文档要求「共建人 3 步逐页确认」。但这是 MVP 合理过渡——共建人确认页面需要工作台通知链路（阶段 3 才建）。需在注释标注「ponytail: MVP 自动确认，阶段 3 后改为共建人逐页确认」 |

### 4. 全局最优 ⚠️

| 检查点 | 结论 | 说明 |
|--------|------|------|
| 数据写入统一 | ✅ | `AppData.sharedSet('camps')`，阶段 1 可读 |
| activity_log 写入 | ✅ | `camp_launch` / `camp_confirm` / `role_change` |
| 身份升级 | ✅ | `changeUserRole` → builder |
| **重复函数声明** | ❌ | **问题 2**：`campWizardNext()` 在改动 2（L82）和改动 4（L247）各定义了一次。改动 2 的版本 `function campWizardNext() { goWizardStep(...); }` 会被改动 4 的版本覆盖。应删除改动 2 中的 `campWizardNext`，只保留改动 4 的完整版本 |
| `updateWizardButtons` 死变量 | ⚠️ | L123 `var total = 7;` 未使用。代码异味，不影响功能，可保留或删除 |

### 5. 长期主义 ✅

| 检查点 | 结论 | 说明 |
|--------|------|------|
| `_campDraft` 结构 | ✅ | 7 步数据独立，可序列化 |
| `offsetDate()` 纯函数 | ✅ | 可复用，无副作用 |
| `calcStep3Total()` | ✅ | 独立计算，不依赖 DOM |
| `launchCamp()` 清理 | ✅ | 启动后 `_campDraft = null`，关闭 overlay |
| 草稿丢弃确认 | ✅ | `cancelCampWizard()` 有 `confirm()` 守卫 |

---

## 问题汇总

| # | 严重度 | 位置 | 问题 | 修正 |
|---|--------|------|------|------|
| **P1** | 中 | 改动 2 L82 | `campWizardNext()` 在改动 2 和改动 4 中重复定义 | 删除改动 2 中的 `campWizardNext = ...` 那行。改动 4 的完整版本覆盖所有逻辑 |
| **P2** | 低 | 改动 3 L193 | `esc()` 在阶段 1 和阶段 2 各定义一次 | 阶段 2 的 `esc()` 加 `if (typeof esc !== 'function')` 守卫，或直接删除（阶段 1 已定义） |
| N1 | 低 | 改动 9 L562 | `sendConfirmations()` MVP 自动确认 | 加注释 `/* ponytail: MVP 自动确认，阶段 3 后改为共建人逐页确认（需要工作台通知链路）*/` |

---

## 确认

阶段 2 实现代码在修正 P1 和 P2 后**通过审查**。3 个 ponytail 简化有明确升级路径，不影响全链路可用性。
