# 阶段 3 设计文档 · 五维度审计

> 2026-07-19 · 审查 `阶段3_营地骨架_UI设计.md`

---

## 结论：2 个需修正，3 个需标注。修正后通过。

---

## 逐条审计

### 1. 少即是多 ⚠️

| 检查点 | 结论 |
|--------|------|
| 6 Tab 管理占位节省工作 | ✅ |
| 日程滚轮+时间线，不做完整编辑器 | ✅ |
| 资金双卡+统计，流水留阶段4 | ✅ |
| 概览 5 卡片导航，不放内容细节 | ✅ |
| **数据源标注错误** | ❌ **P1**：§四日程数据源写 `_campDraft.step2.schedule`，§五资金数据源写 `_campDraft.step3`。但营队启动后 `_campDraft` 被 `launchCamp()` 清空（`_campDraft = null`）。实际数据在 `AppData.shared('camps')` 中找到营队对象后，从 `c.schedule` 和 `c.budget` 读取。文档数据源标注应修正 |

### 2. 复用优先 ⚠️

| 检查点 | 结论 |
|--------|------|
| `showMy()`→`toggleProfile()` 链路 | ✅ |
| 工作台 chip 行 | ✅ |
| DiceBear API | ✅ |
| **成员渲染函数复用** | ⚠️ **N1**：§六说「和档案室成员目录共享视觉语言」，但未标注是否复用 `renderMemberGroup()` 函数。审查 P1 已指出此问题，设计文档中应明确标注 |

### 3. 需求对齐 ⚠️

| 检查点 | 结论 |
|--------|------|
| 每个 Tab 有参考文档 | ✅ |
| 概览跳转行为明确 | ✅ |
| 任务可见性分层 | ✅ |
| **`showMy({presetChip})` 未标注依赖** | ⚠️ **N2**：§二跳转行为表中写 `showMy({presetChip:'营队'})`，但 `showMy()` 当前不接收参数。设计文档应在「现有资产」或「实现清单」中标注此项为对 `showMy` 的新增能力，而非已有功能 |

### 4. 全局最优 ⚠️

| 检查点 | 结论 |
|--------|------|
| 营地 overlay 与工作台独立 | ✅ |
| 信息分层（社区窗口/任务大厅/营地内部） | ✅ |
| chip 可见性一处改动两处生效 | ✅ |
| **冒险者底部栏 2/4 按钮为 toast 占位** | ⚠️ **N3**：§1.2 底部栏冒险者第3/4位（🥬订餐/🏨住宿）在本阶段均为 toast「即将上线」。冒险者进入营地后不能做任何操作（只能浏览5个Tab），体验断裂。但这属于阶段 4 的交付范围，本阶段标注清楚即可 |

### 5. 长期主义 ✅

| 检查点 | 结论 |
|--------|------|
| 每个 Tab 独立函数 | ✅ |
| Tab 栏预留管理占位 | ✅ |
| 6 Tab + 底部栏架构清晰 | ✅ |

---

## 问题汇总

| # | 严重度 | 位置 | 问题 | 修正 |
|---|--------|------|------|------|
| **P1** | 高 | §四 L140 · §五 L168 | 日程/资金数据源写 `_campDraft.step2/3`，但营队启动后草稿已清空。应从 `AppData.shared('camps')` 中的营队对象读取（`c.schedule` / `c.budget`） | 修正为：日程数据源 `c.schedule`（`getCampData().schedule`）；资金数据源 `c.budget`（`getCampData().budget`）。`getCampData()` 从 `AppData.shared('camps')` 中查找 `_campCurrentId` |
| N1 | 中 | §六 L174 | 成员 Tab 与档案室视觉语言一致但未标注函数复用 | 在 §六 加一行：「复用阶段 1 前置B 的 `renderMemberGroup(users, opts)` 函数，本阶段仅传入营地上下文参数」 |
| N2 | 低 | §二 L91 | `showMy({presetChip})` 未标注为新增能力 | 在 §〇 或 §二 标注：「`showMy()` 需新增 `opts` 参数支持——这是本阶段对工作台的唯一改动」 |
| N3 | 低 | §1.2 L50-51 | 冒险者底部栏 2 个 toast 占位 | 无需修改设计。在验收清单加一条：「冒险者底部栏 🥬🏨 点击显示 toast（阶段4替换）」 |

---

## 确认

修正 P1（数据源标注）、补充 N1/N2/N3 标注后，设计文档通过审计。整体架构合理——营地 overlay 与工作台清晰分离、6 Tab 职责明确、概览导览面板模式正确、任务可见性分层设计完整。
