# 审查修复：六维度综合修复方案 — 模块化版

> 2026-07-19 · 基于原方案 v2 + 模块化文件结构适配
> 模块化结构：1 HTML + 1 CSS + 16 JS（加载顺序见下）

---

## 🗂️ 加载顺序（关键）

```
schema.js → utils.js → backup.js → store.js → mobile-bundle.js →
nt.js → auth.js → seed-test-data.js → nt-core.js → app-data.js →
data.js → core.js → ui-village.js → ui-camp.js → ui-wizard.js →
ui-phase4.js → ui-social.js → ui-archive.js
```

**原则：** `data.js` 放全局常量，`core.js` 放公共工具。两者在全部 `ui-*.js` 之前加载，保证可用。

---

## 函数分布速查

| 函数 | 文件 | 行号 |
|------|------|------|
| `showToast()` | `data.js` | 416 |
| `esc()` | `utils.js` | 163 |
| `getCampData()` | `ui-camp.js` | 5 |
| `openCampHome()` | `ui-camp.js` | 10 |
| `renderCampFunds()` | `ui-camp.js` | 388 |
| `renderSettleDetail()` | `ui-camp.js` | 693 |
| `_renderNTPoolStats()` | `ui-camp.js` | 1007 |
| `_doSettle()` | `ui-camp.js` | 1169 |
| `playCloseCeremony()` | `ui-camp.js` | 1275 |
| `launchCamp()` | `ui-wizard.js` | 914 |
| `_orderMeal()` | `ui-phase4.js` | 60 |
| `_cancelMeal()` | `ui-phase4.js` | 104 |
| `openCanteen()` | `ui-phase4.js` | 153 |
| `openInn()` | `ui-phase4.js` | 272 |
| `_openQuickMenu()` | `ui-social.js` | 368 |

---

## 前置 0：`_calcCampTotalNT` — 共享函数

**文件：** `js/core.js`
**位置：** 文件末尾，任意全局函数之后。

**插入：**
```javascript
// 审计修复：统一 NT 池计算公式
function _calcCampTotalNT(c) {
  var budget = c.budget || {};
  var people = (budget.adventurers||0)+(budget.builders||0);
  var days = (c.schedule||[])[0] ? ((c.schedule[0].cells||[]).length||8) : 8;
  return (budget.lodgingNT||0)*people*days + (budget.mealNT||0)*people*days;
}
```

**跨文件依赖检查：** ✅ `core.js` 在 `ui-camp.js` 和 `ui-wizard.js` 之前加载。

---

## P0-1：launchCamp 注资 camp_pool

**文件：** `js/ui-wizard.js` · 函数 `launchCamp()`（L914）
**位置：** `_saveCamp(c)` 之后、`showToast` 之前。

**插入：**
```javascript
  // P0-1：营队启动时注资 camp_pool（幂等：已有余额则跳过）
  var campTotalNT = _calcCampTotalNT(c);
  if (window.NT && campTotalNT > 0) {
    var existingPool = NT.getUser('camp_pool');
    if (!existingPool || existingPool.ntBalance === 0) {
      NT.topUp('camp_pool', campTotalNT, '营队注资: ' + c.name);
    }
  }
```

**跨文件依赖检查：** ✅ `_calcCampTotalNT` 在 `core.js`，先于 `ui-wizard.js` 加载。

---

## P0-2：_doSettle 替换（含超支门禁 + 转账返回值检查）

**文件：** `js/ui-camp.js` · 函数 `_doSettle()`（L1169）
**操作：** 整个函数替换。

**替换为：**
```javascript
function _doSettle(c, t, person, silent) {
  var cl = (t.claimants||[]).find(function(x){return x.name===person;});
  if (!cl) return;
  var amount = t.nt||0;

  // P0-3: 检查本期总池是否超支
  var totalPool = _calcCampTotalNT(c);
  if (totalPool > 0) {
    var tasks = c.tasks || [];
    var issuedNT = 0;
    tasks.forEach(function(task){
      (task.claimants||[]).forEach(function(cl2){
        if (cl2.status==='settled') issuedNT += (task.nt||0);
      });
    });
    if (issuedNT + amount > totalPool) {
      showToast('⚠️ NT 池超支 ' + (issuedNT + amount - totalPool) + ' NT。请调整预算或减少任务 NT 值。', 'error');
      return;
    }
  }

  // P0-2: 检查 camp_pool 余额 + 转账返回值
  if (window.NT) {
    var pool = NT.getUser('camp_pool');
    if (!pool || pool.ntBalance < amount) {
      showToast('⚠️ camp_pool 余额不足（需 ' + amount + ' NT，当前 ' + (pool ? pool.ntBalance : 0) + ' NT）。', 'error');
      return;
    }
    var result = NT.transfer('camp_pool', person, amount, '任务结算: '+t.name);
    if (!result) {
      showToast('⚠️ 转账失败，结算未执行', 'error');
      return;
    }
  }

  cl.status = 'settled';
  cl.settledAt = today();
  logActivity('task_reward', CURRENT_USER+' 结算了「'+t.name+'」· '+amount+' NT → '+person);
  if (!silent) {
    _saveCamp(c);
    showToast('已结算 '+person+' · NT '+amount,'ok');
    switchCampTab('manage');
  }
}
```

**跨文件依赖检查：** ✅ `_calcCampTotalNT` 在 `core.js`，`showToast` 在 `data.js`，均在 `ui-camp.js` 之前加载。

---

## P0-4：COMMUNITY_POOL 低余额告警

**文件：** `js/ui-camp.js` · 函数 `_renderNTPoolStats()`（L1007）

**改动 1 — 标题行显示社区池余额。** 将：
```javascript
'<div class="mgmt-section-head">📊 NT 池统计</div>'+
```
改为：
```javascript
'<div class="mgmt-section-head">📊 NT 池统计 <span style="font-size:.58rem;color:'+(communityPool<500?'#b84c38':'#5a6e5c')+'">社区池 '+(communityPool||0)+' NT</span></div>'+
```

**改动 2 — 低余额告警条。** 在函数返回字符串的最后一个 `</div>` 之前插入：
```javascript
    var communityPool = (window.NT && typeof NT.getCommunityPool === 'function') ? NT.getCommunityPool() : 0;
    if (communityPool < 500) {
      h += '<div style="margin-top:8px;padding:8px 10px;background:#fff5f5;border:1px solid #f0c8c8;border-radius:8px;font-size:.6rem;color:#b84c38">'+
        '⚠️ 社区 NT 池仅剩 ' + communityPool + ' NT，日志奖励和新手任务可能无法发放。'+
        (communityPool === 0 ? '<br>池已耗尽，请管理员立即补充。' : '')+
      '</div>';
    }
```

> **注意：** `communityPool` 变量需在使用前声明。改动 1 的标题行引用了它——确保 `var communityPool = ...` 在标题行之前。

**跨文件依赖检查：** ✅ `NT.getCommunityPool` 在 `nt-core.js`，先于 `ui-camp.js` 加载。

---

## P1-1：共建者 ＋ 按钮连接 _openQuickMenu

**文件：** `js/ui-camp.js` · 函数 `openCampHome()`（L10）

**改动：** 找到非冒险者分支的 `plusBtn.onclick`：

```javascript
// 旧:
    plusBtn.onclick = function(){ showToast('即将上线','warn'); };
// 新:
    plusBtn.onclick = function(){ _openQuickMenu(); };
```

**跨文件依赖检查：** ✅ `_openQuickMenu` 在 `ui-social.js`，但 onclick 是闭包——函数体在点击时才求值，此时全部脚本已加载。

---

## P1-2：食堂/客栈标题显示营地名

**文件：** `js/ui-phase4.js`

**位置 1：** `openCanteen()`（L153）— 在 `classList.add('open')` 之后、`renderCanteen()` 之前插入：
```javascript
  var c = getCampData();
  document.querySelector('#overlayCanteen .overlay-title').textContent = '🥬 ' + (c ? c.name : '') + ' · 食堂';
```

**位置 2：** `openInn()`（L272）— 同上位置插入：
```javascript
  var c = getCampData();
  document.querySelector('#overlayInn .overlay-title').textContent = '🏨 ' + (c ? c.name : '') + ' · 客栈';
```

**跨文件依赖检查：** ✅ `getCampData` 在 `ui-camp.js`，先于 `ui-phase4.js` 加载。

---

## P1-3a：CAMP_ECONOMY 全局常量

**文件：** `js/data.js`
**位置：** 文件末尾，`showToast` 函数之前或之后。

**插入：**
```javascript
// ══ 经济常量（统一调参面）══
// 调参范围：roleBonus 5-50, activityBonus 0-30, canteenMealCost 5-30
var CAMP_ECONOMY = {
  roleBonus: { admin: 20, builder: 15, adventurer: 0 },
  activityBonus: 10,
  canteenMealCost: 10
};
```

**跨文件依赖检查：** ✅ `data.js` 在所有 `ui-*.js` 之前加载。

---

## P1-3b：launchCamp 发放角色/活动 NT

**文件：** `js/ui-wizard.js` · 函数 `launchCamp()`（L914）
**位置：** P0-1 camp_pool 注资代码之后。

**插入：**
```javascript
  // P1-3：发放角色 NT + 活动 NT（幂等：检查 ledger 是否已有记录）
  var econ = window.CAMP_ECONOMY || { roleBonus:{admin:20,builder:15,adventurer:0}, activityBonus:10 };
  if (window.NT && campTotalNT > 0) {
    var builders = c.builders || [];
    var extraNT = 0;
    builders.forEach(function(b) {
      var bonus = econ.roleBonus[b.role] || 0;
      var entries = NT.getLedger({userId: b.name});
      var alreadyPaid = entries.some(function(e){ return e.reason && e.reason.indexOf('角色激励') !== -1; });
      if (!alreadyPaid && bonus > 0) {
        NT.transfer('camp_pool', b.name, bonus, '角色激励: ' + b.role);
        extraNT += bonus;
      }
      var actAlreadyPaid = entries.some(function(e){ return e.reason && e.reason.indexOf('活动参与激励') !== -1; });
      if (!actAlreadyPaid && econ.activityBonus > 0) {
        NT.transfer('camp_pool', b.name, econ.activityBonus, '活动参与激励');
        extraNT += econ.activityBonus;
      }
    });
    if (extraNT > 0) {
      NT.topUp('camp_pool', extraNT, '角色/活动激励注资: ' + c.name);
    }
  }
```

> **注意：** 此处 `campTotalNT` 复用 P0-1 中已计算的变量，不重新声明。

**跨文件依赖检查：** ✅ `CAMP_ECONOMY` 在 `data.js`，`NT.*` 在 `nt-core.js`，均在 `ui-wizard.js` 之前加载。

---

## P1-3c：renderSettleDetail 引用 CAMP_ECONOMY

**文件：** `js/ui-camp.js` · 函数 `renderSettleDetail()`（L693）

**改前：**
```javascript
  var roleNT = member.role==='builder' ? 15 : (member.role==='admin' ? 20 : 0);
  var activityNT = 10;
```

**改后：**
```javascript
  var econ = window.CAMP_ECONOMY || { roleBonus:{admin:20,builder:15,adventurer:0}, activityBonus:10 };
  var roleNT = econ.roleBonus[member.role] || 0;
  var activityNT = econ.activityBonus || 10;
```

**跨文件依赖检查：** ✅ `CAMP_ECONOMY` 在 `data.js`，先于 `ui-camp.js` 加载。

---

## P1-4：食堂费用引用 CAMP_ECONOMY

**文件：** `js/ui-phase4.js`
**函数：** `_orderMeal()`（L60）+ `_cancelMeal()`（L104）

**改动：** 在每个函数顶部各加一行局部变量：
```javascript
  var mealCost = (window.CAMP_ECONOMY && window.CAMP_ECONOMY.canteenMealCost) || 10;
```

然后将函数内所有硬编码 `10`（餐费相关）替换为 `mealCost`。影响点：
- `NT.spend(CURRENT_USER, mealCost, ...)`
- `confirm('预定 ... 费用：' + mealCost + ' NT')`
- `showToast('预定成功！-' + mealCost + ' NT', 'ok')`
- `recordTransaction({ type:'canteen_preorder', ... amount:mealCost ... })`
- `NT.earn(CURRENT_USER, mealCost, ...)`（取消退款）

**跨文件依赖检查：** ✅ `CAMP_ECONOMY` 在 `data.js`，先于 `ui-phase4.js` 加载。

---

## P2-1：敬字亭 → 结营报告 过渡文案

**文件：** `js/ui-camp.js` · 函数 `playCloseCeremony()`（L1275）

**位置：** `onComplete` 回调中的 setTimeout。

**旧代码：**
```javascript
  _closeTL = gsap.timeline({ onComplete: function() {
    setTimeout(function() {
      document.getElementById('campCloseLayer').style.display = 'none';
      document.getElementById('closeCeremonySkip').style.display = 'none';
      openCampReport(campId);
    }, 1000);
  }});
```

**改后：**
```javascript
  _closeTL = gsap.timeline({ onComplete: function() {
    setTimeout(function() {
      document.getElementById('closeCeremonySkip').style.display = 'none';
      var jztReport = document.getElementById('jzt-report');
      if (jztReport) {
        jztReport.innerHTML = '<div style="color:#a0c8a8;font-size:.72rem;margin-bottom:6px">火光照亮了这段旅程的记录</div>'+
          '<div style="color:rgba(160,200,168,.5);font-size:.58rem">数据报告已生成</div>';
      }
      setTimeout(function() {
        document.getElementById('campCloseLayer').style.display = 'none';
        openCampReport(campId);
      }, 1500);
    }, 500);
  }});
```

**跨文件依赖检查：** ✅ `openCampReport` 在同一文件 `ui-camp.js` 中定义（同文件调用）。

---

## P2-2：_wipToast + 替换「即将上线」

### P2-2a：新增 _wipToast

**文件：** `js/core.js`
**位置：** 文件末尾。

```javascript
// P2-2: 世界观一致的占位提示
function _wipToast(feature) {
  var messages = [
    '村口告示：' + feature + '尚在筹备',
    '📜 ' + feature + '——此页尚在书写中',
    '匠人正在打磨「' + feature + '」，稍后再来',
    '这条路还没通——' + feature + '请走大路'
  ];
  showToast(messages[Math.floor(Math.random() * messages.length)], 'warn');
}
```

### P2-2b：替换 _openQuickMenu 中的 toast

**文件：** `js/ui-social.js` · 函数 `_openQuickMenu()`（L368）

将所有 `showToast('X功能即将上线', 'warn')` 替换为 `_wipToast('X')`。约 12 处。

**跨文件依赖检查：** ✅ `_wipToast` 在 `core.js`，`showToast` 在 `data.js`，均在 `ui-social.js` 之前加载。

---

## P2-3：appData() 辅助函数

**文件：** `js/core.js`
**位置：** 文件末尾。

```javascript
// P2-3: 统一数据访问层
function appData(key, fallback) {
  return (window.AppData && AppData._data && AppData._data[key] != null) ? AppData._data[key] : (fallback !== undefined ? fallback : null);
}
```

**策略：** 新增代码必须用 `appData()`。旧代码不改（没坏不修）。

**跨文件依赖检查：** ✅ `core.js` 在所有 `ui-*.js` 之前加载。

---

## 📋 按文件归组的执行顺序

### 第 1 轮：`data.js` + `core.js`（基础设施，其他文件依赖它们）

| 顺序 | 改动 | 文件 | 行 |
|:--:|------|------|:--:|
| 1 | P1-3a: CAMP_ECONOMY | `data.js` | 末尾 |
| 2 | 前置0: _calcCampTotalNT | `core.js` | 末尾 |
| 3 | P2-2a: _wipToast | `core.js` | 末尾 |
| 4 | P2-3: appData() | `core.js` | 末尾 |

### 第 2 轮：`ui-camp.js`（改动最多，4 个函数）

| 顺序 | 改动 | 函数 | 行 |
|:--:|------|------|:--:|
| 5 | P0-2: _doSettle 替换 | `_doSettle()` | 1169 |
| 6 | P0-4: COMMUNITY_POOL 告警 | `_renderNTPoolStats()` | 1007 |
| 7 | P1-1: ＋按钮 | `openCampHome()` | ~10 |
| 8 | P1-3c: renderSettleDetail 引用 | `renderSettleDetail()` | 693 |
| 9 | P2-1: 敬字亭过渡 | `playCloseCeremony()` | 1275 |

### 第 3 轮：`ui-wizard.js`（launchCamp 两处插入）

| 顺序 | 改动 | 函数 | 行 |
|:--:|------|------|:--:|
| 10 | P0-1: camp_pool 注资 | `launchCamp()` | 914 |
| 11 | P1-3b: 角色/活动 NT 发放 | `launchCamp()` | 914 |

### 第 4 轮：`ui-phase4.js`（食堂 + 客栈）

| 顺序 | 改动 | 函数 | 行 |
|:--:|------|------|:--:|
| 12 | P1-2: 面包屑 | `openCanteen()` + `openInn()` | 153, 272 |
| 13 | P1-4: mealCost | `_orderMeal()` + `_cancelMeal()` | 60, 104 |

### 第 5 轮：`ui-social.js`（快捷菜单）

| 顺序 | 改动 | 函数 | 行 |
|:--:|------|------|:--:|
| 14 | P2-2b: _wipToast 替换 | `_openQuickMenu()` | 368 |

---

## ⚠️ 模块化特有风险点

| 风险 | 检查结果 |
|------|----------|
| `_calcCampTotalNT` 在 `core.js`，被 `ui-camp.js` 和 `ui-wizard.js` 调用 | ✅ 加载顺序正确 |
| `CAMP_ECONOMY` 在 `data.js`，被 `ui-camp.js`、`ui-wizard.js`、`ui-phase4.js` 读取 | ✅ 加载顺序正确 |
| `_wipToast` 在 `core.js`，被 `ui-social.js` 调用 | ✅ 加载顺序正确 |
| `appData` 在 `core.js`，被 `ui-archive.js` 等调用 | ✅ 加载顺序正确 |
| P1-1 onclick 闭包引用 `_openQuickMenu`（定义在更晚加载的文件） | ✅ 闭包在点击时求值，全部脚本已加载 |
| `esc()` 在 `utils.js` + `ui-wizard.js:155` 有重复定义 | ⚠️ 两个定义。`ui-wizard.js:155` 有 `typeof esc !== 'function'` 守卫，重复定义无害但建议删除 `ui-wizard.js` 中的 fallback |
| `getCampData()` 被 `ui-phase4.js`（P1-2）调用 | ✅ `ui-camp.js` 在 `ui-phase4.js` 之前加载 |

---

## 🧪 自检清单

- [ ] 前置0: `_calcCampTotalNT` 在 `core.js` 中存在
- [ ] P0-1: 创营后 `NT.getUser('camp_pool').ntBalance === _calcCampTotalNT(c)`
- [ ] P0-1: 二次 launchCamp 不重复注资（幂等）
- [ ] P0-2: 结算后 `NT.getUser(person).ntBalance` 增加了 `t.nt`
- [ ] P0-2: camp_pool 不足时 toast 报错 + 状态不变
- [ ] P0-2: 超支时 toast 报错 + 状态不变
- [ ] P0-4: 社区池 < 500 时管理面板红色告警
- [ ] P1-1: 共建者 `＋` → 快捷菜单 Sheet
- [ ] P1-2: 食堂标题「营地名 · 食堂」
- [ ] P1-3b: 创营后 builder 的 NT 增加 roleBonus + activityBonus
- [ ] P1-3b: 二次 launchCamp 不重复发放（幂等）
- [ ] P1-3c: CAMP_ECONOMY 改值后结算页跟随变化
- [ ] P1-4: CAMP_ECONOMY.canteenMealCost=15 后预定扣 15 NT
- [ ] P2-1: 动画结束后"火光照亮了这段旅程的记录"1.5秒
- [ ] P2-2: 快捷菜单 toast 显示世界观文案
- [ ] P2-3: `appData('activity_log', [])` 在 AppData 未加载时返回 `[]`
- [ ] 加载无 JS 报错（所有跨文件依赖正确解析）
- [ ] NT.verify() 通过

---

> 方案完。共 14 处改动，分布 6 个 JS 文件。按 5 轮顺序执行，每轮自检后进下一轮。