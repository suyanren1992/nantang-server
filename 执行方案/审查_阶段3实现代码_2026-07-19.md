# 阶段 3 实现代码 · 五维度审查

> 2026-07-19 · 审查 `阶段3_实现代码.md` 全部 10 处改动

---

## 结论：3 个需修正，修正后通过

---

## 逐条审查

### 1. 少即是多 ✅

| 检查点 | 结论 |
|--------|------|
| 10 个改动覆盖 6 Tab + 底部栏 + 可见性 | ✅ |
| 管理 Tab 占位而非完整实现 | ✅ |
| `switchCampDay` 空函数、`rmbSpent=0` 硬编码 | ✅ ponytail 简化，有标注 |

### 2. 复用优先 ⚠️

| 检查点 | 结论 |
|--------|------|
| 复用 `showMy()` / `getUsers()` / `AppData` / `showToast` / DiceBear | ✅ |
| 复用 `offsetDate()`（阶段 2 定义） | ⚠️ **P2**：阶段 3 直接调用 `offsetDate()`，无守卫。若阶段 2 未执行会报错 |
| 复用 `.my-tab` CSS 模式做 `.camp-tab` | ✅ |

### 3. 需求对齐 ⚠️

| 检查点 | 结论 |
|--------|------|
| 概览 5 张导航卡片 + 里程碑 | ✅ |
| 日程滚轮 + 完整时间线 | ✅ |
| 资金双卡 + 进度条 | ✅ |
| 成员角色分组 + DiceBear | ✅ |
| 排行 medal + 进度条 | ✅ |
| `showMy({presetChip})` 联动工作台 | ⚠️ **P3**：只给了 `showMy()` 内部追加的代码片段，未标注在现有函数中的插入位置和改动行数。实现者需自行判断 |

### 4. 全局最优 ⚠️

| 检查点 | 结论 |
|--------|------|
| 营地 overlay 与工作台独立 | ✅ |
| `getCampData()` 统一封装 | ✅ |
| 底部栏角色切换集中管理 | ✅ |
| 成员列表数据完整性 | ❌ **P1**：`allMembers` 只包含 `CURRENT_USER` + `builders`，不包含通过邀请码加入的冒险者。实际上 `getUsers()` 中所有 `role==='adventurer'` 且属于本营地的用户都应该出现 |

### 5. 长期主义 ✅

| 检查点 | 结论 |
|--------|------|
| 每个 Tab 独立函数，可分别升级 | ✅ |
| `getCampData()` 封装 | ✅ |
| 排行只排共建者，冒险者不在其中 | ⚠️ 设计如此——冒险者没有任务包，排行按 NT 排序自然为 0。可接受，但应在注释中说明 |

---

## 问题汇总

| # | 严重度 | 位置 | 问题 | 修正 |
|---|--------|------|------|------|
| **P1** | 高 | 改动 5 L339 | 成员列表只含 CURRENT_USER+builders，缺少冒险者 | 追加：`Object.keys(users).filter(function(n){return users[n].role==='adventurer' && isUserInCamp(n);}).forEach(...)` |
| **P2** | 中 | 改动 3 L235 | `offsetDate()` 直接调用，无守卫 | 加 `if (typeof offsetDate !== 'function') { function offsetDate(d,days){...} }` |
| **P3** | 低 | 改动 7 L458-472 | `showMy()` 改动不精确——未标注现有函数结构和插入位置 | 标注「在 `showMy` 函数体末尾、`switchMyTab('tasks')` 调用之后插入。若当前 `showMy` 不调用 `switchMyTab`，则插入在渲染完成后」 |

---

## 确认

修正 P1/P2/P3 后通过审查。整体架构清晰——营地 overlay 与工作台独立、6 Tab 各司其职、底部栏角色切换、导航面板模式正确。
