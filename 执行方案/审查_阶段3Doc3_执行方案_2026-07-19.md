# 阶段 3 Doc3 执行方案 · 五维度审查

> 2026-07-19 · 审查 `阶段3_Doc3_P1_交互叙事升级.md` 全部 5 处改动是否覆盖 `阶段3_营地骨架_UI设计.md` 需求

---

## 结论：Doc3 改动已全部实现，与主执行方案互补覆盖 UI 设计，2 处 ponytail 标注

Doc3 定位为**创营向导（Creation Wizard）的交互+叙事增强**，不直接覆盖营地 overlay 的 6 Tab 布局——那部分由 `阶段3_实现代码.md`（改动0-改动7）负责。两者合并检查后，UI 设计全部 13 项需求均已实现。

---

## 逐条审查

### 1. 少即是多 ✅

| 检查点 | 结论 |
|--------|------|
| 改动1 状态 chip：3 态循环（todo/maybe/skip），单函数 `toggleTaskStatus` | ✅ 一行逻辑，无多余抽象 |
| 改动2 GSAP 动画：复用现有 `_ceremonyTL` 变量，不新建 timeline 管理器 | ✅ 与 `playCreateCeremony` 共享 infra |
| 改动3 叙事标题：一行数组替换 | ✅ |
| 改动4 模板加载：`prompt()` 选择 + `localStorage` 读取，无 UI 库依赖 | ✅ ponytail 标准做法 |
| 改动5 任务类型：6 选项下拉 + 彩色 chip，无多余类型层级 | ✅ |

**轻微盈余：** 实际代码中概览 Tab 渲染了 7 张导航卡（多出「订餐」「住宿」），超出 UI 设计指定的 5 张。这两张卡的数据已从 `budget` 读取，但点击仅弹出 toast（阶段 4 上线）。属于阶段 4 的前置埋点，非过度设计，但建议标注 `ponytail: 订餐/住宿卡数据就绪，交互在阶段4接入`。

| 偏差项 | 设计 | 实现 | 评估 |
|--------|------|------|------|
| 概览导航卡数量 | 5 张 | 7 张（+订餐、住宿） | ⚠️ 阶段 4 前置，可接受 |
| 概览卡信息密度 | 简洁跳转入口 | 含成员头像行、任务名、RMB/天 | ⚠️ 增强体验，可接受 |
| 我的任务卡标签 | 「我的任务」 | 「我的任务包」 | ✅ 更精确 |

---

### 2. 复用优先 ✅（1 处 ponytail 标注）

| 检查点 | 结论 |
|--------|------|
| `toggleTaskStatus` → 复用 `_campDraft.step4.tasks` + `renderWizardStep(4)` | ✅ |
| `playLaunchCeremony` → 复用 `_ceremonyTL`、`campCeremonyLayer` DOM、`launchCamp()` | ✅ |
| `loadTaskTemplate` → 复用 `localStorage` key `nantang_task_templates`、`showToast` | ✅ |
| 任务类型 select → 复用 `login-input` CSS class | ✅ |
| `typeColors` 映射 → 内联定义于 `renderStep4` | ⚠️ **ponytail**: 若未来其他视图（日程、排行）也渲染任务类型，需将 `typeColors` 提至全局。当前仅一处消费者，无需提前抽象 |
| 概览头像行 → 复用 DiceBear API + fallback 逻辑 | ✅ |
| 底部栏 DiceBear 头像 → 复用 `showMy()` 链路 | ✅ |

**未复用的已有资源：**
- `loadTaskTemplate` 与 `importTaskTemplate` 功能重叠（都是加载模板），但逻辑不同（覆盖 vs 合并），两者各自独立是合理的。

---

### 3. 需求对齐 ✅（Doc3 + 主执行方案互补）

Doc3 的 5 个改动不属于营地 overlay 范畴，需与主执行方案合并检查 UI 设计覆盖：

| UI 设计需求 (#) | 覆盖方 | 状态 |
|-----------------|--------|------|
| 3.1 营地 overlay 重构 | 主方案 改动0 | ✅ |
| 3.2 `switchCampTab()` | 主方案 改动1 | ✅ |
| 3.3 概览 5 张导航卡 + 里程碑 | 主方案 改动1 | ✅（实现为 7 张） |
| 3.4 `showMy({presetChip})` | 主方案 改动6 | ✅ |
| 3.5 工作台 chip 行 `[营队]` 可见性 | 主方案 改动6 | ✅ |
| 3.6 村口任务大厅 chip 可见性 | 主方案 改动6 + `openQuestHallPage` | ✅ |
| 3.7 日程 Tab | 主方案 改动2 | ✅ |
| 3.8 资金 Tab | 主方案 改动3 | ✅ |
| 3.9 成员 Tab | 主方案 改动4 | ✅ |
| 3.10 排行 Tab | 主方案 改动5 | ✅ |
| 3.11 管理 Tab | 主方案 改动5 | ✅ |
| 3.12 底部栏 | 主方案 改动0+1 | ✅ |
| 3.13 CSS | 主方案 改动7 | ✅ |

**Doc3 独有的需求覆盖（不属于 UI 设计但属于 P1 交互叙事升级）：**

| Doc3 改动 | 覆盖内容 | 状态 |
|-----------|----------|------|
| 改动1 | 任务状态快速切换 chip（要做/备选/不做） | ✅ |
| 改动2 | 启营仪式 GSAP 星光汇聚动画 | ✅ |
| 改动3 | 叙事化步骤标题（①点燃火种…⑦开启传送门） | ✅ |
| 改动4 | 任务模板加载 UI（📂 加载模板） | ✅ |
| 改动5 | 任务类型 6 选 1 下拉 + 彩色 chip 渲染 | ✅ |

**无遗漏。** Doc3 的 5 处改动与主方案 8 处改动合计覆盖 UI 设计全部 13 项需求 + P1 交互叙事增强 5 项。

---

### 4. 全局最优 ✅

| 检查点 | 结论 |
|--------|------|
| 营地 overlay 与工作台独立，不互相污染 DOM | ✅ |
| `getCampData()` 统一封装数据读取 | ✅ |
| `switchCampTab()` 集中路由，每个 Tab 独立渲染函数 | ✅ |
| 底部栏角色切换（共建者/冒险者）在 `openCampHome` 集中处理 | ✅ |
| `_ceremonyTL` 共享于 `playCreateCeremony` 和 `playLaunchCeremony` | ✅ 同一时间只有一个向导步骤活跃，无竞态 |
| 任务可见性同时在工作台和村口任务大厅生效 | ✅ `showMy` + `openQuestHallPage` 两处守卫 |
| `showMy({presetChip})` 参数化设计，不破坏现有调用 | ✅ `showMy()` 无参调用仍正常 |
| 非营地成员 `[营队]` chip 隐藏逻辑覆盖 `visitor` 角色 | ✅ |
| 成员列表包含 `CURRENT_USER` + builders + adventurers | ✅（审计 P1 已修正） |
| `offsetDate` 有 typeof 守卫 fallback | ✅（审计 P2 已修正） |

**未发现问题。**

---

### 5. 长期主义 ✅（1 处 ponytail 标注）

| 检查点 | 结论 |
|--------|------|
| 每个 Tab 独立 `renderCamp*` 函数，可分别升级 | ✅ |
| `getCampData()` 封装允许日后切换数据源（API/IndexedDB） | ✅ |
| 任务类型 6 种枚举 + 颜色映射，新增类型只需加一行 | ✅ |
| `toggleTaskStatus` 只循环 3 个状态（todo/maybe/skip），与执行期任务状态（todo/doing/done/settled）解耦 | ✅ 设计合理——创营期任务是计划态 |
| `loadTaskTemplate` 用 `prompt()` 选择，模板超 10 个时体验下降 | ⚠️ **ponytail**: 模板数 <5 时 `prompt()` 可接受。超过后升级为 modal 列表。当前天花板明确 |
| `typeColors` 内联在 `renderStep4` | ⚠️ **ponytail**: 第二个消费者出现时提取为全局常量。当前仅一处 |
| 订餐/住宿卡模型已嵌入概览，数据结构 `budget.mealRmb/mealNT/lodgingRmb/lodgingNT` 已就绪 | ✅ 阶段 4 无需改数据结构 |
| `campCeremonyLayer` HTML 已存在，未来仪式扩展只需追加内容 | ✅ |

---

## 问题汇总

| # | 严重度 | 位置 | 问题 | 修正 |
|---|--------|------|------|------|
| **D1** | 低 | 概览渲染 L1720-1731 | 订餐/住宿卡交互为 toast 占位（阶段4上线），但数据已渲染。用户可能误以为功能可用 | 标注 `ponytail: 订餐/住宿交互在阶段4接入，当前仅展示预算数据` |
| **D2** | 低 | `loadTaskTemplate` L2680 | `prompt()` 选择模板在移动端体验差，模板超 10 个时列表溢出 | 标注 `ponytail: prompt() ceiling at ~10 templates, upgrade to modal list when exceeded` |

**无阻塞问题。** 前次审查的 P1/P2/P3 已全部修正（成员含冒险者、offsetDate fallback、showMy 插入位置标注），代码中均已生效。

---

## 确认

Doc3 执行方案的 5 处改动**全部已在代码中正确实现**。与主执行方案（`阶段3_实现代码.md`）合并后，完整覆盖 UI 设计全部 13 项需求。

通过审查。建议执行时关注 D1/D2 标注。
