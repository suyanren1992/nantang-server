# 执行方案五维度审查

> 2026-07-19 · 审查全部 8 份执行方案文档
> 标准：少即是多 / 复用优先 / 需求对齐 / 全局最优 / 长期主义

---

## 一、总体评价

方案整体符合五条标准。发现 3 个需修正的问题和 4 个需标注的注意事项。无阻塞性问题。

---

## 二、逐份审查

### 阶段 1 前置A：信箱系统

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 只做 4 种消息，5 种延后。`lastOpened` 替代逐条已读——复杂度降 90%。不做分类 chip、不做跳转 |
| 复用优先 | ✅ | 复用 `computeInboxMessages()` + `updateInboxBadge()`（mobile-bundle.js）。复用 `.my-topbar` 现有结构。`.my-tabbar` 不动 |
| 需求对齐 | ✅ | tip_received → 送鼓励需求。submitted 聚合 → PC 端信箱完整方案。activity_log → Hub 档案室方案 |
| 全局最优 | ⚠️ | **注意 1：** `updateInboxBadge()` 当前操作 `#inboxDot`（不存在），改为操作顶部 badge。需确认 mobile-bundle.js 中调用点不受影响 |
| 长期主义 | ✅ | `lastOpened` 比 `inbox_seen_ids` 更可维护。activity_log 增量数组。消息实时计算保证数据一致性 |

### 阶段 1 前置B：档案室

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 只做 2 个 Tab，4 个延后。不新建建筑，放在大地书房。不加排序下拉 |
| 复用优先 | ⚠️ | **注意 2：** `toggleArchiveExpand()` 改跳转，与阶段 1 前置A 的工作台入口互不冲突。但两个入口指向同一个 `#overlayArchive`——需确认入口三（工作台 📚 归档文件 → 打开档案室）不会覆盖入口一/二的行为 |
| 需求对齐 | ✅ | 成员目录→Hub 档案室方案。activity_log→Hub 档案室方案。大地书房→用户指定 |
| 全局最优 | ⚠️ | **问题 1：成员渲染函数未明确复用。** 档案室 `renderArchiveMembers()` 和营地成员 Tab（阶段 3）都是「角色分组+头像」。方案未标注共用渲染函数。**建议：** 抽取 `renderMemberGroup(users, opts)` 在两个阶段复用 |
| 长期主义 | ✅ | Tab 预留扩展。数据源独立。 |

### 阶段 1：社区主页与营地窗口

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 不改现有布局，4 项最小改动 |
| 复用优先 | ✅ | `recordTransaction()` / `TX_TYPES` / `getUsers()` 全部复用 |
| 需求对齐 | ✅ | 邀请码弹窗→审查报告。送鼓励→PC 端社区窗口方案。数据源→阶段 2 依赖 |
| 全局最优 | ⚠️ | **注意 3：** `CAMP_MOCK` fallback 需标注清理时机——阶段 2 创营写入 AppData 后，CAMP_MOCK 变成死数据。在方案中加一条「阶段 2 完成后删除 CAMP_MOCK」 |
| 长期主义 | ✅ | `sendEncouragement` 同时写 journal 和 recordTransaction，两个独立数据源 |

### 阶段 2：创营

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 7 步复用同一个 overlay 容器。翻页逻辑统一。草稿对象单一 |
| 复用优先 | ⚠️ | **问题 2：GSAP 未引入。** 方案依赖 GSAP Timeline（创营/启营两个仪式动画），但 `nantang-mobile.html` 中无 GSAP `<script>` 标签。**建议：** 在方案中标注「需引入 GSAP CDN：`<script src="https://cdn.jsdelivr.net/npm/gsap@3.12/dist/gsap.min.js">`」 |
| 需求对齐 | ✅ | 7 步→创营模块完整 UI。动画→三阶段规划 §二。虚位以待→界面总览 |
| 全局最优 | ✅ | 写入 `AppData.shared('camps')`，与阶段 1 的数据源统一一致 |
| 长期主义 | ✅ | 草稿对象可序列化。模板存取用 localStorage。 |

### 阶段 3：营地骨架

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 不新建工作台功能，只加 chip 显示条件。概览 5 卡片作为导航枢纽 |
| 复用优先 | ✅ | 复用 `showMy()` / `toggleProfile()` / `switchMyTab()` / chip 行 |
| 需求对齐 | ✅ | 每个 Tab 有明确参考文档 |
| 全局最优 | ✅ | 任务可见性用 chip 行控制（一处改动，工作台+任务大厅两处生效） |
| 长期主义 | ✅ | Tab 栏预留扩展。管理 Tab 占位。 |

### 阶段 4：营中生活

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 两个 overlay + 流水列表追加 |
| 复用优先 | ✅ | `recordTransaction()` / `setCanteenMenu()` / `getFoodIcon()` 复用已有 |
| 需求对齐 | ✅ | 食堂→任务13a。客栈→任务13b。流水→PC 端对照 §四 |
| 全局最优 | ✅ | 食堂/客栈独立 overlay，不混入营地 6 Tab |
| 长期主义 | ✅ | 客栈申请状态流（⏳→✅/❌）可扩展 |

### 阶段 5：营中管理

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 管理面板 6 区块，每个独立渲染 |
| 复用优先 | ✅ | 复用信箱面板（📬消息）|
| 需求对齐 | ✅ | 6 区块→共建人营地界面。个性化→个性化与信息同步 |
| 全局最优 | ✅ | 概览 Tab 个性化映射规则用纯数据对象，不用条件判断 |
| 长期主义 | ✅ | `CATEGORY_BLOCKS` 映射可扩展 |

### 阶段 6：营后收官

| 标准 | 结论 | 说明 |
|------|------|------|
| 少即是多 | ✅ | 结算/排行/报告三个模块 |
| 复用优先 | ⚠️ | **注意 4：** 同问题 2——GSAP 未引入。敬字亭动画依赖 GSAP |
| 需求对齐 | ✅ | 结算→PC 端对照 §六。排行榜→§七。结营仪式→三阶段规划 §四 |
| 全局最优 | ✅ | 结营触发 `data.archived_periods` 快照，供档案室 📦 往期营期 Tab 使用 |
| 长期主义 | ✅ | 快照结构完整。导出 Markdown 格式可扩展 |

---

## 三、问题汇总

### 需修正（2 个）

| # | 位置 | 问题 | 修正 |
|---|------|------|------|
| **P1** | 阶段 1 前置B §四 + 阶段 3 §六 | 成员目录和营地成员 Tab 都做「角色分组+头像」，未标注共享渲染函数。各自实现会导致重复代码和维护不一致 | 抽取 `renderMemberGroup(users, opts)` 共用函数。在阶段 1 前置B 中实现，阶段 3 中复用 |
| **P2** | 阶段 2 §2.2 + §8.1 · 阶段 6 §3.2 | GSAP 依赖未标注引入方式。项目 `nantang-mobile.html` 中无 GSAP CDN | 在阶段 2 方案中标注：`<script src="https://cdn.jsdelivr.net/npm/gsap@3.12/dist/gsap.min.js"></script>`。阶段 6 复用同一 CDN |

### 需标注（4 个）

| # | 位置 | 注意 | 建议 |
|---|------|------|------|
| **N1** | 阶段 1 前置A §六 | `updateInboxBadge()` 改操作元素后，调用链是否完整 | 编码时确认 mobile-bundle.js 中调用 `updateInboxBadge()` 的时机和 DOM 存在性 |
| **N2** | 阶段 1 前置B §五 | 三个入口指向同一个 `#overlayArchive`，入口三（工作台 → 打开档案室）应在文档中标注和入口一/二的差异（如果有） | 当前无差异则标注「行为完全一致」 |
| **N3** | 阶段 1 §二 | `CAMP_MOCK` fallback 需标注清理时机 | 加一条：「阶段 2 创营上线、AppData 有真实数据后，删除 CAMP_MOCK 引用」 |
| **N4** | 阶段 3 §〇 | 前置条件写「阶段 2 已完成」，但阶段 3 也依赖阶段 1 前置A（信箱面板）和阶段 1（营地入口） | 补全前置条件链 |

---

## 四、确认

整体方案在五条标准下**通过审查**。2 个需修正问题（成员渲染函数复用、GSAP 引入）和 4 个需标注事项不影响方案结构，可在编码前修正。
