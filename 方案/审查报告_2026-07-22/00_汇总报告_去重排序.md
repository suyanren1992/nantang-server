# 审查汇总报告 — 实景游戏移动端代码

> 2026-07-22 · 6 个审查 Agent 并行 · 去重后 40 条发现
> 基于 commit `5b83fc7` (Batch 4 完成后)

---

## 审查 Agent

| # | 角色 | 状态 | 发现数 |
|---|------|------|--------|
| 🎮 | 游戏设计师 | ✅ | 14 |
| ⚙️ | 系统设计师 | ✅ | 19 |
| 💰 | 经济设计师 | ✅ | 11 |
| 🖐️ | UX 设计师 | ✅ | 20 |
| 🧪 | QA 测试 | ✅ | 12 |
| 🎬 | 创意总监 | ✅ | 20 |

---

## 🔴 严重（16 条）

### 经济安全

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 1 | ECON-01 | server/chain_scanner.py:167-169 | 💰🧪 | `chain_scanner._process_log()` 不用 `_get_pool`，`CommunityPool` 行不存在时跳过 `total_issued += amount`。用户获 NT 但等式右端不增——等式破裂 |
| 2 | ECON-02 | server/routes/nt.py:815-835 | 💰 | `POST /api/nt/earn-sync` 无服务端验真。认证用户可发送任意 items 反复调用直到池枯竭（F16 引入） |
| 3 | ECON-03 | server/routes/nt.py:205-208 | 💰 | 转账 CV 通胀：发送方 `max(0, cv-amount)` 截断，但接收方仍获 `int(cv_amount * 0.75)`。发送方 10 CV 转账 100 NT，被截为 0，接收方得 75 CV，凭空创造 90 CV |
| 4 | ECON-04 | server/chain_scanner.py:163 | 🧪 | chain_scanner 更新 `user.nt_balance += amount` 无 `with_for_update()`。同一扫描周期的两个不同交易命中同一用户时丢失更新 |
| 5 | ECON-05 | server/database.py:36-38 | 🧪 | `init_db()` 无唯一约束创建 `CommunityPool`。并发可产生两个池行，后续 `.limit(1)` 只读第一个，第二个池行孤 |
| 6 | ECON-06 | server/routes/nt.py:200-201 | 🧪 | `transfer` 读写 `from_user_obj.nt_balance` 无 `with_for_update()`。两笔并发转账可双双通过余额检查导致透支 |

### 资金流水错标

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 7 | FLOW-01 | server/routes/tasks.py:119 | 💰 | 社区任务创建流水 `from=user.id`，但 NT 来自 `pool.balance`（line 93）。应为 `from=community_pool` |
| 8 | FLOW-02 | server/routes/nt.py:564 | 💰 | 个人任务取消流水 `from=community_pool`，但 NT 来自 `pool.task_escrow`。应为 `from=escrow` |

### 任务状态机

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 9 | TASK-01 | server/routes/nt.py:433-454 | 🎮 | **信任度刷分**：`accept_task` 不禁止 `poster == user.id`。Poster 自领可获 +8 trust（+3 poster verify + +5 assignee verify）零成本 |
| 10 | TASK-02 | server/routes/nt.py:661-665, 513-526 | 🎮 | `escrow_amount=0` 静默孤儿化：`resolve_task` 和 `verify_task(3+reject)` 中 `pool.task_escrow -= 0` 无操作，但创建时注入的托管金永久锁定在池中 |

### 认证与安全

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 11 | AUTH-01 | nantang-mobile/js/api.js:54-71 | ⚙️ | **Token 过期无自动刷新**：`silentRefresh` 已完整实现但整个代码库无调用方。Access token 60 分钟过期后所有 API 静默失败，需手动刷新页面 |
| 12 | AUTH-02 | nantang-mobile/js/core.js:269-270 | ⚙️ | 401 检测用错字段：轮询检查 `srv.detail === 'unauthorized'`，但 `API.request` 返回 `error: '登录过期'`。条件永远不匹配，轮询持续向服务器发 401 |

### 输入验证与数据完整

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 13 | VAL-01 | server/routes/camps.py:39 | 🧪 | `create_camp` 接受裸 `dict`，无 Pydantic 模型。负数 `lodgingNT` × `people × days` → 负 `camp_total` → 污染 `pool.camp_balance` |
| 14 | VAL-02 | server/routes/tasks.py:20 | 🧪 | `reward: int = 5` 无 `le` 上限。社区任务 `reward=999999` 可瞬间耗尽社区池 |
| 15 | DATA-01 | server/routes/camps.py:64-71 + tasks.py:80-121 | 🎮 | **CampTask 与 NTTask 断连**：营地创建产生 `CampTask` 行（status="draft"），但无端点推进到 `NTTask`。营地结算筛选 `status="已结算"` 永远为空 |
| 16 | DATA-02 | nantang-mobile/index.html:536-560 vs nantang-mobile/js/data.js:1-91 | 🎬 | **重复定义互相覆盖**：`TASKS`/`JOURNAL_TYPES`/`NEWBIE_QUESTS`/`ITEM_CATS` 在两处定义。index.html 后加载，静默覆盖 data.js |

---

## 🟡 重要（17 条）

### 服务端竞态/锁

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 17 | RACE-01 | server/routes/nt.py:236-244 | 🧪 | `spend` 扣款 `user.nt_balance -= amount` 无 `with_for_update()` |
| 18 | RACE-02 | server/routes/nt.py:604 | 🧪 | `settle_task` 查询任务无 `with_for_update()`。并发双结算可双双通过状态检查 |

### 经济审计

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 19 | AUDIT-01 | server/routes/nt.py:877-880 | 💰 | `daily_tick` 社区池补填（50 NT/天）无 `_add_ledger`。累计最多 18,250 NT/年不可审计 |
| 20 | AUDIT-02 | server/routes/accommodation.py:74-81 | 💰 | 退房欠费不可回收：余额不足时 `debt` 不清零但 `status=checked_out`，无追讨机制 |
| 21 | AUDIT-03 | server/routes/camps.py:85-87 | 💰 | 营队创建 `_get_pool(db)` 未传 `lock=True` |
| 22 | AUDIT-04 | server/routes/nt.py:523-524 | 💰 | verify_task 3+reject 退款流水 `from=community_pool` 应为 `from=escrow` |

### 数据同步

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 23 | SYNC-01 | server/routes/data.py:329 + server/routes/nt.py:134 | ⚙️ | `sync_all` journal 缺 `discovery_id`、tasks 缺 `escrow_amount`/`settler_id`/`settled_at`/`escrow_amount` |
| 24 | SYNC-02 | nantang-mobile/js/core.js:795-861 | ⚙️ | `_mergeNTSyncData` 和 `_mergeSyncData` 字段映射不一致。`verifier_id`/`verified_at`/`escrow_amount` 在 merge 时被丢弃 |
| 25 | SYNC-03 | server/cron.py:66 + server/routes/nt.py:842 | ⚙️ | cron `tick_daily` 与 endpoint `daily_tick` 共享 `CommunityPool` 但 cron 端无行锁 |

### 客户端

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 26 | UX-01 | nantang-mobile/js/*.js (多处) | 🖐️ | 原生 `confirm()` 弹窗遍布全站（含 10 行公约文本），移动端体验极差。`showConfirm()` 已存在但未使用 |
| 27 | ARCH-01 | nantang-mobile/js/store.js + schema.js + backup.js | 🎬 | 惰性并行数据层：`App.store`/`App.schema`/`App.backup` 与 `AppData` 完全独立，backup.js 备份无人写入的 `camp_data` key |
| 28 | SOV-01 | nantang-mobile/js/auth.js:24 | 🎬 | HTTP 模式下 `saveUsers()` NO-OP，角色变更后 `changeUserRole()` 不持久化，刷新页面丢失 |
| 29 | CLIENT-01 | nantang-mobile/js/nt-core.js:269 | 💰 | 客户端 `cancelTask` 不读 `escrow_amount`，始终按 `reward*slots` 全额退款 |
| 30 | CLIENT-02 | nantang-mobile/js/nt-core.js:246-256 | 🎮 | 客户端 `verifyTask` reject 立即退款 escrow，服务端等 3 次才退款——双模式行为不一致 |
| 31 | ARCH-02 | nantang-mobile/modules/camp-pipeline/ | 🎬 | 三个 TODO 空桩：`camp-budget.js`/`camp-settlement.js`/`camp-reconciliation.js` 只有脚手架 |
| 32 | UX-02 | nantang-mobile/js/app.js:2072,2203,2000 | 🖐️ | visitor 权限提示统一为"入住后可用"但未解释**如何入住** |
| 33 | NET-01 | nantang-mobile/js/api.js:21 | 🧪 | `fetch` 无 `AbortController` timeout，网络挂起时无限等待 |

---

## 🟢 建议（7 条）

| # | ID | 文件 | 来源 | 问题 |
|---|----|------|------|------|
| 34 | TASK-03 | server/models.py:6-14 | 🎮 | `"待提交"` 状态存在于 `TASK_STATUSES` 但无代码路径写入 |
| 35 | CLIENT-03 | nantang-mobile/js/nt-core.js:261-281 | 🎮 | 客户端 `cancelTask` 对 `poster='社区'` 自动注册名为"社区"的幽灵用户 |
| 36 | DEBT-01 | nantang-mobile/js/data.js:537 | 🎬 | `CAMP_MOCK` fallback 应随 Phase 2 完成删除 |
| 37 | DEBT-02 | nantang-mobile/js/nt-core.js:645-668 | 🎬 | Phase C3 `ponytail:` 注释标注"fallback"，但已是正式实现 |
| 38 | DEBT-03 | nantang-mobile/index.html:515 | 🎬 | `schema.js` 注释声明已废弃但仍被 `<script>` 标签加载 |
| 39 | UX-03 | nantang-mobile/css/main.css (多处) | 🖐️ | 部分交互按钮无 `min-height:44px` 声明 |
| 40 | DOC-01 | (无) 项目/实景游戏移动端代码/CLAUDE.md | 🎬 | 项目级 CLAUDE.md 缺失 |

---

## 来源分布

| 来源 | 🔴 | 🟡 | 🟢 | 合计 |
|------|-----|-----|-----|-----|
| 💰 经济 | 3 | 4 | 0 | 7 |
| 🧪 QA | 4 | 3 | 0 | 7 |
| 🎮 游戏设计 | 2 | 0 | 2 | 4 |
| ⚙️ 系统设计 | 2 | 3 | 0 | 5 |
| 🎬 创意总监 | 1 | 3 | 5 | 9 |
| 🖐️ UX | 0 | 3 | 1 | 4 |
| 交叉（多 Agent） | 4 | 1 | 0 | 5 |

> 注：部分发现被多个 Agent 独立识别，表中按主来源归因。交叉行计为去重后的多来源项。
