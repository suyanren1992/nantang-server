# 🎬 创意总监审查报告

> 原始输出 · 2026-07-22 · 发现 20 条

---

## 🔴 严重（7 条）

### 1. HTTP 启动声称"全清旧登录数据"但无清理
{file: `nantang-mobile/index.html`, line: 24-32}

注释说清除旧数据但无实际清理逻辑。file:// → HTTP 切换时 `nt_local_users`、`nt_local_roles`、各种 AppData 缓存仍存在。

### 2. NT earn/spend 在 API token 到达前静默丢失
{file: `nantang-mobile/js/nt-core.js`, line: 43-44}

NT core 在 HTTP 模式跳过 `_saveState()`——对金融主权正确，但页面加载到 API token 到达之间的 NT 操作（earn/spend/createTask）静默丢失。

### 3. HTTP 模式 saveUsers() NO-OP → 角色变更不持久化
{file: `nantang-mobile/js/auth.js`, line: 24}

`saveUsers()` 在 HTTP 模式无操作。`changeUserRole()` 的角色变更仅通过 `AppData.setMe()` 维持，刷新页面消失。

### 4. App.store / App.schema / App.backup 惰性并行数据层
{file: `nantang-mobile/js/store.js` + `schema.js` + `backup.js`}

与 AppData 完全独立、互不通信。`backup.js` 自动触发但备份 `camp_data` key——该 key **从未被实际应用代码写入**。三个文件完全惰性。

### 5. index.html 与 data.js 重复定义互相覆盖
{file: `nantang-mobile/index.html`, line: 536-560 vs `nantang-mobile/js/data.js`, line: 1-91}

`TASKS`、`JOURNAL_TYPES`、`NEWBIE_QUESTS`、`ITEM_CATS`、`ITEM_ICONS`、`MOCK_ITEMS` 在**两处定义**。index.html 后加载，静默覆盖 data.js。修改 data.js 无效——两个真相源。

### 6. 缺失项目级 CLAUDE.md
{file: (不存在) `项目/实景游戏移动端代码/CLAUDE.md`}

无项目特定 Agent 指令——Agent 审查时需要手动指定绝对路径。应包含：项目概览、双模式架构、数据层约定、脚本加载顺序、App.store vs AppData 分割状态。

### 7. CAMP_MOCK fallback 应随 Phase 2 完成删除
{file: `nantang-mobile/js/data.js`, line: 537}

`ponytail: CAMP_MOCK fallback 阶段2创营上线后删除`——Phase 2 已完成。Mock 数据为死代码。

---

## 🟡 重要（8 条）

### 双模式

8. **HTTP 登录时用户列表不缓存** — `core.js:721-729` 每次刷新页面都从服务端重新拉取
9. **autoLogin file:// 路径含死代码** — `core.js:1340` 的 `if (isHTTP)` 在 file:// 路径永远为 false
10. **nt-core.js Phase C3 ponytail 注释过时** — 标注"fallback"但已是正式实现

### 数据主权

11. **AppData 主权边界无文档** — `app-data.js:2` "NT 系统是金融唯一真相源"原则，但 `pendingTransactions`/`pendingVerifications` 为金融邻接数据

### 技术债

12. **Builder 确认 + 任务重分配仍是 ponytail** — `ui-wizard.js:826,866` Phase 3 后应实现
13. **MGMT_DATA 定价硬编码但 `_mlConfig()` 已存在** — `app.js:920` 低价修复——连接两者
14. **Admin bypasses all task claim gates** — `auth.js:208` admin 可绕过所有任务领取门禁——未文档化

### 文件组织

15. **camp-pipeline/ 三个 TODO 空桩** — `camp-budget.js`/`camp-settlement.js`/`camp-reconciliation.js` 仅脚手架
16. **schema.js 注释声明已废弃但仍被 `<script>` 加载** — `index.html:515`
17. **renderTaskCard 统一不完整** — `core.js:359-405` 旧内联渲染模式残留在多处

---

## 🟢 确认正确（5 条）

- `_mergeNTSyncData()` 正确以服务端为权威（余额/CV/XP/角色直接覆盖）✓
- `canUserClaim()`/`canUserSee()` 系统完整且结构良好 ✓
- "A2 过渡完成"注释准确记录迁移 ✓
- `openAdminPanel()` 客户端角色检查 + 服务端 token 验证双重门禁 ✓
- ROLE_CAPABILITIES 矩阵定义完整 ✓
