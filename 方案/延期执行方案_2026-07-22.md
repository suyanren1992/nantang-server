# 延期执行方案

> 2026-07-22 · 13 项 · 4 个时间窗口 · ~7.5h
> 触发条件：即期执行方案 41 项全部完成并验证通过

---

## T1-T6：即期（本次 41 项完成后立即执行 · ~3h）

### T1 — CommunityPool 防多行
**文件**：[server/models.py:74-85](server/models.py#L74)
**改**：加 `singleton = Column(Boolean, default=True, unique=True)`。
**DB 迁移**：`init_db()` 中对已有数据库执行 `ALTER TABLE community_pool ADD COLUMN singleton BOOLEAN DEFAULT 1` + `CREATE UNIQUE INDEX IF NOT EXISTS ...`。
**验证**：`sqlite3 nantang_fresh.db "INSERT INTO community_pool(balance) VALUES(999)"` → UNIQUE constraint failed。

### T2 — 金额端点上限统一
**文件**：[server/routes/nt.py](server/routes/nt.py) · 4 个 Request 模型
**改**：
- `TransferRequest.amount` 已有 `le=10000` ✅
- `EarnSpendRequest.amount` 加 `Field(ge=1, le=50000)`
- `TopUpRequest.amount` 加 `Field(ge=1, le=100000)`
- `DepositIntentRequest.amount` 加 `Field(ge=1, le=100000)`
**验证**：`curl -d '{"amount":50001}' .../spend` → 422 Unprocessable。

### T3 — NTTask 高频列索引
**文件**：[server/database.py init_db()](server/database.py#L23)
**改**：在 `init_db()` 末尾加 4 行：
```sql
CREATE INDEX IF NOT EXISTS idx_nt_tasks_poster ON nt_tasks(poster);
CREATE INDEX IF NOT EXISTS idx_nt_tasks_assignee ON nt_tasks(assignee);
CREATE INDEX IF NOT EXISTS idx_nt_tasks_status ON nt_tasks(status);
CREATE INDEX IF NOT EXISTS idx_nt_tasks_is_system ON nt_tasks(is_system_generated);
```
**验证**：`sqlite3 nantang_fresh.db ".indices nt_tasks"` → 输出 4 个新索引。

### T4 — API.earn 死代码移除
**文件**：[nantang-mobile/js/api.js:100](nantang-mobile/js/api.js#L100)
**改**：函数体替换为 `console.warn('[API] earn deprecated, use /api/nt/verifications/{id}/approve'); return Promise.resolve({ok:false, error:"deprecated"});`。
**验证**：`grep -r "API.earn(" nantang-mobile/js/` → 无调用方。

### T5 — MAX_BEDS 环境变量化
**文件**：[server/routes/accommodation.py:38](server/routes/accommodation.py#L38)
**改**：`MAX_BEDS = int(os.environ.get("MAX_BEDS_PER_ROOM", "6"))`。
**验证**：`MAX_BEDS_PER_ROOM=4 uvicorn main:app` → 第 5 人 checkin → 400。

### T6 — closeOverlay 返回路径
**文件**：[nantang-mobile/js/data.js:534](nantang-mobile/js/data.js#L534)
**改**：新增 `var _overlayStack = []`。打开 overlay 时 push id，关闭时 pop 并打开上一层。`closeOverlay` 默认回到上一级而非总回 villagePage。
**验证**：地图→任务大厅→关闭→回到地图（非村口）。

---

## T7-T8：短期（Phase 2 营地系统前 · ~2h）

### T7 — CampTask 操作端点
**文件**：[server/routes/camps.py](server/routes/camps.py)
**方案**：两选一。
- A（推荐）：废弃 CampTask 表，营地任务直接走 `NTTask(scope='camp')`。最小改动（删除 CampTask 相关代码 ~30 行 + 迁移脚本）。
- B：为 CampTask 复制 NTTask 的 accept/submit/verify/settle/cancel 端点。
**触发条件**：营地功能正式使用前。

### T8 — settle_camp 实际结算
**文件**：[server/routes/camps.py:117-134](server/routes/camps.py#L117)
**改**：遍历该营地所有 `scope='camp'` 的 NTTask，汇总 NT → `pool.camp_balance -= total` → 每人 `nt_balance += total` → 写 ledger。
**验证**：创建营地→添加任务→完成任务→settle_camp→查 camping balance 减少、用户 balance 增加。

---

## T9-T11：中期（D1 词汇统一后 · ~1.5h）

### T9 — data.py 其余 14 端点 Pydantic 化
**文件**：[server/routes/data.py](server/routes/data.py)
**改**：为 journal/activity_log/card_discoveries/canteen_menu/meal_orders/inventory/map_locations/announcements 等建 Pydantic 模型。字段类型、长度限制、必填标记从现有代码中提取。预计 14 个 Model 类 ~80 行。
**验证**：每个端点 curl 一个合法请求→200；curl 一个非法请求→422。

### T10 — 多人任务提交反馈
**文件**：[server/routes/nt.py submit_task](server/routes/nt.py#L628) + [nantang-mobile/js/core.js](nantang-mobile/js/core.js)
**改**：
- 服务端：submit_task 返回 `{ok:true, submitted: N, total: M}`。
- 客户端：卡片展开显示 "2/3 已提交"。
**验证**：3 人任务→2 人提交→卡片显示 "2/3 已提交"。

### T11 — Reviewer hall 审核 UI
**文件**：[nantang-mobile/js/core.js toggleQuestCard](nantang-mobile/js/core.js#L328)
**改**：在 `inHall=true` 分支中，当 `isReviewer` 时渲染 approve/reject 按钮（当前仅在 `inHall=false` 渲染）。
**验证**：审核人打开大厅→待审核任务卡片展开→显示 [通过][退回] 按钮。

---

## T12-T13：远期（UX 迭代 · ~1h）

### T12 — 剩余触控目标补全
**文件**：[nantang-mobile/css/main.css](nantang-mobile/css/main.css)
**改**：`.btn-pri, .btn-outline, .type-tag, .funds-chip` 加 `min-height:44px; display:flex; align-items:center`。与 F17 同模式。
**验证**：Chrome DevTools 检查 4 个选择器 → 高度 ≥44px。

### T13 — _cronActive 传播修复
**文件**：[nantang-mobile/js/core.js _startPolling()](nantang-mobile/js/core.js#L200)
**改**：首次轮询 `/api/data/sync_all` 成功后，若 `data.cron_active` 为 true，设 `window._cronActive = true`。
**验证**：登录后直接进地图（不进村庄）→ `_dailyPoolRefill` 不执行。

---

## 时间线

```
41 项即期完成
  │
  ├──→ T1-T6  (3h)   ← 今天/明天
  │
  ├──→ T7-T8  (2h)   ← Phase 2 营地时
  │
  ├──→ T9-T11 (1.5h) ← D1 词汇统一后
  │
  └──→ T12-T13 (1h)  ← UX 迭代
                         │
                    全部 54 项清零 ✓
```
