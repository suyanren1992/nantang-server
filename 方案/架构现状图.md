---
title: 南塘云村 · 架构现状图
created: 2026-07-26
project: 南塘云村
type: 方案设计
domain: 前端交互
status: 讨论中
phase: 架构
order: 架构.01
summary: 基于 commit d4d1a09 实证。旧架构文档 17 条结论中 6 条过时、1 条错误、10 条属实。双轨已结束，monolithic 已删除。
series: 南塘云村 · 地基四文档
author: 太傅（Claude Code）考古 · 二营验收待
_legacy_tags:
- domain:['架构', '前端', '后端', '南塘云村']
- type:架构
---
# 南塘云村 · 架构现状图

> **锚点**：基于 commit `d4d1a09` 实证 · 2026-07-26
> `git rev-parse HEAD` 实抓

---

## 一、旧文档逐条核验

旧文档：《架构梳理与拆分方案.md》（2026-07-20，双轨时代）。以下逐条标 属实 / 过时 / 错误，附实证。

| # | 旧文档结论 | 现状 | 判 | 实证 |
|---|---|---|---|---|
| 1 | `nantang-mobile.html` 6558 行，生产可用 | 文件已删除。备份 `nantang-mobile-backup-20260717_121928.html`（57KB）留存 | **过时** | `ls nantang-mobile.html` → No such file |
| 2 | `nantang-mobile/` 是「模块骨架，部分功能」 | 已是**唯一前端入口**，index.html 1365 行，加载 18 个 JS 模块 | **过时** | index.html 实 1365 行，含 18 个 `<script>` 标签 |
| 3 | `index.html` 482 行 | 1365 行（3 倍增长），补齐了登录/村口/我的/任务大厅 HTML | **过时** | `wc -l nantang-mobile/index.html` → 1365 |
| 4 | `core.js` 1216 行 | 1798 行（+48%） | **过时** | `wc -l nantang-mobile/js/core.js` → 1798 |
| 5 | `data.js` 611 行 | 715 行（+17%，±30% 阈值内） | **属实** | `wc -l nantang-mobile/js/data.js` → 715 |
| 6 | `ui-cardroom.js` 418 行 | 1368 行（+227%） | **过时** | `wc -l nantang-mobile/js/ui-cardroom.js` → 1368 |
| 7 | `ui-camp.js` 1569 行 | 1587 行（+1%） | **属实** | `wc -l nantang-mobile/js/ui-camp.js` → 1587 |
| 8 | `ui-wizard.js` 977 行 | 1026 行（+5%） | **属实** | `wc -l nantang-mobile/js/ui-wizard.js` → 1026 |
| 9 | `ui-phase4.js` 532 行 | 267 行（-50%） | **过时** | `wc -l nantang-mobile/js/ui-phase4.js` → 267 |
| 10 | `ui-social.js` 443 行 | 447 行（+1%） | **属实** | `wc -l nantang-mobile/js/ui-social.js` → 447 |
| 11 | `ui-archive.js` 418 行 | 422 行（+1%） | **属实** | `wc -l nantang-mobile/js/ui-archive.js` → 422 |
| 12 | `ui-village.js` 113 行 | 112 行（-1%） | **属实** | `wc -l nantang-mobile/js/ui-village.js` → 112 |
| 13 | `api.js` 192 行 | 178 行（-7%） | **属实** | `wc -l nantang-mobile/js/api.js` → 178 |
| 14 | 「卡片室模块没有被任何页面加载和使用」 | index.html:529 `ui-cardroom.js` 已被加载 | **错误** | `grep ui-cardroom nantang-mobile/index.html` 命中 L529 |
| 15 | `core/eventbus.js` 25 行 | 不存在于磁盘，不在 index.html 加载列表 | **属实** | `ls nantang-mobile/core/eventbus.js` → 无；grep index.html 无命中 |
| 16 | 双轨并存（monolithic + 模块骨架） | monolithic 已删除，只剩模块版 | **过时** | L1+L2 证实 |
| 17 | 目标结构含 0-schema/1-login/2-quest-hall 等前缀重命名 | 未实施。文件名保持原始命名（core/app/auth/ui-*） | **属实** | 磁盘上无任何 0-/1-/2- 前缀文件 |

**汇总**：17 条结论中 **10 属实 · 6 过时 · 1 错误**。旧文档已丧失作为架构依据的有效性。

---

## 二、真实架构图（基于 commit d4d1a09 实证）

### 2.1 前端：nantang-mobile/ — 18 模块加载顺序与职责

```
nantang-mobile/index.html (1365行)  ← 唯一入口，加载以下 18 模块：

  ┌─────────────────────────────────────────────────────────────┐
  │ 基础设施层（先加载）                                          │
  │ ├── js/utils.js         (190行)  工具函数                    │
  │ ├── js/mobile-bundle.js (299行)  移动端适配+adminNames       │
  │ ├── js/nt.js            (236行)  NT 经济客户端               │
  │ ├── js/auth.js          (428行)  登录/注册/鉴权              │
  │ └── js/seed-test-data.js( 10行)  测试种子数据                │
  ├─────────────────────────────────────────────────────────────┤
  │ 数据与引擎层                                                  │
  │ ├── js/nt-core.js       (768行)  NT 结算引擎核心             │
  │ ├── js/app-data.js      (699行)  统一数据层（AppData）       │
  │ ├── js/api.js           (178行)  API 适配层                  │
  │ └── js/data.js          (715行)  用户数据/状态管理           │
  ├─────────────────────────────────────────────────────────────┤
  │ 核心 UI 层                                                    │
  │ └── js/core.js          (1798行) 核心 UI 函数（最大模块）     │
  ├─────────────────────────────────────────────────────────────┤
  │ 功能模块层（UI 系列）                                          │
  │ ├── js/ui-village.js    (112行)  营地入口                     │
  │ ├── js/ui-camp.js       (1587行) 营地全部功能                 │
  │ ├── js/ui-wizard.js     (1026行) 7步创营向导                  │
  │ ├── js/ui-phase4.js     (267行)  客栈+食堂                    │
  │ ├── js/ui-social.js     (447行)  团队动态 feed                │
  │ ├── js/ui-archive.js    (422行)  档案室+鼓励                  │
  │ └── js/ui-cardroom.js   (1368行) 卡片室「猜谁在干活」         │
  ├─────────────────────────────────────────────────────────────┤
  │ 应用入口层（最后加载）                                         │
  │ └── js/app.js           (2715行) 应用主逻辑+页面路由+所有overlay│
  └─────────────────────────────────────────────────────────────┘

总行数: 13,265 行 JS + 1,365 行 HTML + 552 行 CSS = 15,182 行前端代码
```

### 2.2 后端：server/ — FastAPI 路由与核心文件

```
server/main.py        (109行)  FastAPI 入口，lifespan，静态文件挂载
server/database.py    (144行)  数据库连接，SQLAlchemy async session
server/models.py      (290行)  数据模型：User/NTLedger/NTTask/CardDiscovery/Verification/...

server/routes/
├── auth.py           (189行)  注册/登录/鉴权
├── nt.py            (1053行)  NT 经济核心：任务创建/认领/提交/审核/提现/校核（最大路由）
├── data.py           (513行)  sync 同步 / presence / 地图数据 / 公约同步
├── camps.py          (221行)  营地创建/管理/结算
├── tasks.py          (187行)  任务大厅 list/review
├── admin.py           (87行)  管理后台：提现审批/用户管理
└── accommodation.py  (166行)  住宿：入住/退房/费用

总路由: 2,417 行
核心: 543 行
```

### 2.3 前端→后端调用对应表

| 前端模块 | 调用的后端路由 | 说明 |
|---|---|---|
| `auth.js` | `routes/auth.py` | 注册/登录 |
| `nt-core.js` + `nt.js` | `routes/nt.py` | NT 记账/任务/校核/提现 |
| `api.js` | `routes/data.py` | sync 同步/presence/公约 |
| `ui-camp.js` + `ui-wizard.js` | `routes/camps.py` | 营地 CRUD |
| `app.js`（任务大厅） | `routes/tasks.py` | 任务列表/审核 |
| `app.js`（我的页） | `routes/admin.py` | 提现审批 |
| `ui-phase4.js` | `routes/accommodation.py` | 住宿 |

### 2.4 旧文件存废

| 文件 | 状态 | 处置建议 |
|---|---|---|
| `nantang-mobile.html` | **已删除** | — |
| `nantang-mobile-backup-20260717_121928.html` | 备份留存（57KB） | D-21 墓碑清理时归档 |
| `nantang-mobile-test.html` | 测试用（93KB） | D-21 墓碑清理时评估 |

---

## 三、拆分差距清单

现状 vs 旧方案目标 vs 补课 13/16 章架构规范：

| 差距 | 旧方案目标 | 现状 | 补课要求 | 优先级 |
|---|---|---|---|---|
| 无模块接口约定 | 每个模块暴露 `init/open/close` | 全局函数直接调用，无统一接口 | `13`：组件复用规则 | P1 |
| EventBus 不存在 | `core/eventbus.js` 25 行 | 磁盘上无此文件，未被加载 | `13`：模块边界 | P1 |
| 目标前缀重命名未执行 | `0-schema/1-login/2-quest-hall` | 文件名保持原始命名 | `16`：目录责任 | P2 |
| core.js 膨胀 48% | 目标 1216 行 | 1798 行（+582 行，6 天内） | `13`：模块边界「一个文件只干一件事」 | P1 |
| app.js 无职责说明 | 未规划此文件 | 2715 行——最大文件，含页面路由+所有 overlay+业务逻辑 | `16`：请求入口/业务/数据访问分层 | P0 |
| 无 CSS 模块化 | 单文件 main.css 552 行 | 仍为单文件 | `13`：样式系统 | P2 |
| 无设计 Token 文档 | DESIGN.md 已写但未接入代码 | 颜色/间距仍硬编码分散在各处 | `13`：设计 Token「所有颜色从统一 Token 取」 | P2 |

**P0 = 阻塞业务开发 · P1 = 下次重构优先 · P2 = 远期整理**

> 此清单是④架构设计文档的地基 + 将来系统性整理大卡的发卡依据。

---

> **太傅注**：对应补课 `13`（前端骨架——模块边界与组件复用）+ `16`（后端骨架——目录责任与分层）。
> 人话原理：文档不随码走 = 三个月后又是一份没人看的旧方案。所以 E-1 要求产出 `arch_check.py`——让机器替你盯着，代码变了图没变就亮灯。
