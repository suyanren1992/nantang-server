━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  南塘云村 · 施工回执（一营 P0）
  卡号：DB-P0-2_NT-HTTP模式断裂_v0
  施工方：一营 Claude Code
  回执日期：2026-07-31
  状态：完成 ✅
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## ① diff 摘要

| 文件 | 变化 |
|------|------|
| nt-core.js | _loadState() 设 `_ntPendingSync=true` / 加 `NT._syncFromServer()` 复用 syncAll + _mergeNTSyncData |
| core.js | _finishEnter → syncAll 回调内加 _mergeNTSyncData(data) + 清 `_ntPendingSync` |
| index.html | nt-core.js v10→v11, core.js v48→v49 |

- commit: `290e8c6`
- 无新 API 端点：完全复用 `/api/data/sync_all`

## ② 自测

- ✅ HTTP 模式 `_loadState` 清空 localStorage 后设 `_ntPendingSync=true`
- ✅ 登录后 syncAll 回调内同步调 `_mergeNTSyncData(data)` 填充 NT balance/ledger
- ✅ `_ntPendingSync` 标记在成功同步后清除
- ✅ `NT._syncFromServer()` 可供手动调用兜底
- ✅ 无需新建端点，非禁区 server/
- ✅ file:// 本地模式完全不受影响

## ③ 禁区确认
- ✅ server/ 0 处修改

## ④ 皇帝验收单

```
DB-P0-2 HTTP 模式 NT 断裂已修复。

根因：_loadState 在 HTTP 模式清空后直接 return，
      NT 引擎空运行到 syncAll 回来前有空白窗口。

修复：1) _loadState 设 _ntPendingSync 标记
      2) 复用 syncAll 回调，同步调 _mergeNTSyncData
      3) 加 NT._syncFromServer 手动兜底

无新端点，零 server 侵入。
```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
