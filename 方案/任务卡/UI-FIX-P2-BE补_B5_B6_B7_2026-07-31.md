━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  南塘云村 · 任务派发单（补卡）
  卡号：UI-FIX-P2-BE补_B5_B6_B7
  阶段：Phase 1 补卡 · BE 端点补全
  施工方：二营 Qoder（后端 BE）
  验收方：丞相 Codex
  立卡：丞相 Codex 2026-07-31 13:00
  法源：UI-FIX-P2-BE 完工后 B5-B8 检查结果
  优先级：P0（3 个端点全缺，FE 强依赖）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【触发】
  二营 B5-B8 检查报告：
  - B5 档案室端点 ❌ 不存在
  - B6 田间端点 ❌ 不存在
  - B7 设置端点 ❌ 不存在
  - B8 校核端点 ✅ 存在

  一营 33 项修缮中，F1（时间线跳转）+ F2（全貌页设置）+ H2（校核室）+ I1（田间界面）= 4 项强依赖这 3 个端点。

【阵地】server/    【禁区】nantang-mobile/

【施工内容 · 3 张端点补卡】

━━━━━━━━━━━━━━━━━━━━━━━━━━
B5补. 档案室端点（GET /api/archive/items）
━━━━━━━━━━━━━━━━━━━━━━━━━━
  路由：server/routes/archive.py
  行为：
    - 拉当前用户可访问的归档文件
    - 字段：id, title, content, category, created_at, author
    - 权限：Depends(auth)
    - 分类：profile/contract/log/event
  端点：
    - GET /api/archive/items?category=xxx
    - GET /api/archive/items/:id（详情）
  测试：3 用例

  数据：复用现有 announcements / activity_log / 公约表
  无需新建表。

━━━━━━━━━━━━━━━━━━━━━━━━━━
B6补. 田间端点（GET /api/fields）
━━━━━━━━━━━━━━━━━━━━━━━━━━
  路由：server/routes/fields.py
  行为：
    - 拉所有地块（plots）
    - 字段：id, plot_name, crop_name, planted_at, harvest_at, stage, health
    - 权限：Depends(auth)
  端点：
    - GET /api/fields（列表）
    - GET /api/fields/:id（详情）
    - POST /api/fields/:id/harvest（收割）
    - POST /api/fields/:id/water（浇水）
    - POST /api/fields/:id/fertilize（施肥）
  测试：5 用例
  备注：复用现有 field 实体（看现有是否有，没有新建）

━━━━━━━━━━━━━━━━━━━━━━━━━━
B7补. 设置端点（GET /api/users/me/settings）
━━━━━━━━━━━━━━━━━━━━━━━━━━
  路由：server/routes/user_settings.py
  行为：
    - 拉当前用户设置
    - 字段：notification (bool), theme (light/dark), language (zh-CN)
    - 权限：Depends(auth)
  端点：
    - GET /api/users/me/settings
    - PATCH /api/users/me/settings（更新）
  测试：4 用例
  备注：如现有 users 表无 settings 字段，需 ALTER TABLE 加 JSON 字段

【施工规则】
  - 1 营 1 窗 串行 B5补 → B6补 → B7补
  - 走 server/ 阵地
  - 禁区 nantang-mobile/
  - **只 commit 不 push**（等 FE 一起推）
  - pytest 全绿（基线 183 + 新增 ≥ 12 用例）
  - OpenAPI 契约更新：方案/契约/openapi_2026-07-31.yaml
  - ?v= 不适用

【执行时间线】
  - 0min：开工
  - 1h：B5补 端点出 → 一营可接 F1（时间线跳转）
  - 2h：B6补 端点出 → 一营可接 I1（田间界面）
  - 3h：B7补 端点出 → 一营可接 F2（设置按钮）
  - 一营 H2（校核室）已可用 B8 端点，**第二波立刻可接**

【回执落盘】
  方案/任务卡/UI-FIX-P2-BE补_回执_二营_2026-07-31.md

【太傅注 3 行】
  - **3 个端点全缺是常态**——前端卡片化跑在后端前面，不算事故
  - **一营强依赖**——B5补 / B6补 / B7补 出前 1 营 F1/F2/I1 阻塞
  - **H2 不依赖**——B8 校核端点已存在，1 营 H2 立刻可接
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
