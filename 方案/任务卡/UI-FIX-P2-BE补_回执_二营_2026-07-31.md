---
created: '2026-07-31'
project: 南塘云村
type: 施工回执
domain: 后端补端点
task_status: 已完工
status: 待验收
series: UI-FIX-P2
---

# UI-FIX-P2-BE补 施工回执 · 二营 Qoder · 2026-07-31

## 🎯 一句话结论

B5/B6/B7 三路由 9 端点 + FieldPlot 表 + User.user_settings 列全部落地，207 passed / 0 failed / 零回归。

## 施工范围

| 子卡 | 端点 | 文件 | 测试 |
|------|------|------|------|
| B5补 | GET /api/archive/items | routes/archive.py (159L) | test_archive.py (3 测) |
| B5补 | GET /api/archive/items/{id} | ↑ | ↑ |
| B6补 | GET /api/fields | routes/fields.py (125L) | test_fields.py (5 测) |
| B6补 | GET /api/fields/{plot_id} | ↑ | ↑ |
| B6补 | POST /api/fields/{plot_id}/harvest | ↑ | ↑ |
| B6补 | POST /api/fields/{plot_id}/water | ↑ | ↑ |
| B6补 | POST /api/fields/{plot_id}/fertilize | ↑ | ↑ |
| B7补 | GET /api/users/me/settings | routes/user_settings.py (82L) | test_user_settings.py (4 测) |
| B7补 | PATCH /api/users/me/settings | ↑ | ↑ |

## 数据层变更

| 变更 | 文件 | 说明 |
|------|------|------|
| FieldPlot 表 | models.py (+43L) | 15 字段：id/plot_name/crop_name/stage/health/… |
| User.user_settings | models.py (+2L) | Text JSON 列（notification/theme/language） |
| idx_field_plots_stage | database.py | CREATE INDEX IF NOT EXISTS |
| user_settings 迁移 | database.py | ALTER TABLE users ADD COLUMN user_settings TEXT |

## 验证

```
cd server && python -m pytest tests/ -x -q
→ 207 passed, 8 skipped, 1911 warnings in 113.72s
```

基线 183 → 新增 12 → 目标 195 → 实际 **207 passed**（含前卡附带的 test_new_user_tasks + test_clean_weekly）。

## OpenAPI 契约更新

`方案/契约/openapi_2026-07-31.yaml` 已更新：
- B5/B6/B7 从注释占位替换为正式端点定义
- 新增 schemas：ArchiveItem / ArchiveListRes / ArchiveDetailRes / FieldPlot / FieldListRes / FieldDetailRes / UserSettings / PatchSettingsReq / SettingsRes
- 总 11 paths，YAML 语法验证通过

## 设计要点

### B5 档案室（复合 ID + 多表聚合）
- 不新建 ArchiveItem 表，聚合 Journal / ActivityLog / Announcement / CovenantSignature
- 复合 ID 格式 `{source}_{db_id}`（如 `journal_123`）
- category 过滤：log → journal + activity_log / event → announcement / contract → covenant_signature

### B6 田间（状态机 + 动作端点）
- FieldPlot 表 15 字段，stage 枚举：休耕/播种/生长/成熟/收割
- harvest：成熟 → 休耕（清空 crop_name / planted_at 等）
- water/fertilize：更新 timestamp + 健康恢复（缺水/缺肥 → 健康）
- 休耕地块浇水/施肥 → 400

### B7 设置（JSON 列 + 默认值补全）
- User.user_settings 存 JSON（不新建 UserSettings 表）
- 默认值：{notification: true, theme: "light", language: "zh-CN"}
- PATCH 局部更新（None = 不改）
- theme 枚举 light/dark，language 枚举 zh-CN/en

## 修复记录

| 问题 | 修复 |
|------|------|
| test_fields._seed_plots() 幂等缺陷 | 加"先查后插"逻辑，已存在则跳过 |

## Commit

```
97fdf79 feat(UI-FIX-P2-BE补): B5档案室+B6田间+B7设置 3路由9端点+12测 二营
```

10 files changed, 1123 insertions(+), 39 deletions(-)

## 禁区确认

- ✅ server/ 阵地
- ❌ nantang-mobile/ 未触碰
- ❌ server/routes/data.py 未触碰

## 🤝 请定夺

1. 无阻塞项。等丞相验收。

## 📎 附件

- OpenAPI 契约：`方案/契约/openapi_2026-07-31.yaml`
- 任务卡：`方案/任务卡/UI-FIX-P2-BE补_B5_B6_B7_2026-07-31.md`

---

**太傅注**：

> 多表聚合优于新建归档表——复用现有数据源，零迁移成本，前端拿到的统一 schema 足够渲染。
> 测试隔离是 session-scoped fixture 的永恒课题：任何 seed 函数必须幂等（先查后插），否则跨文件跑必炸。
> JSON 列存设置是轻量方案——不值得为 3 个字段开一张表，_parse_settings() 补全默认值足矣。

**下一步建议：丞相验收通过后，一营按更新后的 OpenAPI 契约接入前端。**
