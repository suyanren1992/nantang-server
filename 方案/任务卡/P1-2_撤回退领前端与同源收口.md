---
created: '2026-07-27'
project: 南塘云村
type: 任务卡
domain: 前端交互
status: 已发卡
---
# P1-2 撤回退领前端 + T5 门牌 + C2 提醒 + R1/R2 同源收口（一营 · 前端中卡）🔴

> 来源：实测议单 T4/T5/C2 + T4T5C2 二营情报回执（00e51a4）+ P0-1 二营验收回执 R1/R2（dacfc3f）
> 施工：Claude Code（一营）｜验收：豆包 Codex（二营）
> 议定（砚仁 2026-07-27 16:51 + 17:15 三补丁 + 提醒规范）：依赖 P1-1 服务端端点，可并行开发、验收时联调

## 施工内容（五件）

1. **T4 撤回/退领前端**（对接 P1-1 端点）：
   - 我发布的任务：无人领 → 「撤回」按钮 → 成功 toast 🟡「已撤回到草稿箱」；
     已领未提交 → 「申请撤回」→ toast「已申请，等管理员处理」；已提交 → 不显示撤回入口
   - 我领取的任务：「取消认领」按钮 → 成功 toast「已取消认领，任务已回大厅」
   - 管理员：撤回申请列表可见 + 批准/拒绝按钮
2. **T5 门牌统一**：`ui-camp.js:983` `'pending_review'` → `'待审核'`（与 data.js:345/347 写入对齐）；
   `ui-social.js:211` 同查同步改；审核入口补 **admin 兜底可见**（core.js:508-510，现仅 isReviewer）。
3. **C2 提醒**：校核 429 捕获 → 人话 toast「你刚为这位成员作过证，1 小时后再来」（提醒规范🔴拒绝必出声）；
   顺手核：其他「点了没反应」的静默拒绝若发现，列回执报丞相（不擅自扩修）。
4. **R1 同源**：`ui-camp.js:1039` 营地管理页池余额 → 切 `AppData._data._poolBalance`（照 P0-1 村口池卡修法）。
5. **R2 同源**：`ui-archive.js:281` 档案页余额 → 切服务端权威（照 P0-1 资料页修法）。

## 爆炸半径（四答）
- 改几个文件：≤5（core.js / data.js / ui-camp.js / ui-social.js / ui-cardroom.js / ui-archive.js 中实际触及者）
- 影响功能：任务撤回退领、待审核列表、校核提醒、两处余额显示
- 破坏性变更：依赖 P1-1 新端点；旧流程不变
- 回滚：`git revert` 本 commit 一键

## 铁律
- 修 js 同 commit 升 index.html `?v=`；只 commit 不 push；只加具名文件禁 `git add -A`
- 不碰服务端；提醒规范执行「拒绝必出声，钱动必出声，成功可轻声」
- deploy_check 全绿；回执独立落盘 `方案/任务卡/` 随 commit；末尾太傅注三行内

## 判据（验收方实跑贴证据）
1. 无人领撤回 → 任务进草稿箱 + toast 告知 + 余额退回
2. 已领任务撤回入口变「申请撤回」；管理员可见申请并能批准/拒绝
3. 取消认领 → 任务回大厅 + toast；管理员日志可见
4. 营地待审核列表能查出 doSubmit 提交的任务（门牌对齐）；admin 可见审核按钮
5. 同一 doer 1h 内二次校核 → 人话 toast，不再静默回滚
6. 营地池余额、档案页余额与顶栏一致
