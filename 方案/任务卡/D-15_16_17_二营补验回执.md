---
created: '2026-07-29'
project: 南塘云村
type: 验收回执
task_status: 二营补验通过_PASS
domain: 混合（公约闭环/data.py/行锁）
status: 待副署
卡面: 方案/任务卡/D-15.md · D-16.md · D-17.md
验收对象commit: 5602a30（D-15+D-16）· 420caf8（D-17）
派工: 批次2 第6条（验收回执补账）
验收营: 豆包 Codex（二营）
---
# D-15 / D-16 / D-17 补验回执

## 🎯 裁决：三卡全 PASS（补账验收）

批次2 第6条：确认 5602a30/420caf8 验收回执缺失 → 补验。三卡九判据真跑/实核全 PASS。

---

## D-15 社区公约版本化闭环（施工 5602a30 · 一营跨端）
> 三件套：提案入口 + 待校核展示 + 服务端同步

| 判据 | 结论 | 证据 |
|------|------|------|
| 1 管理员发提案→sync_shared 有 config_changes | ✅ | 真跑：POST sync_shared {pendingConfigChanges:[cc1]} 200→GET sync_all 回读 cc1（data.py:432-437,515-516,527） |
| 2 两人校核→apply+configHistory 落一条 | ✅ | 真跑：POST configHistory→GET 回读 cc1 appliedAt/confirms（data.py:438-443,517-518,528） |
| 3 overlay「修改记录」段渲染 | ✅ | 前端 _openConfigProposal（app.js:520）+提案按钮（app.js:514）+verifyConfigChange（app.js:551）在位 |
| 4 换设备重登待校核/历史仍在 | ✅ | 真跑：新登录 token pending=1条 history=1条（服务端权威 MapLocation 持久） |
```
[D15-1] POST sync_shared 200 → GET pendingConfigChanges=[{id:cc1,field:pricing.dorm101,from:20,to:25,...}] PASS
[D15-2] configHistory 回读=[{id:cc1,to:25,appliedAt:2026-07-29,confirms:[u2,u3]}] PASS
[D15-4] 换设备重登 pending=1 history=1 PASS(服务端权威)
```

## D-16 data.py 阵地查漏（施工 5602a30 · 一营小卡）
> 两项：LIKE 通配符第三处 + card_discoveries id 幂等

| 判据 | 结论 | 证据 |
|------|------|------|
| 1 含%用户名 sync_all 不返全量 | ✅ | 源码实核：sync_all `_uid = user.id.replace('%','').replace('_','')`（data.py:452）→ LIKE 用 _uid（:455），照 D-4 同修 |
| 2 同 id 连续两 POST→库只一条 | ✅ | 真跑：两次 POST /api/data/card_discoveries id=disc_fixed → 库内 1 条（add_card_discovery 幂等去重 data.py:153-157） |
| 3 不带 id→正常生成 | ✅ | 真跑：生成 id=disc_1785228332.138538（向后兼容） |
```
[D16-1] sync_all LIKE sanitize (%和_剔除) PASS
[D16-2] 同id两POST → 库内条数=1 rc=200/200 PASS
[D16-3] 不带id → 生成 disc_1785228332.138538 PASS
```

## D-17 行锁查漏补缺（施工 420caf8 · 二营小卡）
> 两项：populate_existing 补齐 + verify_task poster 锁

| 判据 | 结论 | 证据 |
|------|------|------|
| 1 每处 with_for_update 都有 populate_existing | ✅（卡面阵地） | 实核：**nt.py 26 处 + tasks.py 8 处全部 ✓POP**（贴表见下） |
| 2 并发 unclaimed 退还 poster 只加一次 | ✅ | 已由 K-2 真 PG 三锁集成测试覆盖（ee971b2：3 passed/51.68s，含 D-5/D-17/D-26 首验） |
| 3 全量回归 D-5 用例不复发 | ✅ | pytest 47 passed 3 skipped（SQLite 层）；PG 锁层 K-2 独测全绿 |

**判据1 grep 实核**（server/routes/*.py 每处 with_for_update）：
```
nt.py:  208,215,259,316,346,436,523,549,578,589,620,639,663,678,709,729,751,776,793,825,876,888,914,995,1034 → 全 ✓POP（25处）
tasks.py: 131,188,198,240,267,302,331,364 → 全 ✓POP（8处）
```
> **范围外两处（另评，非 D-17 缺陷）**：
> - `covenant.py:157`（G-1 签署 select(User).with_for_update() 无 populate_existing）——G-1 新代码，D-17 施工时尚不存在。**建议挂账**：G-1 签署发 NT 路径同 D-5 类隐患，宜补 populate_existing。
> - `accommodation.py:94`（checkin COUNT 聚合 with_for_update）——聚合查询非身份映射，populate_existing 无意义，无需改。

## 结论
- D-15/D-16/D-17 三卡验收回执补齐，九判据全 PASS。
- **返修清单（不自己动手，列给一营/丞相定夺）**：covenant.py:157 建议补 populate_existing（D-17 同类隐患，G-1 遗留，挂账候选）。

---
> **太傅注**：①D-15 用 MapLocation 当 KV 存 config_changes/config_history，走 sync 双向同步＝「服务端权威+多端最终一致」，换设备不丢提案。②D-16 LIKE 通配符注入：用户名含 `%`/`_` 会被 SQL LIKE 当通配符匹配全量，剔除两字符即堵（补课：LIKE ESCAPE 或字符白名单）。③D-17 populate_existing 是 SQLAlchemy 行锁的「必配件」——只 with_for_update 不 populate_existing，ORM 会返回 session 缓存的陈旧对象，锁形同虚设（安慰剂锁）。
