# 任务卡 A-7：校核确认后奖励 NT 没到账（排查+修复）

> 施工方：Kimi Code · 验收方：Kimi Work · 先排查后动手，排查结论不写清楚不准改代码

## 问题现象
校核室里点"✅ 确认 +N NT"，操作看似成功，但劳动者的 NT 余额没有增加。

## 已知线索（Kimi Work 初步扫过的）
- 确认入口：`nantang-mobile/js/ui-cardroom.js` 约 496/537 行，调 `AppData.verifyAction(vId, CURRENT_USER, true)`
- 线索仅此。链路中段（AppData.verifyAction 内部、NT 入账、服务器同步）**由你排查**

## 第 1 步：排查（只读不改）
顺着链路逐环确认，找出断在哪一环：
1. `AppData.verifyAction`（app-data.js）确认分支做了什么：状态改 verified 了吗？调 NT.earn 了吗？调服务器了吗？
2. NT 入账环节：本地 NT.earn / 服务器 `/api/nt/...` 哪个是权威？确认后余额刷新靠什么（syncAll？手动刷新？）
3. 服务器端：`server/routes/` 里对应的 verify/校核端点，是否真的给 doer 加余额、写账本
4. 前端余额显示：确认成功后有没有触发余额重取，还是其实到账了但界面没刷新

把结论写进 `BUG_TRACKER.md`：**断点在第几环、证据（文件:行号）、修法**，然后才能进入第 2 步。

## 第 2 步：修复
- 按排查结论修，改动最小化，只修断点那一环
- 如果断点多于一环，在 BUG_TRACKER.md 里列清单，逐环单独 commit
- 顺手核查 A-9 相关：确认成功后有没有把这条记录写进档案室（archive）数据源；如果没有，**先只记录不写代码**，归档设计另有安排

## 验收
- 上报一条劳动 → 另一账号校核确认 → 劳动者余额增加 N NT → 界面刷新可见 → 刷新页面后余额仍在（服务器持久化）
- BUG_TRACKER.md 有完整排查记录
- 每环修复单独 commit，message 格式：`fix(A-7): <断点简述>`
- **不 push**，交给砚仁冒烟后统一 push

## 纪律
- 心脏文件（app-data.js / nt-core.js / api.js）改动要克制：只动断点相关函数，不顺手重构
- 排查阶段不许改任何代码
