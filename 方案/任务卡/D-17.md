# D-17 行锁查漏补缺（二营小卡）

> 来源：D-11 勘察额外观察 2、3。nt.py / tasks.py 属二营阵地。
> 施工：豆包 Codex｜验收：Claude Code

## 施工内容（两项）

1. **populate_existing 补齐**：`nt.py` spend（:259）、transfer、`tasks.py` create_task（:107）
   已有 `with_for_update()` 但无 populate_existing——与 D-5 修复前同样的身份映射陈旧安慰剂问题。
   统一补 `.execution_options(populate_existing=True)`
2. **verify_task poster 锁**：poster 余额突变路径（unclaimed 退还 / reject 3 次自动取消 / 争议仲裁 poster 份额）
   未锁 poster——补 `with_for_update()` + populate_existing，照 D-5 verify_task assignee 修法

## 判据（验收方实跑）

1. `grep -n "with_for_update" server/routes/*.py` 每一处都有 populate_existing（贴输出）
2. 最小并发脚本：两个请求同时触发 unclaimed 退还 → poster 余额只加一次（实跑贴结果）
3. 全量回归：既有 D-5 并发用例不复发的
