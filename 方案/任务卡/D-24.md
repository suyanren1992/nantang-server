# D-24 · index.html 版本号漏标补齐

- **档位**：小卡偏中（单阵地单文件，但碰缓存命门）→ 按中卡四步直道
- **阵地**：一营（`nantang-mobile/index.html`）
- **来源**：D-22 部署总检 `server/scripts/deploy_check.py` 实跑发现——?v= 检 FAIL，6 个 js 文件在 index.html 漏标版本号（Codex 施工 S2 时实跑发现，commit 704b57b 验收记录内）
- **定档人**：丞相 2026-07-26

## 施工内容

1. 跑 `python server/scripts/deploy_check.py`，取 ?v= 检的 FAIL 清单（哪 6 个 js）
2. 在 `index.html` 给这些 js 引用补上 `?v=` 版本号，与当前最新版本号对齐
3. 顺查 css 引用是否同漏

## 铁律提醒

这本就是「改 js/css 必升 ?v=」铁律的漏网之鱼——修好后等于给铁律补了牙。

## 验收（二营）

- `python server/scripts/deploy_check.py` ?v= 项 PASS
- grep `index.html` 全部 `.js` / `.css` 引用均带 `?v=`
- 回执末尾附「太傅注」：为什么缓存版本号是移动端 Web 的命门（对应补课部署章节）

## 影响面声明（施工方填）

- 改了哪几个文件 / 碰没碰资金路径或权限 / 回滚怎么滚
