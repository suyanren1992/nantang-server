---
created: '2026-07-27'
project: 南塘云村
type: 回执
task_status: 二营验收通过
domain: 前端交互
status: 讨论中
---

# P0-1 二营验收回执（四关全绿 · 实测真跑贴输出）

> 卡面：[[方案/任务卡/P0-1_任务双创建与三源同源.md]]
> 施工：Claude Code（一营）commit 9775ce1（未 push）
> 验收：豆包 Codex（二营）｜背景对照：P0钱账疑团情报回执（6e2fa81）
> 结论：**四关通过，验收合格**。发现 2 处遗留同源病（一营已列、非本卡范围）+ 1 处 publishDraft 旁链说明，列返修/待议清单，不擅自修。

---

## ① 逻辑关（diff 对照卡面四件套）✅

| 卡面要求 | 实际 diff | 判定 |
|----------|-----------|------|
| doPublish else 删 POST，srvId 落 addTask 回调 | core.js:710-714 else 分支已删 `API.syncTask` 整块，仅留 UI 收尾；srvId 写回落在 app-data.js:128 addTask 回调 | ✅ |
| addTask 回调不再建 tasks[srvId] 影子键 | app-data.js:128 删 `self._data.tasks[srvId]=task`，仅留 `_srvId`/`_ntTaskId`+`_saveShared()`；服务端拒绝回滚保留(:129) | ✅ |
| _mergeSyncData name=t.id 改 t.title | core.js:932 `name:t.title || '未命名任务'`，取不到标题落兜底名，不再拿 id 尾号当名字 | ✅ |
| 余额三处切服务端 _serverBalance | 资料页 core.js:1391 / 充值 core.js:1444 / 提现 core.js:1470 三处均 `AppData._data._serverBalance != null ? … : 本地`；缓存写入点 refreshUserUI core.js:1193 | ✅ |
| 村口池卡切 _poolBalance | app.js:608 `AppData._data._poolBalance != null ? … : NT.getCommunityPool()` | ✅ |

**fallback 会不会又读回本地 NT？** 会——但仅当服务端值取不到（`null`/未同步）时降级，属离线兜底设计，HTTP 在线态永远优先服务端权威值，与卡面「不得读被清空的本地 NT」在正常链路一致。✅

**grep 同源普查（NT.getUser( / NT.getCommunityPool( 全前端）**：
- `_poolBalance` 数据源链路核验：`/api/data/sync_all`（data.py:508 返 pool_balance）→ `_mergeSyncData`（core.js:925-926 写 _poolBalance）→ app.js:608 读。链路闭合 ✅
- `_serverBalance` 链路：`refreshUserUI` 拉 `/api/nt/balance`（core.js:1190）→ 存 _serverBalance（:1193）→ 三处读。闭合 ✅

---

## ② 实测关（本地起服务真跑 · ASGI 直连贴输出）✅

环境：临时 SQLite + `DEV_TOOLS_ENABLED=1`，httpx ASGITransport 直连 `main.app`。

```
=== 判据1: 在线发1任务 ===
POST#1: 200 {'ok': True, 'task_id': 'T260727084158-467ffd'}
  任务数=1 (期望1)  冻结流水=1 (期望1)  余额=4 (期望4=5-1)
  流水: type=task_freeze amount=1 status=pending from=poster_v
=== 判据(幂等): 60s窗口内二次POST ===
POST#2: 200 {'ok': True, 'task_id': 'T260727084158-467ffd', 'idempotent': True}
  二次POST后 任务数=1 (期望仍1)  余额=4 (期望仍4)  idempotent=True
=== 判据3: dev-reset(soft) 后池=500 ===
dev-reset: 200 {'ok': True, 'mode': 'soft', ...}
  sync_all.pool_balance = 500 (期望500)
  DB CommunityPool.balance=500 (期望500)
```

- **判据1**：发 1 任务 → NTTask 只多 1 条、nt_ledger 只多 1 条 task_freeze(pending)、余额只扣 reward×slots=1 份 ✅（大厅无重影/无乱码——服务端只 1 单，前端 name=title 不再出 id 尾号乱码卡；真机渲染留③真机关）
- **幂等叠加**：60s 窗口内二次 POST 返既有 id + `idempotent:True`，不新建不二次冻结（P0-2 第二道锁与 P0-1 前端删双调用协同）✅
- **判据3（池 500）**：dev-reset soft → DB 池 = 500，前端数据源 `sync_all.pool_balance` = 500，非 50 ✅
- **判据2（余额三处一致）**：三处 `_serverBalance` 同源，静态已核（②逻辑关）；三处 UI 一致性需真机看渲染，留③。

---

## ③ 真机关（留砚仁）⏸

浏览器端待砚仁真机复测：(a) 大厅发 1 任务只显示 1 卡、无乱码；(b) 资料页/充值/提现三处余额数字与顶栏完全一致；(c) 村口池卡显 500；(d) 换设备/清缓存重登仍一致。服务端权威值与数据源链路已由①②证实就绪。

---

## ④ 机检关 ✅

- **deploy_check**：3/3 PASS（依赖对账 ✅ · ?v= 一致性「0 处漏 ?v=」✅ · 环境变量清单 ✅；smoke 跳过因未起真站点）
- **?v= 单点递增审计**：index.html app-data.js `14→15`、core.js `20→21`、app.js `19→20` 三处各 +1，与一营回执一致 ✅
- **pytest 回归**：`31 passed, 3 skipped`（3 skip 为 requires_pg 行锁，无 PG 环境正常跳过）——零回归 ✅

---

## 返修/待议清单（不自己动手修）

| # | 位置 | 问题 | 级别 | 建议 |
|---|------|------|------|------|
| R1 | `ui-camp.js:1039` | 营地管理页池余额读 `NT.getCommunityPool()`（本地 0），未切 _poolBalance | 🟡 同源病 | 一营已列「报丞相定夺」；建议下一卡统一切 _poolBalance |
| R2 | `ui-archive.js:281` | 档案页余额读 `NT.getUser(name).ntBalance`（本地），未切 _serverBalance | 🟡 低频 | 同上，非高频页，可择机纳入 |
| N1 | `core.js:725` publishDraft | 草稿发布仍走独立 `API.syncTask`（非经 addTask），本卡未涉及；经查它作用于既有草稿、单次 POST、不叠加 addTask，**非双写** | 🟢 说明 | 无需改；仅备注与 doPublish 是两条独立链路 |

> 说明：R1/R2 均为「显示点读本地而非服务端」的同类同源病，与 T3 病根一致，但不在 P0-1 卡面四件套范围内；一营已如实列出未擅自扩修，符合纪律。是否合并入下一卡由丞相/砚仁定夺。

---

## 太傅注（三行）

- **验收怎么验**：逻辑关看 diff 是否照卡面改（删对了行、切对了数据源）；实测关**真起服务发一单**，直查库看「任务表+流水表+余额」各自只动一次——代码看着对不算数，库里数字对才算数。
- **为什么幂等能兜双击**：P0-1 删了前端第二次快递，P0-2 又在服务端加「60s 内同单只认第一次」的锁；两道一起上，就算网络重试/手抖双击，也只建一单只冻一份。
- **同源一句话**：一个余额数字只能有一个权威出处（服务端），所有页面都问它；R1/R2 还有两处在问本地旧缓存，是同一个病的漏网点，留待下一卡收口。
