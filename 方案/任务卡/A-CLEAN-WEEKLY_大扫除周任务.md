━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  南塘云村 · 任务派发单
  卡号：A-CLEAN-WEEKLY（大扫除周任务 · 游戏化升级）
  阶段：阶段 3/4 · 第 1 张页面
  施工方：一营 Claude Code（前端 FE）
  验收方：丞相 Codex + 双路红队 A/B
  立卡：丞相 Codex 2026-07-31
  法源：方案/设计系统底座_v0 + 方案/卡片化范式v0 + 方案/现有UI体检v0
  范式：W6-UI-CARD-API（通用块面 5 件套）
  后端依赖：CLEAN-WEEKLY-BE（阶段 4，须先派发）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【优先级】HIGH（用户 07-31 御批：游戏化升级特殊通道，不在冻结范围）

【阵地】nantang-mobile/    【禁区】server/（本卡零改动）

【背景】
  砚仁 07-31 御批：现有"快速打扫"流程不友好（步骤不明显/点击不明确/无选英雄感）。
  新流程 = 管理员选位 → 周一发放 → 选英雄式选卡 → 校核闭环。
  「快速打扫」保留为应急通道。
  选卡互斥用 3 秒轮询（BE 端点 /api/clean_weekly/tasks 拉最新状态）。

【施工内容】

  ① 管理员端（admin/npc 角色可见）
     - 选空间：从 13 个建筑物空间（app.js:19-24）勾选 3-6 个
     - 选模式：均分（每人 1 个）/ 按人数（每人 2-3 个）
     - 选时间：本周一 15:00 发放
     - 「发放」按钮 → 调 CLEAN-WEEKLY-BE 的 /api/clean_weekly/distribute

  ② 用户端（所有角色可见）
     - 顶部 banner：「大扫除周任务已发放 · N 张卡可领」
     - 选英雄式选卡：用 UI.TaskCard 三态渲染（doer 字段驱动）
       * 可领 = 卡片可点 + 边框绿色 + 🟢角标
       * 已领 = 卡片灰 + 显示「你已选 + 任务名」
       * 锁定 = 倒计时（轮询 3 秒刷新）
     - 每人限选 1 张（多选=后端拒）

  ③ 选完 → 提交校核
     - 走现有 addVerification 校核闭环（CLEAN-WEEKLY-BE 走该端点）
     - 校核人批 → 自动结算 NT

  ④ 范式使用
     - UI.Card 包整张大扫除页（head: "本周大扫除" / body: 卡片列表 / actions: "提交选卡"）
     - UI.TaskCard 每张选卡
     - UI.Progress 显示「X/Y 人已选」
     - UI.StatusBadge 显示卡片状态（可领/已领/锁定）

  ⑤ 应急通道「快速打扫」保留
     - app.js:6127 _submitCleanEntry 仍可用
     - H 抽屉增加"快速打扫"入口（已有）+ "周大扫除" 入口

【约束】
  - 只 commit 不 push · git add 具名（禁 -A）
  - 不引第三方库
  - 不重写已有，只在大扫除页用范式
  - ?v= bump 与 commit 同号
  - 后端先于前端上线（push 顺序 CLEAN-WEEKLY-BE → A-CLEAN-WEEKLY）

【验收】
  - 管理员端可勾选空间+发放
  - 用户端选英雄式选卡三态正确
  - 选完 → 校核 → NT 结算 闭环通
  - 应急通道「快速打扫」仍可用
  - 4 个组件（Card/TaskCard/Progress/StatusBadge）实际调用

【回执落盘】方案/任务卡/A-CLEAN-WEEKLY_回执_一营_2026-07-31.md
【回执四件套】①diff摘要 ②自测 ③禁区确认(server=0) ④皇帝验收单
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
