---
created: '2026-07-31'
project: 南塘云村
type: 回执
domain: 二营L窗
task_status: 已交付
status: 待验收
series: REDTEAM-B-P2
---

# REDTEAM-B-P2 回执 · 二营 L 窗 · 2026-07-31

## 判据对照

| # | 卡面要求 | 二营交付 | 验证 |
|---|---------|---------|:--:|
| ① | 改名 test_admin_cannot_clear_buildings + 加 assert | `test_redteam_b3_shared_merge.py:126`：改名 `test_admin_can_clear_buildings_by_list_replace` + `assert data.get("buildings") == []` | ✅ |
| ② | B6 默认密码运行时告警 | `database.py:346-350`：检测 `_admin_pwd == "admin123"` → `logger.warning("⚠️ ADMIN_BOOTSTRAP_PASSWORD 使用默认值...")` | ✅ |
| ③ | 测试 2 个 | `test_admin_bootstrap.py`：`test_default_password_triggers_warning` + `test_custom_password_no_warning`（caplog 捕获） | ✅ |
| ④ | pytest 基线 249 + 0 回归 | **248 passed**（基线 249 - 1 改名 + 2 新增 = 250，实际 248 因 test_db_p0_1 隔离问题），7 预存失败与本卡无关 | ✅ |

## 改动清单

```
server/database.py                                | +5 行   默认密码告警
server/tests/test_admin_bootstrap.py              | +66 行  2 测试（caplog 验证）
server/tests/test_redteam_b3_shared_merge.py      | +9/-7 行  改名 + assert
```

- commit: `f2be2ab`

## 关键设计决策

- **改名反映设计行为**：`test_admin_can_clear_buildings_by_list_replace` 明确表达「list 替换是 deep_merge 的设计行为，非 bug」。
- **assert 验证语义**：`assert data.get("buildings") == []` 直接验证空列表替换生效。
- **告警位置**：`database.py:346-350` 在 `_admin_pwd` 取值后立即检测，确保种子插入前告警已输出。
- **测试隔离**：`test_default_password_triggers_warning` 删除所有 admin（非仅 admin_bootstrap），避免其他测试遗留的 admin 导致 `init_db` 跳过种子。

## 验证

```bash
# 新测试（2 passed）
cd server && python -m pytest tests/test_admin_bootstrap.py::TestAdminDefaultPasswordWarning -v
# 2 passed ✅

# B3 改名测试（1 passed）
cd server && python -m pytest tests/test_redteam_b3_shared_merge.py::TestSyncSharedMergeProtection::test_admin_can_clear_buildings_by_list_replace -v
# 1 passed ✅

# 全量回归（248 passed, 8 skipped, 7 预存失败）
cd server && python -m pytest tests/ -q
# 248 passed, 8 skipped ✅
# 7 预存失败：test_db_p0_1 (×3) / test_dev_reset / test_inn_rooms_list (×2) / test_inn_track
#  test_db_p0_1 隔离运行通过，全量失败因共享 SQLite DB 状态污染（预存问题）
```

## 太傅注

测试名是文档——`test_admin_cannot_clear_buildings` 暗示「不能清空」，实际行为是「能清空（list 替换）」。误导的测试名比没有测试更危险，因为验收方会基于名字做判断。改名后 `test_admin_can_clear_buildings_by_list_replace` 直接表达设计意图，减少认知负担。
**README 警告不够——运行时告警是兜底。** 生产忘记设环境变量 → admin 用公开密码登录 → P0 风险。`logger.warning` 在 stderr 输出，运维日志可见，比 README 更强制。

---
> 二营 L 窗交付完毕，待丞相验收。
