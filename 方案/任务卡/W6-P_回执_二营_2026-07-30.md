---
created: '2026-07-30'
project: 南塘云村
type: 回执
domain: presence 断链修复
series: Wave 6
card: W6-P
施工方: 二营（Qoder）
验收方: 丞相 Codex
---
# W6-P 回执 · presence 断链修复 · 二营

## 一、勘察结论：已实现，无需重复施工

按卡面施工内容③「施工前先 git log 核对现状——避免重复施工/覆盖」，核对结果如下：

| 施工内容 | 现状 | 来源 commit | 判定 |
|---|---|---|---|
| ① sync_shared presence 写入 | `data.py:419-431` 已实现：按用户分 key `presence:{uid}` 存储，带 `updatedAt` 防回写 | `df21937`(D-12) + `79ff178`(D-12返修) | **已存在** |
| ② sync_all presence 下发 | `data.py:513-520` 查 `presence:*` 合并 + `L533` 在返回体 | 同上 | **已存在** |
| ③ 核对 git log 避免重复 | `df21937` + `79ff178` 均在 main 分支历史中 | — | ✅ 已核 |
| ④ 新增测试 | `test_e2e_smoke.py:125-134` 已有 presence 端到端测试（alice 写入 → bob sync_all 读回） | D-20 | **已存在** |

**四项施工内容全部已由 D-12 / D-12返修 / D-20 完成，零代码改动。**

## 二、证据链

### 2.1 写入端（sync_shared · data.py L419-431）

```python
# D-12 返修: presence 翻牌状态——分用户 key 存储，防并发互踩
_presence = req.get("presence")
if _presence and isinstance(_presence, dict):
    for uid, pdata in _presence.items():
        if not isinstance(pdata, dict): continue
        pk = f"presence:{uid}"
        pr = (await db.execute(select(MapLocation).where(MapLocation.key == pk))).scalar_one_or_none()
        if not pr:
            pr = MapLocation(key=pk); db.add(pr)
        existing = json.loads(pr.data) if pr.data else {}
        if pdata.get("updatedAt", "") >= existing.get("updatedAt", ""):
            pr.data = json.dumps(pdata, ensure_ascii=False)
```

### 2.2 下发端（sync_all · data.py L513-533）

```python
# D-12 返修: presence 翻牌状态——合并所有分用户 key
pr_rows = (await db.execute(select(MapLocation).where(MapLocation.key.like("presence:%")))).scalars()
presence = {}
for pr in pr_rows:
    uid = pr.key.replace("presence:", "")
    pdata = _safe_json(pr.data) if pr.data else {}
    if pdata:
        presence[uid] = pdata
# ... 返回体包含 "presence": presence
```

### 2.3 测试（test_e2e_smoke.py L125-134）

```python
# 6. presence: alice 上报 → bob 拉 sync_all 看到
await client.post("/api/data/sync_shared", headers=_h(alice_tok), json={
    "presence": {alice_id: {"flipped": True, "at": "2026-07-26T01:00:00"}},
})
r = await client.get("/api/data/sync_all", headers=_h(bob_tok))
assert "presence" in data
assert alice_id in data["presence"]
assert data["presence"][alice_id].get("flipped") is True
```

## 三、测试结果

- 冒烟测试 `pytest tests/test_e2e_smoke.py -q` → **1 passed**（presence 端到端断言全绿）
- 全量回归 `pytest tests/ -q` → **136 passed, 8 skipped**（基线不变，0 回归）
- 跑测环境：`JWT_SECRET=test-secret-key-for-ci-32bytes-long`，`.venv`，`-p no:cacheprovider`。

## 四、Commit

**零改动，无 commit。** 四项施工内容均由 D-12(`df21937`) + D-12返修(`79ff178`) + D-20(`704b57b`) 完成。

## 五、禁区确认

- `nantang-mobile/`：**零改动**（本卡无 commit）。

## 六、room.people 永空 · 真因判断

服务端 presence 写入+下发链路完整且测试 PASS。**「room.people 永空」症状不在服务端**，真因在前端渲染层——客户端收到 `sync_all.presence` 后如何映射到 `room.people` 属前端逻辑，归 W6-P-FE（一营阵地 `nantang-mobile/`）。

## 七、太傅注（三行）

- 勘察先于施工：任务卡明确要求"先 git log 核对现状"，核对发现四项全已实现——不造假动作比重复施工更重要。
- D-12 系列是前车之鉴：初版单 key `presence` 被二营打回（并发互踩），返修改分用户 key `presence:{uid}` 才是现行正确方案。
- 症状≠病灶：room.people 永空是前端渲染症状，服务端数据链路已通——诊断要分清"数据没到"和"到了没渲染"。
