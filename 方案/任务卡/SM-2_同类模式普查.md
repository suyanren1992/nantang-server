---
title: "SM-2 同类模式普查（幽灵函数/异常吞噬/忘同步）"
type: 任务卡
domain: ["南塘云村", "技术债", "模式普查"]
created: 2026-07-27
author: "丞相（Kimi Work）"
status: "排队（SM-1 上线后开拔）"
---

# SM-2 · 同类模式普查（一营施工 → 二营验收）

> 来源：2026-07-27 00:38 砚仁问「有没有其他同样的问题」→ 丞相连夜三模式扫描（只读）。
> 性质：**嫌疑清单非确诊**——每条须施工营逐个实证（证真修/证伪销），不许照单全改。
> 定档：中卡。排 SM-1 上线后开拔，避免同文件撞车。

## 一、模式 A · 幽灵函数嫌疑（5 个）

onclick 调用但 JS 未抓到定义（也可能是 window.xxx=/无 var 全局赋值等扫描盲区——先证伪）：
`_toggleInbox` / `_renderInbox` / `_markInboxRead` / `_computeInboxMessages` / `_setArchiveCardFilter`
→ 收件箱与档案室筛选区域，「按了没反应」高发区。实证法：浏览器控制台逐个调用看 ReferenceError。

## 二、模式 B · 异常吞噬嫌疑（80+ 处，须分类）

catch 体无 showToast/alert 的全量清单在 BUG_TRACKER.md「SM-2 扫描存档」节。
**不是全修**——先分两类：① 用户点击路径上的静默（=SM-1 子项 5「冰箱打不开」同型，修：加错误提示壳）；
② 后台轮询/存储兜底静默（合理，销）。判据：用户主动作触发的调用链上的 catch 才有提示义务。

## 三、模式 C · 本地写忘同步嫌疑（11 处）

| 位置 | 嫌疑 |
|---|---|
| `app.js:715/1077/2113/2117/2211/2582/2617/2666` + `ui-cardroom.js:1106` | `AppData.addVerification(` ×7+1——校核室区域，与砚仁报障同域 |
| `core.js:633` | `AppData.addTask(`——**发任务疑似第二入口**（SM-1 修的是 697 doPublish）；二营收 SM-1 时先核是否同一调用链，非同一则本项证真 |
| `data.js:36` | `AppData.addJournal(` 无同步 |

实证法（每处）：读调用点上下文 → 该写操作是否应持久化到服务端？应而未 = 证真（照 SM-1 子项 1 修法补 sync 分支）；本地缓存性质 = 证伪销。

## 四、必交物与验收判据

- 每条嫌疑一行结论：证真（修法+commit）/ 证伪（一句话理由）——无证据行打回
- 修 js/css 必升 `?v=`（同 commit）；影响面声明+爆炸半径四答
- 验收：二营抽查 ≥30% 证伪行 + 全部证真行 diff；真机无痕复测证真项
- **禁区**：本卡是普查不是重构——不许顺手改风格/改命名/改架构（爆炸半径声明里写死）

> **太傅注**：补课 17。人话原理：一个 bug 是虫子，一类 bug 是窝——扫描闻窝方向，普查端窝；
> 但端窝带筷子不带推土机：逐条实证，不许照单全改，更不许顺手装修。
