# 任务卡 C-4：数据库挂盘持久化（路 A，已批准）

> ① 监察确认 → ② 施工 → ③ 监察校验 · 单独 commit，不 push

## 背景与决策
SQLite 在 Render 临时磁盘，部署/重启即清库（cc79a2d 排查结论）。砚仁已批准**路 A：Render 挂持久磁盘**（奏折：`方案/C-4_数据库持久化_决策奏折.md`）。

## ① 监察确认（Kimi Code，只读）
在 Render 后台确认：当前 Hobby workspace 的 web service 能否直接添加 Persistent Disk？
- 能 → 继续 ②
- 不能（提示需付费实例）→ **停下来回奏砚仁**（升 $7 Starter 需重新批），不许擅自升级付费

确认挂载路径可用后，在 BUG_TRACKER.md 记录结论。

## ② 施工（Claude Code）
1. `server/database.py`：数据库文件路径改读环境变量，如
   `DB_PATH = os.environ.get("NT_DB_PATH", "nantang.db")`，SQLAlchemy 连接串拼这个变量。保持默认值向后兼容（本地开发不受影响）
2. 若有其他文件硬编码了 db 路径（`_reset_db.py` / 备份脚本 / `chain_scanner.py`），一并改读同一变量
3. Render 后台操作（由砚仁/监察执行，非代码）：Disks → Add Disk → 挂载路径 `/var/data`，1GB；环境变量加 `NT_DB_PATH=/var/data/nantang.db`
4. **数据迁移**：首次挂盘后库是空的——正好当前库也是全新的（无存量用户），直接让应用自动建表即可；在 BUG_TRACKER.md 注明"无存量数据迁移"

## ③ 监察校验（Kimi Code）
- 部署后：注册新用户 → 在 Render 手动重启服务 → 用户仍能登录，数据还在
- `py_compile` / 服务启动日志无报错
- 结论写 BUG_TRACKER.md

## 验收
Render 重启/重新部署后数据不丢。commit：`feat(C-4): SQLite 路径环境变量化，Render 挂盘持久化（路A已批）`
