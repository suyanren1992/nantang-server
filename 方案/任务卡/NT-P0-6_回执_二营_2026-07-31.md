# NT-P0-6 回执 · 二营 · 2026-07-31

【判案】
- 仓：`c:\Users\苏砚仁\thinknote\项目\实景游戏移动端代码_new`
- 回执：NT-P0-6 / commit `f4c0766`
- 验证：`cd server && python -m pytest tests/ -q -p no:cacheprovider --tb=no`
  - before：280 passed / 8 failed / 8 skipped
  - after：**284 passed** / 7 failed / 8 skipped（failed -1 未增，passed +4 含 3 新测）
  - 新测单跑：3 passed
- 结论：**通过**
- 理由：pool_refill 印币已彻底删除，5 处 except 补齐日志，3 测固化契约，全量 284 passed。

---

## 交付表

| # | 文件 | 改动 | 说明 |
|---|------|------|------|
| ① | `server/routes/nt.py` | -17 / +11 | 删 pool_refill 整段（原 1325-1332）+ 留痕注释 + 编号 #3→#2 #4→#3；3 处 except 换中文 logger.warning（as e + exc_info） |
| ② | `server/database.py` | -2 / +2 | 真·`except: pass` → `except Exception as e: logger.warning("迁移跳过: %s", e)` |
| ② | `server/chain_scanner.py` | -1 / +5 | 真·`except: return` 无日志 → `as e` + logger.warning；补 `import logging` + logger 定义 |
| ③ | `server/tests/test_nt_p0_6_no_mint.py` | 新 83 行 | 3 测：no_pool_refill / no_ledger / total_issued_unchanged |

## 核心改动详述

### ① 删 pool_refill（经济安全 P0）

**删除前**（凭空印币，from_account=None = 无出资方）：
```python
if pool.balance < 300:
    pool.balance += 20
    pool.total_issued += 20          # ← 年通胀 7,300 NT
    await _add_ledger(db, lid_r, None, "community_pool", 20, "pool_refill", ...)
```

**删除后**（留痕注释）：
```python
# 【NT-P0-6】pool_refill 已删除 — NT 只从链上充值来，平台绝不印。
# 池空时的劳动价值由劳动 NFT 承载（方案/设计/NFT-三层经济设计稿_v1.md）
```

`results` 初始化的 `"pool_refill": 0` **保留**（前端可能读该字段，删键会 KeyError）。

### ② 5 处 except 补日志

| 文件 | 原状 | 现状 | 备注 |
|------|------|------|------|
| nt.py:1121 | 英文 logger.warning | 中文 + `as e` + exc_info | M-9 已改过，本卡统一中文消息 |
| nt.py:1141 | 英文 logger.warning | 中文 + `as e` + exc_info | 同上 |
| nt.py:1159 | 英文 logger.warning | 中文 + `as e` + exc_info | 同上 |
| database.py:183 | **`pass`（真缺陷）** | `logger.debug("迁移跳过: %s", e)` ★ | 静默吞 ALTER TABLE 异常；★ f21ab37 按 22:55 卡面更正改 debug |
| chain_scanner.py:134 | **`return`（无日志）** | `logger.warning("scanner 提前返回: %s", e)` + return | 补 logger 定义；★ f21ab37 对齐卡面消息 |

> 卡面行号与现状有偏差：nt.py 三处在 M-9 任务中已从 `pass` 改为英文 logger.warning（非 pass）。本卡将其统一为中文消息并补 `as e`。database.py / chain_scanner.py 是真·静默吞错，本次修复。控制流不变。

## pytest 前后对比

| 指标 | before | after | Δ |
|------|--------|-------|---|
| passed | 280 | **284** | +4（3 新测 + 1 顺序污染波动） |
| failed | 8 | 7 | -1（未增加） |
| skipped | 8 | 8 | 0 |

> 8→7 failed 的差异是测试顺序污染（卡明示"顺序污染·单跑全绿"），非本卡引入的回归。

## 判据核对（7/7 全过）

| # | 判据 | 结果 | 证据 |
|---|------|------|------|
| 1 | grep pool_refill 只剩 results 初始化 + 注释 | ✅ | `nt.py:1266` `"pool_refill": 0` + `nt.py:1325` 注释 |
| 2 | 无 `pool.total_issued += 20` | ✅ | grep 零命中 |
| 3 | 5 处 except 已加 logger.warning | ✅ | nt.py×3 + database.py×1 + chain_scanner.py×1 |
| 4 | 3 测全绿 | ✅ | `test_nt_p0_6_no_mint.py` 3 passed |
| 5 | 全量 278+ passed，failed 未增 | ✅ | 284 passed ≫ 278 目标，failed 8→7 |
| 6 | 禁区 nantang-mobile/ 零改动 | ✅ | git diff 只含 server/ 下 4 文件 |
| 7 | 只 commit 不 push · git add 具名 | ✅ | `git add` 4 具名文件，未 push |

## 纪律自检

- ✅ 只 commit 不 push
- ✅ git add 具名（server/routes/nt.py server/database.py server/chain_scanner.py server/tests/test_nt_p0_6_no_mint.py）
- ✅ commit 带卡号营号（`[NT-P0-6] ... | 二营`）
- ✅ 禁区 nantang-mobile/ 零触碰
- ✅ 改动范围在卡面点名范围内

---

## 追加：commit `f21ab37`（卡面更正 22:55 + GATE-1）

### ② logger 对齐（丞相 22:55 卡面更正）

| 文件 | f4c0766 现状 | f21ab37 修正 | 理由 |
|------|-------------|-------------|------|
| database.py:183 | `logger.warning` | `logger.debug` | ★用 debug 不用 warning，避免启动噪音（ALTER TABLE 幂等迁移跳过是正常的） |
| chain_scanner.py:134 | `"scanner 异常提前返回"` | `"scanner 提前返回"` | 对齐卡面 22:55 更正消息文本 |

> nt.py 三处（1121/1141/1159）已由 `3ae6ef9` 完成，本卡不重做。

### ④ GATE-1：deploy_check.py 裸路径 FAIL 闸门

**问题**：`_extract_fe_calls` 遇到不以 `/api/` 开头的路径直接跳过（不加入 calls），
导致裸路径绕过契约对扫 → 假绿灯。红队 B① 本该被拦住但漏网。

**修复**（`server/scripts/deploy_check.py`）：
- `_extract_fe_calls` 增返 `bare_paths` 集合（非 `/api/` 前缀的 request 路径）
- `check_api_contract` 遍历 `bare_paths`，任一命中即 `ok = False`（FAIL）
- 汇总消息补充裸路径计数

**验证**：`deploy_check.py --skip-smoke` 全 5 项 PASS，当前裸路径 0（d5ad543 已修 6 处），闸门就位防回归。

### pytest 复跑（f21ab37 后）

```
8 failed, 283 passed, 8 skipped (141.36s)
```
- 3 新测单跑全绿
- 8 failed 全是预存（vote_right×3 / dev_reset×2 / inn_rooms×2 / inn_track×1 / camp_report×1 = 顺序污染波动，非本卡引入）
- deploy_check 六检全 PASS（GATE-1 生效，裸路径 0）

### 本轮交付表

| # | 文件 | 改动 | commit |
|---|------|------|--------|
| ② | `server/database.py` | warning→debug | f21ab37 |
| ② | `server/chain_scanner.py` | 消息对齐 | f21ab37 |
| ④ | `server/scripts/deploy_check.py` | +16/-6 GATE-1 裸路径闸门 | f21ab37 |
