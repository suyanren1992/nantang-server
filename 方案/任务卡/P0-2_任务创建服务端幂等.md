---
created: '2026-07-27'
project: 南塘云村
type: 任务卡
domain: 待人工
task_status: 已发卡
status: 讨论中
---
# P0-2 任务创建服务端幂等（二营 · 服务端小卡）🔴

> 来源：实测议单 T1/T2 + 二营情报回执未明项（create_task 无任何幂等/重复校验）
> 施工：豆包 Codex（二营）｜验收：Claude Code（一营）
> 议定（砚仁 2026-07-27 14:38 准）：钱的事双保险——前端双写修复（P0-1）之外的第二道锁。

## 施工内容

`server/routes/tasks.py` `create_task`（:84-132）加幂等防重：
- 同一 poster + 同一 title，在时间窗（建议 60 秒）内已存在任务 → **不新建、不二次冻结**。
- 返回策略二选一：① HTTP 200 幂等返回已存在任务 id（前端无感）；② 409 明确拒绝。
  取舍写进施工方案并说明理由，回执注明选择。
- 边界写清：同用户隔时间窗后发同名任务应允许；同名但 reward/slots 不同的处理策略写明。

## 爆炸半径（四答）
- 改几个文件：1（tasks.py；测试另计）
- 影响功能：仅任务创建端点
- 破坏性变更：响应约定新增分支，需在回执写明对前端的影响（P0-1 已删双调用，正常流程无感）
- 回滚：`git revert` 本 commit 一键

## 铁律
- **禁区**：withdraw / confirm / reject 资金端点零改动（丞相闸口必查）
- 只 commit 不 push；只加具名文件；pytest 零回归
- 回执独立落盘 `方案/任务卡/` 随 commit；末尾太傅注三行内

## 判据（验收方实跑，逐条贴输出）
1. 模拟双 POST（同 title+poster，间隔 <1s）→ 服务端只建 1 单、`nt_ledger` 只多 1 条冻结流水
2. 正常单次 POST 不受影响（建单+冻结正常）
3. 时间窗外同名任务可正常创建
4. pytest 全绿零回归
