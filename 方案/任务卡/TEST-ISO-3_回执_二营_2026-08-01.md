# TEST-ISO-3 回执 · 二营 Qoder

【判案】→ 仓 `nantang-mobile` → 回执 TEST-ISO-3 / commit（见下）→ 验证 亲跑 → **通过**

---

## 判据对照

### ① conftest.py FK 为 ON，无 OFF 残留 ✅

删除了 `_force_fk_off` 的 `checkout` 监听（原 `PRAGMA foreign_keys=OFF`），替换为 `connect` 监听 `PRAGMA foreign_keys=ON`。无 OFF 残留。

```python
@event.listens_for(engine.sync_engine, "connect")
def _set_fk_on(dbapi_connection, connection_record):
    cursor = dbapi_connection.cursor()
    cursor.execute("PRAGMA foreign_keys=ON")
    cursor.close()
```

### ② 0 failed，passed ≥ 294 ✅

**第一次**：`294 passed, 8 skipped in 219.10s`
**第二次**：`294 passed, 8 skipped in 215.54s`

### ③ 连跑 2 次结果一致 ✅

```
# Run 1
294 passed, 8 skipped, 1512 warnings in 219.10s (0:03:39)

# Run 2
294 passed, 8 skipped, 1512 warnings in 215.54s (0:03:35)
```

两次完全一致：`294 passed, 8 skipped`。

### ④ 反向验证：撤 flush → 转红 ✅（唯一真判据）

**撤掉** `test_vote_right_today_eligible` 的 `await s.flush()`，跑单测：

```pytest
FAILED tests/test_db_p0_1.py::TestVoteRightStrict::test_vote_right_today_eligible
======================== 1 failed in 1.39s ========================

E   sqlalchemy.exc.IntegrityError: (sqlite3.IntegrityError) FOREIGN KEY constraint failed
E   [SQL: INSERT INTO tenancies (user_id, room_id, bed_num, checkin_date, check_out_date,
     track, room_type, last_deducted, debt, accommodation_due, status, last_active_at)
     VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)]
E   [parameters: ('p01_vote_today', 'p01_room_a', 1, '2026-07-31', None, 'coop', None, None,
     0, 0, 'active', None)]
```

**恢复** flush 后同测：

```pytest
tests/test_db_p0_1.py .     [100%]
1 passed in 1.36s
```

> 证明：FK 约束真实生效，不是又被绕开。撤掉 flush → User 和 Tenancy 同 flush → SQLAlchemy 先插 tenancies → users 里无此行 → FK 失败。加上 flush → User 先落库 → Tenancy 的 FK 引用合法 → 通过。

### ⑤ diff 仅含 conftest + 2 测试文件 ✅

```
server/tests/conftest.py            | 17 ++++++++---------
server/tests/test_db_p0_1.py        |  3 +++
server/tests/test_inn_rooms_list.py | 19 ++++++++++++++++---
3 files changed, 27 insertions(+), 12 deletions(-)
```

无 models.py 改动（选甲路：s.add(User) → await s.flush() → s.add(Tenancy)）。

### ⑥ deploy_check 六检 PASS ✅

```
============================================================
  依赖对账            PASS
  ?v= 一致性         PASS
  JS语法检查          PASS
  API契约           PASS
  环境变量            PASS
============================================================
EXIT_CODE=0
```

### ⑦ 实测秒数 ✅

219.10s / 215.54s，均 < 250s 上限。

### ⑧ 只 commit 不 push ✅（见下）

---

## 改动摘要

| 文件 | 改动 | 说明 |
|------|------|------|
| `conftest.py` | 删 FK OFF checkout 监听，加 FK ON connect 监听 | 与生产 database.py:81 同构 |
| `test_db_p0_1.py` | 3 处加 `await s.flush()` | TestVoteRightStrict ×3：s.add(User) 后先 flush，再 add Tenancy |
| `test_inn_rooms_list.py` | 加 `_add_user` 辅助函数 + 2 测各加 `await _add_user(uid)` | test_occupied_dates_interval / test_coop_and_checked_out_not_counted：先建 User 再建 Tenancy |

**实现选择**：甲路（s.add(User) → await s.flush() → s.add(Tenancy)），最小改动，不动生产模型。

## 禁区确认

✅ 未触碰 `nantang-mobile/` 目录。
✅ 未改任何业务断言。
✅ 未改 room_id。
✅ 未 skip / xfail / 局部 FK OFF 豁免。

## 后续建议

`_reseed_baseline()` 中的 `s.add(_User(...))` 后无 flush，后续 `s.add(CommunityPool/InnRoom/...)` 不依赖 User 的 FK，当前安全。但若后续种子数据增加 FK 到 User 的行，需同法加 flush。

---

*太傅注：测试环境的假绿灯比生产 bug 更危险——生产 bug 用户会报，假绿灯没人知道。TEST-ISO-1 的 294/0 是关掉约束换来的，今天这 294/0 是扛着约束跑出来的。以后每次看到全绿，先问自己：约束开了吗？*

**建议下一卡：把 FK ON 的校验加入 CI 闸门，防止任何人再关掉。**
