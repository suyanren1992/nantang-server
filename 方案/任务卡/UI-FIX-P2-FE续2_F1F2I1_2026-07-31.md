━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  南塘云村 · 任务派发单（一营续单 2）
  卡号：UI-FIX-P2-FE续2_F1F2I1
  阶段：Phase 2 续 2 · 1 营接 B5补/B6补/B7补 端点
  施工方：一营 Claude Code（前端 FE）
  验收方：丞相 Codex
  立卡：丞相 Codex 2026-07-31 13:35
  法源：UI-FIX-P2-FE 续单 + UI-FIX-P2-BE 补完工
  优先级：P0（砚仁 P0 第二批）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【派单理由】
  二营 B5补/B6补/B7补 已完工（3 路由 9 端点 12 测 + OpenAPI 已更新）。
  1 营 F1/F2/I1 强依赖全部解除，可立刻接：
    - F1 时间线拆开：依赖 B5补 GET /api/archive/items ✅
    - F2 全貌页设置按钮：依赖 B7补 GET /api/users/me/settings ✅
    - I1 田间排查：依赖 B6补 GET /api/fields + 4 端点 ✅

【阵地】nantang-mobile/    【禁区】server/

【施工内容 · 3 项 P0】

━━━━━━━━━━━━━━━━━━━━━━━━━━
F1. 全貌页 vs 个人工作台 时间线/档案室 跳转串
━━━━━━━━━━━━━━━━━━━━━━━━━━
  现象：从个人工作台点「时间线」→ 进「全貌页时间线」；从时间线点「档案室」→ 打不开
  修复：
    1. 拆「我的时间线」vs「全貌时间线」两个 route
    2. 个人工作台默认进「我的时间线」
    3. 档案室入口用 B5补 端点：调 API.getArchiveItems(category)
    4. 接口契约：openapi_2026-07-31.yaml B5补
  验收：点档案室能进，看到内容

━━━━━━━━━━━━━━━━━━━━━━━━━━
F2. 全貌页设置按钮（重写）
━━━━━━━━━━━━━━━━━━━━━━━━━━
  现象：点击无反应 / 弹错 / 重复弹
  修复：
    1. 删除现有 F2 旧代码（多次补丁没解决根因）
    2. 重写 _openUserSettings()：调 API.getUserSettings() + 渲染设置 UI
    3. 走 B7补 端点 PATCH 更新
    4. 接口契约：openapi_2026-07-31.yaml B7补
  验收：点设置能开，能改，能保存

━━━━━━━━━━━━━━━━━━━━━━━━━━
I1. 田间界面排查
━━━━━━━━━━━━━━━━━━━━━━━━━━
  现象：UI-FIX-P0 ⑨ 修了卡片化，但砚仁实测还是旧版
  排查：
    1. ?v= bump 是否生效
    2. ecfadf9 改的代码是否真的触达目标路径
    3. 是否有别的代码路径覆盖
  修复：
    1. 改用 B6补 端点 API.getFields() 拉数据
    2. 完整 UI.Card 范式重写田间页
    3. 4 块田（番茄/玉米/红薯/枣树）每块 Card
    4. 接口契约：openapi_2026-07-31.yaml B6补
  验收：4 块田显真实数据，能浇水/施肥/收割

━━━━━━━━━━━━━━━━━━━━━━━━━━

【施工规则】
  - 1 营 1 窗 串行 3 项
  - 每项 1 commit
  - 走 nantang-mobile/ 阵地
  - 禁区 server/
  - **只 commit 不 push**（等收尾一起推）
  - ?v= bump 与 commit 同号
  - 不引第三方库
  - 调 API 用 OpenAPI 契约文件

【紧急召唤】
  - 调 B5补/B6补/B7补 端点发现字段缺 → 立刻 call 丞相
  - F2 重写发现 settings 端点字段不对 → 立刻 call 丞相

【回执落盘】
  方案/任务卡/UI-FIX-P2-FE续2_回执_一营_2026-07-31.md

【执行时间线】
  - 0min：开工
  - 1h：F1 时间线拆开
  - 2h：F2 设置按钮重写
  - 3h：I1 田间排查 + 范式重写
  - 3.5h：commit + 回执

【太傅注 3 行】
  - **3 项 P0 砚仁亲报**——必须先 commit 先验证
  - **F2 重写不要堆补丁**——删除旧代码重写干净
  - **I1 范式重写**——这是田间页真正落地，不再修修补补
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
