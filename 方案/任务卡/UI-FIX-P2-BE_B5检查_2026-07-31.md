# B5 检查：GET /api/archive/items（档案室）

> 2026-07-31 | 检查人：Qoder 二营 | 卡号：UI-FIX-P2-BE B5

## 端点存在性

| 检查项 | 结果 |
|--------|------|
| 端点是否存在 | ❌ **不存在** |
| 最近似端点 | `GET /api/data/archive_summary/{target_id}`（ZX-4 F12，按用户聚合计数） |

## 字段对照

| 卡面要求字段 | 现有 archive_summary 字段 | 匹配 |
|-------------|------------------------|------|
| id | ❌ 无（返回聚合计数，非条目列表） | ✗ |
| title | ❌ 无 | ✗ |
| content | ❌ 无 | ✗ |
| created_at | ❌ 无 | ✗ |
| category | ❌ 无 | ✗ |

## 结论

**archive_summary 是聚合统计端点（完成任务数/校核数/住宿信息），非档案条目列表。**

前端卡片化 UI ⑲「楚门档案室」需要的 `GET /api/archive/items`（条目列表+id/title/content/category/created_at）**不存在**。

## 建议

需新立卡建设 `GET /api/archive/items`。数据来源：
- Journal（日志类条目）
- NTTask 已完成记录
- Verification 已完成记录
- CardDiscovery 已确认记录

需要聚合多表 → 统一 ArchiveItem 视图。复杂度 ⭐⭐⭐。

**是否需要新立卡：✅ 是**
