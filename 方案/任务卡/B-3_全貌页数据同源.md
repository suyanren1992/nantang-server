# 任务卡 B-3：全貌页与建筑页数据同源 + 大扫除补全房间

> 施工方：Kimi Code · 验收：Kimi Work · 先查后改，单独 commit，不 push

## 背景
1817 对照文档（`存档/` 或旧目录里有，内容已收录进阶段文件）指出：全貌页四张管理卡片数据来自 `MGMT_DATA`，与建筑页房间数据不同源——改了一边另一边不变。

## 任务一：数据同源（先查后改）
1. 排查 `MGMT_DATA`（app-data.js / data.js）与建筑页房间数据（floors/rooms）各自的数据源
2. 让四张卡片（大扫除/住宿/田地/厨房·冰箱）改从建筑页同一份数据读取——**改读取不改界面**
3. 把结论和改法写进 BUG_TRACKER.md

## 任务二：大扫除补全房间（设计稿「改造三」，~10 行）
`_collectCleaningRooms()`（app.js:962-964）去掉 `cleaning` 标记前置条件，收集所有建筑里除宿舍（id 以 dorm 开头）外的所有房间，让正厅/走廊/楼梯/洗手台等都能被打扫。

## 验收
- 在建筑页改了冰箱物品/田地/房间状态，全貌页卡片同步变化（同一数据源）
- 大扫除列表出现之前没有的房间
- commit message：`fix(B-3): 全貌页四卡片与建筑页数据同源 + 大扫除补全公共空间`
