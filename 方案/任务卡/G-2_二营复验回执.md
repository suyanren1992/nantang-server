---
created: '2026-07-29'
project: 南塘云村
type: 复验回执
task_status: 二营复验通过_PASS
domain: 前端交互
status: 待副署
卡面: 方案/任务卡/G-2_签署页拦截翻页与配置批.md
复验对象commit: 74374f8（一营返修）
原打回: c3d8fd7（二营验收 CHANGES REQUESTED · D1-D5）
派工: 批次2 第5条
复验营: 豆包 Codex（二营）
施工营: Claude Code（一营）
---
# G-2 v1.1 二营复验回执 · 签署页+拦截+翻页+C1-C9

## 🎯 裁决：PASS（复验通过）

一营返修 74374f8 按原打回五条逐条复验：**D1/D2/D3/D5 四条真跑联调全 PASS，D4 挂账口径同意**。四关：①✅ ②✅ ③留砚仁 ④✅。

---

## 一、四件套

### 四关结论
| 关 | 结论 | 依据 |
|----|------|------|
| ①逻辑关 | ✅ 过 | D1/D2/D3/D5 修复行在当前 HEAD 存续（app.js:161-162,615,641-645,2649；api.js:134） |
| ②实测关 | ✅ 过 | G-2↔G-1 真端点联调六项全 PASS（真跑贴证见下） |
| ③真机关 | — 留砚仁 | 签署页视觉/翻页交互留真机 |
| ④机检关 | ✅ 过 | deploy_check 4/4 PASS；?v= 回显 ['20','23','24'] 单点递增 |

### 二、原打回五条复验
| # | 原缺陷 | 返修 | 复验结论 |
|---|--------|------|----------|
| **D1** BLOCKER | status 无 ok 键→签署判定恒走本地 mock | `_fetchCovenantStatus` 改判 `typeof r.signed !== 'undefined'`（app.js:641） | ✅ 真跑：status keys=[sign_reward,signed,signed_at,version]，判定采信服务端 signed=False→签后 True |
| **D2** BLOCKER | sign 无 nt_earned→钱动提醒恒不响 | `_doSignCovenant` 改读 `r.reward_granted && r.reward`（app.js:615） | ✅ 真跑：sign 返 reward_granted=True reward=10→提醒**响** |
| **D3** 中 | covenant_text 未对接 G-1 text 端点 | api.js +covenantText（:134）；`_covenantTextCache` 服务端权威→config 兜底（app.js:161-162）；_initMap 异步拉取（app.js:2649） | ✅ 真跑：GET /text 返完整 config（version=v12）→缓存生效 |
| **D4** 中 | 预定入口未加未签拦截 | 挂账（F3 预定未上线） | ✅ 同意挂账：平台无预定入口，无处可拦；F3 上线时补 |
| **D5** 低 | 离线 mock 可伪造签署 | `_fetchCovenantStatus` 失败不回退本地缓存，显示联网提示（app.js:643-645） | ✅ 死链已断：登录态一律服务端权威，本地伪造签署在线态不被采信 |

### 三、实测关真跑贴输出（G-2↔G-1 联调 · 死证）
```
[D1] GET /status keys: [sign_reward, signed, signed_at, version]
     前端判 r.signed!==undefined => True; signed=False  PASS
[D3] GET /text keys: [appendices,chapters,note,sign_reward,sign_terms,sign_types,title,version] version=v12  PASS
[D2] POST /sign keys: [already_signed,balance,ok,reward,reward_granted,signed,version]
     ok=True reward_granted=True reward=10 balance=10 → 提醒响  PASS
[D1后置] 签后 status signed=True  PASS(服务端权威)
[幂等]   二次 sign already_signed=True reward_granted=False balance=10  PASS(不重复发)
[判据5 换设备] 新登录 status signed=True  PASS(服务端权威跨设备)
```

### 四、机检关
```
deploy_check: 依赖对账 PASS · ?v=一致性 PASS · 环境变量 PASS · 部署冒烟 PASS（4/4）
版本号回显: ['20','23','24']（app-data/app/core，单点递增；G-2返修 api14→15 app21→22，叠加 G-3 api15→16 app22→23）
服务端零触碰：G-2 返修仅动 index.html/api.js/app.js（前端），covenant 端点未改
```

## 五、遗留 / 挂账
1. **D4 预定拦截**挂账 F3 上线（同意）。
2. **C9 L2 定价偏高约 18 项**——原验收已同意挂账线下定级（砚仁事），本复验不重开。
3. 无新增缺陷。

---
> **太傅注**：①字段错配是「前后端契约漂移」的典型——前端读 `r.ok/nt_earned`、服务端返 `signed/reward_granted`，键名对不上则静默走兜底逻辑（补课：契约测试 contract test，前后端字段以 schema 为准）。②D1 修复的真价值＝「服务端权威」——签署状态跨设备一致，本地缓存只做纯离线兜底，不能反向污染在线判定。③D5 断死链后，离线伪造签署在联网即失效，堵住「白嫖签署奖励+绕过拦截」的双重薅羊毛口。
