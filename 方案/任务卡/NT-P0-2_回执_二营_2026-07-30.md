# NT-P0-2 回执 · 二营（豆包 Codex）

> 卡号：NT-P0-2 reserve 语义落地（方案 A）+ 追账
> 阵地：server/routes/nt.py（verify + withdraw 区块）+ server/scripts/nt_p0_2_reconcile.py（追账脚本）+ server/tests/test_nt_reserve_equation.py（新增测试）
> commit：本卡单独 commit，具名 add，不 push

## 四件套

### 1. 做了什么
- **B. verify() 等式改造**（nt.py verify 区块）：
  - `total_system` 去掉 `+ (pool.reserve or 0)`，新：`total_user_balance + balance + task_escrow + camp_balance + frozen`（reserve 移出等式）。
  - 新增 `reserve_covers_frozen = (pool.reserve or 0) >= (pool.frozen or 0)`，加入 checks 返回体，并纳入 `pass`（`... and reserve_covers_frozen`）。
  - checks 中 `reserve` 保留（仅展示，注释标明不再计入 total_system）。
- **C→准入加固**（withdraw 区块，只改准入不动三向移动）：
  - 准入从 `reserve < amount` 收紧为 `reserve < frozen + amount`，detail 明示（储备/已冻结/本次三值）。
  - user/reserve/frozen 三向移动代码（`user-=X / reserve-=X / frozen+=X`）**零改动**（守禁区）。
- **A. SQL 追账脚本** `server/scripts/nt_p0_2_reconcile.py`（只读）：对比「已结算提现总额」与「旧等式 diff（含 reserve）/ 新等式 diff（不含 reserve）」，坐实根因；核销调平需人工报砚仁签字后另行执行，脚本不代写库。

### 2. 怎么验证的
- `python -m py_compile routes/nt.py scripts/nt_p0_2_reconcile.py tests/test_nt_reserve_equation.py` 通过。
- 新增测试 `tests/test_nt_reserve_equation.py` 四例：①total_system 不含 reserve（关系式重算，reserve=777 不进）；②reserve_covers_frozen True/False 两态；③withdraw 准入 reserve<frozen+amount → 400；④reserve>=frozen+amount → 准入 200。
- 既有回归自检（静态核对）：无测试断言 verify `pass`/`diff` 绝对值；`test_accommodation_settlement/daily` 仅比较 total_system 前后相对相等（reserve 不变，不受影响）；`test_e2e_smoke`/`test_nt_camps` 提现 reserve=10000、frozen=0，收紧后 `10000>=0+amount` 仍准入——零回归。
- ⚠️ 本地无 fastapi/sqlalchemy/pytest 且离线，**无法本地实跑 pytest / 追账脚本**；判据 + 追账 SQL 结果交验收方（一营）在真库实跑贴输出。

### 3. 爆炸半径
- 改 1 文件（nt.py：verify 一处、withdraw 准入一处）+ 新增 1 脚本 + 1 测试文件。
- 影响功能：verify 等式（reserve 移出 + 新增字段）、withdraw 准入门槛（收紧）。
- 破坏性变更：verify 返回体新增 `reserve_covers_frozen`（前端可选展示）；准入变严（reserve 需覆盖 frozen+amount）。回滚：`git revert` 本 commit。

### 4. 禁区遵守
- card-confirm 区块（归 P0-1）零改动；锁序（归 P0-5）零改动；withdraw 三向资金移动零改动——只改等式与准入。

## 判据对照
1. verify 不含 reserve 计入 total_system（旧 diff 永久 -X 消除）→ test_total_system_excludes_reserve。
2. reserve_covers_frozen 字段存在且值正确 → test_reserve_covers_frozen_flag。
3. withdraw 准入 reserve<frozen+amount → 400 → test_reserve_below_frozen_plus_amount_400。
4. 追账 SQL 结果 = |历史 diff| → 脚本 nt_p0_2_reconcile.py 产出（真库实跑，截图附验收）。
5. 核销后 verify diff=0 → 人工报砚仁签字核销后由验收复核（本卡不代写库）。
6. pytest 零回归 → 待验收方实跑（静态回归自检已过）。

---
太傅注三行：
1. 方案 A 改动面最小如二营复审所判：资金流向一行不改，只动 verify 等式 + 新增背书检查 + 准入加固，风险收敛在只读/门槛两处。
2. 准入收紧为 `reserve>=frozen+amount` 让 reserve_covers_frozen 从「事后告警」升级为「准入保证」，闭环二营复审遗留备忘；历史 diff 核销涉改 total_issued，须砚仁签字，脚本只证根不代写。
3. 本机缺运行时依赖+离线，pytest 与追账脚本均未实跑，已明示交验收方真库实跑；仅 commit 不 push，禁区（card-confirm/锁序/三向移动）零改动。
