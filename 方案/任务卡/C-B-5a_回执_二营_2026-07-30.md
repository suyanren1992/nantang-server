# C-B-5a 回执 · 二营（豆包 Codex）

> 卡号：C-B-5a 素社民宿房型列表端点
> 阵地：server/routes/accommodation.py（新增端点）+ server/tests/test_inn_rooms_list.py（新增测试）
> commit：本卡单独 commit，具名 add，不 push

## 四件套

### 1. 做了什么
- `server/routes/accommodation.py`：在 `accommodation_status` 之后、`role_router` 之前，新增 `GET /api/accommodation/inn-rooms` 端点。
  - 查 `InnRoom.status=='active'` 全部房型；对每间房查其 `track=='inn' & status=='active'` 的活跃预订，将 `[checkin_date, check_out_date)` 半开区间展开为 `occupied_dates` 日期数组（去重升序）。
  - 返回 `{"rooms":[{id,label,room_type,beds,rate,dietary,status,occupied_dates}]}`。
  - 公开接口，不加鉴权（房型信息公开；与前端拉取清单场景对齐）。
- 顶部 import 增补 `date, timedelta`（原仅 `datetime`）。

### 2. 怎么验证的
- `python -m py_compile routes/accommodation.py tests/test_inn_rooms_list.py` 通过。
- 新增测试 `tests/test_inn_rooms_list.py` 三例：①返回 6 间房 + 字段齐备；②区间占用展开正确（[02-10,02-13) → 10/11/12，13 不含）；③coop 轨 / 已退房预订不计入占用。
- ⚠️ 本地环境无 fastapi/sqlalchemy/pytest 且无网络，**无法本地实跑 pytest**；判据 1-3 交验收方（一营 Claude Code）实跑贴输出。

### 3. 爆炸半径
- 改 1 文件（accommodation.py 纯新增端点 + 一行 import）+ 新增 1 测试文件。
- 影响功能：零（纯新增只读端点，不触碰任何钱路/既有端点）。
- 破坏性变更：无。回滚：`git revert` 本 commit。

### 4. 禁区遵守
- 其他 routes/*.py 未动；未加前端调用、未改前端。

## 判据对照
1. GET /api/accommodation/inn-rooms 返回 6 间房 → 端点 + 测试 test_returns_six_rooms 覆盖（seed 6 间：梅/兰/竹/菊/quadA/quadB）。
2. occupied_dates 正确反映活跃预订区间 → test_occupied_dates_interval 覆盖。
3. pytest 全绿 → 待验收方实跑。

---
太傅注三行：
1. 端点严格照任务卡骨架落地，半开区间 [checkin, checkout) 语义与既有 `_intervals_overlap`（相邻不重叠）一致，退房日不占用。
2. conftest 用 create_all 不走 init_db seed，故测试自行 seed 6 房；生产由 database.py:165 幂等 seed，双轨不冲突。
3. 本机缺运行时依赖+离线，未能实跑 pytest，已在回执明示，判据交一营实跑；施工仅 commit 不 push。
