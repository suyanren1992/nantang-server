# 任务卡 D-11：前后端契约勘察（勘察卡，只读不写代码）🟠

> 勘察方：豆包 Codex（二营） · 结论写 BUG_TRACKER.md · 丞相据结论决定施工卡
> 来源：《全面权限扫描_B方案_2026-07-24》M-13 ~ M-17

## 背景
审查发现多个服务端端点前端从未对接、或前后端行为脱节。这些**不是纯 bug，涉及产品决策**（该不该接、接了长什么样），所以先勘察后定夺，不直接施工。

## 勘察清单（逐项给结论）
1. **M-13** `POST /api/nt/verifications/{id}/reject`（nt.py:891 附近）：服务端驳回端点完备但前端从未调用。前端校核流程里"驳回"现在怎么处理的？接上这个端点需要动哪些 UI？
2. **M-14** 提现：前端仅写 localStorage，不调 `/api/nt/withdraw`。前端提现入口在哪（还是根本没有入口）？接上后端两阶段冻结流程需要哪些界面元素？
3. **M-15** Cashout 管理员审批前端未对接 → total_issued 脱节。管理后台现状如何？
4. **M-17** 4 个服务端端点前端从未调用（含 checkout → 入住后无法退房）。逐个列出端点、功能、前端缺失影响。
5. **M-16** card_discoveries 的 id 被服务端覆盖（data.py:149，A-7 同类问题）：确认现状与影响面。

## 产出格式（写进 BUG_TRACKER.md）
每个条目：**端点现状 → 前端缺口 → 建议（接 / 不接 / 降级处理）→ 预估改动面（文件清单）**。
只读不改，不写代码。拿不准的标「需丞相定夺」。
