# D-26 · NT 抵住宿费自动日扣（P0 经济闭环锚点 · 先勘察后施工）

- **档位**：🔴 大卡（资金路径 + 跨端影响），先勘察请旨后施工
- **阵地**：二营主察（`server/`），一营验
- **来源**：功能清单 F4——P0 中唯一未完全确认项；`nt.py:163` 有 ponytail 注释
  「C2/C2.5 就位后补 accommodation」；砚仁点名必须验证
- **定档人**：丞相 2026-07-26

## 第一段 · 勘察（只读，二营）

1. 读 `nt.py:163` ponytail 注释上下文 + `accommodation.py:78-105` checkout 欠费结算
   + `models.py` Accommodation 相关模型，搞清：住宿费现在到底怎么算、何时扣、谁触发
2. 查外层设计项目 C2/C2.5 是什么（方案目录/产品说明书），日扣是否已有设计稿
3. 回答三个问题呈丞相：
   a. 现状：住宿费是日扣、一次性、还是只在退房时结算？
   b. 缺口：自动日扣缺什么（cron？触发点？余额不足欠费记录？）
   c. 方案：最小施工方案 + 影响面（资金路径必配行锁，对齐 D-5/D-17 判据）
4. **实测验证**（砚仁点名）：造测试用户→产生住宿费→NT 扣减→余额不足欠费记录→退房结算，
   全流程断言（可复用 D-20 e2e 骨架）

## 第二段 · 施工（2026-07-26 砚仁定夺「准」放行，中卡直道，二营施工一营验）

按勘察最小方案三项：

1. **cron 接入日扣**：`cron.py` 每日任务接入 `daily-tick` 逻辑（不再依赖前端登录 lazy 触发）
2. **补扣漏扣天数**：按住宿记录补算历史漏扣（一次性迁移逻辑，幂等可重跑）
3. **daily-tick 鉴权收敛**：`POST /api/system/daily-tick` 收敛为 admin-only 或服务端内部调用（堵 G5）

边界（不做什么）：费率分叉（G3）与欠费追缴触发器（G4）降级 P1 另发卡，本卡不动；
BED_RATES 费率数值本卡不改。

铁律：资金路径行锁对齐 D-5/D-17 判据（`with_for_update` + `populate_existing`）；
勘察段四断言测试骨架附在 BUG_TRACKER D-26 节，施工时落成正式测试。

## 验收（一营）

- 勘察回执三问题答全、实证到行号
- 实测链路四条断言全过
- 太傅注：为什么"日扣"和"退房一次结算"在经济闭环里不是一回事（对应补课数据库/后端章节）

## 影响面声明（施工方填）

- 勘察段：只读零写入
