# DB-P0-3 施工回执

| 字段 | 值 |
|------|------|
| 卡号 | DB-P0-3_reject_reason写入_v0 |
| 施工方 | 二营 D 窗 Qoder |
| 日期 | 2026-07-31 |
| 阵地 | server/ |
| 禁区 | nantang-mobile/ (未触碰) |

## 施工内容

### ① reject_verification 加 reject_reason 参数
- `nt.py` reject_verification 函数签名：
  - 旧：`async def reject_verification(vfy_id: str, user: User = ...)`
  - 新：`async def reject_verification(vfy_id: str, reject_reason: str = Body("", embed=True), user: User = ...)`

### ② 写入 vfy.reject_reason
- `vfy.reject_reason = (reject_reason or "").strip()[:500]`（strip + 截断 500 字）

### ③ 响应加 reject_reason
- 返回 JSON 加 `reject_reason` 字段，前端可展示

### ④ 测试（4 项）

| # | 判据 | 测试 |
|---|------|------|
| 1 | 正常 reject + reject_reason → 写入 | TestRejectReasonWritten |
| 2 | 空 reject_reason → 空字符串 | TestEmptyRejectReason |
| 3 | 长 reject_reason → 截断 500 字 | TestLongRejectReason |
| 4 | verify_task reject_reason 不受影响 | TestVerifyTaskRejectUnchanged |

### ⑤ OpenAPI 契约更新
- `方案/契约/openapi_2026-07-31.yaml` 新增 `POST /nt/verifications/{vfy_id}/reject` 路径定义

## pytest 结果

```
基线: 207 passed + 8 skipped (零回归)
新增: 4 passed
总计: 211 passed + 8 skipped
```

## diff 摘要

| 文件 | 改动 |
|------|------|
| server/routes/nt.py | reject_verification +reject_reason 参数 + 写入 + 响应 |
| server/tests/test_db_p0_3_reject_reason.py | 新文件 4 测试 |
| 方案/契约/openapi_2026-07-31.yaml | +reject_verification 路径定义 |

## 禁区确认

- nantang-mobile/: 零改动 ✓
- routes/data.py: 零改动 ✓
- verify_task 路径: 零改动 ✓

## 太傅注 3 行

- **不修这 bug 校核体验差**——前端只能看到「已拒绝」看不到原因
- **不破坏 verify_task 路径**——那个端点已经正确，拒改
- **OpenAPI 同步**——拒绝契约漂移
