---
created: '2026-07-27'
project: 南塘云村
type: 任务卡
domain: 前端交互
status: 已发卡
---
# P0-1 任务双创建修复 + 余额/资金池同源（一营 · 前端中卡）🔴

> 来源：实测议单 T1/T2/T3/C11 + 二营情报回执（commit 6e2fa81，丞相复核三锚点属实）
> 施工：Claude Code（一营）｜验收：豆包 Codex（二营）
> 议定（砚仁 2026-07-27 14:38 准）：**选项 B**——`addTask` 保留同步作唯一的门，`doPublish` 删重复 POST；
> 服务端幂等双保险另立 P0-2（二营）。先议后卡首案。

## 施工内容（四件，缺一不可）

1. **双创建修复（T1/T2）**
   - `core.js:710-719` `doPublish()` else 分支：删除重复的 `API.syncTask(data, cb)` 调用——
     `AppData.addTask`（app-data.js:127）已是唯一的门。保留 `addResult.ok` 校验与收尾
     （清表单/关弹层/filterQuests/renderMyTasks/refreshUserUI），srvId 由 addTask 回调写回。
   - `app-data.js:127-130` addTask 回调：不再 `self._data.tasks[srvId]=task` 新建键；
     改为 `self._data.tasks[task.name]._srvId=srvId; ._ntTaskId=srvId; _saveShared()`。
     服务端拒绝→回滚本地的逻辑保留。
   - `core.js:937` `_mergeSyncData`：`name: t.id` 改为服务端任务真实标题（`t.title`）；
     取不到标题的条目不得拿 id 尾号当名字渲染。
2. **余额同源（T3）**——三处统一切服务端 `/api/nt/balance`（异步取值后渲染/校验；
   可复用 refreshUserUI 已拉到的值，但不得读被清空的本地 NT）：
   `core.js:1395`（资料页）· `core.js:1448-1450`（充值表单）· `core.js:1474-1475`（提现校验——
   现用本地 0 会误拦所有提现，必修）。
3. **资金池同源（C11）**：`app.js:608` `_renderPoolCard` 由 `NT.getCommunityPool()`
   改读 `AppData._data._poolBalance`（服务端 sync 写入，core.js:930-931；校核室已在用，
   ui-cardroom.js:404 可对照）。本地 `_dailyPoolRefill` 不得再影响该显示。
4. **同源遗漏普查**：grep `NT.getUser(` / `NT.getCommunityPool(` 全前端，其余显示点若同源病
   一并列入回执（不擅自扩修，报丞相定夺）。

## 爆炸半径（四答）
- 改几个文件：≤3（core.js / app-data.js / app.js）
- 影响功能：任务发布链路、余额显示三处、村口池卡
- 破坏性变更：无（不改接口、不改数据模型）
- 回滚：`git revert` 本 commit 一键

## 铁律
- 修 js 同 commit 升 index.html `?v=`；只 commit 不 push；只加具名文件禁 `git add -A`
- 不碰服务端任何文件；不碰资金发放路径（提现只改前端校验的数据源，不改服务端逻辑）
- deploy_check 全绿；回执独立落盘 `方案/任务卡/` 随 commit；末尾太傅注三行内

## 判据（验收方实跑，逐条贴证据）
1. 在线发 1 个任务 → 大厅只出现 1 个、无乱码卡；服务端 `nt_ledger` 只多 1 条冻结流水；
   余额只扣 reward×slots 一份
2. 资料页/充值/提现三处余额与顶栏一致（同读服务端）
3. seed 后村口池卡显示 500（非 50）
4. 换设备/清缓存重登 → 以上全部仍一致
