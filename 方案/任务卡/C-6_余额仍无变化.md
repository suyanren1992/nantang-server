# 复修卡 C-6：校核确认后余额仍无 +N（A-7 复修，含工作台显示）

> 施工方：Kimi Code · 先查后改 · 单独 commit，不 push

## 现象
A-7（e10349b）部署后，用户实测：卡片式上报确认后**仍没有余额 +N**，个人工作台里的余额也没变化。

## 先排除一个干扰
A-7 修复只对**新上报**的记录生效（存量记录 id 对不上）。先确认用户测的是否新上报：若是存量记录→属预期，在 BUG_TRACKER.md 注明即可；若新记录也不到账→继续查。

## 排查方向
1. 新上报→确认：approve 端点这次是否真走到了（看服务器日志/直接调接口），余额是否真加了（查库）
2. **工作台余额显示链路**：工作台读的是本地缓存还是 /api/nt/balance？确认后是否触发刷新，还是要等 30s 轮询？用户感知"没变化"可能只是界面没刷新
3. 池子扣款环节：approve 里池扣+人加是否同事务，池余额不足时是否静默失败

## 修法
- 到账但界面不刷新 → 确认成功后立即拉一次余额刷新工作台
- 没到账 → 按断点修，真因写 BUG_TRACKER.md

## 验收
新上报→确认→10 秒内工作台余额 +N 可见，刷新页面仍在。
commit：`fix(C-6): 校核到账与余额刷新（真因:___）`
