---
created: '2026-08-01'
type: 任务卡
编号: W7-COMWIN-1
标题: 社区窗口动态流内容源勘察（只读）
派给: 二营
优先级: P1（UI-3 留下的口，#18 待办阻塞营地内部 UI 设计）
轨: C 只读勘察 + 单文档写入
前置: 无 —— 现在可派
禁区: 不得改 server/ 与 nantang-mobile/ 任何文件（仅产出勘察文档）
法源: W7-UI-3 卡片（社区窗口右侧「查看窗口」按钮已有，**数据源未接**）+ 待办总账 #18 + 丞相 08-01
---

> ⚠ **本卡遵循：`Schema/施工流程.md` v0.5**（铁律 10 入口强制）
> **档位**：中卡 —— 单文档产出，但结论影响 #2 NOTIF-1、#7 EVENT-1、#17 W7-CAMP-UI-1，定档存疑升档
> **触发铁律 8（架构图更新）**：否
> **自检命令（v0.5 M-6）**：无。质量靠自检表
> **施工方开工前必答「影响面（爆炸半径）四问」**
> **回执必填**（v0.5 M-2 · 中卡）：commit hash / 验证原始输出 / 爆炸半径四问 / 未验事项 / git status / 太傅注
> **复用勘察**（铁律 11）：勘察不新造

# W7-COMWIN-1 · 社区窗口动态流内容源勘察

## 一、为什么开

皇帝 08-01：

> 「设计窗口是一直会更新内容的啊，所以社区窗口的部分是要持续地去做更新的」
> 「社区窗口的部分是要持续地去做更新的啊，不管里面有发了什么任务，然后做了哪些事情，最近动态是什么，然后可以打赏这些本身已经做了」

UI-3 已画了**社区副本页 · 第二段「社区窗口」**（详见 `data.js:686-706` 与 `社区副本_信息架构_设计稿_v0.md`），右侧「查看窗口」按钮可点，但**数据源未接**。后端已存在：

| 已存在 | 位置 | 命中数 |
|---|---|---|
| `activity_log` 模型 | `models.py:216` | 9 处落点（`archive.py:24,51,99` / `data.py:125,126,131,132` / `tasks.py:266`） |
| `/api/notifications/list` | `community.py:229` | 全员同一份，**无收件人/已读/小红点** |
| 活动流 `🕐 最近动态` | `core.js:1390` 的 `AppData._data.activity_log` | 内存态，**不持久** |

⚠ **三个看似都叫"动态流"，但其实是三件不同的事**。勘察的目的就是分清。

## 二、本卡是【勘察卡】，不是设计卡

| 你要做 | 你不要做 |
|---|---|
| 回答第三节六个问题 | ❌ 不出新方案 |
| 描述现有代码**现在跑的是什么** | ❌ 不评价好坏（好坏由皇帝/丞相定） |
| 标出哪些可直接复用 / 哪些需改造 / 哪些是死代码 | ❌ 不动一行代码 |

零写入，唯一允许的产出 = 勘察文档。

## 三、问题清单（六问，全答才合格）

### P-1 · `activity_log` 表的真相
`models.py:216` 定义了 `activity_log` 表。勘察：

1. 表的完整 schema（字段 / 类型 / 索引 / 外键）
2. **9 个写入点**（`archive.py:24,51,99` / `data.py:125,126,131,132` / `tasks.py:266`）**每条写的是什么类型的活动**？（任务完成？归档？数据初始化？）
3. 有没有**读取端点**？如果有，URL、返回结构、是否分页
4. 现有数据量（亲跑 SQL `SELECT COUNT(*) FROM activity_log`）
5. 是否带**收件人/已读字段**？（决定能不能做小红点）
6. 与 `User.id` 的关联方式（写入时存了 `user_id` 还是只存 `username`？）

### P-2 · `/api/notifications/list` 的真相
`community.py:229` 注释已知「全员同一份」。

1. 这个端点的**实际 SQL** 是什么？是从 `activity_log` 读，还是单独的通知表？
2. 有没有**类型筛选**参数（`?type=task_done` 之类）？
3. 调它的前端代码在哪（`ui-social.js` / `core.js` / `app.js` ？）—— 显示在哪里？
4. 既然「全员同一份」，**为什么之前这么设计**？有没有历史 commit/注释解释？

### P-3 · 三件"动态流"的关系

| 名字 | 落点 | 持久？ | 收件人？ |
|---|---|---|---|
| `activity_log` | `models.py:216` | ✓ | 待勘察 |
| `/api/notifications/list` | `community.py:229` | ✓ | 全员（无收件人） |
| `AppData._data.activity_log` | `core.js:1390` | ✗ 内存态 | 无 |

**请明确回答**：
- 这三者是同一份数据的三个副本？还是三个不同维度的数据？
- 如果是三个副本，**谁是源头、谁是缓存、谁是镜像**？
- 内存态那份（`core.js:1390`）刷新后是否消失？消失后前端怎么 fallback？

### P-4 · 社区窗口应展示的内容范围（**只描述现状，不定方案**）

社区窗口在 UI-3 已有草图。勘察**现有数据**能撑起哪些内容：

1. 营地动态（开营/结营/任务完成/打赏）—— 现有 `activity_log` 能查吗？
2. 物品动态（上架/售出/共享厨房预订）—— ITEM-1 未做，现在没数据
3. 事件动态（公开度三档的事情栏）—— EVENT-1 未做，现在没数据
4. 通知（点名 @ / 任务指派 / 营员报名）—— 与 `/api/notifications/list` 关系？
5. 打赏动态 —— PC 端 `ui-social.js:345` 有展示记录，**数据从哪来**？

### P-5 · 性能与并发

1. 9 个写入点是否各自带事务？写入失败会回滚吗？
2. 读取端点（如果有）是否带索引？全表扫描在 10 万行时会怎样？
3. 有没有**限流/分页**？
4. 若要做"最近 7 天"过滤，**有没有现成的索引**？

### P-6 · 与现有卡的关系（**勘察列出现状，不做规划**）

| 卡 | 关系 |
|---|---|
| `#2 NOTIF-1` 通知系统 | 通知系统与动态流是同一份还是两份？ |
| `#7 EVENT-1` 事情栏 | 公开度 public 事件要不要进动态流？ |
| `#31 物品流转` | 物品上架/售出要不要进动态流？ |
| `#15/#16` 弹幕/评论 | 与动态流的关系？ |
| `W7-CAMP-UI-1` 营地内部 UI | 营地内的「营地公告」与社区窗口的关系？ |

## 四、交付物与自检表

**交付**：`方案\社区窗口动态流_勘察v0.md`（新建）

自检逐项打勾：
- [ ] P-1 六问全答，9 个写入点每条列了写什么类型
- [ ] P-2 四问全答，附实际 SQL
- [ ] P-3 三份数据关系明确答（源头/缓存/镜像）
- [ ] P-4 五类内容现状**全列**（含"现在没数据"的明确标注）
- [ ] P-5 性能与并发四问全答
- [ ] P-6 五条关系明确答
- [ ] 结论附「可直接复用 / 需改造 / 死代码」三档分类
- [ ] **不替皇帝/丞相定方案** —— 只描述现状

## 五、不做什么（边界）

- 不改任何代码
- 不出新方案
- 不评估「社区窗口该不该做小红点」—— 那是要皇帝定的产品问题
- 不动 `/api/notifications/list` 的契约

## 六、影响面（爆炸半径）

| 问 | 答 |
|---|---|
| 调用方 | 本勘察结论会被 `#2 NOTIF-1` / `#7 EVENT-1` / `#18` 落地卡 / `W7-CAMP-UI-1` 引用 |
| 被依赖方 | `activity_log` 表、`/api/notifications/list` 端点、内存态 `AppData._data.activity_log` |
| 关联测试 | 无 |
| 回滚路径 | 单文档 revert，零业务风险 |

---
**太傅注**
- 补课：勘察不替人定方案。**写"现在跑的是什么"就够了，不写"应该跑什么"**。
- 一句原理：三个同名"动态流"是**历史债**——不同时期不同人加的，没合并过。勘察第一件事是分清它们是不是同一回事。
- 不这样做会怎样：丞相出方案时把三份当一份写，落地方案改了三份实际只需要改一份；或反之。

---

## 施工回执 · W7-COMWIN-1 · 二营

**commit hash**：`0c6fcdd`
**阵地**：全仓只读（server/ + nantang-mobile/）
**交付物**：`方案/社区窗口动态流_勘察v0.md`（320 行，P-1～P-6 六问全答）

### 验证原始输出

| 验证项 | 方法 | 结果 |
|---|---|---|
| activity_log 表 schema | Read models.py:215-220 | 4 字段（id/time/type/text），无外键无索引 |
| _INDEX_DDL 索引清单 | Read database.py:85-122 | 30 条索引，无 activity_log |
| tasks.py 写入点 | Grep `_log_activity(` | 5 个调用（299/327/353/360/389），均只 db.add 不 commit |
| data.py 写入点 | Read data.py:131-137 | 1 个 POST 端点，db.add + await db.commit |
| archive.py 引用 | Read archive.py 全文 | 24=常量 / 51=标签 / 99=只读查询，**均非写入** |
| 数据量 | 亲跑 SQL `SELECT COUNT(*)` | nantang_fresh.db = **3 行**，全 type='system'（新手引导） |
| notifications/list SQL | Read community.py:229-276 | 三表聚合（Announcement + ActivityLog + Verification），无类型筛选 |
| 前端链路 | Grep core.js/app-data.js/ui-archive.js/ui-social.js/data.js | sync_all→activity 合并、localStorage 持久、社区窗口 data.js:774 读前5条 |

### 爆炸半径四问

| 问 | 答 |
|---|---|
| 调用方 | 结论将被 NOTIF-1 / EVENT-1 / #18 落地卡 / CAMP-UI-1 引用 |
| 被依赖方 | activity_log 表、/api/notifications/list 端点、AppData._data.activity_log 内存态 |
| 关联测试 | 无（纯文档产出） |
| 回滚路径 | 单文档 revert，零业务风险 |

### 未验事项

- 历史 commit 深挖（/api/notifications/list 设计初衷追溯）—— 超出勘察范围
- 真 PG 库 activity_log 数据量 —— 仅验证了本地 SQLite nantang_fresh.db

### git status

本卡改动文件（具名 add）：
- 新增 `方案/社区窗口动态流_勘察v0.md`
- 新增 `方案/任务卡/W7-COMWIN-1_社区窗口动态流勘察.md`（含本回执）

nantang-mobile/ 下已有改动（css/main.css, ui-camp.js, ui-social.js）**非本卡产生，不纳入本卡 commit**。

---
**太傅注（回执）**
- 补课：勘察的价值在独立验证而非转述。草稿说「数百条」，亲跑 SQL 只有 3 条——差 100 倍。不跑就是抄。
- 一句原理：九个「写入点」里只有六个真写入，三个是引用。不逐行读就会把常量和只读查询当写入点统计。
- 不这样做会怎样：丞相按「九个写入点都要改」排工，实际只需改六个——虚增 50% 工作量评估。
