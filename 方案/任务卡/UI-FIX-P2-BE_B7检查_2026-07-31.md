# B7 检查：GET /api/users/me/settings（设置）

> 2026-07-31 | 检查人：Qoder 二营 | 卡号：UI-FIX-P2-BE B7

## 端点存在性

| 检查项 | 结果 |
|--------|------|
| 端点是否存在 | ❌ **不存在** |
| 最近似端点 | `GET /api/nt/sync`（含 User 基础信息，但无 settings） |

## 字段对照

| 卡面要求字段 | User 模型字段 | 匹配 |
|-------------|-------------|------|
| notification | ❌ 无此字段 | ✗ |
| theme | ❌ 无此字段 | ✗ |
| language | ❌ 无此字段 | ✗ |

## 结论

**用户设置（通知/主题/语言）后端完全未建设。** User 模型中无对应字段。

前端卡片化 UI 设置面板目前用的 localStorage 或硬编码。

## 建议

需新立卡建设：
1. `UserSettings` 表（或 User 模型扩列：notification_prefs/theme/language）
2. `GET /api/users/me/settings` 读当前用户设置
3. `PUT /api/users/me/settings` 更新设置

复杂度 ⭐⭐（纯 CRUD，无资金/校核闭环）。

**是否需要新立卡：✅ 是**
