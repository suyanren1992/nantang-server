# 任务卡 B-8：卡片室两处交互修正

> 施工方：Claude Code · 验收：Kimi Work · 单独 commit，不 push

## 修正一：「我需要帮忙」跳任务大厅
上报劳动表单里选「我需要帮忙」时，不应在当前流程里消化，应跳到任务大厅（quest hall）发起求助任务。
- 位置：卡片室上报表单（ui-cardroom.js，搜"我需要帮忙"/help 相关）
- 改法：选中该选项 → 关闭卡片室弹窗 → 调 `openQuestHallPage()`（core.js 已有），并可预填任务描述
- 若跳转发现在线/离线行为不同，在 BUG_TRACKER.md 记录

## 修正二：「上报劳动」不显示上报人
上报劳动产生的卡片/记录，在校核阶段**不显示上报人名字**——自己上报自己作证有王婆卖瓜之嫌，校核者应只针对"这件事"判断。
- 待校核卡片（`_renderVerifyCard`，ui-cardroom.js:456 起）把 `v.doer` 名字隐去，显示"有成员"+劳动内容；校核通过后才在档案/时间线里显示名字
- 注意：校核者自己的待办里若 doer 是自己，本来就该让别人校核，不做特殊处理

## 验收
- 「我需要帮忙」正确跳任务大厅
- 待校核列表看不到上报人名字，通过后时间线/档案显示
- commit message：`fix(B-8): 我需要帮忙跳任务大厅 + 待校核卡片隐去上报人`
