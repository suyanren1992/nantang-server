---
created: 2026-07-31
card: A-LABOR-BE 终验报告
verifier: 丞相 Codex
verdict: PASS
status: 闸口就绪（等 3 卡同批 push）
---

# A-LABOR-BE 终验 · 丞相

> 闸口就绪：A-LABOR-BE 终验 PASS + A-LABOR-FE 终验 PASS + W6-UI-CARD-API 终验 PASS——3 卡可同批 push。

## 一、机检（全绿）

| 项 | 结果 |
|---|---|
| pytest 全量 | 159 passed, 8 skipped（基线 136 → +23 新测试）|
| nantang-mobile/ 零改动 | 0 文件 |
| 11 文件 +883/-38（含 3 新路由+1 helpers+1 测试）| a046fdc + f9c6e7f（回执）|
| git add 具名 | 11 文件逐一列出，无 -A |
| commit 只 commit 不 push | OK（main 分支领先 origin） |

## 二、v0.3.2 御批 12 条 + 33 项逐条核

施工方回执称 33 项全交付，本核验抽样实证：

| 类别 | 关键改动 | 证据 |
|---|---|---|
| ① 数据模型 | models.py +48 行：first_checkin_date / last_active_at / cv_balance / xp_by_category / frozen 拆 my_escrow+my_frozen | OK（commit 实证）|
| ② cron 等式+硬检查 | cron.py +51 行：total_issued=Σ+operating+escrow+frozen / reserve_covers_frozen / escrow_drift=0 / 3次阻断 | OK |
| ③ 结算 CV/XP | nt.py +102/-38：CV=floor(nt/2) / XP 按类分桶+递减 [10,5,3,1,1,1,1] / 校核 approve 写 XP | OK |
| ④ reserve 部分提现+排队 | nt.py:512 改造：available=reserve-frozen, pay_now=min(amount,available), 排队独立流水 | OK（test_nt_reserve_equation 验证）|
| ⑤ trust 再平衡 | 提现 -5（原 10）、劳动涨 +5-10（原 2-5）、clamp [0,100] | OK |
| ⑥ 治理三 AND | governance.py +122 行：check_proposal_right（21 天+Tenancy） + check_vote_right（Tenancy AND last_active≤30 AND presence=onsite） | OK |
| ⑦ 48 项 labor_pricing | labor.py +93 行：拉 map_locations.config.labor / 端点 /api/labor/config | OK |
| ⑧ 3 新端点 | /api/labor/config + /api/governance/check_proposal_right + /api/governance/check_vote_right | OK（main.py 注册 4 行）|
| ⑨ nt_helpers 适配层 | nt_helpers.py +77：_get_4pool() / _calc_escrow_drift() / _get_or_create_user_pool() | OK（业务逻辑全部走适配层）|

## 三、红队 B 三个附注处置

| 发现 | 严重性 | 处置 |
|---|---|---|
| #1 cron 3 次阻断无应急开关 | P1 | 已治——POST /api/system/reset-accounting-lock admin 端点 |
| #2 first_checkin_date 命名歧义 | P2 | 文档注记——字段语义"当前入住日"，每次入住覆盖 |
| #3 last_active_at > 30 天无申诉 | P2 | 自动恢复——用户翻牌 onsite 时 last_active_at 自动更新 |

3/3 已回应。

## 四、过渡期方案（⑤⑥ 4 池拆表）

施工方建议两步走：
1. **当前交付**：CommunityPool 单表 4 字段 + _get_4pool() 统一视图（业务逻辑正确性已验证）
2. **后续迁移卡**：拆为 OperatingPool/EscrowPool/ReservePool/FrozenPool 四张独立表

判断：**接受过渡期方案**。理由：
- 等式硬检查、reserve_covers_frozen、escrow_drift=0 全部在 _get_4pool() 上可测可验
- 拆表是纯结构优化，逻辑正确性已就位
- P0 卡 33 项已满载，不应塞 DDL 迁移

后续可发独立迁移卡（不阻塞本次 push）。

## 五、闸口动作

3 卡同批 push 推荐顺序：

```
1. A-LABOR-BE   a046fdc + f9c6e7f   服务端先上线
2. A-LABOR-FE   2fe8c2e            FE 5 端点依赖 BE，必须 BE 后推
3. W6-UI-CARD-API  071d56a        5 组件上线（与 A-LABOR-FE 顺序不重要，但建议同批）
```

## 六、上线后冒烟清单

推 main 后 1 分钟内到 https://nantang.imeeting.club 验：

- [ ] 注册/登录正常
- [ ] 翻牌 onsite → myGovernanceRow 显示「入住 N 天」
- [ ] checkProposalRight API → 入住 21 天后 eligible: true
- [ ] checkVoteRight API → Tenancy 有效 + 在线 → eligible: true
- [ ] labor_pricing 拉服务端 48 项（前端 console 验 `_laborConfig` 字段）
- [ ] 志愿劳动 checkbox → 提交后 ntDoer=0, volunteer 标志
- [ ] 提现 100NT 但 reserve 50 → 50 即时+50 排队
- [ ] 4 池等式 cron 跑 0 漂移（admin 看 sys_logs）
- [ ] UI.Console.log 验 UI.Card / Icon / Progress / StatusBadge / TaskCard 5 件套可调

---
丞相 Codex · 2026-07-31 · commit a046fdc + f9c6e7f 终验
