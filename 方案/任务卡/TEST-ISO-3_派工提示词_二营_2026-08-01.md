━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  二营派工提示词（砚仁复制贴给 Qoder）
  落盘：2026-08-01 00:30
  派工方：丞相 Codex
  接收方：二营 Qoder
  卡号：TEST-ISO-3
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【二营接一卡·单窗·约 1 小时】

═══════════════════════════════════════════
TEST-ISO-3：FK 恢复 ON + 修 5 红
═══════════════════════════════════════════

任务卡：方案/任务卡/TEST-ISO-3_FK恢复ON修5红_v0.md
（先完整读卡，再动手。卡内有丞相三探针的根因实证，别重复挖。）

【先看清一件事：你的 294 passed / 0 failed 是关掉外键换来的】

  你在 TEST-ISO-1 里加了这段（conftest.py:38-43）：

      @event.listens_for(engine.sync_engine, "checkout")
      def _force_fk_off(dbapi_conn, connection_record, connection_proxy):
          cur.execute("PRAGMA foreign_keys=OFF")

  生产 database.py:81 是 PRAGMA foreign_keys=ON。
  → 测试比生产宽松。悬空外键在测试里永远照不出来，上线才炸。
  → 这与 GATE-1 刚修掉的「假绿灯」是同一类病，只是搬进了 conftest。

  丞相把 OFF 改 ON 跑全量，实测：
      5 failed, 289 passed, 8 skipped in 196.25s
      test_db_p0_1.py::TestVoteRightStrict ×3
      test_inn_rooms_list.py::TestInnRoomsList ×2
      均为 sqlite3.IntegrityError: FOREIGN KEY constraint failed

【根因丞相已挖到底，直接用，别再猜】

  三次探针（新建空库 + FK ON）：
    A：同一 flush 内 add(User) + add(Tenancy)        → FAIL
    B：先 add(User) commit，再 add(Tenancy) commit   → OK
    C：同 flush + str 日期排除类型干扰，打印 INSERT 顺序
       → FAIL，顺序 = ['tenancies']  ← User 的 INSERT 根本没发出去

  结论：Tenancy.user_id 有 ForeignKey("users.id")，但全仓 relationship() 数 = 0，
  SQLAlchemy 无从推断表间依赖，同 flush 按 mapper 注册序先插了子表 tenancies。

  **这 5 条不是烂测试，是写法踩了 ORM 的坑。**
  测试数据本身合法：room_id 没有 FK 约束（指向 map_locations 是弱引用），
  它不是失败原因。

  **生产侧无此问题**：accommodation.py:114/175 建 Tenancy 时，
  user 来自已 commit 的登录态，不在同 flush 内新建。
  → 纯测试写法问题，非业务 bug。**不要去动 accommodation.py。**

【施工 2 步】

  第一步 · 拆掉 FK OFF（必做）
    删除 conftest.py 的 _force_fk_off 事件监听，改为 FK ON，与生产一致。
    注意：_isolate_db teardown 清表须保持 FK 逆序 DELETE。
    FK ON 后顺序错会直接报错 —— 这正是我们要的约束，不是麻烦。

  第二步 · 修 5 条测试的 arrange 段（你自选实现）
    目标：让 User 的 INSERT 先于 Tenancy 落库。
    · 甲（推荐）：s.add(User) → await s.flush() → s.add(Tenancy) → await s.commit()
                 最小改动，一行 flush
    · 乙：拆成两次 commit
    · 丙：models.py 给 Tenancy 加 relationship("User") 让 ORM 自己排序
         —— 治本但动生产模型，需额外回归。**若选此路必须在回执里单独说明理由。**

    落点：server/tests/test_db_p0_1.py（3 处）
          server/tests/test_inn_rooms_list.py（2 处）

【硬约束】

  1. 不许改任何业务断言 —— 只能改测试的数据准备(arrange)段
  2. 不许 skip / xfail / 不许给这 5 测加 FK OFF 局部豁免
  3. 不许把 room_id 改成真实房间号来「绕」—— room_id 无 FK，改它属于答非所问
  4. 全量耗时 < 250s（当前 196~227s 已贴近上限，勿再拖慢）
  5. 走 server/ 阵地 · 禁区 nantang-mobile/
  6. git add 具名（禁 -A）· 1 commit
  7. **只 commit 不 push**

【判据 8 条·第 4 条是唯一真判据】

  1  conftest.py 中 PRAGMA foreign_keys 为 ON，无 OFF 残留
  2  全量 pytest → 0 failed，passed ≥ 294
  3  连跑 2 次结果一致 + 贴完整 summary 尾行
  4  反向验证：临时把某测的 flush 撤掉 → 该测转红，附前后两段输出
     （证明 FK 约束真生效，不是又被绕开）
  5  git diff 仅含 conftest.py + 那 2 个测试文件（选丙则含 models.py 并说明）
  6  deploy_check 六检全 PASS · exit 0
  7  全量耗时 < 250s，贴实测秒数
  8  只 commit 不 push · git add 具名

  判据 4 说明：TEST-ISO-1 的教训是「全绿」本身不构成证据。
  你上次交的 0 failed 全绿，正是因为约束被关掉了。
  这次必须证明「能照出红」—— 撤掉 flush 该测就该转红。
  **回执里没有判据 4 的前后两段原文，丞相直接打回，不看其他条。**

【跑测环境（本机已知坑）】

  cd server
  $env:JWT_SECRET='test-secret-key-for-ci-32bytes-long'
  ..\.venv\Scripts\python.exe -m pytest tests -q -p no:cacheprovider --basetemp=..\.scratch\iso3

  - 必带 -p no:cacheprovider（.pytest_cache 权限拒绝）
  - 必带 --basetemp 指向 .scratch（本机 Temp 目录 WinError 5）
  - 用 .venv\Scripts\python.exe，系统 Python 没装 pytest

【回执落盘】

  方案/任务卡/TEST-ISO-3_回执_二营_2026-08-01.md
  必含：① 判据 4 反向验证前后两段原文 ② 连跑 2 次 summary 尾行
        ③ 耗时实测秒数 ④ 禁区确认 ⑤ diff 摘要 ⑥ 后续建议

  注意：方案/ 已于 e891b8d 入仓，回执写完要 git add 具名提交（可与代码同 commit
  或单独 commit，说明即可）。

【完工后】
  丞相复核 → 红队验 → 砚仁批 push。
  1 营 1 窗不动。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

砚仁复制上面全文，贴给二营 Qoder 单窗。
