# 回执 · CR-3/4 资金安全补丁

- **卡号**：CR-3/4（合卡）
- **施工方**：豆包 Codex（二营）
- **日期**：2026-07-30
- **状态**：施工完成，待副署 / 待呈闸（只 commit 未 push）

## 一、改动清单（具名）
| 文件 | 改动 | 说明 |
|---|---|---|
| server/routes/accommodation.py | :31 `_get_pool(db)` → `_get_pool(db, lock=True)` | BUG-1 住宿结算写池补行锁 |
| server/routes/admin.py | :145/172/288 三处 `_get_pool(db)` → `lock=True` | BUG-1 dev-reset/seed 写池补锁 |
| server/routes/nt.py | verify_task partial 分支 → `if unclaimed > 0 and not is_camp` | BUG-2 营地任务不退款（防造币） |
| server/tests/test_cr4_camp_partial_settle.py | 新增 2 用例 | 营地不送钱+守恒 / 个人退款回归 |

## 二、对派发单的偏离与理由（重要）
派发单 BUG-2 原药方为「`if is_camp` 提前，未领 `camp_balance += unclaimed`」。
**施工中实测证明该药方仍造币**，故未采纳，改为「营地任务不退款」。

**证据（实测）**：
- CR-1 已修复：`tasks.py:146` 营地任务 `escrow_amount=0`，`:150` 创建时不扣款/不冻结 camp_balance。
- 结算 `nt.py:722` `camp_balance -= total_payout(实付)`；未领份额的钱**从未离开** camp_balance。
- 三方案实测 verify() 结果：
  | 方案 | poster 余额 | camp_balance delta | total_system delta |
  |---|---|---|---|
  | 原 buggy（poster += unclaimed） | +20 凭空 | -10 | **+20 造币** |
  | 派发单（camp_balance += unclaimed） | 0 | 0（应-10） | **+10 造币** |
  | 本卡（not is_camp 不退） | 0 ✅ | -10 ✅ | **0 守恒** ✅ |

## 三、测试
```
pytest tests/ -q
→ 125 passed, 8 skipped, in ~69s
```
（8 skipped = requires_pg：SQLite 下行锁无效，PG 专项跳过，符合 conftest 设计）
新增用例 `test_cr4_camp_partial_settle.py` 2 条全绿。

## 四、Commit
- 消息：`fix(CR-3/4): 住宿裸池+营地送钱 · 二营`
- 仅 commit，未 push（等御笔 push 令）。
- `git add` 具名四文件，未用 `-A`。

## 五、遗留发现（另案，非本卡范围）
营地支付奖励账本记 `from='escrow'` 而非 `'camp_pool'`，导致 `verify().camp_pool_drift`
出现 `-total_payout` 缺口（本次测得 -10）。这是营地支付路径**既有**账本记账缺口，
与本卡（锁 + 送钱）无关，不在本卡修复范围。建议另开卡：营地任务奖励结算账本
`from_user` 由 `escrow` 改为 `camp_pool`，使 camp_pool_ledger 随支付同步下降。
