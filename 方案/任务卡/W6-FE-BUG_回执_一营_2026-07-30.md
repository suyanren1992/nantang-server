# W6-FE-BUG 施工回执（一营 · Claude Code）

**时间**：2026-07-30  
**卡片**：W6-FE-BUG 前端三bug集中修复  
**Commit**：`ea6f063`  
**状态**：施工完成，待验收

## ① diff 摘要

| 文件 | 改动 | ?v= |
|------|------|-----|
| `nantang-mobile/js/api.js` | `syncDiscovery` 补 `.then` 接收服务端返回 id（之前 fire-and-forget） | 22→23 |
| `nantang-mobile/js/ui-cardroom.js` | 创建发现后调 `API.syncDiscovery(disc)`，服务端 id 写回 `_saveDiscoveries()` | 13→14 |
| `nantang-mobile/index.html` | 版本号 bump | — |

### BUG-1 / BUG-2：已修复（前次提交）

| 卡号 | API 方法 | 位置 | 调用链 |
|------|----------|------|--------|
| ① 校核退回 | `API.rejectVerification(id, reason)` | api.js:135 | app-data.js:383 → 退回按钮(core.js:163 / app.js:851 / ui-cardroom.js:520,561) |
| ② 提现 | `API.withdraw(amount)` | api.js:136 | core.js:1576 `submitWithdraw()` |
| ② 管理员审批提现 | `API.confirmWithdraw(entryId)` / `API.rejectWithdraw(entryId)` | api.js:143-144 | core.js:1658 `approveWithdraw()` / 1667 `rejectWithdrawAdmin()` |

## ② 自测

```bash
$ node --check js/api.js && node --check js/ui-cardroom.js
api.js OK
ui-cardroom.js OK

$ grep -n "syncDiscovery" nantang-mobile/js/api.js
198:  syncDiscovery: function(disc) {      # 定义 → return .then()

$ grep -n "API.syncDiscovery" nantang-mobile/js/ui-cardroom.js
1092:    API.syncDiscovery(disc).then(...)  # 调用 → 写回服务端 id
```

## ③ 禁区

```
$ git diff --stat HEAD~1 -- server/
（空输出 — server/ 零改动）
```

## ④ 皇帝验收单

| # | 判据 | 状态 |
|---|------|------|
| H1 | `syncDiscovery` 返回 Promise（非 fire-and-forget） | ✅ |
| H2 | 调用处 `.then` 接收服务端 id 写回 `disc.id` | ✅ |
| H3 | `.catch` 静默兜底（本地已保存，不断 UI） | ✅ |
| H4 | `node --check` 全绿 | ✅ |
| H5 | `?v=` bump（api.js 23 / ui-cardroom.js 14） | ✅ |
| H6 | `server/` zero diff | ✅ |
| H7 | 具名 `git add`（3 文件，禁 -A） | ✅ |
| H8 | 不 push | ✅ |

### 皇帝亲验命令

```bash
# 验证 syncDiscovery 返回 Promise
grep -A3 "syncDiscovery: function" nantang-mobile/js/api.js | grep "return"

# 验证调用处写回
grep -A2 "API.syncDiscovery" nantang-mobile/js/ui-cardroom.js | grep "disc.id"

# 禁区
git diff --stat HEAD~1 -- server/   # 应为空
```
