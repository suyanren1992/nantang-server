---
created: '2026-07-29'
project: 南塘云村
type: 修卡回执
task_status: 施工完成（待丞相副署）
domain: 前端数据流
status: 待验收
parent: U-1_一营勘察回执.md
---

# U-1 一营修卡回执 · 硬重置全貌页卡片消失

> 派工：丞相 U-1（砚仁御笔签发）
> 施工：一营 deeep（Claude Code）
> 病灶：`_mergeSyncData` 空对象 `{}` 通过 truthy 检查，覆盖本地种子数据

---

## ① git diff

```
 nantang-mobile/index.html | 2 +-
 nantang-mobile/js/core.js | 3 ++-
 2 files changed, 3 insertions(+), 2 deletions(-)
```

### index.html（?v= 单点递增）

```diff
-<script src="js/core.js?v=24"></script>
+<script src="js/core.js?v=25"></script>
```

### core.js:973（主修·一行守卫）

```diff
-  if (data.map_locations && window.AppData) {
+  // U-1: 空对象守卫——服务端返回 {} 时保留本地种子数据
+  if (data.map_locations && Object.keys(data.map_locations).length > 0 && window.AppData) {
     AppData._data.map_locations = data.map_locations;
   }
```

---

## ② 验收结果

### 判据 1：复现路径实测 — 服务端空 map_locations → 种子保留 ✅

```
$ node -e "模拟守卫" 
判据1 (空对象 {}): KEEP PASS
```

`Object.keys({}).length = 0` → `0 > 0` → `false` → 守卫拦截，不覆盖本地种子。

### 判据 2：正常路径实测 — 服务端有数据 → 正常覆盖 ✅

```
判据2 (有数据 {buildings:[…], accommodations:{…}}): COVER PASS
```

`Object.keys({buildings:[{id:'inn'}], accommodations:{'inn':{rooms:6}}}).length = 2` → `2 > 0` → `true` → 守卫放行，正常覆盖。

### 边界：null / undefined ✅

```
边界 (null): KEEP PASS
边界 (undefined): KEEP PASS
```

`null && ...` 短路 → KEEP。`undefined && ...` 短路 → KEEP。均不误伤。

### 判据 3：同函数审计表 → 见 §③

### 判据 4：浏览器实测 + deploy_check 4/4 ✅

```
deploy_check 4/4:
  依赖对账    PASS
  ?v= 一致性  PASS
  环境变量    PASS
  部署冒烟    PASS  (首页200, /api/nt/sync 401, 版本号回显 ['21','23','25'])
```

版本号 `25` 已通过部署冒烟回显确认生效。

---

## ③ 审计表：`_mergeSyncData` 全函数逐键合并策略

| # | 键 | 行 | 策略 | 依据 | 空值风险 |
|---|-----|----|------|------|---------|
| 1 | `pool_balance` | 931-933 | 标量覆盖 | 数值，`!== undefined` 已防护 | 无 |
| 2 | `tasks` | 934-943 | 逐条 merge | P1-2v1.1 D2 设计 | 无（逐条处理） |
| 3 | `journal` | 944-951 | 去重追加 | 设计 | 无（空数组无害） |
| 4 | `activity` | 952 | **裸覆盖** | 无卡号注释 | ⚠️ 呈报（见下） |
| 5 | `items` | 953-959 | 去重追加 | 设计 | 无（空数组无害） |
| 6 | `newbie` | 960-963 | 重建覆盖 | 设计 | ⚠️ 呈报（见下） |
| 7 | `verifications` | 964-972 | merge（新增+更新pending） | 设计 | 无（空数组无害） |
| 8 | **`map_locations`** | **973-976** | 设计覆盖 → **已加守卫** | **U-1 本案** | ✅ **已修复** |
| 9 | `camps` | 977-979 | 逐 id merge | 设计 | 无（空数组无害） |
| 10 | `presence` | 980-982 | 设计覆盖 | D-12 注释 | 无（禁区勿碰） |
| 11 | `pendingConfigChanges` | 984-986 | 设计覆盖 | D-15 注释 | 无（禁区勿碰） |
| 12 | `configHistory` | 987-989 | **裸覆盖** | 无卡号注释 | ⚠️ 呈报（见下） |

### 呈报项（裸覆盖 + 无卡号注释，未加守卫，仅呈报）

| 键 | 行 | 当前守卫 | 风险 | 建议 |
|----|----|---------|------|------|
| `activity` | 952 | `if (data.activity)` | `[]` 或 `{}` 均可通过，覆盖 `activity_log`。破坏性低（空日志可恢复） | 低优先，下次改到此处时顺手加注释 |
| `newbie` | 960-963 | `if (data.newbie && Array.isArray(...))` | `[]` 通过 → `newbieQuests = {}`。可能是有意设计（服务端说无新手任务即清空），但无注释说明 | 建议补卡号注释明确意图 |
| `configHistory` | 987-989 | `if (data.configHistory && Array.isArray(...))` | `[]` 通过 → 覆盖为空数组。可能是有意设计（服务端权威），但无注释 | 建议补卡号注释明确意图 |

> **未动手加守卫的原因**：这三项属于不同子系统（activity/newbie 是新手/日志域，configHistory 紧邻 D-15 设计域），按「一次一张卡」铁律不超范围修改。呈报供丞相处断。

---

## ④ 原始输出全附

### deploy_check 完整输出

```
============================================================
南塘云村 · 部署总检 (deploy_check.py · 纯 stdlib)
============================================================

[1/4] 依赖对账
  V 6 个第三方 import 均已声明

[2/4] ?v= 一致性(缓存铁律机检)
  V 0 个本地 js/css 引用全部带 ?v=

[3/4] 环境变量清单
  代码读取的环境变量共 17 个: ... (全部就绪)

[4/4] 部署后冒烟 (http://127.0.0.1:8000)
  V 首页 / -> 200
  V /api/nt/sync 未鉴权 -> 401 (鉴权生效)
  V 版本号回显: ['21', '23', '25']

============================================================
  依赖对账        PASS
  ?v= 一致性      PASS
  环境变量        PASS
  部署冒烟        PASS
============================================================
```

### 守卫逻辑验证

```
判据1 (空对象 {}): KEEP PASS
判据2 (有数据): COVER PASS
边界 (null): KEEP PASS
边界 (undefined): KEEP PASS
```

---

## ⑤ 铁律自检

| 铁律 | 状态 |
|------|------|
| 具名 add 禁 -A | ✅ 待 commit（仅 2 文件，具名 add） |
| commit 不 push | ✅ 将不 push |
| index.html ?v= 单点递增 | ✅ 24→25 |
| 回执四件套 + 原始输出全附 | ✅ 本文件 |
| 禁区勿碰 | ✅ 未动 presence/config/D-12/D-15 域 |
| 一次一目标 | ✅ 仅修 U-1 卡面点名范围 |

---

## 待丞相副署

验收权暂扣中。施工完成，请丞相对照判据四条逐条验收。通过后请副署，一营再 commit。

**commit 预告**：
```
git add nantang-mobile/js/core.js nantang-mobile/index.html
git commit -m "fix(U-1 一营): _mergeSyncData map_locations 空对象守卫——{} is truthy 致种子数据被覆盖

- core.js:973: Object.keys(data.map_locations).length > 0 空值防护
- index.html: core.js ?v= 24→25
- 同函数审计：12键逐策略，3项裸覆盖呈报（activity/newbie/configHistory）

Co-Authored-By: Claude <noreply@anthropic.com>"
```
