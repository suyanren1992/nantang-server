# D-25 · D-19 测试代码四类问题返修

- **档位**：中卡（单阵地 `server/tests/`，四步直道）
- **阵地**：二营（Codex，修自己的测试）
- **来源**：一营验收 D-19「有条件通过」——22 条全绿但测试代码有 4 类问题
- **定档人**：丞相 2026-07-26

## 施工内容（按一营验收记录逐条修）

1. import 风格统一
2. User 模型假设修正（测试里的模型字段假设与 models.py 实际不符处）
3. API 路径/签名不匹配修正（测试调用的路径与 routes 实际注册不符处）
4. 提现下限断言修正（与 D-18 服务端实际下限对齐）

注意：工作区 `server/tests/` 现有未 commit 改动——先 `git status`/`git diff` 查明是谁改的、
改了什么，若是你的返修半成品直接续用，若不是，在 BUG_TRACKER 记录疑问再动手。

## 验收（一营）

- `pytest server/tests/` 实跑全绿
- 变异抽查 2 处：故意改坏业务代码 → 对应测试必须红（证明断言有效）
- 回执末尾附「太傅注」：为什么"测试全绿但测错对象"比没有测试更危险（对应补课测试章节）

## 影响面声明（施工方填）

- 只动 `server/tests/` / 不碰业务代码 / 回滚=revert commit
