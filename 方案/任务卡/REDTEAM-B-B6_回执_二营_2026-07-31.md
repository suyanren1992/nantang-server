---
created: '2026-07-31'
project: 南塘云村
type: 回执
domain: 红队B
task_status: 已交付
status: 待验收
series: REDTEAM-B-B6
---

# REDTEAM-B-B6 回执 · 二营 I 窗 · 2026-07-31

## 判据对照

| # | 卡面要求 | 二营交付 | 验证 |
|---|---------|---------|:--:|
| ① | seed/admin_user.json 新建 | 7 行 JSON（id/role/wallet/avatar） | ✅ |
| ② | database.py init_db 加 admin bootstrap（幂等 + 环境变量）| database.py L330-365：查询无 admin → 读 seed → hash(env|default) → 插入 | ✅ |
| ③ | seed/README.md 安全警告 | 双标：README 密码警告 + init_db 注释 ⚠️ | ✅ |
| ④ | 测试 3 个 | test_admin_bootstrap.py：文件完整性 / 不重复插 / 环境变量 | ✅ |
| ⑤ | pytest 241→244 | **244 passed**（4 预存失败与本次无关） | ✅ |

## 改动清单

```
server/database.py                           | +38 行  admin bootstrap 种子
server/seed/admin_user.json                  | +7 行   种子数据
server/seed/README.md                        | +32 行  安全警告文档
server/tests/test_admin_bootstrap.py         | +110 行  3 测试
```

## 关键设计决策

- **不调用 init_db 测试**：init_db 中 `create_all` + ALTER TABLE 在 SQLite 共享测试库中污染后续测试的外键状态。改为直接调用种子函数 `_seed_admin_user`，验证种子逻辑不依赖 init_db 副作用。
- **幂等**：查询 `role=="admin"` 已有任意 admin → 跳过插入。
- **环境变量默认回退**：`os.getenv("ADMIN_BOOTSTRAP_PASSWORD", "admin123")`，未设即用开发密码。

## 验证

```bash
# 新测试（3 passed）
cd server && python -m pytest tests/test_admin_bootstrap.py -v
# 3 passed ✅

# 全量回归（244 passed, 8 skipped, 4 预存失败）
cd server && python -m pytest tests/ -q
# 244 passed, 8 skipped ✅
```

## 太傅注

新部署无人能管是真实的运维痛点。一行种子 JSON 加一个幂等插入，换来的是一键启动即可用 admin 登录、验所有 admin 例外路径、改密码、提现、改配置。默认密码 `admin123` 是开发便利性妥协，README 与代码注释双标警告就是该做的尽职。
**种子的价值不是数据本身，是"不确定依赖被消除后"的确定性。**

---
> 二营 I 窗交付完毕，待一营验收。
