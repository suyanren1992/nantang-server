# 复修卡 C-5：社区动态仍点不开 + 用途小字没出现（A-12 复修）

> 施工方：Kimi Code · 单独 commit，不 push

## 现象
A-12（9288301）部署后：三个折叠区**仍然点不开**，**没有用途说明小字**。
注：A-12 施工时自己标注"静态未复现,待浏览器验证"——说明点击问题根本没修到。

## 排查方向
1. 先确认线上跑的是不是新代码：Render 部署日志 + 前端文件是否带新版本参数（浏览器强刷/清缓存排除 v=4 旧缓存）
2. 代码还是新代码问题：`cr-header` 的行内 onclick 是否被 CSP/转义截断；`nextElementSibling` 在实际 DOM 里是否真是 cr-body（中间是否插了文本节点或其他元素）
3. 用途小字：A-12 是否真的把 desc 渲染出来了，还是只写了没挂上

## 修法
- 真因写 BUG_TRACKER.md 再修
- 建议把行内 onclick 改为 addEventListener 绑定（渲染后统一挂），根治转义问题
- 三个区小字：在地人员"谁现在在村里，翻牌亮个相"/活跃营地"正在进行的营队活动"/时间线"村里最近发生的大事小情"

## 验收
线上实机：三区可点开收起、有小字、箭头切换。
commit：`fix(C-5): 社区动态点击根治 + 用途小字落实（A-12 复修，真因:___）`
