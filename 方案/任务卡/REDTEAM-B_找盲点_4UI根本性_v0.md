━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  南塘云村 · 任务派发单（红队 B 找盲点）
  卡号：REDTEAM-B_找盲点_4UI根本性_v0
  阶段：砚仁 20:15 批甲·4 项 UI 根本性问题排查
  施工方：红队 B（第三方 AI·找盲点视角）
  验收方：丞相 Codex + 1 营 + 2 营
  立卡：丞相 Codex 2026-07-31 20:18
  法源：砚仁 20:10 原话「根本性的问题」+ 4 项 UI 修复未生效
  优先级：P0（砚仁怒报·根因排查）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【背景】
  砚仁 20:00-20:15 多次报：
  - 田间管理没解决
  - 校核提示字仍糊（与背景融）
  - 共享厨房没看到
  - 世界终端不见
  - 「整体跟之前没啥区别」

  4 项 commit 已推（CI 绿·部署应完）：
  - 田间 I1：a0b7608
  - 校核 H1：2babdcf
  - 世界终端 ⑫：8a15f05
  - 共享厨房 FE：❌ 真未做（BE 完·FE 缺）

  砚仁 20:15 批甲：派红队 B 找盲点
  → 不预设修法·纯找根因
  → 1 营重做时按红队 B 报告修

【施工内容 · 4 项盲点排查】

━━━━━━━━━━━━━━━━━━━━━━━━━━
## A. 田间管理盲点排查（commit a0b7608）
━━━━━━━━━━━━━━━━━━━━━━━━━━

  排查问题：
  1. _showFieldSheet 入口在哪里调？是否被其他函数覆盖？
  2. I1 commit 改的是 renderFieldSheet 还是别处？
  3. 是否只改了样式没改数据流？
  4. 砚仁看到的是「点击没反应」还是「样式错」还是「数据空」？

  排查路径：
  1. git show a0b7608 --stat 看改了哪些文件
  2. git show a0b7608 -- nantang-mobile/js/app.js | grep -A 20 "showFieldSheet\|FieldCard\|field"
  3. 搜 _showFieldSheet 调用点
  4. 搜田间相关 onclick 事件

━━━━━━━━━━━━━━━━━━━━━━━━━━
## B. 校核提示字盲点排查（commit 2babdcf）
━━━━━━━━━━━━━━━━━━━━━━━━━━

  排查问题：
  1. H1 commit 改了哪里？Card 包裹了哪个元素？
  2. 提示字（"不能校核自己"）是哪个函数写的？
  3. 背景色 token 用对了吗？（--g-card / --g-bg / --g-text）
  4. 是否只改了空态没改有数据态？

  排查路径：
  1. git show 2babdcf -- nantang-mobile/js/app.js | grep -A 10 "校核\|verify\|Card"
  2. 搜 _showCardPopup 内 verifyAction 相关代码
  3. 搜 .ui-card 样式确认 token 生效
  4. 看 main.css --g-card / --g-bg / --g-text 颜色对比度

━━━━━━━━━━━━━━━━━━━━━━━━━━
## C. 世界终端盲点排查（commit 8a15f05）
━━━━━━━━━━━━━━━━━━━━━━━━━━

  排查问题：
  1. ⑫ commit 在 admin 仪表盘加了世界终端入口，但砚仁（admin 角色）实测不见
  2. 角色判断：user.role === "admin" 分支在哪？
  3. 入口是否被 role 过滤掉了？
  4. 砚仁当前是不是 admin 角色？

  排查路径：
  1. git show 8a15f05 -- nantang-mobile/js/app.js | grep -A 15 "世界终端\|admin"
  2. 搜 role 分支：if (user.role === "admin")
  3. 搜 "世界终端" 字符串
  4. 看砚仁登录后 user.role 实际值

━━━━━━━━━━━━━━━━━━━━━━━━━━
## D. 共享厨房 FE 盲点排查
━━━━━━━━━━━━━━━━━━━━━━━━━━

  排查问题：
  1. 厨房面板入口在哪？全貌页 _renderMgmtCards 是否有 🍳 卡？
  2. 厨房入口调 openKitchenPage 还是 openQuickMenu('厨房')？
  3. BE 10 端点已就绪（973f855），FE 是否接了？
  4. 砚仁看到的"没看到"= 入口缺失 / 点击没反应 / 数据空？

  排查路径：
  1. grep -rn "openKitchenPage\|_openKitchen" nantang-mobile/js/
  2. 看 _renderMgmtCards 厨房卡 onclick 调什么
  3. 看 ui-cardroom / ui-camp / app.js 厨房入口
  4. 看 api.js kitchen 端点接入情况

【约束】
  - 不预设修法
  - 纯找根因（盲点 = 路径/角色/数据/文化盲区）
  - 1 commit（1 份盲点报告）
  - 落方案/任务卡/REDTEAM-B_找盲点_4UI根本性_回执_红队B_2026-07-31.md
  - **只 commit 不 push**

【产出】
  - 4 项盲点报告（每项 1 段）：
    · 现象（砚仁看到的）
    · 根因（commit 修了什么 / 实际缺什么）
    · 建议修法（不写代码·只指方向）
  - 整体盲点类型归类（路径/角色/数据/文化）
  - 优先级排序（哪个先修）

【回执落盘】
  方案/任务卡/REDTEAM-B_找盲点_4UI根本性_回执_红队B_2026-07-31.md

【太傅注 3 行】
  - **不预设修法**·红队 B 的价值在「砚仁/丞相/施工方都看不到的角度」
  - **4 维度盲区**·路径（入口在哪）/ 角色（谁看得到）/ 数据（数据契约对得上吗）/ 文化（产品意图理解对吗）
  - **报告 = 处方**·1 营重做时按报告修·不再凭印象
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━