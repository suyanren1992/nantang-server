# NT-P0-1 回执 · 二营（豆包 Codex）

> 卡号：NT-P0-1 修社区池=0 联动 bug（卡片确认拒绝+提醒兜底）
> 阵地：server/routes/nt.py（card-confirm 区块）+ server/tests/test_nt_pool_insufficient.py（新增测试）
> commit：本卡单独 commit，具名 add，不 push

## 四件套

### 1. 做了什么
- `server/routes/nt.py` card_confirm 区块：把 `_grant_from_pool` 调用包进 try/except。
  - 池余额不足（_grant_from_pool 抛 400「社区池余额不足…」）时：
    - `logger.warning("[NT-P0-1] card-confirm 社区池不足，需 admin 注资…")` 落日志提醒 admin（disc/to/need/detail 全带）；
    - 重抛 `HTTPException(400, "社区池余额不足，请联系管理员注资（原 detail）")`——明确 400、非 500、非卡死。
  - 其他异常原样上抛（不吞）。
- 模块顶部新增 `import logging` + `logger = logging.getLogger("nt")`。
- **未改 _grant_from_pool**：授奖权威校验（锁池/并发/池不足）仍由 helper 内部保障；本卡只在 card-confirm 层加提醒+清晰化，earn 对外行为零影响。

### 2. 怎么验证的
- `python -m py_compile routes/nt.py tests/test_nt_pool_insufficient.py` 通过。
- 新增测试 `tests/test_nt_pool_insufficient.py` 两例：①pool=0 → 400 + detail 含「请联系管理员」+ caplog 断言 admin 提醒落日志 + 未扣池/未到账/状态仍 pending；②pool 充足正常确认到账。
- 既有 `test_nt_card_confirm.py::test_pool_insufficient_400` 断言 detail 含「余额不足」+ 状态 pending——新 detail「社区池余额不足，请联系管理员注资（…）」仍含「余额不足」，零回归。
- ⚠️ 本地无 fastapi/sqlalchemy/pytest 且离线，**无法本地实跑 pytest**；判据交验收方实跑。

### 3. 爆炸半径
- 改 1 文件（nt.py：+2 行 import/logger，card-confirm 一处 try/except）+ 新增 1 测试文件。
- 影响功能：仅 card-confirm 池不足时降级为「明确 400 + admin 日志提醒」；池充足与其他端点零影响。
- 破坏性变更：错误 detail 文案变化（仍 400，仍含「余额不足」），前端如按文案匹配需适配（建议前端按 status_code 判定）。回滚：`git revert` 本 commit。

### 4. 禁区遵守
- withdraw / confirm（提现确认）/ reject 资金端点零改动；transfer / withdraw 逻辑未动。
- 未改前端。admin 提醒采用「日志标记」（任务卡明许），错误路径 raise 会回滚事务，故不写 DB 通知（避免被回滚不落库）。

## 判据对照
1. pool=0 → card-confirm 返回明确 400（非 500/超时）→ 端点 + test 覆盖。
2. admin 可观察「池不足」提醒（日志）→ logger.warning + caplog 断言覆盖。
3. 池充足正常确认不受影响 → test_pool_sufficient_still_ok 覆盖。
4. pytest 零回归 → 待验收方实跑（既有 6 测文案兼容已核）。

## 前端接线提示（供一营）
- card-confirm 池不足统一返回 HTTP 400，detail = 「社区池余额不足，请联系管理员注资（社区池余额不足（当前 X NT，需 Y NT））」。
- 前端应按 **status_code==400** 判定并弹「池不足，请联系管理员」友好提示，勿卡在 loading；勿按 detail 文案精确匹配。

---
太傅注三行：
1. 根因非「500 崩」而是「池不足无出口 + admin 无感」——本卡在 card-confirm 层加提醒+清晰化，不动 helper 权威校验，最小面收敛。
2. admin 提醒走 logger 而非 DB：错误路径 HTTPException 触发事务回滚，DB 通知会被一并回滚不落库，故遵任务卡「日志标记」口径。
3. 本机缺运行时依赖+离线未实跑 pytest，已明示；仅 commit 不 push，禁区（withdraw/confirm/reject）零改动。
