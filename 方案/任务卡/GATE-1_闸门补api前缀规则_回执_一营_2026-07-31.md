# GATE-1 闸门补 api 前缀规则 · 回执 · 一营

**卡号**：GATE-1_闸门补api前缀规则_v0  
**验收**：一营 Claude Code（对抗验收方）  
**日期**：2026-07-31  
**二营 Commit**：`537935e`（fix(GATE-1): deploy_check /api前缀强制规则 + nt.py注释更正 + 3测）  
**验收环境**：当前 HEAD `538e76b`（含 TEST-ISO conftest 修复 + .gitignore）  
**结论**：✅ **通过**

---

## ① 代码审阅

| 文件 | 变更 | 审阅 |
|---|---|---|
| `server/scripts/deploy_check.py` | +33/-7，新增 `check_api_prefix()` 纯函数 | ✅ 扫全 `js/*.js`，正则 `this.request\(…\)` 正确匹配单引号字面量，拼接路径（`'/api/tasks/' + id`）前缀判准 |
| `server/routes/nt.py` | -1 行，删 `【NT-P0-6】pool_refill` 死链注释 | ✅ 保留句改为「池空时直接拒绝派工（见本函数上游 400 分支），不补印、不欠账。」，NFT 指向已清 |
| `server/tests/test_gate1_api_prefix.py` | +41 行，3 测 | ✅ 结构完整：裸路径检出 / 前缀通过 / 回归哨兵 |

`check_api_prefix` 函数签名纯函数，可单测；`check_api_contract`([deploy_check.py:320](server/scripts/deploy_check.py#L320)) 已集成调用。

---

## ② 反向验证（唯一真判据）

### 2a. 裸路径注入 → FAIL

```diff
+  _testBarePath: function() { return this.request('GET', '/fields'); },
```

```text
[4/6] API 契约机检
  X 裸路径(非 /api/ 前缀): api.js:282: GET /fields
  X 存在 1 个裸路径(非 /api/ 前缀), 请补全 /api/ 前缀后重跑
  API契约           FAIL
```

**exit code 1** ✅

### 2b. 还原 → PASS

```text
[4/6] API 契约机检
  V 前端 78 调用全部命中后端路由(路径/方法一致), 裸路径 0
  API契约           PASS
```

**exit code 0** ✅

### 2c. git diff 清空

```text
$ git diff -- nantang-mobile/
(空输出)
```

✅ 注入行已完全还原。

---

## ③ 测试结果

### 3a. 当前 HEAD（`538e76b`，含 TEST-ISO conftest 修复）

```text
$ python -m pytest server/tests/ -p no:cacheprovider --tb=no -q
........................................................................
........................................................................
........................................................................
........................................................................
294 passed, 8 skipped, 1929 warnings in 220.34s (0:03:40)
```

| 指标 | 值 |
|---|---|
| passed | **294** |
| failed | **0** |
| skipped | 8 |
| gate1 3 测 | 3/3 ✅ |

### 3b. GATE-1 commit `537935e`（clean checkout，无 conftest 修复）

丞相实测：**286 passed / 8 failed**。8 红为 FOREIGN KEY constraint failed（`test_db_p0_1` / `test_dev_reset` / `test_inn_rooms_list` / `test_inn_track`），根因是 `init_db()` → PRAGMA foreign_keys=ON 污染连接池，与 GATE-1 无关。`538e76b`（conftest 切 `create_all` + FK OFF checkout）已消除全部 8 红。

### 3c. gate1 3 测独立跑

```text
$ python -m pytest server/tests/test_gate1_api_prefix.py -v
test_deploy_check_detects_bare_path PASSED
test_deploy_check_accepts_api_prefix PASSED
test_current_api_js_all_prefixed PASSED
======================== 3 passed in 1.25s ========================
```

---

## ④ 判定

**通过。** 规则正确、判据可复现、测试覆盖充分。

### 顺带处理

| 项 | 处理 | Commit |
|---|---|---|
| `conftest.py` 悬空改动（FK constraint 假红） | 已提交：`create_all` + 轻量 `_reseed_baseline()` + FK OFF checkout | `538e76b` |
| `nantang_fresh.db.bak` 遗留 | 已删除 + `.gitignore` 补 `*.db.bak` | `538e76b` |

### 已知未处理

`056b3f5` 把 nt.py 里 `【NT-P0-6】` 溯源标记整行删掉——建议补回，但属 TEST-ISO 线，非本卡范围。
