---
created: '2026-07-28'
project: 南塘云村
type: 回执
task_status: 二营施工完成待验收
domain: 激励经济
status: 讨论中
---

# G-1 公约签署凭证 + 文本 config 二营施工回执

> 卡面：方案/任务卡/G-1_公约签署凭证与文本config.md｜施工基底：main 510cbf9
> 施工：豆包 Codex（二营·后端）｜验收：Claude Code（一营）
> 公约全文源：方案/社区公约与共居住约定_v12.md

## 涉及文件（路径:行号）

| 文件 | 变更 | 锚点 |
|------|------|------|
| `server/models.py` | +CovenantSignature 表（user+version 唯一约束） | models.py:2（import UniqueConstraint）/ models.py:296-304 |
| `server/routes/covenant.py` | 新文件——sign/status/text 三端点 + 默认文本 config | covenant.py（全 169 行） |
| `server/main.py` | 注册 covenant.router | main.py:10 / main.py:89 |
| `server/tests/test_covenant.py` | 新文件——6 测试覆盖判据 1-4 + 契约边界 | test_covenant.py |

## 四件施工归因与证据链

**① 签署凭证模型**（covenant.py 无表逻辑，表在 models）
- `covenant_signatures`：id / user_id / covenant_version / sign_type / reward_granted / signed_at
- `UniqueConstraint("user_id","covenant_version")` 防同版本重复签 → models.py:304（锚点）
- create_all 自动建表（database.py:40），无需手写迁移

**② 签署端点 `POST /api/covenant/sign`**（covenant.py:107-166）
- 当前版本取自 covenant_text config 的 `version`（covenant.py:112）
- 签旧版本（客户端显式传非当前版本）→ 409（covenant.py:116-118）
- 幂等：已签当前版本 → 直接返回 `already_signed:true`，不重复发 NT（covenant.py:121-133）
- 首签发 NT（**钱随单走**）：`pool.balance -= reward`（covenant.py:155）+ `user.nt_balance += reward`（:159）+ `_add_ledger(type="covenant_sign", from="community_pool", to=user)`（:161-162）
- 金额从 config `sign_reward` 读取，**不硬编码**（covenant.py:113）
- **续签不发**：仅历史零签署（真·首次签署）才 grant_reward（covenant.py:135-140），已签过任一版本者续签 reward_granted=false

**③ 状态端点 `GET /api/covenant/status`**（covenant.py:88-104）
- 返回 `{version, signed, signed_at, sign_reward}`；signed 按 (user, 当前version) 查签署记录

**④ 公约文本 config**（covenant.py:23-59 默认值 + :62-77 读取/惰性 seed）
- 全文存 MapLocation key=`covenant_text`（JSON：version + chapters 正文十章 + appendices 附页 A-E + sign_terms 签署页条款五项 + sign_types）
- 初始内容提取自 v12（正文十章、附页 A-E、签署页五条款逐字录入）
- **禁另开写通道**：本路由无任何文本写入端点；`_get_covenant_config` 仅在缺失时惰性 seed 默认值，**绝不覆盖已存在的修订**（covenant.py:72-74）——后续修改经 D-15 `pendingConfigChanges` 落地到本表（data.py:417-421）

## 判据实跑贴输出（5 条 · 真跑非静态）

```
【判据3-未签】GET status: {version: v12, signed: False, signed_at: None, sign_reward: 10}
【判据1-首签】POST sign: {ok:True, signed:True, already_signed:False, reward_granted:True, reward:10, version:v12, balance:10}
  ledger流水: [('covenant_sign','community_pool','演示用户',10,'签署公约 v12')]   ← 一条
  池: 10000 -> 9990 (减 10)                                                    ← pool 同步减
【判据3-已签】GET status: {version:v12, signed:True, signed_at:'2026-07-28T11:46:30...', sign_reward:10}
【判据2-幂等重签】POST sign: {ok:True, signed:True, already_signed:True, reward_granted:False, balance:10}
  重签后: 签署记录=1条, covenant_sign流水=1条, 池=9990                          ← 零副作用
【契约-签旧版本】POST sign v11 -> 409 {detail:'请签署当前版本 v12（你提交的是 v11）'}
【判据4-文本config】version=v12 sign_reward=10 章节数=10 附页数=5 签署条款数=5    ← 版本号对应 v12
```

**判据5（pytest 零回归）**：

```
tests/test_covenant.py ...... 6 passed
全量：43 passed, 3 skipped（PG 锁测试无 PG 环境自动 skip）, in 27.28s
（基底 P1-1 时为 37 passed → +6 新测试，零回归）
```

## 影响面初判
- **新增域**：签署凭证 + 文本 config；既有入住/预定/资金端点零改动
- **会计等式守恒**：发 NT 是 pool.balance→user.nt_balance 的转移，total_issued 不变，`/api/nt/verify` 审计等式 total_system==total_issued 不受影响（首签前后两侧同增同减）
- **破坏性**：新增表+端点，不改旧行为；回滚 `git revert` 本 commit 即可

## 禁区零改动核验
- `git diff 510cbf9 -- server/routes/nt.py server/routes/tasks.py | grep withdraw/confirm/reject` = **空**（未碰提现/确认/驳回资金端点）

## 未明项（单列不硬猜）
1. **「首签发一次」口径**：卡面端点描述「首次签当前版本→发10NT」与议定/公约 E-7「每人只发一次首次签署，续签不发」存在字面张力。本卡按**公约权威文本**实现（历史零签署才发；续签新版本不发），已加 `test_resign_new_version_no_reward` 佐证。若砚仁/一营认定应「每版本首签各发一次」，改 covenant.py:135-140 的 grant 判定即可——**请验收方裁定**。
2. **sign_type 校验**：当前 sign_type 自由传入（新入住/老成员补签/续签），未做白名单强校验，也未与 reward 逻辑挂钩（reward 只认历史签署数）。是否需按 sign_type 强约束，挂账待议。
3. **文本 config 的 D-15 联调**：本卡只保证 covenant_text 落 MapLocation 且被 sign/status 读取；与前端 pendingConfigChanges 提案-校核-生效的端到端联调属 G 系列后续卡，未在本卡验证。

## 太傅注（三行）
- **补课章节**：幂等设计（唯一约束 + 先查后写）/ 会计等式守恒（发 NT = 池到人转移，发行量不变）/ 配置外置（金额进 config 不硬编码）。
- **人话原理一**：签署发钱最怕「双击发两次」。用 (user,version) 数据库唯一约束当最后一道闸，加上端点先查已签直接幂等返回——两层防重，钱只出一次。
- **人话原理二**：10 这个数字写进 covenant_text config 而非代码里，将来涨到 20 改 config 走提案就行，不用改代码重新部署——「钱随单走、数随约改」。
