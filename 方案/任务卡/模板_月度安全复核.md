---
id: T-sec-YYYY-MM
created: YYYY-MM-DD
owner: 丞相
status: 🟡待复核 / 🟢已闭环 / 🔴有发现
---

# 月度安全复核 · YYYY-MM

> 复制本卡 → 改文件名 `T-sec-YYYY-MM.md` → 填表 → 交对营验收 → 丞相 push。

## 1. 依赖漏洞扫描

- 执行命令：`python server/scripts/security_scan.py`
- 报告路径：`server/scripts/security_report_YYYY-MM.md`
- 漏洞数：______
- 处理：
  - 高危（CVSS ≥ 7.0）：______（立刻发卡 / 已升级）
  - 中低：______（记入下月）

## 2. 权限端点抽查（重放 D-4 / D-10 类攻击）

每个端点至少抽 1 条，实测 → 贴返回码：

| 攻击类型 | 端点 | 重放 payload | 期望 | 实测 |
|---|---|---|---|---|
| SQL LIKE 通配符（D-4 复发） | POST /api/auth/register | `{"name":"%","password":"x"}` | ok:false 含"用户名仅限" | |
| 水平越权看他人数据（D-4 复发） | GET /api/tasks/list（% 用户登录后） | N/A | 不含他人任务 | |
| 垂直越权结算任意营地（D-10 复发） | POST /api/camps/{别人的营地}/settle（普通用户） | N/A | 403 | |
| CORS 通配（D-2 复发） | OPTIONS /api/auth/login Origin=evil.pages.dev | N/A | 无 access-control-allow-origin 回显 | |
| 邀请码绕过（D-3 复发） | POST /api/auth/register 空码 + INVITE_CODES 已设 | N/A | ok:false 含"邀请码" | |

## 3. 密钥轮换到期检查

对照 `方案/运维/密钥清单.md`：

- [ ] JWT_SECRET 距上次轮换 < 12 个月
- [ ] DATABASE_URL 密码距上次轮换 < 3 个月
- [ ] Render API Key 无过期人员持有
- [ ] `grep -rE "(sk-|ntrk_|0x[a-fA-F0-9]{64})" nantang-mobile/ server/` 无真密钥入 git

## 4. 墓碑复发检查

- [ ] `server/_reset_db.py` 等迁移遗留脚本是否仍应删除
- [ ] `nantang-mobile/js/` 下无未被 index.html 引用的死文件
- [ ] nantang_fresh.db.bak 等本地备份未进 git
- [ ] 本月新增硬编码 URL / 测试账号已清理

## 5. 部署总检

- 执行：`python server/scripts/deploy_check.py --skip-smoke`
- 依赖对账：PASS / FAIL
- ?v= 一致性：PASS / FAIL（若 FAIL 列具体文件）
- 环境变量清单无新增未登记变量

## 结论

- 本月安全态势：🟢 健康 / 🟡 有遗留 / 🔴 需立刻处理
- 发现项（发卡跟踪）：______
- 对营验收人：______  日期：______
