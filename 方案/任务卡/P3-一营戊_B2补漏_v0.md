━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  卡号：P3-一营戊
  施工营：一营（Claude Code · FE）1 窗串行
  阵地：nantang-mobile/  【禁区 server/ 零触碰】
  立案：丞相 Codex 2026-07-31 22:30
  法源：丞相亲证 — 1营丁 B 项（76e488d）只改了部分色值，
        ui-cardroom.js 仍残留 11 处 #999/#aaa（对比度 2.6:1 · 不达 WCAG AA 4.5:1）
        砚仁反馈原话「这个字后面这个字还是看不清，会跟背景融在一起」
  优先级：P2（可用性 · 砚仁两次反馈同一问题）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【施工项（2 项）】

  ① ui-cardroom.js 11 处低对比色值 → CSS token
     全部 #999 / #aaa / #888 → var(--g-text-muted)
     亲证行号（丞相 22:20 grep）：
     ┌──────┬────────────────────────────────────────────────┐
     │ 行    │ 上下文                                          │
     ├──────┼────────────────────────────────────────────────┤
     │ 438  │ ui-card 空状态「📋 暂无此类记录」color:#aaa       │
     │ 567  │ 校核卡时间戳 color:#aaa                          │
     │ 645  │ 「🃏 暂无此分类的牌」color:#aaa                   │
     │ 812  │ 「暂无空间数据」color:#aaa                        │
     │ 882  │ 「此空间暂无该分类的劳动项」color:#aaa            │
     │ 959  │ 「暂无用户」color:#aaa  ★同时有全角分号 bug        │
     │ 970  │ 用户行箭头「→」color:#aaa                         │
     │ 1014 │ step 标签 color:#aaa（background:#f0f0f0 保留）   │
     │ 1216 │ stColor expired 分支 '#aaa'                      │
     │ 1229 │ 「猜错」提示 color:#999                           │
     │ 1239 │ 时间戳 color:#aaa                                │
     └──────┴────────────────────────────────────────────────┘
     ⚠️ 1216 是 JS 三元赋值给变量，改成 'var(--g-text-muted)' 字符串同样有效
     ⚠️ 1014 的 background:#f0f0f0 是底色不是文字色，不动

  ② 修全角分号 bug（ui-cardroom.js:959）
     现状：style="color:#aaa;text-align:center;padding:20px；font-size:.7rem"
                                                        ^ 中文全角分号 U+FF1B
     后果：padding 之后整条 style 声明被浏览器丢弃，font-size 不生效
     改为：半角 ;

  ③ ?v= bump（index.html）
     ui-cardroom.js 当前版本 +1

【判据】
  ┌────┬──────────────────────────────────────────────┬──────┐
  │ #  │ 判据                                          │ 证据 │
  ├────┼──────────────────────────────────────────────┼──────┤
  │ 1  │ Select-String ui-cardroom.js "#999|#aaa|#888" │      │
  │    │ → 0 matches（或只剩 background 底色）          │      │
  │ 2  │ 959 行全角「；」→ 半角「;」                     │      │
  │ 3  │ node --check js/ui-cardroom.js → OK           │      │
  │ 4  │ index.html ?v= 已 bump                        │      │
  │ 5  │ python server/scripts/deploy_check.py         │      │
  │    │   --skip-smoke → 五检全 PASS                   │      │
  │ 6  │ 禁区 server/ 零改动                            │      │
  │ 7  │ 只 commit 不 push · git add 具名               │      │
  └────┴──────────────────────────────────────────────┴──────┘

【为什么这卡重要】
  砚仁已就「校核卡片提示文字看不清」反馈 2 次。
  1营丁 B 项建了 UI.Alert 通用块（正确·可复用），但只改了空状态容器背景，
  没扫全文件的写死色值 → 砚仁仍然看不清。
  这次要 grep 归零，不留残余。

【回执落盘】
  方案/任务卡/P3-一营戊_B2补漏_回执_一营_2026-07-31.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
