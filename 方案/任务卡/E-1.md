# E-1 · 架构核验考古 + arch_check.py 机检

- **档位**：大卡（跨端勘察 + 新机检脚本）· 考古段只读零写入，脚本段中卡直道
- **阵地**：一营主察 `nantang-mobile/`，二营主察 `server/` 接口契约；arch_check.py 由一营写、二营验
- **来源**：砚仁 2026-07-26 谕——旧《架构梳理与拆分方案.md》（07-20，双轨时代）已过时，
  降级为「待证伪的假设清单」；一切架构结论以代码实证为准
- **定档人**：丞相 2026-07-26

## 第一段 · 考古回执（只读，两营分工）

1. **逐条核验**《架构梳理与拆分方案.md》：每条结论标 属实 / 过时 / 错误，
   附实证（文件:行号 / 命令输出 / commit hash）
2. **产出今日真实架构图**（写入 `方案/架构现状图.md`）：
   - 模块版九个 js 的实际依赖关系、行数、职责重叠点
   - 服务端 routes/ 各文件职责与前端调用对应表
   - 旧单文件版 `nantang-mobile.html` 存废与角色（若存且可达=活墓碑，结论喂给 D-21）
   - 头部锚点：`基于 commit \`xxxx\` 实证 · 2026-07-26`（git rev-parse HEAD 实抓）
3. **产出拆分差距清单**：现状 vs 旧方案目标 vs 补课 13/16 章架构规范——
   此清单是④架构设计文档地基 + 将来系统性整理大卡的发卡依据

## 第二段 · arch_check.py 施工（一营写，纯新增）

写入 `nantang-mobile/scripts/arch_check.py`（或卡面施工时定更合适位置，回执说明）。
**docstring 即 spec，照此写，不另立规范文件**：

```python
"""arch_check.py — 架构图与代码一致性机检（纯 stdlib，零依赖）。

检查项：
  1. 模块存在性：架构图所列 js 模块是否存在于磁盘
  2. 行数漂移：任一模块行数变化超 ±30% 亮黄灯
  3. index.html 引用对账：<script> 清单 vs 磁盘实有文件
  4. import 依赖对账：架构图所绘依赖关系 vs 代码实际 import
  5. 旧单文件版存废：nantang-mobile.html 是否存在

输出：exit 0=通过 / 1=黄灯(警告) / 2=红灯(阻断)
用法：python arch_check.py [project_root]
挂载点：每日闸（vault_gate 调用） / 部署总检（deploy_check 调用）
锚点："基于 commit xxxx 实证"，从 git rev-parse HEAD 自动抓
"""
```

黄灯阈值：架构图锚点落后 main ≥10 commits。

## 验收（二营）

- 考古回执：核验逐条有实证，无凭印象结论；架构现状图锚点为实抓 commit
- `python arch_check.py` 实跑 exit code 符合 docstring 语义；五项检查真跑真报
- 回执末尾附「太傅注」：为什么"文档不随码走=三个月后又是一份旧方案"（对应补课 13/16 章）

## 影响面声明（施工方填）

- 纯新增文件 + 文档，零业务代码改动 / 回滚=删新增文件
