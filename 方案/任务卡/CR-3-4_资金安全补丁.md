# 任务卡 CR-3/4：资金安全补丁（住宿裸池 + 营地送钱）

- **卡号**：CR-3/4（合卡）
- **优先级**：HIGH（涉钱漏洞）
- **施工方**：豆包 Codex（二营）
- **验收方**：丞相 Kimi Work
- **日期**：2026-07-30
- **禁区**：`nantang-mobile/` 零改动
- **回执**：`方案/任务卡/CR-3-4_回执_二营_2026-07-30.md`

## BUG-1：住宿结算裸池写入（原 CR-3）
- `accommodation.py:31` `_settle_tenancy` 写 `pool.balance` 前用裸 `_get_pool(db)`，缺 `lock=True`；并发结算丢更新。
- `admin.py:145/172/288` dev-reset/seed 三处写池同缺锁，顺手补齐（对齐全项目锁型）。

## BUG-2：营地 partial 结算送钱（原 CR-4）
- `nt.py` verify_task partial 退款分支 `if poster: poster.nt_balance += unclaimed`，
  营地任务只要 poster 存在就把未领份额打进 poster 个人余额 → 凭空送钱。

### ⚠️ 施工中修正派发单药方
- 派发单 BUG-2 建议「`if is_camp` 提到 `if poster` 之前，未领回流 `camp_balance`」。
- **经实测该药方仍会造币**：CR-1 修复后营地任务 `escrow_amount==0`，创建时**不冻结** camp_balance；
  结算 `camp_balance -= 实付`，未领份额的钱**从未离开** camp_balance。若再 `camp_balance += unclaimed`
  → camp_balance 凭空 +unclaimed。
- **正确修复**：营地任务 partial **不退款**（`if unclaimed > 0 and not is_camp`）。仅个人/社区任务
  （钱已进 task_escrow）才退未领份额。已用 `total_system` 守恒实测验证：修复后 delta==0、poster 余额=0。

## 施工内容
1. ✅ `accommodation.py:31` → `_get_pool(db, lock=True)`
2. ✅ `admin.py:145/172/288` → 三处 `lock=True`
3. ✅ `nt.py` partial 分支 → `if unclaimed > 0 and not is_camp`（营地不退款）
4. ✅ 新增 `tests/test_cr4_camp_partial_settle.py`（2 用例：营地不送钱+守恒 / 个人退款回归）

## 验收
- `pytest tests/` 全绿：125 passed, 8 skipped（requires_pg）
- 只 commit 不 push；`git add` 具名；commit 带卡号

## 遗留（非本卡范围，另案）
- 营地支付奖励账本记 `from='escrow'` 而非 `'camp_pool'`，致 `verify().camp_pool_drift`
  出现 `-total_payout` 缺口。属营地支付路径既有账本缺口（与锁/送钱无关），建议另开卡修。
