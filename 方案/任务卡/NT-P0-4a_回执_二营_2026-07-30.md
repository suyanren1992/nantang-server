# NT-P0-4a 回执 · 二营（豆包 Codex）

> 卡号：NT-P0-4a sync 加 my_escrow / my_frozen / my_accommodation_due 三字段
> 阵地：server/routes/nt.py（sync 返回体）+ server/tests/test_nt_sync_fields.py（新增测试）
> commit：本卡单独 commit，具名 add，不 push

## 四件套

### 1. 做了什么
- `server/routes/nt.py` sync 端点在 return 前计算并新增三字段：
  - `my_escrow` = 复用上方已算的 `frozen`（本人发布且未释放任务 escrow 合计）——与 `frozen_balance` 同值。
  - `my_frozen` = `SUM(nt_ledger.amount) WHERE from_user==me AND type=='withdraw' AND status=='pending'`（真「提现待审」语义，coalesce 0）。
  - `my_accommodation_due` = 本人 `status=='active'` 的 Tenancy.debt（无入住=0；沿任务卡取 Tenancy.debt）。
- 返回体新增三 key，紧接 `frozen_balance` 之后；`frozen_balance` **保留不删**（过渡期）。

### 2. 怎么验证的
- `python -m py_compile routes/nt.py tests/test_nt_sync_fields.py` 通过。
- 新增测试 `tests/test_nt_sync_fields.py` 两例：①三字段存在且值正确（escrow=30、my_escrow==frozen_balance、my_frozen=50 仅计 pending、accommodation_due=15）；②无入住/无提现时三字段=0。
- ⚠️ 本地无 fastapi/sqlalchemy/pytest 且离线，**无法本地实跑 pytest**；判据交验收方实跑。

### 3. 爆炸半径
- 改 1 文件（nt.py sync 端点，+计算块 +3 返回 key）+ 新增 1 测试文件。
- 影响功能：sync 返回体纯增量（旧前端忽略新 key，向后兼容）。
- 破坏性变更：无。回滚：`git revert` 本 commit。

### 4. 禁区遵守
- data.py（一营阵地）零改动；withdraw 资金流向代码零改动；未改前端；未动其他端点。

## 判据对照
1. sync 返回含三字段 → 端点 + test 覆盖。
2. my_escrow = 本人发布未释放 escrow 汇总（= frozen_balance 现况值）→ test 断言 my_escrow==frozen_balance。
3. my_frozen = 本人 pending 提现合计 → test 覆盖（settled 不计）。
4. my_accommodation_due = 本人活跃入住 debt（无入住=0）→ test 两例覆盖。
5. frozen_balance 仍保留 → 未删，test 断言其存在。
6. pytest 零回归 → 待验收方实跑。

## 契约同步说明（供一营 P0-4b）
- 契约 v0.2 §4 三字段已定稿；本卡为后端落地。前端可经 ALIAS `my_escrow ?? frozen_balance` 过渡，一版后删 frozen_balance。

---
太傅注三行：
1. my_escrow 直接复用现况 frozen 计算，保证「my_escrow == frozen_balance」恒等，前端过渡零风险。
2. ⚠️ 语义存疑一处：`my_accommodation_due` 任务卡判据取 `Tenancy.debt`（未结欠费），但「住宿应付」在设计稿 §9.1 资金分布视图语义更接近 `accommodation_due`（应计未结）。本卡严格照判据取 debt；若砚仁/丞相意在应计额，改 `_my_ten.debt`→`_my_ten.accommodation_due` 一行即可，建议另卡或回执确认。
3. 本机缺运行时依赖+离线未实跑 pytest，已明示；仅 commit 不 push，data.py 与钱路零改动。
