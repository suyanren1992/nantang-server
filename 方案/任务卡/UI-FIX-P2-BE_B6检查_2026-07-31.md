# B6 检查：GET /api/fields（田间列表）

> 2026-07-31 | 检查人：Qoder 二营 | 卡号：UI-FIX-P2-BE B6

## 端点存在性

| 检查项 | 结果 |
|--------|------|
| 端点是否存在 | ❌ **不存在** |
| 最近似端点 | 无（server/routes/ 零匹配） |

## 字段对照

| 卡面要求字段 | 现有端点字段 | 匹配 |
|-------------|------------|------|
| id | ❌ | ✗ |
| crop_name | ❌ | ✗ |
| planted_at | ❌ | ✗ |
| harvest_at | ❌ | ✗ |
| stage | ❌ | ✗ |
| health | ❌ | ✗ |

## 结论

**田间管理后端完全未建设。** 没有 Crop 表、没有 Field 表、没有相关端点。

前端卡片化 UI ⑧「田间卡片化」目前用的硬编码数据（app.js 内嵌作物列表）。

## 建议

需新立卡建设：
1. `Crop` 或 `Field` 数据表
2. `GET /api/fields` 田间列表端点
3. `POST /api/fields` 种植端点
4. `PUT /api/fields/{id}` 生长阶段更新

复杂度 ⭐⭐⭐⭐（涉及生长周期计算+定时 cron）。

**是否需要新立卡：✅ 是**
