# D-21 墓碑代码清理（一营 · 跨端）

> 来源：对照体检病根「墓碑代码从未清理」——`_reset_db.py`、M1-M5 硬编码、mobile-bundle 死分支。
> 施工：Claude Code（跨端全权，可动 server/ 仅本卡点名文件）｜验收：豆包 Codex
> 教学注：本卡是太傅课素材——每清一块碑，给砚仁指认它对应补课哪一章

## 施工内容（两步走，先清单后动手）

**第一步 · 墓碑清单**（只读）：
1. `_reset_db.py`（SQLite 时代遗物，Neon 后永不可用）+ 全仓 grep 谁还引用它
2. M1-M5 硬编码（建筑/定价/图片 URL，权限扫描报告 M 系列）逐个列：位置、被谁引用、处置建议
3. mobile-bundle.js 死分支（`if(window.NM)` else 段）+ 其他可达性死代码
4. 清单写 BUG_TRACKER.md，每项标：删除 / 移 `_archive/` / 保留（理由）

**第二步 · 清理**（丞相复核清单 → 砚仁批准后动手）：
按批准清单执行；移动/删除遵守总则（先问后动）

## 判据（验收方实跑）

1. 清单每项有处置结论，grep 验证无残留引用（贴输出）
2. 清理后 `pytest`（D-19 体系）+ 前端 node 语法检查全绿
3. `?v=` 同步升级
