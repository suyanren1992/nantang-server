# 任务卡 D-8：sync_shared 混合命名统一（两步走）🟠

> 施工方：Claude Code（一营，跨端卡全权，可动 server/routes/data.py 对应端点） · 监察：Kimi Code · 验收：Kimi Work · 单独 commit，不 push
> 来源：《全面权限扫描_B方案_2026-07-24》H-8

## 位置
- `nantang-mobile/js/app-data.js:226` 附近：同一请求体 `map_locations`（snake）与 `canteenMenu`（camel）混用
- `server/routes/data.py:387` sync_shared 端点

## 问题
两端目前"错成一样"故能通，但任何一方按规范统一就断裂——计时炸弹。

## 修法（两步走，防线上断裂）
**第一步（本卡只做第一步）——服务端先兼容：**
`data.py` 读取共享字段时，camel/snake 两种 key 都认（`body.get('canteenMenu') or body.get('canteen_menu')` 模式），写回/存储统一 snake_case。
**第二步（另开卡，至少隔一个上线版本）——前端切换：**
app-data.js payload 全改 snake_case，确认线上服务端已是兼容版后才可以上。
> 本卡 commit 只含服务端兼容改动；前端那步在 BUG_TRACKER.md 里登记为后续卡 D-8b。

## 验收
- 兼容版上线后：旧前端（camel）同步正常；手工构造 snake_case 请求体同步也正常
- 服务端存储内字段统一为 snake_case
- commit message：`fix(D-8): sync_shared 服务端兼容 camel/snake 双 key（H-8 第一步，统一 snake 前铺路）`
