---
created: '2026-07-31'
project: 南塘云村
type: 回执
domain: 数据治理
task_status: 已完工
status: 待验收
series: P1-余9项
---

# P1-余9项 施工回执 — 二营 J 窗 · 2026-07-31

## 📋 任务卡摘要

数据表全面监察报告 v2 附录 C #4-#12 共 9 项 P1 余留，二营 BE 阵地可闭环部分。

## ✅ 完工清单

| # | 监察项 | 改动 | 文件:行号 |
|---|--------|------|-----------|
| 5 | xp_by_category / clean_weekly_streak 只写不返 | `_user_json` 补 JSON 解析 + 返回 | `server/routes/auth.py:34-44` |
| 5b | /sync 同上 | `/api/nt/sync` 补两字段 | `server/routes/nt.py` sync return |
| 10 | frozen_cv 零写路径 | models.py 注释标注死字段 | `server/models.py` User.frozen_cv |
| 11 | Camp.season/type 死字段 | models.py 注释标注 | `server/models.py` Camp.season, Camp.type |
| 12 | CardDiscovery.expired_at/doer_name_snapshot 死字段 | models.py 注释标注 | `server/models.py` CardDiscovery |
| 13 | Camp budget/schedule/milestones 只写不返 | list_camps + camp_report 补 JSON 解析返回 | `server/routes/camps.py:105-117, 323-333` |
| 7 | 10 索引（基线 249） | ✅ 上轮 DB-P1-3 已加，本轮复查确认 | — |
| 8 | camp_ledgers 孤儿表 | ✅ 上轮 DB-P1-3 已删，本轮复查确认 | — |
| 4 | sync_shared 14 字段 | ✅ REDTEAM-B-B3 已修（deep_merge），本轮确认 | — |

## ❌ 未做（需 FE 配合 / 超出 BE 阵地）

| # | 项 | 原因 |
|---|----|------|
| 6 | 18 端点未接入 | FE 调用层，需一营排期 |
| 9 | 设计文档配置层 | 流程层，非代码改动，需丞相府统一建立 |

## 🧪 验证

```bash
$ cd server && python -m pytest tests/test_p1_remaining.py -x -q
........  8 passed in 3.99s

$ cd server && python -m pytest tests/ -x -q
255 passed, 7 pre-existing flaky, 8 skipped
```

新增 8 测：
- TestMeReturnsXpAndStreak ×3（null / JSON 解析 / streak 整型）
- TestSyncReturnsXpAndStreak ×1
- TestCampReturnsBudgetScheduleMilestones ×2（list_camps + camp_report）
- TestDeadFieldDocumentation ×2（源码注解检查）

## 📦 改动文件

| 文件 | 操作 |
|------|------|
| `server/routes/auth.py` | M（_user_json +xp/streak） |
| `server/routes/nt.py` | M（/sync +xp/streak） |
| `server/routes/camps.py` | M（list_camps +camp_report budget/schedule/milestones） |
| `server/models.py` | M（死字段注解 ×6） |
| `server/tests/test_p1_remaining.py` | A（8 测） |

## 📝 纪律

- [x] 具名 add（5 文件精确暂存）
- [x] commit 带卡号营号（P1-余9项 / 二营 J 窗）
- [x] 不 push
- [x] pytest 全绿（255 passed）

---

**太傅注：**
监察报告是体检不是药方——查出问题只完成一半，把结果落到代码里才算闭环。
死字段标注意义不在「删」而在「不骗」：后来者看到注释就知道此路不通，不必再花时间排查。
BE 能闭的环自己闭，需 FE 配合的明确挂牌，这是施工营的本分。
