# 即期执行方案

> 2026-07-22 · 41 项代码改动 + 2 项验证 · 4 批次 · ~10h
> 分支：`fix/6-critical-bugs`
> 原则：每批次改完→立刻启动服务器验证→通过再进下一批。发现新问题记 backlog，不中断。

---

## 执行前自检（5 分钟，先做）

```bash
# 1. 确认在正确分支
cd "C:/Users/苏砚仁/thinknote/项目/实景游戏移动端代码"
git branch --show-current   # 应输出 fix/6-critical-bugs

# 2. 确认服务端能启动
cd server
rm -f nantang_fresh.db
JWT_SECRET=test uvicorn main:app --port 8000 &
sleep 2
curl http://127.0.0.1:8000/api/health   # 应输出 {"status":"ok"}

# 3. 确认能注册+登录
curl -s -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"test","password":"test12345678"}'
# 应输出 {"ok":true,...}

# 4. 停掉服务器继续
kill %1
```

如果以上 4 步有一条失败，**停下来修好再继续**。

---

## Batch 1：数据安全 + 行锁 (8 项 · ~1.5h)

| # | 文件:行 | 改动 | 验证 |
|---|--------|------|------|
| F1 | camps.py:39 | `get_current_user → require_admin` + budget `Field(ge=0, le=10000)` | 用 visitor token POST /api/camps → 403 |
| F2 | nt.py:759 | `select(VfyModel).with_for_update()` | 启动服务→注册两个用户→创建 verification→两次并发 approve（第二次应 400） |
| F3 | accommodation.py:72 | `pool.balance += t.debt` + `_add_ledger(...)` | 创建 tenancy（SQLite 直接 INSERT）→设 debt=10→POST /api/accommodation/checkout→GET /api/nt/verify → pass=true |
| F4 | data.py:285 | `get_current_user → require_admin` | visitor token POST /api/data/sync_shared → 403 |
| F5 | nt.py:609 | `select(NTTask).with_for_update()` | 创建 2 人任务→两个用户同时 submit→两次 evidence 都在 DB 中 |
| F8 | cron.py:29 | `_get_pool()` 加 `lock` 参数 | `python -c "from cron import _get_pool; print('lock param exists')"` |
| F9 | nt.py:659,830 | `dispute_task` + `reject_verification` 各加 `.with_for_update()` | grep 确认两处都有 `.with_for_update()` |
| F10 | nt.py:44 + cron.py:19 | `_ledger_id()` 加 `token_hex(3)` | 循环 1000 次生成 ID→无重复 |

**提交**：`git commit -m "fix: Batch 1 — 数据安全+行锁 (F1-F5,F8-F10)"`

---

## Batch 2：经济完整性 + nt_helpers (5 项 · ~2h)

| # | 文件:行 | 改动 | 验证 |
|---|--------|------|------|
| F6 | data.py:115 | `doer = user.id` 强制；`int()` + 上限 1000 | curl POST verification→查 DB doer 字段=当前用户 |
| F7 | nt.py 8 处 + tasks.py:64 + accommodation.py:94 | 新增 `_safe_assignees()`，8 处替换 | 手动写损坏 JSON 到 DB→调 accept/submit/verify/cancel/dispute→全部 200（不 500） |
| F11 | tasks.py:167 | escrow 退还后加 `_add_ledger(...)` | 创建任务→删除→查 nt_ledger 表有对应 entry |
| F3 | accommodation.py:72 | 与 Batch 1 同点，确认 ledger 参数正确 | 同 Batch 1 |
| F29 | 新建 nt_helpers.py | 提取 `_get_pool`(含 lock)、`_add_ledger`、`_ledger_id`(防碰撞版)、`_safe_assignees`。nt.py + cron.py 改为 import。 | `python -c "from nt_helpers import _get_pool, _add_ledger, _ledger_id, _safe_assignees"` → 无报错 |

**提交**：`git commit -m "fix: Batch 2 — 经济完整性+nt_helpers (F6,F7,F11,F29)"`

---

## Batch 3：客户端正确性 + 存储清理 (6 项 · ~1.5h)

| # | 文件 | 改动 | 验证 |
|---|------|------|------|
| F12 | 根目录 js/ | 删除 12 陈旧文件：`app-data.js, auth.js, backup.js, mobile-bundle.js, nt-core.js, nt.js, schema.js, seed-test-data.js, store.js, ui-cardroom.js, utils.js, nt-three-pool-redesign.md` | 浏览器打开 nantang-mobile/index.html → 无 404 |
| F13 | core.js L814,834,302 | 3 处加 `t.publisher = t.poster \|\| t.publisher` | HTTP 模式登录→任务大厅→任务卡片显示发布者名称 |
| F15 | nt.py:164 + data.py:318 | sync 加 `is_system_generated` + `assignees` | `curl /api/nt/sync` → 返回含 `is_system_generated` 字段 |
| F16 | app-data.js:386 | 离线 earn → `_pendingEarnQueue`，上线后批量 POST | file:// 模式下校核→切 HTTP 登录→查服务端 balance 变化 |
| F36-1 | nt-core.js `_loadState()` | HTTP 模式不读 nt_core_v1 + 清旧数据 | HTTP 登录→DevTools localStorage 无 `nt_core_v1` key |
| F36-2 | core.js:869 + auth.js:339 | `setItem('nt_users')` 加 `file:` guard | HTTP 登录→DevTools localStorage 无 `nt_users` key |

**提交**：`git commit -m "fix: Batch 3 — 客户端正确性+存储清理 (F12,F13,F15,F16,F36)"`

---

## Batch 4：UX + 技术债 + 补丁 + D1 (22 项 + 2 验证 · ~5h)

### F17-F22：UX 修正（6 项 · ~45min）
| # | 文件 | 改动 | 验证 |
|---|------|------|------|
| F17 | main.css L286,151,116,338,490 | 5 选择器加 `min-height:44px` | Chrome DevTools 375px 视口→点击测试 5 处 |
| F18 | core.js L1117,643,911 + data.js L481 | 4 处错误消息改用户文本 | 触发各错误场景→确认消息非技术语言 |
| F19 | core.js L272,887 + app.js L1143,1171 + api.js L52 | 6 处 catch 加 console.warn | 断网操作→console 有 warn 输出 |
| F20 | index.html 13 个 overlay | ✕ vs ← 统一 | 浏览所有 overlay→关闭按钮图标一致 |
| F21 | core.js L921 | file:// 注册加 `_saveLocalUser` | file:// 注册→刷新→头像正确 |
| F22 | index.html L190 | `#cbBtnPlus` display:none | 营地页面→无 "+ 更多" 按钮 |

### F23-F28：技术债（7 项 · ~1h）
| # | 文件:行 | 改动 | 验证 |
|---|--------|------|------|
| F23 | data.js:319 | doSubmit 逐人调 API | 多人任务→每人提交→查服务端 evidence JSON map 有两人 |
| F24 | nt.py:139 | frozen 改为 `escrow_amount if not None else ...` | `curl /api/nt/sync` → frozen_balance 正确 |
| F25 | nt.py:542 | verify_task 每人一条 ledger | 创建 2 人任务→验证→查 nt_ledger 有 2 条 task_reward |
| F26 | nt.py:274 | spend scope 白名单 | curl spend scope=invalid → 400 |
| F27 | camps.py:27 + data.py:186,188,228 | `except (json.JSONDecodeError, TypeError)` | 写损坏 JSON→不吞 KeyboardInterrupt |
| F28 | cron.py:79 | JSON 模块级缓存 | `python -c "from cron import _TEMPLATES; print(type(_TEMPLATES))"` → list |

### F30-F34：交叉检查补遗（5 项 · ~30min）
| # | 文件:行 | 改动 | 验证 |
|---|--------|------|------|
| F30 | archives/auth_utils.py:7 | 替换为 `SECRET_KEY = "REDACTED"` | grep `.archives` 无 `nantang-cloud-village-secret` |
| F31 | nt.py:869 | `_NIGHTLY_RATE = int(os.environ.get("NIGHTLY_RATE", "10"))` | curl /api/system/daily-tick → 仍扣 10 NT |
| F32 | tasks.py:162 | guard 加 `"已争议"` | 创建任务→争议→curl DELETE → 400 |
| F33 | tasks.py:145-150 | 删除死代码 5 行 | `python -c "import ast; ast.parse(open('routes/tasks.py').read())"` → OK |
| F34 | core.js:467 | `t.settler\|\|t.settler_id\|\|t.publisher` | admin 结算任务→展开卡片→显示结算按钮 |

### F38-F40：卡片室补丁（3 项 · ~15min）
| # | 文件:行 | 改动 | 验证 |
|---|--------|------|------|
| F38 | ui-cardroom.js:1122 | confirmDiscovery 后 `API.request('POST', '/api/nt/transfer', ...)` | 卡片室确认发现→查转账记录 |
| F39 | app.js:1908 | `_openVerificationPanel` 开头加 isMember 门 | visitor 开校核弹窗→toast "入住后可用" |
| F40 | app.js:2037 | `_submitKitchenEntry` 开头加 isMember 门 | visitor 放物品→toast "入住后可用" |

### D1：三套词汇统一（~2.5h）
**文件**：server/models.py + routes/*.py + nantang-mobile/js/*.js · ~50 处
**内容**：`TASK_STATUSES` 常量定义 → sync 下发 → 客户端动态读取。
**覆盖**：F13（临时补丁删除）、状态中英混杂、NTTask.status default。
**验证**：全流程——创建→接取→提交→审核→结算→取消→争议→每个状态卡片显示正确。

### 收尾验证（2 项 · ~15min）
- F13/F16 验证：确认 poster/publisher 映射 + 离线 earn 同步
- F36 验证：HTTP 模式无 nt_core_v1 + file:// 模式 nt_users 存在

**提交**：`git commit -m "fix: Batch 4 — UX+技术债+补丁+D1 (F17-F28,F30-F34,F38-F40,D1)"`

---

## 提交后最终验证（5min）

```bash
# 1. 启动服务端
cd server && rm -f nantang_fresh.db && JWT_SECRET=test uvicorn main:app --port 8000 &

# 2. 全流程测试
python -c "
import urllib.request, json
BASE = 'http://127.0.0.1:8000'
# 注册 admin + 注资池
admin = json.loads(urllib.request.urlopen(urllib.request.Request(f'{BASE}/api/auth/register', data=b'{\"name\":\"admin\",\"password\":\"test12345678\"}', headers={'Content-Type':'application/json'})).read())
tok = admin['token']
# 池注资
json.loads(urllib.request.urlopen(urllib.request.Request(f'{BASE}/api/nt/topup', data=b'{\"user\":\"community_pool\",\"amount\":500,\"reason\":\"test\"}', headers={'Content-Type':'application/json','Authorization':f'Bearer {tok}'})).read())
# 创建社区任务
r = json.loads(urllib.request.urlopen(urllib.request.Request(f'{BASE}/api/tasks', data=b'{\"title\":\"test\",\"reward\":5,\"slots\":1,\"poster\":\"社区\"}', headers={'Content-Type':'application/json','Authorization':f'Bearer {tok}'})).read())
print('create task:', r.get('ok'))
# 会计检查
r = json.loads(urllib.request.urlopen(urllib.request.Request(f'{BASE}/api/nt/verify', headers={'Authorization':f'Bearer {tok}'})).read())
print('accounting pass:', r['pass'], 'diff:', r['checks']['diff'])
print('ALL CHECKS PASSED' if r['pass'] and r['checks']['diff'] == 0 else 'FAILED')
"
```

`pass=true diff=0` → 上线就绪。
