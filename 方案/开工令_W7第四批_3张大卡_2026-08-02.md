# 🚀 开工令 · W7 第四批（2026-08-02 · 砚仁批"发"）— 更新于同日

> **砚仁批**：发（2026-08-02 即时）
> **施工方**：一营 + 二营（按 v0.5 角色切换）
> **纪律**：v0.5 + 铁律 12（修复要修对，commit 要诚实）
> **更新 08-02**：NT-DECIMAL 回滚取消，AUDIT-P0 提前为第 1 张

---

## 派卡总览

| 卡 | 派给 | 档 | 状态 | 开工顺序 |
|---|---|---|---|---|
| ~~W7-NT-DECIMAL~~ | ~~二营~~ | ~~P0~~ | ❌ 取消（代价太高，回滚） | — |
| **W7-AUDIT-P0** | 二营 | 🔴 大 P0 | 🟢 待派 | **第 1 张** |
| **W7-CAMP-UI-2** | 一营 | P1 大 | 🟡 待派 | **第 2 张**（独立可并行） |

**砚仁未答项处理**（丞相府默认决策）：

| 未答项 | 丞相府默认 | 理由 |
|---|---|---|
| AUDIT-P0 派卡粒度（13 条一卡 vs 分卡） | **13 条一卡** | 原方案；砚仁未表态 = 默认执行；不阻塞段 A 修旧节奏 |
| MICRO-TASK 是否派 | **派 · 排 #1 完后** | 砚仁未说砍 = 继续走；卡面待 #1 完才能精写 |
| #15 #16 #18 设计 | **延后** | 砚仁未答 + 不阻塞当前派卡 |

---

## 1. W7-NT-DECIMAL（🔴 大 P0 · 二营 · 阻塞链最前）

**目标**：NT 字段从 Integer 改 BigInteger（按"分"存整数）
**影响范围**：全站 6+ 模型（User / Pool / NTLedger / Camp / NTTask / Escrow）
**风险点**：数据库迁移必须幂等
**铁律 12 必填**：
- ✅ 等式守恒推导段（涉钱卡硬性）
- ✅ 一项一 commit（迁移/模型/测试/守恒验证 各自独立）

**自检命令**（卡面已写）：
```
$env:PYTHONUTF8="1"; $env:JWT_SECRET="devsecret"
.\.venv\Scripts\python.exe -m pytest server/tests/test_nt_reserve_equation.py server/tests/test_p0_ssot_chain.py -q
```

**回执格式**（M-2 大卡六必填 + 铁律 12 增强）：
1. commit hash 列表（一项一 commit）
2. 验证命令原始输出（基线 vs 改后）
3. 爆炸半径四问
4. 未验事项
5. git status 输出
6. 太傅注
7. **等式守恒推导段**（铁律 12 硬性：user / pool / frozen / total_issued / escrow / camp 六项 ±方向）

---

## 2. W7-AUDIT-P0（🔴 大 P0 · 二营 · 13 条一卡）

**目标**：体检报告 13 条 A 类真卡死逐条修
**前置**：AUDIT-1 报告 ✅ `102a896`
**铁律 12 必填**：
- ✅ 13 条一卡派（丞相府默认）
- ✅ 13 条**各自独立 commit**（message 必诚实列修改项）
- ✅ 不能再 c5dbfb1 那种合并

**自检命令**（后端改时跑）：
```
$env:PYTHONUTF8="1"; $env:JWT_SECRET="devsecret"
.\.venv\Scripts\python.exe -m pytest server/tests/ -q
```

**回执格式增强**：
- 13 条修法清单（每条 commit hash + 验证）
- 6 必填 + 太傅注
- 每条独立 commit 验证（`git log --oneline` 列出 13+ 个 hash）

---

## 3. W7-CAMP-UI-2（P1 大 · 一营 · B 轨串行）

**目标**：营地内部 UI 设计稿 v1 落地
**前置**：CAMP-UI-1 设计稿 ✅ + UI-4-返修 ✅ + UI-4-正名收尾 ✅
**M-3 前端四件必交**（v0.5 新增）：
- 改前改后截图
- 干跑留痕（node 隔离脚本）
- 手动验收清单
- 未跑声明

**铁律 11 复用勘察**（卡面已写）：
- UI.Sheet / UI.IconGrid / UI.Field 已就位（M4b ✅）
- PC 端范式（`方案/PC端UI范式参考_勘察v0.md`）直抄

**回执格式增强**：
- 4 文件改动（HTML / data.js / app.js / +1）
- 浏览器实测截图
- 6 必填 + 太傅注

---

## 砚仁 3 个未答项（再确认）

| # | 问题 | 丞相府默认 | 砚仁可推翻？ |
|---|---|---|---|
| 1 | AUDIT-P0 13 条一卡 vs 分卡 | 13 条一卡 | ✅ 随时 |
| 2 | MICRO-TASK 是否派 | 派 · 排 #1 完后 | ✅ 随时 |
| 3 | #15 #16 #18 设计 | 延后 | ✅ 随时 |

---

**派卡人**：丞相
**接令时间**：2026-08-02 即时
**施工方回执截止**：无（v0.5 默认一卡一会话）
