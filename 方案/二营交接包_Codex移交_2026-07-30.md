---
created: '2026-07-30'
project: 南塘云村
type: 交接包
domain: 施工二营
移交方: Codex（原二营 → 升任丞相）
接任方: Qoder（原丞相 → 转任二营）
status: source
---
# 二营交接包 · Codex 移交（读此文件即可复任二营）

> 背景：2026-07-30 砚仁钦定职位互换——Codex 升丞相，Qoder 转二营。
> 本文件是 Codex 以原二营身份写给接任二营的完整交接。读完即可开工。

## 一、二营是干什么的（法统里的位置）
- **阵地**：`server/`（后端 + 涉钱 + 设计稿）。**禁区 `nantang-mobile/` 零改动**。
- **只施工不验收**：接卡→改码→写测试→写回执→具名 commit。**不 push**（push 是御笔）。
- **实证优先**：涉钱/涉安全逻辑必须实测验证，不能只按字面改。**发现派发单药方本身有误，要纠正并留证**（先例：CR-3/4 发现药方会造币、W5-B 发现 H-5 药方在真 schema 上跑不起来——都实测后偏离并写明理由）。

## 二、环境（已就绪，照抄即可）
- 真仓：`C:\Users\苏砚仁\thinknote\项目\实景游戏移动端代码_new`（独立 git，remote=nantang-server，分支 main）。
- venv：`.venv\Scripts\python.exe`，依赖已装（fastapi/pytest/httpx 等）。
- **跑测试命令**（务必带这三个环境要点）：
  ```
  cd server; $env:JWT_SECRET='test-secret-key-for-ci-32bytes-long'; ..\.venv\Scripts\python.exe -m pytest tests/ -q -p no:cacheprovider
  ```
  - `-p no:cacheprovider`：因 `.pytest_cache` 权限拒绝，必须加。
  - 全量基线（截至 W5-B）：**136 passed, 8 skipped**（8 skip 均 `requires_pg`，无 PG 连接串时自动跳）。
  - 全量约 70-75 秒，单文件约 5-7 秒。**跑全量给 timeout ≥ 120000ms**。

## 三、铁律（违一条即打回）
1. **具名 `git add`（禁 `-A`）**——两营共用一棵 git 工作树，这是唯一防撞护栏。
2. **只 commit 不 push**。
3. commit message 带卡号 + 营号：`fix(卡号): 摘要 · 二营`。
4. **回执四件套**：改动清单 / 测试结果 / commit 信息 / 遗留。落 `方案/任务卡/编号_回执_二营_日期.md`。
5. **太傅注三行**（施工方视角的三句要害提醒，附回执末尾）。
6. 全程**中文**回执（先例：日语回执被隐含纠正过）。
7. commit 前 `git diff --cached --name-only` 自查：**禁区零改动、不夹带非本卡文件**。

## 三·补（2026-07-30 收敛更新）：Qoder 降职接任专段

> 接任者 = **Qoder(千问)**，原第二任丞相，token 烧尽卸任、恢复后**降职为二营**。
> 与从零上任不同：你熟项目、熟法源，缺的是"接受降职 + 二营纪律 + 首活是终验"。

### 席位现状（钉死版）
| 席 | AI | 模型 | 状态 |
|---|---|---|---|
| 丞相 | Codex | 豆包 | 在任（原二营升任） |
| 一营 | Claude Code(一言) | DeepSeek | 前端，已复验 W5-B/CR-3/4 PASS |
| **二营** | **Qoder** | **千问** | **恢复后接任（本包对象）** |

### 你的第一件活：终验 W5-B / CR-3/4（不是施工）
- 一营已初验 PASS。你做**二营终验**（后端本行、借机熟悉代码库、形成双营交叉）。
- 命令：`pwsh server\scripts\丞相验收.ps1 -Hash ab6a68a`（W5-B）+ `-Hash 4f2624d`（CR-3/4）
- 人工核 4 争议点：W5-B 的 H-5 误诊是否成立；CR-3/4 的 BUG-2"营地不退款"守恒是否成立。详见 `方案/任务卡/复验单_W5B-CR34_一营_2026-07-30.md`。
- 回报一行：`终验 PASS <卡号>` 或 `异议 <卡号> <理由>` → 丞相对账 → 砚仁出闸。

### 降职心态提示（Codex 也是原二营，同理）
丞相/二营只是分工不同，不是高低。丞相不写代码、二营才碰代码——**降职=重新拿回写代码的手**。你熟法源的优势在二营位一样值钱（能自己判药方对不对）。

## 四、此刻在途（2026-07-30 交接快照，保质期 24h）
| # | 事项 | 状态 | 接任二营要做的 |
|---|---|---|---|
| 1 | **W5-B（H-4/6/7 改码 + H-5 判误诊）** commit `ab6a68a` | 已 commit 未出闸 | **⚠️ 利益冲突提醒**：这是 Codex 以二营身份提交的。Codex 升丞相后不宜自验。**此卡由接任二营（Qoder）独立复验**，或砚仁指派他窗复核，再报丞相副署 |
| 2 | CR-3/4 commit `4f2624d` | 已 commit 未出闸 | 同上，Codex 提交，已有回执，待丞相副署 |
| 3 | `.venv/` gitignore | 未决（延续 CR-3/4 + W5-B） | 为跑测新建的 `.venv/` 未入 `.gitignore`，请砚仁定夺是否处理；未经许可勿擅改 `.gitignore` |
| 4 | 本地领先 origin **3 笔**：`ab6a68a`(W5-B) `6253bc2`+`9d52638`(W5-A 一营) | 未 push | 出闸是御笔，二营不碰 |
| 5 | 工作区非本营改动 | `.gitignore`/`AGENTS.md`/`BUG_TRACKER.md`/`方案/中堂备忘录.md` 有 M 状态 | **不属二营改动，勿纳入任何 commit** |

## 五、接任二营开工顺序
1. 读本文件 → 2. 读 `方案/中堂备忘录.md` + `BUG_TRACKER.md` → 3. `git log --oneline -5` 对状态 → 4. 向丞相（Codex）报到：「二营已接任，环境已核，请派卡」。
5. **改前必重新 `git status` + 重新定位行号**（他人提交会让行号漂移）。

## 六、Codex 留给接任二营的三句私房话
1. 药方≠病灶：派发单给的行号/改法只是线索，真 schema 跑一遍才算数——CR-3/4 和 W5-B 两次都是实测才逮出药方错。
2. 截尾是本能：pytest 输出只看尾部汇总行，别把几百行 warning 灌进上下文——省 token 从施工就要养成。
3. 回执写给"验收的人"看：改动清单给行号、测试给数字、遗留写清楚——让丞相能逐条对，不用来回问。
