# 南塘云村 v1.0 — 全项目审查报告

> 2026-07-22 · 6 Agent 并行审查 · 服务器+客户端全栈 · ~120 条原始发现

---

## 🔴 阻断（立即修复 · 15 项）

### XSS / 安全

| # | 问题 | 来源 | 文件 |
|---|------|------|------|
| S1 | `esc()` 在 onclick 中无效 — 用户名字段可注入 JS | 🧪 | utils.js:163 |
| S2 | 服务端无输入净化，用户提交直接存 DB | 🧪 | tasks.py / data.py |
| S3 | `GET /api/auth/users` 无认证，公开所有用户名 | 🧪 | auth.py:173 |

### 经济崩溃 / 会计等式

| # | 问题 | 来源 | 文件 |
|---|------|------|------|
| E1 | `verify_task` 未领取份额退款后 task_escrow 未足额扣减 — NT 凭空产生 | 💰 | nt.py:558 vs 576-586 |
| E2 | 营地任务 escrow 永久锁死在 task_escrow — 无路径释放 | 💰 | nt.py:553-558 + tasks.py:117 |
| E3 | `withdraw` 端点 NameError 崩溃 — `func` 未导入 | 💰 | nt.py:347 |
| E4 | 营地创建凭空印钞 — 无人扣款，total_issued 直接增加 | 💰 | camps.py:108-109 |

### 状态机混乱

| # | 问题 | 来源 | 文件 |
|---|------|------|------|
| G1 | 三套状态词汇并存（服务端中文/客户端中文/nt-core英文），无映射层 | 🎮 | models.py / data.js / nt-core.js |
| G2 | `verify_task` 向所有 assignee 付款，不论是否提交 — 可白嫖 | 🎮 | nt.py:547-549 |
| G3 | 3/5 新手任务无法完成（无 completion hook） | 🎮 | data.js:49-55 |

### 数据主权

| # | 问题 | 来源 | 文件 |
|---|------|------|------|
| D1 | 14 个 localStorage 键无集中管理，`resetAllData()` 用 `clear()` | 🎬 | app-data.js, auth.js, core.js |
| D2 | HTTP 模式下 `saveUsers()` 是 NO-OP — 角色变更/资料编辑刷新丢失 | 🎬 | auth.js:24 |
| D3 | `_saveShared` 只同步 4/18 字段到服务端 | 🎬⚙️ | app-data.js:222-229 |
| D4 | 离线 earn 队列重放导致双重入账 | 🎬 | app-data.js:401-418 |

---

## 🟡 高危（第二批 · 24 项）

### 并发 / 竞态

| # | 问题 | 来源 |
|---|------|------|
| R1 | `POST /api/tasks` 创建任务余额检查与扣款间无行锁 | 💰🧪 |
| R2 | `POST /spend` pool 无锁 | 💰 |
| R3 | `POST /transfer` pool 无锁 | 💰 |
| R4 | 住宿扣费依赖客户端登录触发，非 cron 驱动 | 💰 |
| R5 | 住宿欠费不记 NTLedger | 💰 |

### 架构 / 代码组织

| # | 问题 | 来源 |
|---|------|------|
| A1 | index.html 含 800+ 行业务逻辑（信箱/创营/结营/排行） | 🎬 |
| A2 | `TASKS`/`JOURNAL_TYPES` 在 data.js 和 index.html 双重定义 | 🎬 |
| A3 | 19 个 JS 脚本按固定顺序加载，无依赖声明 | 🎬 |
| A4 | 权限检查三种模式共存（内联/`userCan`/`isMemberByRole`） | 🎬 |
| A5 | `map-app.js` 与 `app.js` 代码重复但加载路径不同 | 🎬 |

### 交互 / UX

| # | 问题 | 来源 |
|---|------|------|
| U1 | `Game.confirm` 使用浏览器原生 confirm() | 🖐️ |
| U2 | 加载中文字无超时回退 — 永远显示 "加载中…" | 🖐️ |
| U3 | `showToast` 三参数重载行为不可预测 | 🖐️ |
| U4 | 全页无加载骨架 — 白屏闪烁 | 🖐️ |
| U5 | 卡片展开/收起 `display:none` 硬切 — 无动画 | 🖐️ |
| U6 | 日期用本地时区，服务端用 UTC — 跨日偏差 | 🧪 |

### 数据层

| # | 问题 | 来源 |
|---|------|------|
| D5 | Journal 永不服务端同步 | ⚙️ |
| D6 | `DELETE /api/tasks` 离线任务用本地名当服务端 ID | ⚙️ |
| D7 | `card_discoveries` ID 时间戳碰撞风险 | ⚙️ |
| D8 | `syncAll` + `nt/sync` 登录时双重拉取 | ⚙️ |

---

## 🟢 低优（第三批 · 14 项）

- 60+ ponytail 标记无追踪注册表
- `BED_RATES` 硬编码
- `nt.js` vs `nt-core.js` 语义混淆
- `api.js` 6 个函数无调用方
- 数据库迁移无版本号
- `MAX_BEDS` 全局而非按房间
- 轮询退避无恢复机制
- `_profileSeed` 每次刷新覆盖
- CV 转账小额 100% 蒸发
- `contribution_pool` 只进不出
- 服务端代码不在仓库中

---

## 修复顺序

```
第一批 S1-S3 + E1-E4 + G1-G3 + D1-D4    ← 3h
  │
第二批 R1-R5 + A1-A5 + U1-U6 + D5-D8      ← 4h
  │
第三批 14 项低优                            ← 2h
  │
全部 ~53 项清零
```
