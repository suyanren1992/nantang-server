# 南塘云村 · 阶段文件（现状总览 + 12 条需求梳理）

> 2026-07-23 · 本文档是当前阶段的唯一入口：完成了什么、环境在哪、12 条新需求怎么分类怎么排期。

---

## 一、当前环境与部署（事实状态）

| 项 | 状态 |
|---|---|
| 正牌工作目录 | `thinknote\项目\实景游戏移动端代码_new`（main 分支，与 GitHub `suyanren1992/nantang-server` 同步） |
| 线上地址 | 前端+后端同源：`https://nantang.imeeting.club`（Render 服务 `nantang-server.onrender.com`，push 即自动部署） |
| 旧域名 | `nantang-api.imeeting.club` 被旧服务 `ntyc-sjyx-12v.onrender.com`（他人账号）占用，待原部署人释放 |
| 数据库 | Render 上全新库；第一个注册用户 = 管理员 |
| Render 环境变量 | JWT_SECRET / PLATFORM_WALLET_ADDRESS（社区多签 0xf0aa…1a0d）/ NT_TOKEN_CONTRACT（0x7563…5Bd0）/ OP_RPC_URL |
| 已知缺口 | 构建命令缺 `web3` 依赖 → 链上查验 scanner 未运行（日志报 No module named 'web3'） |
| 目录扶正 | ⚠️ 未完成：旧目录被占用无法改名（见文末「待用户配合」） |

### 已推送的修复（线上已生效）
| Commit | 内容 |
|---|---|
| d8f9d30 | refresh cookie 恢复 secure=True（HTTPS 下保持登录） |
| f074c51 | 名字乱码修复（encodeURIComponent → CURRENT_USER） |
| 640a87a | api.js 同源化 base=''（前端不再指向临时隧道地址） |
| f1d17e0 | 充值表单 z-index 250→310 防遮挡 + 档案室卡片记录 Tab 接线 |
| ea5d8d9 | 死代码移档（map-app/feishu/eventbus/重复 theme.css，只移不删） |
| 86fcc57 | 五份设计定稿迁入 `设计/` + 第 1 周执行方案 |

### 设计定稿位置（`设计/` 目录）
NT 经济两代人 / 社区公约与新手引导 / 世界终端 / 硬件接入三代路线 / DeepSeek 结算方案收录

---

## 二、12 条需求梳理（2026-07-23 01:55 提出）

### A 类 · 直接可修的 Bug（不需要设计决策，核查后逐条修）

| # | 问题 | 初步判断 | 改动范围预估 |
|---|---|---|---|
| 5 | 全貌页下方滑动模块绿色框偏左、半边出屏 | carousel 居中计算问题（initCarousel / scroll-snap） | core.js，~10 行 |
| 6 | 注册/登录页密码没有"小眼睛"查看（回归） | 之前实现过，被某轮改造冲掉 | index.html + core.js，~15 行 |
| 7 | 校核室确认劳动后奖励 NT 没到账 | 需核查确认链路：ui-cardroom.js 确认 → 服务器 verify → 余额入账，可能断在任何一环 | 待核查 |
| 9 | 确认劳动后没有归档 | 校核通过后未写档案室记录，与卡 4 已接线的 Tab 配合补 | ui-cardroom.js + 后端 |
| 12 | 全貌页"社区动态"点不进去 | _collapsibleSection 手风琴可能绑定丢失或被遮挡 | app.js，~10 行 |

### B 类 · 需要设计确认的改造（先出方案，砚仁点头后再动）

| # | 需求 | 方案来源 |
|---|---|---|
| 1 | 冰箱/田地/大扫除面板布局统一 | **已有现成设计稿**：`方案/UI改造设计_田地冰箱大扫除_2026-07-22.md`（大扫除为模板，冰箱抄 shared-kitchen.html 双门设计，CSS class 全部已在 theme.css） |
| 2 | 田间管理仿大扫除格局：子区域、作物选择、务农类型、生长规律与季节 | 同一份设计稿已画好 BEFORE/AFTER；生长规律/季节需新增作物数据表（作物→生长天数→季节），这是新数据设计 |
| 3 | 全貌页 6 段与建筑页数据联通（厨房/冰箱/田地一致） | 1817 对照文档已定位：`MGMT_DATA` 需与建筑页房间数据同源，改数据读取而非界面 |
| 8 | 卡片室"我需要帮忙"跳任务大厅；"上报劳动"不显示上报人 | 交互改动小，但涉及卡片室设计原则（自证 vs 他证），要确认跳法与文案 |
| 10 | 确认校核时加提示：校核是做证明、看见别人的付出、不能做假证 | 纯文案+确认弹窗，~10 行，但文案要砚仁定稿 |
| 11 | 增加脑力劳动渠道 | 劳动类型扩展：需定义脑力劳动类别（写作/设计/翻译/策划？）、校核方式（脑力劳动成果如何"被看见"——附件/链接？） |

### C 类 · 参考文档（已读，结论收录）

| # | 文档 | 结论 |
|---|---|---|
| 4 | `方案/2026-07-22_0512_换房计费审查.md` | 换房计费逻辑✅通过；两项非阻塞优化：O1 BED_RATES 环境变量化（~5行）、O2 换房客户端先请求成功再改本地（~15行） |

### 附带发现（1817 对照文档揭露的结构性问题）
- `poolCard` 是空函数——社区池透明面板从未实现（全貌页第 6 段）
- 新手指引进度卡 `_renderNewbieCard` 定义了但没挂到任何 section
- 快捷入口 3 个 vs 管理卡片 4 张，入口不一致

---

## 三、建议执行顺序

**第一批（A 类 bug，逐条修、逐条单独 commit、push 即上线）：**
1. 卡 5 → 滑动模块居中
2. 卡 6 → 密码小眼睛回归
3. 卡 12 → 社区动态点不进去
4. 卡 7 → 校核奖励没到账（先核查链路再修，可能是最大的一条）
5. 卡 9 → 确认劳动后归档（接着卡 7 修，同一条链路）

**第二批（B 类，等你点头）：**
6. 卡 1+2 → 田间管理面板改造（照 UI 改造设计稿施工，含子区域+作物数据表）
7. 卡 1 → 冰箱双门面板（抄 shared-kitchen.html）
8. 卡 3 → 全貌页与建筑页数据同源
9. 卡 8+10 → 卡片室交互与校核提示文案（文案你定）
10. 卡 11 → 脑力劳动渠道（先出一页设计稿再施工）

**纪律不变：** 只移不删、一次一个目标、心脏文件（nt-core.js / app-data.js）谨慎、改完跑冒烟清单、每条单独 commit。

---

## 四、待用户配合

- [ ] 关闭在旧目录 `实景游戏移动端代码` 打开的程序（Kimi Code 终端、VS Code、资源管理器），然后告诉我，我完成目录扶正：
  `实景游戏移动端代码` → `实景游戏移动端代码_退役20260722`，`实景游戏移动端代码_new` → `实景游戏移动端代码`
- [ ] B 类需求逐条确认（特别是 2、8、10、11）
- [ ] 找旧部署人释放 `nantang-api.imeeting.club`（不紧急）
- [ ] 修复构建依赖：构建命令加 `web3`（链上查验 scanner 才能跑）
