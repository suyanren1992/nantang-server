# 第 1 周执行方案 · 核心闭环修复（含目标卡）

> 2026-07-22 · 可执行 · 前置：存档与 git 基线已完成（38cf911 / c66a9b7）
> 使用方式：贴一张目标卡执行一张，全部单线程；每张卡做完跑冒烟清单，再贴下一张。

---

## 0. 纪律

1. **一次一个目标**，每个修复单独 commit
2. 心脏文件 `js/nt-core.js`、`js/app-data.js` 本周不动
3. 每完成一张卡，跑一遍冒烟清单（见第 3 节）
4. 发现目标卡与实际代码不符，先回 Kimi Work 对齐，不要自由发挥

---

## 1. 目标卡（按顺序执行）

### 卡 1 · B1 cookie secure（🔴 最高优先）

```
目标：修复 server/routes/auth.py:33 的 refresh cookie：在 set_cookie("nt_rt", ...) 中加回 secure=True。
背景：此修复 2026-07-20 曾提交（3fa3633），被 Round 1 重构（2d286f3）冲掉，属回归。
完成条件：该行同时包含 secure=True 和 httponly=True；grep 验证。
约束：不修改任何其他文件。
提交信息："fix: 恢复 refresh cookie secure=True（防回归：曾被 Round 1 冲掉）"
```

### 卡 2 · B2 轮询 401 判断

```
目标：修复 js/core.js 约 206 行的轮询停止条件：后端 401 返回 {"detail":"Unauthorized"}（大写 U），
当前代码判断 tasks.detail === 'unauthorized'（小写）永不匹配。
修复：改为大小写不敏感匹配，或改用 tasks.ok === false 判断；
并在检测到 401 时先静默调一次 API.silentRefresh() 续期，续期失败再停轮询。
完成条件：模拟登录过期后，控制台不再每 10 秒抛 TypeError；轮询行为正确。
约束：只改轮询判断与续期逻辑，不动轮询频率和其他调用方。
单独 commit。
```

### 卡 3 · B3 充值表单遮挡

```
目标：修复个人资料弹窗被遮挡导致充值表单无法输入：
js/core.js 约 1032 行 profileCard 的 z-index（当前 250）提升至 310 以上
（高于 #newbieGuideOverlay 的 280 和 #inboxPanel 的 300）。
完成条件：新手引导浮层或信箱面板打开时，充值输入框仍可点击、可输入、可提交。
约束：只改 z-index 一处；不改浮层本身逻辑。
单独 commit。
```

### 卡 4 · B5 卡片记录 Tab 断线

```
目标：修复档案室「🃏 卡片记录」Tab 点击无反应：
js/ui-archive.js 的 switchArchiveTab（约 249 行）补上 'cards' 分支，
调用已存在的 renderArchiveCards（index.html 约 1111 行）。
完成条件：点击卡片记录 Tab 能正确渲染，状态筛选 chips 与搜索可用。
约束：只接线，不改 renderArchiveCards 内部逻辑（如发现它自身有 bug，另开新卡）。
单独 commit。
```

### 卡 5 · B6 activity_log 不落盘

```
目标：修复社区活动日志刷新即丢：
① js/app-data.js 的 _saveShared（约 216 行）持久化白名单加入 activity_log；
② js/ui-archive.js 的 logActivity（约 41 行）写入后调用 AppData._save()。
完成条件：file:// 离线模式下执行若干操作后刷新页面，档案室日志 Tab 记录仍在。
约束：只加白名单和保存调用，不改日志结构。
单独 commit。⚠️ 本卡触碰 app-data.js（心脏文件），提交前全量冒烟。
```

### 卡 6 · B7 社区页时间线数据源错误 + undefined 时间

```
目标：按议题四结论改造：
① 从社区副本页（index.html overlayCommunity）删除「📜 南塘时间线」section；
② 档案室日志 Tab 升级为时间线视图：按日分组渲染 activity_log；
③ 修复 ui-phase4.js renderTimeline（约 517 行）j.date 不存在导致 "undefined" 的渲染 bug
   （该函数仍被地图信息馆页 _renderTimelineSection 使用，不能删，只能修）。
完成条件：社区页只保留营地列表+活动公告；档案室出现按日分组时间线；无 undefined 文本。
约束：不动地图信息馆页的 _renderTimelineSection 数据逻辑。
单独 commit。
```

### 卡 7 · 死代码移档（砚仁已确认：先移不删）

```
目标：将确认的死代码移入 存档/死代码_20260722/（不删除）：
- nantang-mobile/js/map-app.js（旧版地图，未加载）
- nantang-mobile/js/feishu.js（废弃飞书通道，未加载）
- nantang-mobile/core/eventbus.js（未加载）
- nantang-mobile/ui/theme.css（与 css/theme.css 重复）
- nantang-mobile/modules/ 下 7 个空目录（保留 camp-pipeline.zip 移入存档同目录）
执行前先 grep 每个文件在 index.html 及全部 js 中的引用，确认零引用才移动；
移动后打开页面确认无 404、无控制台报错。
完成条件：应用功能与移动前完全一致。
单独 commit。
```

### 卡 8 · 现状盘点表

```
目标：在项目根目录创建《00_现状盘点表.md》，作为唯一真相源。
格式：| 功能 | 状态(✅已验证/🟡写了未验/🔴坏/⚫没做/🗑已归档) | 备注 |
覆盖：登录/注册、任务大厅、卡片室、NT 账本、充值/提现、住宿、食堂、打扫、
     田地、物品/冰箱、营地、档案室、新手引导、地图、社区副本、信箱。
每张卡修完的功能回表更新状态。
约束：初始状态标注以 KIMI_CODE_交接.md 的 Bug 清单和探索结论为准，
拿不准的一律标 🟡 不写 ✅。
单独 commit。
```

---

## 2. 本周不做（明确排除）

- 充值线上同步（B8）：依赖第一代 NT 经济方案落地，第 5 周做
- 大扫除 27 间房（B9）、田地双实现（B10）：第 2-3 周，按全貌页改造方案执行
- 公约、权限统一、reset-db、staging：第 5 周
- 档案室 NT 门槛、八卦室、NPC 引导：见 设计/ 三份定稿，第 4-6 周

## 3. 冒烟清单（每张卡做完跑一遍）

```
□ 登录 / 刷新后保持登录
□ 发布一个任务
□ 接取一个任务
□ 卡片室发起一次发现
□ 猜一次卡片并确认
□ NT 余额正确变动
□ 住宿入住一间房
□ 食堂订一次餐
□ 提交一次充值申请（表单可输入）
□ 打开档案室四个 Tab
```

## 4. 完成后

第 1 周目标卡全部 ✅ 且冒烟通过 → 回 Kimi Work 对齐 → 进第 2-3 周：
按 `方案/2026-07-22_1900_全貌页面板综合改造方案.md` 执行（P0 大扫除 B9 + 田地 B10 → P1 冰箱双门 + 3 个未挂载函数）。
