# 校核室完整实现方案（可执行版）

> 2026-07-22 · 6 Agent 审查通过 · 4 commits · ~110 行净增 · ~3h
> 分支起点：`fix/6-critical-bugs`

---

## 执行前确认

```bash
cd "C:/Users/苏砚仁/thinknote/项目/实景游戏移动端代码"
git branch --show-current   # 必须输出 fix/6-critical-bugs
git status --short           # 应该干净（无未提交改动）
```

---

## Commit 1：7 个 P0 阻断修复（~30 行 · ~1h）

### 1-1 · _mergeSyncData 补 verifications 合并
**文件**：`nantang-mobile/js/core.js`，函数 `_mergeSyncData` 末尾（`if (data.newbie ...` 之后，`}` 之前）

```javascript
// 在 data.newbie 处理块之后、函数结束 } 之前插入：
if (data.verifications && Array.isArray(data.verifications)) {
  var localVfys = AppData._data.pendingVerifications || [];
  var localById = {}; localVfys.forEach(function(v){ localById[v.id] = v; });
  data.verifications.forEach(function(sv) {
    if (!localById[sv.id]) { localVfys.push(sv); }
    else if (localById[sv.id].status === 'pending') { Object.assign(localById[sv.id], sv); }
  });
  AppData._data.pendingVerifications = localVfys;
}
```
**验证**：HTTP 模式登录→上报劳动→刷新页面→打开卡片室→待验证 tab 有数据

### 1-2 · approve 端点加角色门
**文件**：`server/routes/nt.py`，函数 `approve_verification`，`# 非自校核` 检查之后

```python
# 在 raise HTTPException(400, "不能校核自己的操作") 之后插入：
if user.role == "visitor":
    raise HTTPException(status_code=403, detail="入住后可使用校核")
```
**验证**：visitor token curl POST /api/nt/verifications/{id}/approve → 403

### 1-3 · User 余额写入加行锁
**文件**：`server/routes/nt.py`，函数 `approve_verification`，doer 查询行

```python
# 改前：select(User).where(User.id == vfy.doer)
# 改后：
doer = (await db.execute(
    select(User).where(User.id == vfy.doer).with_for_update()
)).scalar_one_or_none()
```
同一函数中 verifier 余额写入前加：
```python
# 在 user.nt_balance += verifier_reward 之前插入：
verifier = (await db.execute(
    select(User).where(User.id == user.id).with_for_update()
)).scalar_one_or_none()
# 然后改为 verifier.nt_balance += verifier_reward
```
**验证**：grep `approve_verification` 上下文→确认 doer 和 verifier 两处 User 查询都有 `.with_for_update()`

### 1-4 · verifyAction 改为悲观更新
**文件**：`nantang-mobile/js/app-data.js`，函数 `verifyAction`，HTTP approve 分支

```javascript
// 改前：先设 status='verified' 再 fire-and-forget
// 改后：先调 API，成功回调中更新本地状态
if (!isOffline) {
  var self = this;
  API.request('POST', '/api/nt/verifications/' + vfy.id + '/approve',
    {doer: vfy.doer, action: vfy.action, nt_amount: vfy.ntAmount, verifier_reward: vfy.verifierReward}
  ).then(function() {
    vfy.status = 'verified'; vfy.verifier = verifierName; vfy.verifiedAt = new Date().toISOString();
    self._saveShared(true);
    if (typeof renderCardRoom === 'function') renderCardRoom();
  }).catch(function(err) {
    showToast('校核失败: ' + ((err && err.detail) || '网络错误，请重试'), 'error');
    vfy.status = 'pending'; vfy.verifier = null; vfy.verifiedAt = null;
    self._saveShared(true);
  });
  return { ok: true };  // 异步处理，先返回
}
```
**验证**：断网→点确认→toast "校核失败: 网络错误"→按钮恢复可点击

### 1-5 · doer 删除后 NT 回流
**文件**：`server/routes/nt.py`，函数 `approve_verification`，doer 存在性检查之后

```python
# 在 if doer and nt_amount > 0: 分支后加 else:
if doer and nt_amount > 0:
    doer.nt_balance += nt_amount
    ...
else:
    pool.balance += nt_amount  # doer 不存在，回流社区池
```
**验证**：删除 doer 用户→approve→GET /api/nt/verify → pass=true diff=0

### 1-6 · 客户端只保留结构性校验
**文件**：`nantang-mobile/js/app-data.js`，函数 `verifyAction`，删除以下代码块：

```javascript
// 删除：章0.6 验证冷却（lines 343-349）
// 删除：每日上限仅计通过次数（lines 351-358）
// 保留：vfy 存在性检查、状态检查、自校核检查
```
并在 API 429 响应处理中加：
```javascript
.catch(function(err) {
  var msg = (err && err.detail) || '网络错误';
  if (err && err.status === 429) {
    msg = msg.indexOf('1h') !== -1 ? '你在 1 小时内已经验证过 TA 了' :
          msg.indexOf('10') !== -1 ? '今天已验证 10 次，明天再来吧' : msg;
  }
  showToast(msg, 'warn');
  ...
})
```
**验证**：连续 approve 同一对两次→第二次 toast 友好中文消息

### 1-7 · 离线队列统一走 approve 路径
**文件**：`nantang-mobile/js/app-data.js`，函数 `_drainPendingEarns`

```javascript
// 改前：POST /api/nt/earn-sync
// 改后：遍历队列，逐个 POST /api/nt/verifications/{vfyId}/approve
_drainPendingEarns: function() {
  if (!this._data._pendingEarnQueue || !this._data._pendingEarnQueue.length) return;
  if (typeof API === 'undefined' || !API.token) return;
  var queue = this._data._pendingEarnQueue.slice(0);
  this._data._pendingEarnQueue = [];
  var self = this;
  var seen = {};
  queue.forEach(function(item) {
    var key = item.vfyId || (item.doer + '|' + item.ts);
    if (seen[key]) return; seen[key] = true;  // 去重
    API.request('POST', '/api/nt/verifications/' + item.vfyId + '/approve',
      {doer: item.doer, action: item.action, nt_amount: item.amount, verifier_reward: item.reward || 0}
    ).catch(function() {
      self._data._pendingEarnQueue.push(item);  // 失败放回队列
    });
  });
}
```
**验证**：离线模式校核→切 HTTP 登录→队列回放→查 verification 状态变为 verified

---

## Commit 2：双 tab UI + 路径选择器（~65 行 · ~1h）

### 2-1 · renderCardRoom() 加 tab 栏
**文件**：`nantang-mobile/js/ui-cardroom.js`，函数 `renderCardRoom` 最开头（`var el = ...` 之后、`var discs = ...` 之前）

```javascript
var _cardroomTab = sessionStorage.getItem('_cardroomTab') || 'guess';

var h = '';
h += '<div class="my-tabbar" style="margin-bottom:8px">';
h += '<div class="my-tab '+(_cardroomTab==='guess'?'on':'')+'" onclick="sessionStorage.setItem(\'_cardroomTab\',\'guess\');renderCardRoom()">🃏 猜猜是谁</div>';
h += '<div class="my-tab '+(_cardroomTab==='verify'?'on':'')+'" onclick="sessionStorage.setItem(\'_cardroomTab\',\'verify\');renderCardRoom()">✓ 去验证</div>';
h += '</div>';

if (_cardroomTab === 'guess') {
  h += '<div style="font-size:.6rem;color:#aaa;margin-bottom:8px">谁做了这件事？猜对有奖</div>';
} else {
  h += '<div style="font-size:.6rem;color:#aaa;margin-bottom:8px">确认社区成员的劳动</div>';
}

if (_cardroomTab === 'verify') { el.innerHTML = h + _renderVerifyTab(); return; }
// 否则继续现有逻辑...
```

### 2-2 · 新增 _renderVerifyTab()
**文件**：`nantang-mobile/js/ui-cardroom.js`，在 `renderCardRoom` 之前

```javascript
function _renderVerifyTab() {
  var vfys = (window.AppData && AppData._data.pendingVerifications) || [];
  var pending = vfys.filter(function(v){ return v.status === 'pending'; });
  var resolved = vfys.filter(function(v){ return v.status !== 'pending'; });

  var users = typeof getUsers === 'function' ? getUsers() : {};
  var role = (users[CURRENT_USER] || {}).role || 'visitor';
  var isOnsite = role !== 'visitor';

  var h = '';
  // 池余额预警
  var poolBalance = (window.AppData && AppData._data._poolBalance) || 999;
  if (poolBalance < 200) {
    h += '<div style="background:#fff5f5;border:1px solid #f0c8c8;border-radius:8px;padding:6px 10px;margin-bottom:8px;font-size:.6rem;color:#b84c38">⚠️ 社区池余额 ' + poolBalance + ' NT，验证奖励可能延迟发放</div>';
  }

  // 待确认
  if (pending.length) {
    h += '<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:6px">';
    h += '<span style="font-weight:700;font-size:.72rem;color:#5a6e5c">⏳ 待确认 (' + pending.length + ')</span>';
    h += '<span style="font-size:.55rem;color:var(--green-primary);cursor:pointer" onclick="renderCardRoom()">🔄 刷新</span>';
    h += '</div>';
    pending.forEach(function(v) {
      var icon = ({cleaning:'🧹',stock_in:'📦',stock_out:'🗑',farming:'🌿',cooking:'🍳',labor_report:'📝'})[v.type]||'📋';
      h += '<div class="vfy-card" id="vfy-'+v.id+'" style="background:#fffdf9;border:1.5px solid #c8c0b0;border-radius:10px;padding:10px 12px;margin-bottom:6px;transition:opacity .3s">';
      h += '<div style="display:flex;justify-content:space-between"><span>'+icon+' <b>'+esc(v.doer)+'</b> · '+esc(v.action)+'</span><span style="color:#8a6a20;font-weight:700">+'+v.ntAmount+' NT</span></div>';
      h += '<div style="font-size:.55rem;color:#aaa;margin-top:4px">'+(v.createdAt||'').slice(0,16)+'</div>';
      if (isOnsite) {
        h += '<div style="display:flex;gap:6px;margin-top:6px"><button class="btn-sm danger" style="flex:1;font-size:.6rem" id="btn-reject-'+v.id+'" onclick="var b=document.getElementById(\'btn-reject-'+v.id+'\');b.disabled=true;b.textContent=\'⏳\';AppData.verifyAction(\''+v.id+'\',CURRENT_USER,false);setTimeout(function(){renderCardRoom()},800)">🙅 不是</button><button class="btn-sm pri" style="flex:1;font-size:.6rem" id="btn-approve-'+v.id+'" onclick="var b=document.getElementById(\'btn-approve-'+v.id+'\');b.disabled=true;b.textContent=\'⏳ 验证中…\';AppData.verifyAction(\''+v.id+'\',CURRENT_USER,true);setTimeout(function(){renderCardRoom()},800)">✅ 确认 +'+v.ntAmount+'NT</button></div>';
      } else {
        h += '<div style="font-size:.58rem;color:#8a6a30;margin-top:6px;background:#fef8ee;padding:4px 8px;border-radius:6px">☁️ 你是云村民，暂不能验证<br><span style="text-decoration:underline;cursor:pointer" onclick="closeCardRoom();openMapPage()">入住后成为在地成员 →</span></div>';
      }
      h += '</div>';
    });
  } else {
    if (resolved.length > 0) {
      h += '<div style="text-align:center;padding:20px;color:#5d8c52;font-size:.7rem">🎉 全部验证完毕！<br><span style="font-size:.6rem;color:#aaa">去「猜猜是谁」看看？</span></div>';
    } else {
      h += '<div style="text-align:center;padding:20px;color:#aaa;font-size:.7rem">📋 还没有人上报劳动<br><span style="font-size:.6rem">在地图或快捷入口上报后，这里会出现待验证项</span></div>';
    }
  }

  // 已处理
  if (resolved.length) {
    h += '<div style="font-weight:700;font-size:.65rem;color:#5a6e5c;margin:12px 0 6px">📋 最近已处理 (' + resolved.length + ')</div>';
    resolved.slice(0,10).forEach(function(v){
      var st = v.status==='verified'?'✅ 已通过':v.status==='permanently_rejected'?'🚫 已拒绝':'❌ 已驳回';
      h += '<div style="font-size:.62rem;padding:4px 0;border-bottom:1px solid #f0f0f0">'+st+' '+esc(v.doer)+' · '+esc(v.action)+' <span style="color:#aaa">'+(v.verifiedAt||v.rejectedAt||'').slice(0,10)+'</span></div>';
    });
  }
  return h;
}
```
**验证**：浏览器打开卡片室→点击"去验证" tab→有 pending 时显示卡片+按钮，无 pending 时显示引导文案。云村民显示升级入口。

### 2-3 · 路径选择器
**文件**：`nantang-mobile/js/ui-cardroom.js`，在 `openSelfReport` 之前

```javascript
function openPathChooser() {
  var h = '<div style="background:#fff;border-radius:16px;width:320px;max-width:92vw;padding:20px;box-shadow:0 12px 40px rgba(0,0,0,.25)">';
  h += '<div style="font-weight:700;font-size:.82rem;text-align:center;margin-bottom:14px">你想做什么？</div>';
  h += '<div onclick="closeDiscoveryForm();openSelfReport()" style="background:linear-gradient(135deg,#e8f0e8,#dce8d8);border:2px solid var(--green-primary);border-radius:14px;padding:14px;margin-bottom:10px;cursor:pointer;position:relative">';
  h += '<div style="position:absolute;top:8px;right:10px;font-size:.55rem;background:var(--green-primary);color:#fff;padding:2px 8px;border-radius:10px">⭐ 推荐流程</div>';
  h += '<div style="font-size:1.2rem;margin-bottom:4px">✅ 我做完了求验证</div>';
  h += '<div style="font-size:.6rem;color:#5a6e5c">做完劳动后上报，等待另一位成员验证通过 +NT</div></div>';
  h += '<div onclick="closeDiscoveryForm();openPublishTask()" style="background:#fff;border:1.5px solid #d0d9ce;border-radius:14px;padding:14px;margin-bottom:10px;cursor:pointer">';
  h += '<div style="font-size:1.2rem;margin-bottom:4px">📝 我需要帮忙</div>';
  h += '<div style="font-size:.6rem;color:#5a6e5c">发布委托任务，请别人帮忙做一件事 → 社区成员接单 → 共同完成</div></div>';
  h += '<button class="btn-sm sec" style="width:100%;font-size:.62rem" onclick="closeDiscoveryForm()">取消</button></div>';
  _showModal(h);
}
```
**接线**：卡片室底部 `.overlay-close` 里 `📝 上报劳动` 的 onclick 改为 `openPathChooser()`。
**验证**：点上报劳动→弹路径选择器→两个卡片可点→进入对应流程。

---

## Commit 3：UX 修正（~15 行 · ~30min）

### 3-1 · 服务端错误消息用户化
**文件**：`server/routes/nt.py`，approve_verification 中两个 429 的 detail

```python
# 改前：raise HTTPException(status_code=429, detail="同对 1h 内已操作")
# 改后：
raise HTTPException(status_code=429, detail="你在 1 小时内已经验证过 TA 的记录，请稍后再来")

# 改前：raise HTTPException(status_code=429, detail="今日验证已达上限 10 次")
# 改后：
raise HTTPException(status_code=429, detail="今天已验证 10 次，明天再来吧")
```

### 3-2 · 拒绝前二次确认
**文件**：`nantang-mobile/js/ui-cardroom.js`，`_renderVerifyTab` 中拒绝按钮

```javascript
// 拒绝按钮 onclick 改为：
onclick="if(confirm('确定认为这条记录不属实吗？\\n拒绝后该成员可重新上报（最多3次）')){...现有逻辑...}"
```
**验证**：点拒绝→弹出确认框→取消则不执行。

---

## Commit 4：池预警 + 索引 + verifier 定价（~15 行 · ~30min）

### 4-1 · verifications 复合索引
**文件**：`server/database.py`，`init_db` 函数末尾

```python
await conn.execute(text(
    "CREATE INDEX IF NOT EXISTS idx_verifications_status_created ON verifications(status, created_at DESC)"
))
```
**验证**：`sqlite3 nantang_fresh.db ".indices verifications"` → 含新索引

### 4-2 · 池余额同步到客户端
**文件**：`server/routes/data.py`，`sync_all` 返回值

```python
# 在 return dict 中加：
"pool_balance": (await db.execute(select(CommunityPool).limit(1))).scalar_one_or_none().balance or 0,
```
**文件**：`nantang-mobile/js/core.js`，`_mergeSyncData`

```javascript
// 在函数中加：
if (data.pool_balance !== undefined) {
  if (!AppData._data) AppData._data = {};
  AppData._data._poolBalance = data.pool_balance;
}
```
**验证**：登录→DevTools `AppData._data._poolBalance` 有值

### 4-3 · verifier_reward 提至 1/3
**文件**：`nantang-mobile/js/app-data.js`，`addVerification`

```javascript
// 改前：verifierReward = Math.ceil(ntAmount/5)||1
// 改后：
verifierReward = Math.max(2, Math.ceil(ntAmount/3))
```
**验证**：上报 5NT 劳动→verifierReward=2；上报 10NT→verifierReward=4

---

## 提交后最终验证（5min）

```bash
# 启动服务端
cd server && rm -f nantang_fresh.db && JWT_SECRET=test uvicorn main:app --port 8000 &

# 全流程
python -c "
import urllib.request, json
B='http://127.0.0.1:8000'
def req(m,u,d=None,t=None):
    h={'Content-Type':'application/json'}
    if t: h['Authorization']=f'Bearer {t}'
    b=json.dumps(d).encode()if d else None
    return json.loads(urllib.request.urlopen(urllib.request.Request(B+u,data=b,headers=h,method=m)).read())

# 注册 + 池注资
a=req('POST','/api/auth/register',{'name':'a','password':'12345678'})
at=a['token']
v=req('POST','/api/auth/register',{'name':'v','password':'12345678'})
vt=req('POST','/api/auth/login',{'name':'v','password':'12345678'})['token']
req('POST','/api/nt/topup',{'user':'community_pool','amount':500,'reason':'t'},at)

# 上报→校核
vf=req('POST','/api/data/verifications',{'type':'cleaning','action':'test','nt_amount':10,'verifier_reward':3},vt)
print('verification created:',vf['id'][:20])
r=req('POST',f'/api/nt/verifications/{vf[\"id\"]}/approve',{'doer':'v','action':'test','nt_amount':10,'verifier_reward':3},at)
print('approve:',r.get('ok'))

# visitor 应被拒
r2=req('POST',f'/api/nt/verifications/{vf[\"id\"]}/approve',{'doer':'v','action':'test'},vt)
print('visitor approve:',r2.get('detail','ok'))  # 应显示 403

# 会计
r=req('GET','/api/nt/verify',None,at)
print('accounting:',r['pass'],'diff:',r['checks']['diff'])
print('ALL PASS' if r['pass'] else 'FAILED')
"
```

`pass=true diff=0` → 完成。
