# 修复执行方案（审查后）

> 2026-07-22 · 基于 6 个 Agent 审查 · 40 条发现全覆盖
> 原则：每批改完→启动服务器验证→通过 commit→进下一批。失败停下来汇报。

---

## 统计

| 严重度 | 数量 | 批号 |
|--------|------|------|
| 🔴 严重 | 16 | Batch A-C |
| 🟡 重要 | 17 | Batch D-F |
| 🟢 建议 | 7 | Batch G |

---

## Batch A：经济安全紧急修复（6 项 · ~2h）

> 覆盖：ECON-01~06

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| A1 | ECON-01 | chain_scanner.py:149-169 | `select(CommunityPool)` → `from nt_helpers import _get_pool`，确保池行不存在时自动创建，`total_issued` 正确递增 | 模拟链上事件 → `total_issued` 与 `sum(balances)` 相等 |
| A2 | ECON-02 | nt.py:815-835 | `earn-sync` 加日上限（`os.environ.get("EARN_SYNC_DAILY_LIMIT", "5")`）+ 单次总额 ≤ `os.environ.get("EARN_SYNC_MAX_BATCH", "50")` NT + `pool.balance` 不足时 break。上限 env var 化，与 NIGHTLY_RATE（F31）一致 | POST 6 次 earn-sync → 第 6 次 429 |
| A3 | ECON-03 | nt.py:205-208 | 接收方 CV 增量 = `int(min(cv_amount, from_user_obj.contribution_value) * 0.75)` ——不超发送方实际扣减量 | 发送方 CV=10，转 100 NT → 接收方 +7 CV（非 75） |
| A4 | ECON-04 | chain_scanner.py:163 | `user.nt_balance += amount` 前加 `.with_for_update()` 或改用事务内行锁 | 两个链上事件同地址→余额正确累加 |
| A5 | ECON-05 | database.py:36-38 + models.py:89-99 | `CommunityPool` 加唯一约束 `singleton`（models.py 已有此列！）→ 改用 `INSERT OR IGNORE` 或先查后插的事务保护。`_get_pool` 本身就是比 `init_db` 更好的创建方式，考虑废弃 `init_db` 中的池创建 | 并发两个 `init_db()` → 仅一行 CommunityPool |
| A6 | ECON-06 | nt.py:192-201 | `transfer` 查 `from_user_obj` 时加 `.with_for_update()` | 并发转账→不超余额 |

**提交**: `git commit -m "fix: Batch A — 经济安全紧急修复 (ECON-01~06)"`

---

## Batch B：任务状态机 + 资金流水错标（6 项 · ~1.5h）

> 覆盖：TASK-01~02, FLOW-01~02, AUDIT-01, AUDIT-04

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| B1 | TASK-01 | nt.py:433-440 | `accept_task` 加 `if task.poster == user.id: raise HTTPException(400, "不能认领自己发布的任务")` | poster try accept own task → 400 |
| B2 | TASK-02 | nt.py:661-665, 513-526 | `resolve_task` 和 `verify_task`(3+reject) 加 `if task.escrow_amount <= 0: raise HTTPException(409, "任务无托管金额")` | 设 escrow_amount=0 → resolve → 409 |
| B3 | FLOW-01 | tasks.py:119 | 社区任务创建 `_add_ledger(db, lid, "community_pool", "escrow", ...)` — from 改为 community_pool | 创建社区任务→查 nt_ledger from_user="community_pool" |
| B4 | FLOW-02 | nt.py:564 | 个人任务取消 `_add_ledger(db, lid, "escrow", refund_target, ...)` — from 改为 escrow | 创建并取消任务→查 nt_ledger from_user="escrow" |
| B5 | AUDIT-01 | nt.py:877-880 | `pool_refill` 加 `_add_ledger(db, lid, None, "community_pool", 50, "pool_refill", f"每日补填 {today}")` | daily-tick → 查 ledger 有 pool_refill 条目 |
| B6 | AUDIT-04 | nt.py:523-524 | verify_task 3+reject 退款 `_add_ledger(db, lid, "escrow", ...)` — from 改为 escrow | 3 次 reject → 查 ledger from_user="escrow" |

**提交**: `git commit -m "fix: Batch B — 任务状态机+流水错标 (TASK-01~02, FLOW-01~02, AUDIT-01,04)"`

---

## Batch C：认证 + 竞态 + 输入验证（7 项 · ~2h）

> 覆盖：AUTH-01~02, RACE-01~02, VAL-01~02, ECON-05 收尾

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| C1 | AUTH-01 | api.js:24-25 | `request()` 收到 401 时：设 `_refreshInProgress` 标志防并发重入 → 调 `silentRefresh` → 成功则重试原请求一次 → 失败则直接返回 401（不递归）。`silentRefresh` 本身也返回 401 时不再循环 | 过期 token→API 调用→自动 refresh→重试成功；refresh 也过期→直接 401 |
| C1b | AUTH-01 | core.js:883-892 | 🟢可选：`_finishEnter` 加定时器每 50 分钟主动调 `API.silentRefresh()`。C1 单独已覆盖，此项锦上添花，可延后 | — |
| C3 | AUTH-02 | core.js:269-270 | `_pollCycle` 401 检测加上 `srv.error === '登录过期'` | 过期 token 轮询→poll 正确停止 |
| C4 | RACE-01 | nt.py:236 | `spend` 用户余额更新加 `.with_for_update()` | 并发 spend→不超余额 |
| C5 | RACE-02 | nt.py:604 | `settle_task` NTTask 查询加 `.with_for_update()` | 并发 settle→仅一次成功 |
| C6 | VAL-01 | camps.py:38-55 | 新增 `CampCreateRequest(BaseModel)` — budget 子模型 `CampBudget(adventurers: int=Field(0,ge=0), builders: int=Field(0,ge=0), lodgingNT: int=Field(0,ge=0), mealNT: int=Field(0,ge=0))`。name/max 加 Field 约束。camps.py 已从 CampTask 迁移到 NTTask 直连（line 64-73），本项仅补 Pydantic | 负数 lodgingNT → 422 |
| C6b | — | data.py:31,49,69,116 | 🟢可选：data.py 其余裸 dict 端点（add_journal/add_activity_log/add_card_discovery/add_verification）统一加 Pydantic。不涉及资金操作，风险低，可延后。在 CLAUDE.md 记录此决策 | — |
| C7 | VAL-02 | tasks.py:20 | `reward: int = Field(5, ge=1, le=10000)` | reward=999999 → 422 |

**提交**: `git commit -m "fix: Batch C — 认证+竞态+输入验证 (AUTH-01~02, RACE-01~02, VAL-01~02)"`

---

## Batch D：数据同步完整性（5 项 · ~1.5h）

> 覆盖：SYNC-01~03, SOV-01, RACE-02 收尾

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| D1 | SYNC-01 | data.py:329 + nt.py:134 | `sync_all` journal 加 `discovery_id`；tasks 加 `escrow_amount`/`settler_id`/`settled_at` | curl sync_all → 返回这些字段 |
| D2 | SYNC-02 | core.js:795-861 | `_mergeNTSyncData` 映射 `settler_id`/`settled_at`/`verified_at`/`verifier_id`/`escrow_amount`；`_mergeSyncData` 映射 `is_system_generated`/`escrow_amount` | HTTP 登录→任务卡片显示结算人+托管金额 |
| D3 | SYNC-03 | cron.py:55-75 | cron `tick_daily` 的 `_get_pool` 传 `lock=True`（已有参数，仅需改调用处） | 00:05 cron + 用户登录并发→无数据竞争 |
| D4 | SOV-01 | auth.js:336 + core.js:755-758 | `changeUserRole()` 后额外调用 `_saveLocalUser` 更新 `nt_local_roles`；或 `/api/user/role` 服务端角色变更后客户端重新 sync | 管理员改角色→刷新页面→角色保持 |

**提交**: `git commit -m "fix: Batch D — 数据同步完整性 (SYNC-01~03, SOV-01)"`

---

## Batch E：经济审计修复（4 项 · ~1h）

> 覆盖：AUDIT-02~03, CLIENT-01, CLIENT-02

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| E1 | AUDIT-02 | accommodation.py:74-81 | 退房时余额不足：保留 `debt` 字段 + 加 `debt_unpaid` 流水记录 + toast 提示欠费 | 设 debt=10, balance=0 → 退房 → debt 仍为 10，ledger 有 debt_unpaid |
| E2 | AUDIT-03 | camps.py:85 | `_get_pool(db)` → `_get_pool(db, lock=True)` | grep 确认 |
| E3 | CLIENT-01 | nt-core.js:269 | 客户端 cancel 改用 `escrow_amount`（需要从 sync 已有字段读取） | 离线创建任务→手动改 escrow→取消→退款金额=escrow |
| E4 | CLIENT-02 | nt-core.js:246-256 | 客户端 reject 改为：加 `reject_count`，第 1-2 次不退款，第 3 次退款（对齐服务端行为） | 客户端 reject × 3 → 第 3 次才退款 |

**提交**: `git commit -m "fix: Batch E — 经济审计修复 (AUDIT-02~03, CLIENT-01~02)"`

---

## Batch F：客户端体验 + 数据主权（6 项 · ~2h）

> 覆盖：UX-01~02, ARCH-01, DATA-02, NET-01, DOC-01

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| F1 | DATA-02 | index.html:536-560 | 删除 index.html 中 `TASKS`/`JOURNAL_TYPES`/`NEWBIE_QUESTS`/`ITEM_CATS`/`ITEM_ICONS`/`MOCK_ITEMS` 的重复 `<script>` 块。data.js 为单一真相源 | 浏览器加载→TASKS 等变量源自 data.js |
| F2 | ARCH-01 | store.js + schema.js + backup.js + index.html | 三个文件整体删除 + index.html 中对应的 3 个 `<script>` 标签移除（含 schema.js 废弃注释行）。若未来需要 backup：从 AppData 的 `nt_app_v2_shared` key 导出 | index.html 无 schema.js/store.js/backup.js 引用→无 404 |
| F3 | UX-01 | core.js:104, app.js:1272,1995, auth.js:418 | 替换原生 `confirm()` 为 `showConfirm()`（data.js:496 已存在） | 各触发场景→自定义 UI 弹窗出现 |
| F4 | NET-01 | api.js:21 | `fetch` 加 `AbortController`，默认 30s timeout。已有的 `AbortError` catch 开始生效 | 断网→30s 后 "请求超时" 非无限等待 |
| F5 | DOC-01 | (新建) `项目/实景游戏移动端代码/CLAUDE.md` | 创建项目级 CLAUDE.md：项目概览、双模式架构、数据层约定、脚本加载顺序、关键 ponytail 清单 | Agent 可自动读取项目上下文 |
| F6 | UX-02 | app.js:2072,2203,2000 | visitor 权限提示改为 "入住后可用（前往 🏠 住宿→入住）" 并加引导 | visitor 点校核→看到引导路径 |

**提交**: `git commit -m "fix: Batch F — 客户端体验+数据主权 (DATA-02, ARCH-01, UX-01~02, NET-01, DOC-01)"`

---

## Batch G：清理与技术债（6 项 · ~1h）

> 覆盖：TASK-03, CLIENT-03, DEBT-01~02, UX-03, ARCH-02, DATA-01（DEBT-03 合并入 F2）

| # | ID | 文件 | 改动 | 验证 |
|---|----|------|------|------|
| G1 | TASK-03 | models.py:6-14 + nt.py:466 | 删除 `TASK_STATUSES` 中的 `"submitted": "待提交"` 一行，清理 `verify_task` 白名单中的对应引用。3 行 diff——该状态从未被设置，是死常量 | grep models.py + nt.py 无 "待提交" 引用 |
| G2 | CLIENT-03 | nt-core.js:261-281 | `cancelTask` 对 `poster='社区'` 退款到 `COMMUNITY_POOL` 而非自动注册"社区"用户 | 离线创建社区任务→取消→NT 回社区池非幽灵用户 |
| G3 | DEBT-01 | data.js:537 | 删除 `CAMP_MOCK` fallback 及其引用 | grep data.js 无 CAMP_MOCK |
| G4 | DEBT-02 | nt-core.js:645-668 | 更新 ponytail 注释：从 "fallback for file:// mode" 改为 "file:// 模式 NT 实现" | grep 确认注释已更新 |
| G5 | UX-03 | main.css (多处) | 补充遗漏的触摸目标样式：`#myPage` 关闭按钮、新手引导关闭按钮、日程弹窗按钮 | Chrome DevTools 375px→全部按钮 ≥44px |
| G6 | ARCH-02 | models.py + camp-pipeline/ | camps.py 已迁移到 NTTask 直连（scope="camp", camp_ref_id）。清理残余：删除 `CampTask` 模型类（models.py:139-154）、删除 camp-pipeline/ 三个 TODO 空桩。`camp_tasks` 表若已无数据可 DROP | grep 确认 CampTask 无引用；camp-pipeline/ 目录不存在 |

**提交**: `git commit -m "fix: Batch G — 清理+技术债+营地桥接 (TASK-03, CLIENT-03, DEBT-01~03, UX-03, DATA-01)"`

---

## Batch H：最终验证（全流程 · ~30min）

```bash
cd "C:/Users/苏砚仁/thinknote/项目/实景游戏移动端代码/server"
rm -f nantang_fresh.db
JWT_SECRET=test uvicorn main:app --port 8000 &

# 1. 会计等式
curl -s -X POST http://127.0.0.1:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"admin","password":"test12345678"}'
# ... 完整测试脚本（注册→注资→创建任务→接取→提交→审核→结算→验证）

# 2. 新增验证项
# - chain_scanner 正确创建 CommunityPool（total_issued 对齐）
# - earn-sync 日上限 5 次
# - poster 自领 → 400
# - camp create → Pydantic 校验（负数 → 400）
# - reward=999999 → 422
# - transfer CV 通胀修复（CV=10, 转100NT → 接收方 ≤7 CV）
# - settle_task 双并发仅一次成功
```

---

## 覆盖矩阵

| 发现 ID | 批号 | 状态 |
|---------|------|------|
| ECON-01 chain_scanner total_issued | A1 | 📋 |
| ECON-02 earn-sync 无验真 | A2 | 📋 |
| ECON-03 转账 CV 通胀 | A3 | 📋 |
| ECON-04 chain_scanner 无行锁 | A4 | 📋 |
| ECON-05 CommunityPool 初始化竞态 | A5 | 📋 |
| ECON-06 transfer 无行锁 | A6 | 📋 |
| TASK-01 poster 自领刷信任度 | B1 | 📋 |
| TASK-02 escrow=0 孤儿化 | B2 | 📋 |
| FLOW-01 社区任务流水来源 | B3 | 📋 |
| FLOW-02 个人任务取消流水 | B4 | 📋 |
| AUDIT-01 pool_refill 无流水 | B5 | 📋 |
| AUDIT-04 验证回流流水来源 | B6 | 📋 |
| AUTH-01 token 无自动刷新 | C1,C2 | 📋 |
| AUTH-02 401 检测字段错误 | C3 | 📋 |
| RACE-01 spend 无行锁 | C4 | 📋 |
| RACE-02 settle 无行锁 | C5 | 📋 |
| VAL-01 create_camp 裸 dict | C6 | 📋 |
| VAL-02 reward 无上限 | C7 | 📋 |
| SYNC-01 sync_all 缺字段 | D1 | 📋 |
| SYNC-02 merge 字段映射不一致 | D2 | 📋 |
| SYNC-03 cron 无行锁 | D3 | 📋 |
| SOV-01 角色变更不持久化 | D4 | 📋 |
| AUDIT-02 退房欠费不可回收 | E1 | 📋 |
| AUDIT-03 营队无行锁 | E2 | 📋 |
| CLIENT-01 客户端 cancel 不读 escrow | E3 | 📋 |
| CLIENT-02 reject 行为不一致 | E4 | 📋 |
| DATA-02 重复定义覆盖 | F1 | 📋 |
| ARCH-01 惰性并行数据层 | F2 | 📋 |
| UX-01 原生 confirm | F3 | 📋 |
| NET-01 fetch 无 timeout | F4 | 📋 |
| DOC-01 缺 CLAUDE.md | F5 | 📋 |
| UX-02 visitor 权限提示 | F6 | 📋 |
| TASK-03 "待提交"未用 | G1 | 📋 |
| CLIENT-03 社区幽灵用户 | G2 | 📋 |
| DEBT-01 CAMP_MOCK | G3 | 📋 |
| DEBT-02 Phase C3 注释 | G4 | 📋 |
| DEBT-03 schema.js 标签 | F2 | 📋（合并） |
| UX-03 遗漏触摸目标 | G5 | 📋 |
| DATA-01 CampTask→NTTask | G6 | 📋（camps.py 已直连，清理残余） |

> 40/40 全覆盖，零遗漏
