# 南塘云村阶段文档适配汇总 v1

> 对照 7 份阶段设计文档，逐份审计现有实现，仅采纳可承载的升级项。
> 2026-07-20

---

## §01 身份 + 住宿 + NT 基础 → ✅ 全覆盖

| 文档内容 | 实现 | 文件 |
|----------|------|------|
| 身份系统（三种角色） | `auth.js` + `ROLE_CAPABILITIES` | auth.js |
| 邀请码生成/验证/消耗 | `AppData.inviteCodes` | app-data.js |
| 登录/注册 | `auth.js` | auth.js |
| 住宿管理（按床位） | `_showStaySheet` + 日历选择 | app.js |
| NT 三池（社区/营队/个人） | `nt-core.js` | nt-core.js |

**采纳：0 项。地基已打好。**

---

## §02 空间地图与物品标记 → 采纳 3 项

### 1. 物品数据结构升级

```javascript
// 现有：{ name, location, putBy, putDate, expiryDays, status }
// 升级后加：
{ emoji: '🥬', quantity: 2, unit: '棵', expiryDate: '2026-07-09', consumeType: null, note: '' }
```

**文件**：app.js（`_syncItemToAppData` 写入 + `_showFridgeSheet` 显示）

### 2. 过期提醒三级

```
≤ 0天 → 🔴 过期 | ≤ 1天 → 🟡 即将 | ≤ 3天 → 🔵 注意 | > 3天 → 不显示
```

**文件**：app.js（`_showFridgeSheet` 调整阈值）

### 3. 消耗类型选择

快捷录入厨房 sheet 增加四种消耗方式：
```
[✓ 吃掉 +1NT] [🗑 扔掉 0] [🪴 堆肥 +2NT] [📦 移走 +1NT]
```

**文件**：app.js（`_openKitchenQuick` + `_syncItemToAppData`）

**改动量：~50 行**

---

## §03 卫生脏污度与打扫审核 → 采纳 2 项

### 1. 前后照片字段

`pendingVerification` 条目加可选字段：
```javascript
{ photoBefore: null, photoAfter: null }
```
校核面板有照片时显示缩略图。

**文件**：app-data.js（字段）+ app.js（显示逻辑）

### 2. 打扫中状态

```javascript
spaces[spaceId].cleaning = { by: 'userName', since: 'ISO时间' }
// 1h 后自动清除，防止多人重复提交
```

**文件**：app-data.js（数据模型）+ app.js（_collectCleaningRooms 读状态）

**改动量：~25 行**

---

## §04 委托任务系统 → 未读，待后续

---

## §05 在地记录与NT激励 → ✅ 全覆盖

| 文档内容 | 实现 |
|----------|------|
| 记录类型（厨房/田间/修缮等） | `JOURNAL_TYPES` |
| 记录发布+NT奖励 | `addJournal` + `AppData.addJournal` |
| 时间线 | `_renderTimelineSection` |

**采纳：0 项。已全覆盖。**

---

## §06 云村民互动层 → 采纳 2 项

### 1. 点赞系统

时间线条目右侧加 `❤️ N`，每条可点赞一次，不产生 NT。

```javascript
// timeline entry 加字段
{ likes: 0, likedBy: [] }
```

**文件**：app.js（`_renderTimelineSection` + 点赞切换）

### 2. 物品留言字段

inventory 物品加 `comments[]` 字段（纯数据，UI 后续）：
```javascript
{ comments: [{ user, content, time }] }
```

**文件**：app-data.js（字段）+ app.js（`_syncItemToAppData` 初始化）

**改动量：~25 行**

---

## §07 管理后台深化 → 采纳 1 项

### 各人 NT 余额排行

资金池卡片底部加前 5 名用户 NT 余额：

```
💰 社区资金池  2,000 NT
📈 本月收入 380 · 📉 本月支出 240
👤 小杨 320 · 阿楠 280 · 小红 210 · 大飞 150 · 朝林 90
```

**数据来源**：`NT.getUser()` + `getUsers()`（已有）

**文件**：app.js（`_renderPoolCard`）

**改动量：~10 行**

---

## 执行汇总

| 来源 | 采纳项 | 改动量 |
|------|--------|:--:|
| §02 | 物品字段 + 过期三级 + 消耗类型 | ~50 行 |
| §03 | 照片字段 + 打扫中状态 | ~25 行 |
| §05 | — | 0 |
| §06 | 点赞 + 留言字段 | ~25 行 |
| §07 | 各人 NT 余额排行 | ~10 行 |
| **合计** | **8 项** | **~110 行** |

**涉及文件**：app.js（6 项）+ app-data.js（2 项）