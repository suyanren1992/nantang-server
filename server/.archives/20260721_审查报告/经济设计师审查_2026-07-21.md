# 经济设计师审查 — 会计等式 / 池子 / 托管 / 住宿扣费

> 2026-07-21 · 审查范围：nt-core.js, core.js, data.js, app-data.js, nt.py, tasks.py

## 逐路径验证会计等式

服务端所有路径成立。`total_system = total_user_balance + pool.balance + pool.task_escrow + pool.camp_balance` 保持恒定（除 BUG 1 外）。

## 🔴 严重

### BUG 1. 服务端每日住宿扣费 NT 凭空蒸发
位置：`server/routes/nt.py` 第 713-722 行
用户 `nt_balance` 减了 10 NT，但 `pool.balance`/`pool.camp_balance` 均未增加，`total_issued` 不变，无 `NTLedger` 记录。
`total_user_balance + pool.balance + task_escrow + camp_balance = total_issued - NIGHTLY_RATE × 入住人数`

## 🟡 高

### BUG 2. 服务端/客户端住宿费率不一致 (10 vs 30-150)

### BUG 3. 客户端/服务端池子补填独立执行

## 🟡 中

### BUG 4. 校核 approve ledger from 字段标为 "escrow"（实际是 community_pool）

## 🟢 低

### BUG 5. 客户端/服务端住宿扣费有潜在双重执行风险（当前无实际影响）
