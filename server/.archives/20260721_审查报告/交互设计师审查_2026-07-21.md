# 交互设计师审查 — 登录/注册 / 错误反馈 / 空状态 / 导航

> 2026-07-21 · 审查范围：index.html, core.js, app.js, ui-phase4.js, ui-camp.js, data.js

## 🔴 阻断

### 1. core.js line 806 — 悬空的 `.catch` 导致整个脚本解析失败（与系统设计师重复）

## 🟡 功能异常

### 2. `openInn()` 打开空壳 overlay + 独立弹窗，双层 UI
### 3. `closeInn()` 硬编码跳回 `overlayCampHome`
### 4. `_applyStay()` 用 `confirm()` 展示公约 — 移动端体验差 + 无法持久化签署记录
### 5. visitor 看到无法完成的 sign_covenant 任务
### 6. Toast "入住后可用" — 原因与结果间接关联
### 7. 多个写操作失败静默吞噬异常（checkout, checkin, daily-tick）

## 🟢 建议

### 8. hash 路由：`_showStaySheet` 和 inbox 不参与 hash
### 9. `_checkoutBed` 角色降级无感知反馈
### 10. 工作台（visitor）空状态缺乏行动引导
### 11. `overlayInn` DOM 残留
### 12. 信箱外点关闭正常
