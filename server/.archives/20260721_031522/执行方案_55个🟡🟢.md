# 🟡🟢 执行方案 · 55 个已验证发现

> 2026-07-21 · 基于 [核查报告](00_🟡🟢核查报告.md)
> 已排除 10 个误报 + 12 个需调整/重复，最终 55 个真实 🟡 + 2 个 🟢 建议

---

## 分级

| 级别 | 数量 | 标准 |
|------|:----:|------|
| **A** 🟡 本轮修 | 19 | 明确的正确性问题/UX缺陷/数据完整风险 |
| **B** 🟡 下轮修 | 23 | 重要但非紧急，或依赖其他修复 |
| **C** 🟢 有空修 | 12 | 改善项，不影响核心功能 |

---

## A 级 · 本轮修复（20 个）

### A1. 经济一致性（6个）

**A1-1 + A1-2 🟡 绑定决策 — CV 增减规则统一（spendToPool + verifyTask）**
文件：`nantang-mobile/js/nt-core.js`

**决策：** spend 不减 CV（花钱不影响历史贡献），earn 和 verifyTask 加 CV/XP（劳动和任务完成增加贡献）。与 server P1-1/P1-2 对齐。

**A1-1（spendToPool 第428行）— 删除减 CV：**
```javascript
// 删除此行：
// u.contributionValue = Math.max(0, (u.contributionValue||0) - amount);
```

**A1-2（verifyTask approved 路径 第213行）— 加 CV/XP：**
```javascript
assignee.contributionValue = (assignee.contributionValue||0) + t.reward;
assignee.experienceValue = (assignee.experienceValue||0) + t.reward;
```

**A1-3 🟡 nt-core.js:383 — cashOut 混入 totalPaid**
文件：`nantang-mobile/js/nt-core.js`
删除 `u.totalPaid += amount;` 或重命名 `totalPaid` → `totalSpent`。

**A1-4 🟡 nt-core.js:354-356 — earn/spend wrappers 签名误导**
文件：`nantang-mobile/js/nt-core.js`
参数传递硬编码 pool='community'。在 P1 camp_pool 全面支持前，暂时加注释说明：
```javascript
// ponytail: 三池支持前，earn/spend wrapper 固定走 community pool，scope 仅影响 ledger type 标签
```

**A1-5 🟡 nt-core.js:460-464 — PUBLIC_CV_POOL 无 sink**
文件：`nantang-mobile/js/nt-core.js`
最简单的方案：定期（如每周）将 PUBLIC_CV_POOL 按活跃度分配给社区成员。MVP 阶段至少加注释标注为已知设计债：
```javascript
// ponytail: PUBLIC_CV_POOL 单向积累无出口。当 >10000 时需实现分配机制（如按周活跃度分发给成员）
```

**A1-6 🟡 app-data.js:428 — overdueNT 存房间不存用户**
文件：`nantang-mobile/js/app-data.js`
已在 P1-9（🔴 方案）覆盖，同步修复。

### A2. 数据完整性（4个）

**A2-1 🟡 nt-core.js:694-698 — 初始注资检查 fragile**
文件：`nantang-mobile/js/nt-core.js`
不用内存 ledger 推断，改为持久化 flag：
```javascript
var _initialFunded = localStorage.getItem('nt_initial_funded');
if (!_initialFunded) {
  COMMUNITY_POOL += 2000; _totalIssued += 2000;
  _addLedger('system', '__community_pool__', 2000, 'deposit', 'initial_funding');
  localStorage.setItem('nt_initial_funded', '1');
  _saveState(true);
}
```

**A2-2 🟡 app-data.js:223 — sync payload inventory 字段值错误**
文件：`nantang-mobile/js/app-data.js`
```javascript
// line 223: inventory: this._data.items  →  inventory: this._data.inventory
```

**A2-3 🟡 app-data.js:88-91 — ntBalance 从本地读（server 变更不感知）**
文件：`nantang-mobile/js/app-data.js`
refreshUserUI 已有异步拉取服务端余额（core.js:862），但未同步回 AppData。修复：收到服务端余额后写入本地 NT：
```javascript
if (srv && !srv.detail && !srv._offline) {
  var ntUser = NT.getUser(CURRENT_USER);
  if (ntUser) { ntUser.ntBalance = srv.balance; ntUser.contributionValue = srv.cv; ntUser.experienceValue = srv.xp; }
}
```

**A2-4 🟡 core.js:819 — _mergeSyncData 丢弃 items/newbie**
文件：`nantang-mobile/js/core.js`
扩展 _mergeSyncData 处理服务端返回的 items 和 newbie：

```javascript
// items：服务端返回的是用户物品数组，合并到 AppData._data.items[user]
if (data.items && Array.isArray(data.items)) {
  if (!AppData._data.items[AppData._currentUser]) AppData._data.items[AppData._currentUser] = [];
  var existing = AppData._data.items[AppData._currentUser];
  data.items.forEach(function(it) {
    if (!existing.find(function(e) { return e.id === it.id; })) existing.push(it);
  });
}
// newbie：服务端返回的是数组，写入 AppData._data.newbieQuests
if (data.newbie && Array.isArray(data.newbie)) {
  AppData._data.newbieQuests = {};
  data.newbie.forEach(function(q) { AppData._data.newbieQuests[q.quest_id] = q; });
}
```

### A3. UX 缺陷（6个）

**A3-1 🟡 core.js:733 — 注册失败仅报告第一个错误**
文件：`nantang-mobile/js/core.js`
收集所有校验错误一次性显示：
```javascript
var errors = [];
if (!n) errors.push('请输入名字');
if (p.length < 6) errors.push('密码至少6位');
if (errors.length) { showToast(errors.join('；'), 'error'); return; }
```

**A3-2 🟡 core.js:1259 — silentRefresh 错误不分级**
文件：index.html autoLogin 块
API.silentRefresh 区分网络断/token过期/服务器错误，autoLogin 据此显示不同提示。

**A3-3 🟡 core.js:1253 — autoLogin 无 loading 反馈**
文件：index.html autoLogin 块
显示轻量加载提示："正在恢复会话…"，超时5秒后降级显示登录页。

**A3-4 🟡 core.js:581 — deleteDraft 无确认**
文件：`nantang-mobile/js/core.js`
```javascript
showConfirm('确定删除草稿「' + name + '」？', function() { AppData.deleteTask(name); ... });
```

**A3-5 🟡 core.js:271 — "发布委托"不可点击**
文件：`nantang-mobile/js/core.js`
改为可点击链接：
```javascript
h += '<span style="color:var(--green-primary);cursor:pointer;text-decoration:underline" onclick="openPublishTask()">+ 发布委托</span>';
```

**A3-6 🟡 core.js:1206 — closeMyPage 不设 hash**
文件：`nantang-mobile/js/core.js`
```javascript
window.location.hash = '';
```

### A4. 数据刷新缺口（4个，已验证为非误报的）

**A4-1 🟡 core.js:198-214 — polling token 过期持续 401**
文件：`nantang-mobile/js/core.js`
在 fetchTasks/fetchDiscoveries 回调中检查 401 并 stopPolling：
```javascript
API.fetchTasks(function(tasks) {
  if (tasks && tasks._offline) return; // 网络错误忽略
  if (tasks && tasks.detail === 'unauthorized') { _stopPolling(); return; }
  // ... 正常处理
});
```

**A4-2 🟡 api.js:23 — 网络错误不区分类型 + _offline 调用方不检查（合并修复）**
文件：`nantang-mobile/js/api.js`
同一个 catch 块，同时修复错误分型和 _offline 处理：
```javascript
catch(e) {
  if (e.name === 'TypeError') return {ok:false, error:'网络不通', _offline:true};
  if (e.name === 'AbortError') return {ok:false, error:'请求超时', _offline:true};
  return {ok:false, error:'网络异常', _offline:true};
}
// 调用方统一约定：_offline 时不弹 toast（由调用方按需处理），非 _offline 的 error 才弹
```

**A4-4 🟡 data.js:500 — closeOverlay 无条件显示 villagePage**
文件：`nantang-mobile/js/data.js`
增加参数控制：
```javascript
function closeOverlay(id, showVillage) {
  // ...
  if (showVillage !== false) document.getElementById('villagePage').classList.remove('hidden');
}
```

---

## B 级 · 下一轮（22 个）

### B1. NT 系统完善（8个）

| # | 文件 | 问题 | 修复方向 |
|---|------|------|---------|
| B1-1 | nt.py:213 | camp_pool_balance 从 ledger 动态算 | CommunityPool 加 camp_balance 持久化列 |
| B1-2 | nt.py:141 | earn 并发 pool 超支 | SQLAlchemy `with_for_update()` 行锁 |
| B1-3 | nt.py:134 | earn 无 amount 上限 | 加 10000 NT 单次上限（来源：client 端 bridge 已有 10000 上限，server 对齐；非经济模型推导值，可后续根据实际运营数据调整） |
| B1-4 | nt.py:topup | 无 cashout 端点 | 镜像 topup 新增 `/api/nt/cashout` |
| B1-5 | nt-core.js:472 | _processedTxIds 无过期 | setInterval 清理 7 天前 txId |
| B1-6 | nt-core.js:71 | registerUser 不设 role | 加可选 role 参数 |
| B1-7 | nt-core.js:450 | transfer 权限依赖全局 CURRENT_USER | 加显式 caller 参数 |
| B1-8 | nt-core.js:53 | _saveState 静默失败 | 失败时 showToast 警告 |
| B1-9 | core.js:881 | 营队NT从TASKS算 | 改为从 NT Ledger 汇总（与 B1-1 server camp_pool_balance 持久化同批，确保 client-server 同步逻辑一致） |

### B2. 服务端完整化（7个）

| # | 文件 | 问题 | 修复方向 |
|---|------|------|---------|
| B2-1 | tasks.py:115 | completed_at 打回不清 | 打回时设 `task.completed_at = None` |
| B2-2 | tasks.py:53 | list_tasks 缺时间字段 | 返回 accepted_at/completed_at |
| B2-3 | data.py:108 | get_verifications 返回不全 | 补全所有模型字段 |
| B2-4 | data.py:39 | get_activity_log 无认证 | 加 `Depends(get_current_user)` |
| B2-5 | data.py:105 | get_verifications 无范围限制 | 非 admin 仅返回自己的 |
| B2-6 | data.py:324 | sync_all 注释/行为不一致 | 加 user 过滤或改注释 |
| B2-7 | auth.py:63 | pool 创建非原子 | init_db 预创建 CommunityPool row |

### B3. 功能缺口（7个）

| # | 文件 | 问题 | 修复方向 |
|---|------|------|---------|
| B3-1 | core.js:489 | reviewer 默认自己 | 默认空，提示选择审核人 |
| B3-2 | core.js:376 | 审核打回可无限循环 | 加 rejectCount，超3次自动关闭 |
| B3-3 | core.js:7 | isTaskOverdue 不触发变更 | 页面加载时处理逾期任务 |
| B3-4 | core.js:204 | type↔category 值域不兼容 | 统一枚举值 |
| B3-5 | core.js:204 | sync 不映射 assignee/evidence | 同步时加这两个字段 |
| B3-6 | data.js:348 | unclaimTask orphan | 加发布者审批流程 |
| B3-7 | api.js:90 | syncTask 缺 req_photo/req_file | 补发这两个字段 |

---

## C 级 · 有空修（15 个）

### C1. 轻量改进

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| C1-1 | index.html:633 | inbox 分类重复计算 ×6 | 缓存一次 _computeInboxMessages() 结果 |
| C1-2 | index.html:27-63 | 登录页无 safe-area | CSS `padding-bottom: env(safe-area-inset-bottom)` |
| C1-3 | index.html:686 | inbox top:50px 硬编码 | 改用 `calc(50px + env(safe-area-inset-top))` |
| C1-4 | core.js:673 | avatar picker 滚动偏离 | 改用 `position: fixed` + viewport 相对定位 |
| C1-5 | 全局 CSS | onclick 无 touch 优化 | `[onclick] { touch-action: manipulation }` |
| C1-6 | index.html:711 | 旧营队种子孤行 | 删除残留对象 |
| C1-7 | index.html:505 | TASKS 重复声明 | 删除 index.html 中的声明 |

### C2. 设计债

| # | 文件 | 问题 | 说明 |
|---|------|------|------|
| C2-1 | app-data.js:148 | addItem 无 deleteItem | 如有出库需求再补 |
| C2-2 | app-data.js:20 | pendingTransactions 原则违反 | 改注释或迁移数据 |
| C2-3 | ui-wizard.js:52 | 创营向导验证不完整 | 补 Step 2-4,6 验证 |
| C2-4 | ui-wizard.js:557 | NT 总池漏 extraExpense | 纳入 NT 标记的额外支出 |
| C2-5 | ui-archive.js:176 | 两套 inbox 消息源 | 统一为一个 computeInboxMessages |
| C2-6 | nt.js:18 | ROLE_TITLES 重复 | 删除 nt.js 中版本，统一用 core.js |

### 🟢 建议（2个，非 bug，改善项）

| # | 文件 | 建议 |
|---|------|------|
| 🟢1 | camps.py:35 | Camp 创建用 Pydantic 模型替代裸 dict |
| 🟢2 | models.py:85 | Camp 模型加 season 列 |

---

## 执行顺序

```
A1 经济一致性 (6)
  ↓
A2 数据完整性 (4)
  ↓
A3 UX 缺陷 (6)
  ↓
A4 刷新缺口 (3)
  ↓
B1 NT 完善 (9) → B2 服务端 (7) → B3 功能缺口 (7)
  ↓
C1 轻量改进 (7) → C2 设计债 (6)
  ↓
🟢 改善建议 (2)
```

---

## 文件变更汇总

| 文件 | A级 | B级 | C级 |
|------|:--:|:--:|:--:|
| `server/routes/nt.py` | — | 3 | — |
| `server/routes/tasks.py` | — | 2 | — |
| `server/routes/data.py` | — | 5 | — |
| `server/routes/auth.py` | — | 1 | — |
| `server/models.py` | — | — | 🟢1 |
| `nantang-mobile/js/nt-core.js` | 6 | 5 | — |
| `nantang-mobile/js/app-data.js` | 3 | — | 2 |
| `nantang-mobile/js/core.js` | 9 | 4 | 1 |
| `nantang-mobile/js/data.js` | 1 | 1 | — |
| `nantang-mobile/js/api.js` | 3 | 1 | — |
| `nantang-mobile/index.html` | 2 | — | 5 |
| `nantang-mobile/js/ui-wizard.js` | — | — | 2 |
| `nantang-mobile/js/ui-archive.js` | — | — | 1 |
| `nantang-mobile/js/nt.js` | — | — | 1 |
| 全局 CSS | — | — | 1 |

---

*方案 v2 完毕。A级（本轮19）+ B级（下轮23）+ C级（有空12）= 54 个已验证 🟡🟢。v2 修正：A4-2/A4-3 合并、删除 C2-1、A1-1/A1-2 CV 决策绑定、B1-3 标注来源、C2-4 提升到 B1-9、A2-4 可执行化。*
