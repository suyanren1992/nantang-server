# QA 测试审查报告

> 审查日期：2026-07-21
> 审查人：🧪 QA 测试 Agent
> 审查范围：输入验证 / 权限 / 错误处理 / 边界 / 竞态 / 空指针

---

## 一、输入验证

```
🔴 | server/routes/auth.py:108-110 | /api/auth/users 无任何认证 | 任何人可 GET，暴露所有用户名和头像种子 | 加 Depends(get_current_user)

🔴 | server/routes/auth.py:50-51 | register 不验证用户名 | 空字符串、超长(>255)、包含换行符可入库 | 加长度和内容校验

🔴 | server/routes/tasks.py:21 | CreateTask 不校验 slots 下限 | slots=0 或 -1 可传入，无人可接但照常冻结 NT | Pydantic Field(ge=1)

🔴 | server/routes/camps.py:78 | schedule[0] IndexError 当 schedule 为空列表 | schedule=[] → IndexError 500 | 先检查 len(schedule)>0

🔴 | server/routes/data.py:299-301 | sync_shared 未验证 req 是 dict | payload 为 None/string → AttributeError 500 | isinstance(req, dict) 检查

🟡 | server/routes/nt.py:109 | transfer 不检查 to 是否为空字符串 | req.to="" 触发一次无用 DB 查询 | 加 if not req.to

🟡 | server/routes/data.py:187-188 | get_canteen_menu 不校验 date 格式 | date="abc" 静默无结果 | 校验 YYYY-MM-DD
```

## 二、权限检查

```
🔴 | server/routes/nt.py:297-299 | verify_task 空 reviewer 时无权限控制 | reviewer=None 时任意登录用户可验证任意任务 | 改为 `if user.role != "admin" and (not task.reviewer or task.reviewer != user.id)`

🔴 | server/routes/nt.py:281-282 | accept_task 在已有 assignee 时直接覆盖 | 多人同时 accept，后到者覆盖先到者 | 加 `if task.assignee: raise 409`

🟡 | server/routes/data.py:39-42 | get_activity_log 无认证 | 活动日志公开可读 | 加 Depends(get_current_user)

🟡 | server/routes/data.py:105-111 | get_verifications 无范围限制 | 任何登录用户可看所有校核记录 | 非 admin 只返回自己的
```

## 三、错误处理

```
🔴 | server/routes/camps.py:30,193-194 / data.py:193-194,235 | json.loads 无 try/except | 损坏的 JSON → 500 | 每个 json.loads 加 try/except

🟡 | server/routes/auth.py:63-65 | register 中 pool 查询和创建非原子 | 两个并发注册可能同时创建 CommunityPool | INSERT OR IGNORE 或在 init_db 预创建

🟡 | nt-core.js:53-65 | _saveState 可能静默失败 | localStorage quota exceeded | 失败时 showToast

🟡 | api.js:23 | request catch 返回离线对象，调用方可能不检查 _offline | 混淆网络错误和业务错误
```

## 四、边界条件

```
🔴 | nt-core.js:392-393,418-419 | earnFromPool/spendToPool 不校验 amount>0 | amount=0 或负数破坏经济 | 加 `if (amount <= 0) return _err()`

🔴 | server/routes/nt.py:131 | transfer 并发下可能绕过余额检查 | 两次并发 transfer 余额变负 | SELECT ... FOR UPDATE 行级锁

🟡 | server/routes/nt.py:141-143 | earn 并发下 pool 可能超支 | pool.balance 变负 | FOR UPDATE 锁

🟡 | server/routes/nt.py:134-155 | earn 未限制单次 amount 上限 | amount=9999999 → 经济破裂 | 加 10000 上限

🟡 | server/routes/nt.py:252 + tasks.py:38-39 | task_id 用微秒 %f 有碰撞风险 | 同一微秒两次调用 → 主键冲突 | 加随机后缀
```

## 五、并发/竞态

```
🔴 | core.js:543-562 | doPublish 无防抖 | 双击"确认发布"可重复冻结 NT | 加 _publishing 锁

🔴 | core.js:567-577 | publishDraft 同样无防重入 | 同上

🟡 | core.js:197-215 | _startPolling 每 10 秒固定轮询，不等待上次完成 | 网络慢时请求堆积 | setTimeout 递归代替 setInterval

🟡 | app-data.js:218-231 | _saveShared 防抖内嵌 fetch，两次快速操作可能乱序覆盖 | 加序列号乐观锁
```

## 六、空指针 / undefined

所有核心路径的 TASKS[name] 调用均有 `if(!t)return` 保护。安全。

## 七、数据一致性

```
🔴 | api.js:101-105 | syncTaskUpdate 用本地 task name 作为 server task_id | 服务端 PUT 期望服务端 ID，收到客户端 name → 404 静默失败 | 用 task._srvId

🔴 | nt-core.js:370 vs nt.py:182-200 | server topUp 只增 total_issued 不补 pool，earn 消耗 pool | 运行一段时间后 pool 必枯竭，所有 earn 被拒绝 | 明确 pool 补充机制

🔴 | core.js:561 | doPublish 只更新 AppData 不更新局部 data | 双击时第二次可能再次冻结 NT | 在 doPublish 第一行读最新 _ntTaskId

🟡 | nt-core.js:694-698 | 初始注资仅检查 ledger 中是否有 initial_funding | 手动删 ledger → 再次注资 | 单独 persist _initialFunded flag

🟡 | app-data.js:88-91 | ntBalance 从本地读，server 端变更不感知 | 其他人转账给你，本地余额不更新 | 登录后 fetch balance 应用到本地 NT
```

## 八、补充发现

```
🔴 | ui-cardroom.js:1029-1036 | NT.spend 传递浮点数 0.1 | 客户端余额被 corrupt 为浮点，服务端 Pydantic int 截断为 0 | 改为 1

🟡 | core.js:819 | _finishEnter 调 syncAll 但 _mergeSyncData 丢弃 items/newbie | 服务器库存和新手任务数据丢失

🟡 | server/routes/data.py:324-354 | sync_all 返回全量任务，注释写"我的任务" | 无 user 过滤 | 改注释或改查询

🟡 | nt-core.js:209 | verifyTask 硬编码 verifiedBy='admin' | verifierId 参数被忽略 | 用 verifierId 参数

🟡 | index.html:505 vs data.js:11 | TASKS 声明重复 | 加载顺序敏感 | 统一在 core.js 惰性引用
```

---

## 汇总

| 级别 | 数量 | 关键系统 |
|------|------|----------|
| 🔴 严重 | 18 | auth, nt, tasks, camps, data, nt-core, core |
| 🟡 重要 | 14 | api, nt-core, app-data, ui-cardroom |
| 🟢 一般 | 5 | core, api, data |

### Top 5 必须优先修复：

1. `camps.py:78` — schedule[0] IndexError 当 schedule 为空列表 → 营地创建崩溃
2. `auth.py:108` — /api/auth/users 无认证 → 全员用户名泄露
3. `nt.py:298` — verify_task 空 reviewer 时无权限 → 任何人可验证任何任务
4. `core.js:543-561` — doPublish 双击双重冻结 NT → 经济损失
5. `ui-cardroom.js:1029-1036` — 浮点数 0.1 corrupt 客户端余额 → 免费操作
