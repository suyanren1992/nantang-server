# QA 测试审查 · 输入验证与边界

> 审查范围：输入验证、权限、竞态、空指针、边界
> 文件：server/routes/auth.py, server/routes/nt.py, server/routes/tasks.py, server/routes/data.py, nantang-mobile/js/nt-core.js, nantang-mobile/js/core.js

## 发现 26 项

### 输入验证
1. 🔴 core.js:552 — 负奖励绕过：`parseInt(value)||5` 接受负值。NT.createTask 无 `<=0` 检查。
2. 🟡 auth.py:51 — 无最大密码长度。多 MB 密码可冻结 bcrypt。
3. 🟡 auth.py:59 — req.role 完全未验证。接受任何字符串。
4. 🟡 nt.py:275-278 — 任务标题零长度无上限。
5. 🟡 data.py:26-35,47-54 — add_journal/add_activity_log 零字段验证。
6. 🟡 nt.py:116 — transfer 无上限（不像 earn 的 10000 上限）。
7. 🟢 nt.py:112,118 — 无自转账守卫。自转 → 25% CV 磨损沉默销毁。

### 权限绕过
8. 🔴 data.py:129 — add_verification doer 冒充。`req.get("doer", user.id)` 允许伪造。
9. 🟡 data.py:75 — add_card_discovery guesser 冒充。
10. 🟡 data.py:312-336 — sync_shared 绕过 admin 检查（地图写入）。
11. 🟡 data.py:275-284 — add_announcement 无角色门控。
12. 🟡 auth.py:108-112 — list_users 向所有认证用户泄露用户名。
13. 🟡 tasks.py:44-60 — list_tasks 返回全部任务不过滤（vs sync_all 正确限定范围）。

### 竞态条件
14. 🔴 nt.py:307-313 — accept_task 双重接取竞态。任务的 SELECT 无 `with_for_update()`。
15. 🔴 core.js:559,563,564 — _publishing 守卫错误路径上永不重置 → 永久死锁。
16. 🔴 core.js:580,581 — publishDraft 同 #15。
17. 🟡 nt.py:327 — verify_task 无 `with_for_update()`。

### 空指针/未定义安全
18. 🔴 nt-core.js:221 — verifyTask 对已删除 poster 崩溃（`poster.totalPaid` 为 null）。
19. 🔴 nt-core.js:252-253 — cancelTask 对已删除 poster 崩溃。
20. 🟡 nt.py:338-341 — verify_task assignee 解引用在 null 检查后（目前由 ORM 行为安全保护，但脆弱）。
21. 🟢 data.py:129 — `dict.get` 语义与显式 `None` 冲突。

### 边界违规
22. 🟡 nt-core.js:635 — _getUser 对未知用户自动注册 → 拼写错误导致资金丢失。
23. 🟡 nt.py:227-228 — cashout 在检查前设负 total_issued。
24. 🟢 auth.py:33 — httponly=False 使 refresh token 可被 JS 读取。
25. 🟢 nt-core.js:399,412 — topUp/cashOut 缺客户端角色检查（服务端是权威的）。
26. 🟢 nt.py:143-153 — earn pool-scope 死代码 `hasattr(req, 'pool')`。
