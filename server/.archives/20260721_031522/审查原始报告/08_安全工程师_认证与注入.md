# 安全工程师审查 · 认证与注入

> 审查范围：认证、注入、数据泄漏、令牌安全
> 文件：server/routes/auth.py, server/routes/nt.py, server/routes/tasks.py, server/routes/data.py, server/database.py, server/auth_utils.py

## 发现 25 项

### 认证漏洞
1. 🔴 auth_utils.py:7 — JWT 密钥硬编码 `"nantang-cloud-village-secret-key-change-in-production"`
2. 🔴 auth_utils.py:33-37 / auth.py:36-41 — Refresh token 可当 access token 使用（不检查 type claim）
3. 🔴 auth.py:33 — refresh token cookie `httponly=False` → XSS 可读取
4. 🔴 auth.py:82-93 — 无 refresh token 轮换/失效 → 泄漏 token 在 7 天内无限可用
5. 🔴 auth.py:72-79 — 登录无暴力破解限速

### 令牌暴露
6. 🟡 auth.py:79 — Refresh token 在响应 body 中返回（不仅 cookie）

### 授权漏洞
7. 🔴 auth.py:49-69 — 任意用户可注册为 admin（role 字段未验证）
8. 🔴 nt.py:143 — 任意用户可通过 earn 从社区池无限提取 NT
9. 🔴 data.py:327 — sync_shared 无 admin 检查即可覆盖共享地图
10. 🔴 data.py:128 — Verification doer 字段用户可控（伪造工作记录）
11. 🟡 tasks.py:119 — 任务状态可任意设置为"已结算"绕过工作流
12. 🟡 data.py:76 — Card discovery 字段用户可控
13. 🟡 data.py:275 — Announcements 可被任何用户伪造

### 注入
14. 🟢 未发现 SQL 注入向量（所有查询使用 SQLAlchemy ORM 参数化查询）
15. 🟢 API 响应中未发现 XSS 向量

### 数据泄漏
16. 🔴 main.py:30-35 — CORS `allow_origin_regex=r".*"` + `allow_credentials=True` → CSRF
17. 🟢 auth.py:108-112 — /users 不泄露密码哈希，但枚举全部用户名是隐私问题
18. 🟢 auth.py:23-29 — _user_json() 正确排除 password_hash

### 令牌安全（详述）
19. 🔴 auth_utils.py:7 — JWT 密钥硬编码（同 #1）
20. 🟡 auth.py:96-99 — 登出时令牌不在服务端失效
21. 🟡 auth_utils.py:10 — 7 天 refresh 无绝对最大会话长度

### 其他
22. 🟡 auth.py:36-41 — get_current_user 默默返回 None 而非抛 401
23. 🟡 多处 — 用户输入字符串无 HTML 净化（由前端负责）
24. 🟡 auth.py:52 — 密码长度下限仅 6 字符
25. 🟢 nt.py:275 vs tasks.py:63 — 两条 create-task 路径不同校验
