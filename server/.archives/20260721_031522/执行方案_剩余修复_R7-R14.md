# 执行方案 · 剩余修复（R7-R14）

> 2026-07-21 · 基于 R1-R6 实际改动 vs 方案交叉比对
> 已修：15/31🔴 + 10/46🟡 · 未修：16/31🔴 + 36/46🟡

---

## R1-R6 已覆盖（无需再修）

| 编号 | 问题 | 轮次 |
|------|------|:--:|
| S1 | `];` 语法错误 | R1 |
| S4 | `_publishing` 死锁 | R4 |
| S15 | doer 可伪造 | R1 |
| S16 | sync_shared 绕过 admin | R1 |
| M9 | 登录名字校验 | R1 |
| M15 | logout 停轮询 | R6 |
| M16 | UI 罚金文案 -1 NT | R4 |
| M17 | sync_shared camp 校验 | R1 |
| M18 | canteenMenu 校验 | R1 |
| M27 | resolveDispute | R3 |
| M30 | init_db 预建 pool | R5 |
| M37 | 密码最大长度 | R1 |

---

## R7 · 安全止血（6 项 🔴 · ~20 行 · 1 commit）

> 门：`python -c "from main import app"` + curl 验证

### R7-1. JWT 密钥从环境变量读取
**源：** S9 · `auth_utils.py:7`
```python
# 改前
SECRET_KEY = "nantang-cloud-village-secret-key-change-in-production"
# 改后
SECRET_KEY = os.environ.get("JWT_SECRET", "nantang-cloud-village-secret-key-change-in-production")
# ponytail: 默认值仅用于本地开发。生产环境通过 Render/环境变量注入。
```

### R7-2. get_current_user 拒绝非 access token
**源：** S10 · `auth.py:36-41`
```python
# get_current_user 中 return None 之前加：
if payload.get("type") != "access":
    return None
```

### R7-3. refresh token cookie 改为 httpOnly + 从 body 移除
**源：** S11 + S29 · `auth.py:33,66-68,76-78,90-92`
```python
# 改前
response.set_cookie("nt_rt", token, httponly=False, samesite="lax", max_age=7*86400, path="/")
# 改后
response.set_cookie("nt_rt", token, httponly=True, samesite="lax", max_age=7*86400, path="/")

# register/login/refresh — 三处均从返回 body 中删除 "refresh_token":
return {"ok": True, "token": create_access_token(u.id, u.role), "user": _user_json(u)}
# 不再返回 refresh_token 字段
```

### R7-4. 非首个用户硬编码 role="visitor"
**源：** S12 · `auth.py:58`
```python
# 改前
role="admin" if is_first else req.role,
# 改后
role="admin" if is_first else "visitor",
```

### R7-5. CORS 限定域名
**源：** S14 · `main.py:32-35`
```python
# 改前
allow_origin_regex=r".*",
# 改后
allow_origins=[
    "http://localhost:8000",
    "http://127.0.0.1:8000",
    os.environ.get("FRONTEND_ORIGIN", "http://localhost:8000"),
],
```
导入 `import os`（在 main.py 顶部已有 `import os`）。

### R7-6. overdueNT 改 `=` 替代 `+=`
**源：** S2 · `app-data.js:432`
```javascript
// 改前
user.overdueNT = (user.overdueNT || 0) + unpaid;
// 改后
user.overdueNT = unpaid;
```

**R7 验证：**
```bash
# Server
python -c "from main import app; print('OK')"
# 检查 /api/auth/refresh 不再返回 refresh_token
# 检查 cookie set-cookie 有 httponly
# 注册第二个用户确认 role=visitor
# overdueNT 公式正确
```

---

## R8 · 经济数据完整性（6 项 🔴+🟡 · ~30 行 · 1 commit）

> 门：`NT.verify()` pass=True diff=0

### R8-1. earn camp-scope 写对池名
**源：** S17 · `server/nt.py:168`
```python
# 改前
await _add_ledger(db, lid, "community_pool" if req.scope == "camp" else None, user.id,
# 改后
pool_name = "camp_pool" if req.scope == "camp" else "community_pool"
await _add_ledger(db, lid, pool_name, user.id,
```

### R8-2. cancel_task 加 ledger
**源：** S8 · `server/nt.py:385-393`
```python
# 在 task.status = "已取消" 之前加：
lid = _ledger_id()
await _add_ledger(db, lid, "escrow", task.poster or "community_pool", task.escrow_amount,
                  "task_cancelled", f"取消任务: {task.title}", task_id)
task.escrow_amount = 0
```

### R8-3. verify(rejected) poster 存在时加 ledger
**源：** S7 · `server/nt.py:358-366`
```python
# 在 poster.nt_balance += task.reward 之后加：
lid = _ledger_id()
await _add_ledger(db, lid, "escrow", task.poster, task.reward,
                  f"task_{req.status if hasattr(req,'status') else 'rejected'}",
                  f"任务退回: {task.title}", task_id)
task.escrow_amount = 0
```

### R8-4. accept_task 加行锁
**源：** S18 · `server/nt.py:307`
```python
# 改前
result = await db.execute(select(NTTask).where(NTTask.id == task_id))
# 改后
result = await db.execute(select(NTTask).where(NTTask.id == task_id).with_for_update())
```

### R8-5. verify_task 加行锁
**源：** M45 · `server/nt.py:327`
```python
# 改前
result = await db.execute(select(NTTask).where(NTTask.id == task_id))
# 改后
result = await db.execute(select(NTTask).where(NTTask.id == task_id).with_for_update())
```

### R8-6. verify(approved) 加 CV
**源：** M5 · `server/nt.py:342-343`
```python
# 在 assignee.experience_value += task.reward 之后加：
assignee.contribution_value += task.reward
```

**R8 验证：**
```bash
# 创建任务→接取→取消→检查 ledger 有 task_cancelled 记录
# earn camp scope → 检查 ledger from_user == "camp_pool"
# verify approved → 检查 assignee CV 增加
# 会计等式: /api/nt/verify → pass=true diff=0
```

---

## R9 · 任务状态机加固（5 项 · ~25 行 · 1 commit）

> 门：任务全流程通过 + 状态不可跳转

### R9-1. verify_task 移除"进行中" + 加"退回修改"
**源：** S19 + S31 · `server/nt.py:333`
```python
# 改前
if task.status not in ("待审核", "进行中", "待提交"):
# 改后
if task.status not in ("待审核", "待提交", "退回修改"):
```

### R9-2. verify_task(rejected) 设"退回修改"而非"已取消"
**源：** S31 · `server/nt.py:362-367`
```python
# 改前
pool.balance += task.reward
pool.task_escrow -= task.reward
task.status = "已取消"
# 改后
# escrow 保持冻结，仅改状态
task.status = "退回修改"
task.reject_reason = reject_reason
task.reject_count = (task.reject_count or 0) + 1
if (task.reject_count or 0) >= 3:
    # 超 3 次 → 释放 escrow 给 poster 或社区池
    if poster:
        poster.nt_balance += task.reward
    else:
        pool.balance += task.reward
    pool.task_escrow -= task.reward
    task.status = "已取消"
    task.escrow_amount = 0
```

NTTask 模型需加 `reject_count = Column(Integer, default=0)`。

### R9-3. update_task 禁止直接设状态
**源：** S22 · `server/tasks.py:101-102`
```python
# 删除这两行：
if req.status:
    task.status = req.status
# 替换为：
if req.status:
    raise HTTPException(status_code=400, detail="状态请通过专用端点变更: /api/nt/tasks/{id}/cancel|submit|verify")
```

### R9-4. nt-core.js verifyTask(rejected) 设回 active 而非 disputed
**源：** S25 · `nt-core.js:230-235`
```javascript
// 改前
t.status = 'disputed';
// 改后
t.status = 'active';  // 允许修改后重提交
// disputeTask() 才是真正的争议入口
```

### R9-5. nt-core.js verifyTask + cancelTask null guard
**源：** S5 + S6 · `nt-core.js:221,253`
```javascript
// 行 221 改前
poster.totalPaid += t.reward;
// 行 221 改后
if (poster) poster.totalPaid += t.reward;

// 行 253 改前
poster.ntBalance += t.reward;
// 行 253 改后
if (poster) poster.ntBalance += t.reward;
```

**R9 验证：**
```bash
# 尝试 PUT /api/tasks/{id} status=已结算 → 400
# verify rejected → 状态为 退回修改（非 已取消）
# reject 3 次 → 自动取消 + escrow 释放
```

---

## R10 · 同步 + 数据层修复（5 项 · ~20 行 · 1 commit）

> 门：任务状态服务端变更后客户端 polling 可见

### R10-1. _mergeSyncData 更新已有任务
**源：** S24 · `core.js:699-702`
```javascript
// 在 if (!dup) 分支之后加 else：
} else if (dup) {
  Object.assign(dup, {
    status: t.status, assignee: t.assignee, evidence: t.evidence,
    completed_at: t.completed_at, settled_at: t.settled_at
  });
}
```

### R10-2. _mergeSyncData 合并而非替换 journal
**源：** M19 · `core.js:703`
```javascript
// 改前
if (data.journal) { AppData._data.journal = []; data.journal.forEach(...); }
// 改后
if (data.journal) {
  var existingIds = new Set((AppData._data.journal||[]).map(function(j){return j.time+j.type+j.content;}));
  data.journal.forEach(function(j) {
    var key = j.time + j.type + j.content;
    if (!existingIds.has(key)) { AppData._data.journal.push({type:j.type, content:j.content, time:j.time, space_id:j.space_id}); existingIds.add(key); }
  });
}
```

### R10-3. syncAll 错误检查
**源：** M3 · `core.js:718`
```javascript
// 改前
if(typeof API!=='undefined'&&API.token){API.syncAll(_mergeSyncData);}
// 改后
if(typeof API!=='undefined'&&API.token){
  API.syncAll(function(data) {
    if (data && !data.detail && !data._offline && data.ok !== false) _mergeSyncData(data);
  });
}
```

### R10-4. _dailyPoolRefill guard 持久化
**源：** S3 · `app-data.js:218`
```javascript
// _saveShared payload 中加：
_lastPoolRefill: this._data._lastPoolRefill,
```
并在 `_dailyPoolRefill` 末尾调 `this._saveShared()`。

### R10-5. sync_shared 内层 dict 校验
**源：** M17+M18 已修（R1），此条确认无需再动。

**R10 验证：**
```bash
# 服务端改任务状态→等 10s→客户端 polling 更新本地
# 页面刷新 2 次→检查社区池不重复 refill
```

---

## R11 · 权限 + 数据范围（6 项 · ~15 行 · 1 commit）

> 门：非 admin 访问受限端点返回 403/401

### R11-1. earn 端点加 require_admin
**源：** S13 · `server/nt.py:144`
先 grep 确认无客户端调用方：
```bash
grep -r "API.earn\|/api/nt/earn" nantang-mobile/
```
若无调用方 → 删除端点。若有 → 改 `get_current_user` 为 `require_admin`。

### R11-2. list_tasks 限定范围
**源：** M21 · `server/tasks.py:47`
```python
# 改前
result = await db.execute(select(NTTask).order_by(NTTask.created_at.desc()))
# 改后
result = await db.execute(
    select(NTTask).where(
        (NTTask.poster == user.id) | (NTTask.assignee == user.id)
    ).order_by(NTTask.created_at.desc())
)
```

### R11-3. add_announcement 加 admin
**源：** M23 · `server/data.py:276`
```python
# 改前
@router.post("/announcements")
async def add_announcement(req: dict, user: User = Depends(get_current_user), ...):
    if not user: raise HTTPException(status_code=401)
# 改后
@router.post("/announcements")
async def add_announcement(req: dict, user: User = Depends(require_admin), ...):
```

### R11-4. card_discovery guesser 强制
**源：** M22 · `server/data.py:76`
```python
# 改前
guesser=req.get("guesser"),
# 改后
guesser=user.id,
```

### R11-5. list_users 加分页
**源：** M20 · `server/auth.py:108-112`
```python
# 改前
result = await db.execute(select(User))
return [{"name": u.id, ...} for u in result.scalars()]
# 改后
@router.get("/users")
async def list_users(user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db),
                     limit: int = 50, offset: int = 0):
    if not user: raise HTTPException(status_code=401)
    result = await db.execute(select(User).limit(limit).offset(offset))
    return [{"name": u.id, "avatar_seed": u.avatar_seed} for u in result.scalars()]
```

### R11-6. _getUser 保留自动注册但加 warn
**源：** M24 · `nt-core.js:635`
破坏性变更推迟。当前阶段仅增强 warn 信息：
```javascript
// 改前
console.warn('[NT] 自动注册未知用户: '+id);
// 改后
console.warn('[NT] 自动注册未知用户: '+id+' — 可能为拼写错误，调用栈:', new Error().stack);
```

**R11 验证：**
```bash
# visitor 调 /api/tasks → 仅返回自己任务
# visitor 调 POST /api/announcements → 403
# guest 调 POST /api/data/card_discoveries → guesser 强制为 user.id
```

---

## R12 · 客户端数据修复（5 项 · ~15 行 · 1 commit）

> 门：node -c js/*.js 全部通过 + NT.verify() pass

### R12-1. doPublish 负奖励拦截
**源：** S26 · `core.js:552`
```javascript
var nt = parseInt(document.getElementById('pubNT').value, 10) || 5;
// 之后加：
if (nt <= 0) { showToast('奖励必须大于0', 'error'); _publishing = false; return; }
```

### R12-2. earnFromPool/spendToPool 加整数检查
**源：** M25 · `nt-core.js:427,455`
```javascript
// earnFromPool + spendToPool 的 amount <= 0 检查后加：
if (!Number.isInteger(amount)) { console.error('[NT] amount must be integer, got', amount); return null; }
```

### R12-3. filterQuests done 区含"已完成"
**源：** M46 · `core.js:262`
```javascript
// 改前
var done=items.filter(function(t){return t.status==='待结算'||t.status==='已结算'});
// 改后
var done=items.filter(function(t){return t.status==='待结算'||t.status==='已结算'||t.status==='已完成'});
```

### R12-4. 空状态"发布委托"链接化
**源：** M41 · `core.js:274`
```javascript
// 改前
'<div>暂无待领取任务，「+ 发布委托」创建新任务</div>'
// 改后
'<div>暂无待领取任务，<span style="color:var(--green-primary);cursor:pointer;text-decoration:underline" onclick="openPublishTask()">+ 发布委托</span> 创建新任务</div>'
```

### R12-5. inbox 面板 safe-area 内联同步
**源：** M14 · `index.html:692`
```javascript
// 改前
panel.style.top = '50px';
// 改后
panel.style.top = 'calc(50px + env(safe-area-inset-top, 0px))';
```

**R12 验证：**
```bash
# node -c js/*.js → 全通过
# 手动测试：发布表单输入 -10 → toast 拦截
# 手动测试：inbox 面板在 iPhone 模拟器中不被 notch 遮挡
```

---

## R13 · autoLogin + UX（4 项 · ~15 行 · 1 commit）

> 见 api.js 已由用户更新（离线模式 + silentRefresh 分级），以下调整配套逻辑

### R13-1. autoLogin 超时标志防竞态
**源：** M11 · `index.html:1262-1295`
```javascript
var _refreshTimedOut = false;
var _refreshTimeout = setTimeout(function() { _refreshTimedOut = true; document.body.style.visibility = 'visible'; showToast('会话恢复超时，请手动登录', 'warn'); }, 5000);
API.silentRefresh(function(u, errType) {
    if (_refreshTimedOut) return;
    clearTimeout(_refreshTimeout);
    // ... 原逻辑
});
```

### R13-2. autoLogin no_token 静默
**源：** M12 · `index.html`（已在 api.js 更新中实现 silentRefresh 分级，此条确认调用方处理正确）

### R13-3. 登录加载状态 — 持久元素替代 toast
**源：** M13 · `index.html:1262`
```javascript
// 在 loginPage 中加：
var loadingEl = document.getElementById('loginLoading');
if (!loadingEl) {
  loadingEl = document.createElement('div');
  loadingEl.id = 'loginLoading';
  loadingEl.textContent = '正在恢复会话…';
  loadingEl.style.cssText = 'text-align:center;padding:16px;color:#5a6e5c;font-size:.75rem';
  document.getElementById('loginPage').appendChild(loadingEl);
}
// silentRefresh 回调中：
if (loadingEl) loadingEl.remove();
```

### R13-4. 邀请码读取并传递
**源：** M10 · `core.js:742-768`
```javascript
// enterVillage 注册路径中：
var inviteCode = document.getElementById('regInviteCode')?.value?.trim() || '';
// 传给 API.asyncAuth 最后一个参数
API.asyncAuth('register', n, p, 'visitor', _profileSeed, function(result) { ... });
```
注意：API.asyncAuth 当前签名不接收 inviteCode 参数，需同步更新 `api.js:32` 的 `asyncAuth` 函数签名和 body。

---

## R14 · 剩余低优 + 清理（~10 项 · 1 commit）

> 不阻塞的项集中处理

### R14-1. 死代码删除
- **L3**: `nt-core.js:102-125` — 删除 deposit/withdraw 不可达函数体
- **L4**: `nt-core.js:310-380` — 删除 `_batchSettle_dead`（移入 `docs/nt-batch-settle-algorithm.md` 归档）
- **L6**: `index.html:144` — 删除未使用的 `#myInboxPanel` 静态 div

### R14-2. 数据质量
- **M42**: `server/tasks.py:15` + `server/nt.py:32` — title 加 `min_length=1, max_length=200`
- **M43**: `data.py:26-35` — journal content 长度 ≤10000，type 非空
- **M44**: `server/nt.py:116` — transfer 加 `amount > 10000` 上限

### R14-3. 纵深防御
- **M47**: `auth_utils.py:10` — 加 `ABSOLUTE_MAX_DAYS = 30`，refresh 端点检查
- **M48**: `main.py` — 加 `X-Content-Type-Options: nosniff` 中间件
- **M38**: `auth.py:52` — 密码下限 6→8

### R14-4. 架构债标注（不写代码）
- L1 三套并行 NT 模型 → `CLAUDE.md` 加架构债章节
- M1/M2 状态词汇/字段名 → 归入 L1 Phase 2

**R14 验证：**
```bash
# 死代码删除后 grep 确认无引用
# transfer 10001 NT → 400
# register 密码 7 字符 → 400 "密码至少8位"
```

---

## 执行总表

| 轮次 | 内容 | 项数 | 改动规模 |
|------|------|:--:|:--:|
| **R7** | 🔴 安全止血 | 6 | ~20 行 |
| **R8** | 🔴 经济完整性 | 6 | ~30 行 |
| **R9** | 🔴 任务状态机 | 5 | ~25 行 |
| **R10** | 🟡 同步+数据层 | 4 | ~20 行 |
| **R11** | 🟡 权限+范围 | 6 | ~15 行 |
| **R12** | 🟡 客户端修复 | 5 | ~15 行 |
| **R13** | 🟡 autoLogin+UX | 4 | ~15 行 |
| **R14** | 🟢 清理+纵深 | 10 | ~20 行 |

**总计：46 项 · ~160 行 · 8 commits**

每轮 1 commit，独立可 revert。R7 先堵安全漏洞，R8-R9 修数据完整性，R10-R13 修客户端+UX，R14 收尾。
