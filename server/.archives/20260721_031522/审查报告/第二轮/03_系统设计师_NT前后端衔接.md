# 第二轮审查 · 系统设计师 · NT 前后端衔接

> 2026-07-21 · Agent: systems-designer
> 审查范围：6 条 NT 链路的前后端衔接验证

---

## Chain A: poster='社区' createTask — ⚠️ 需修正

设计文档引用 `nt.py:73` 但该行已标记 deprecated。实际插入点为 `tasks.py:68-95`。
`TaskCreate` model 缺 `poster` 字段——`poster` 从 `user.id` 硬编码（L82）。

## Chain B: verifyAction 服务端化 — ⚠️ 需修正

端点已存在于 `POST /api/nt/verifications/{vfy_id}/approve`（非 `/api/verify`）。
已有规则1（非自校核，L661）。缺规则2-4（1h冷却、日10次、3次驳回）。
客户端 `verifyAction` 存在双付 bug：同时调 `NT.earn()`（客户端）+ `POST .../approve`（服务端）。

## Chain C: cron 任务生成 — ⚠️ 需修正

无 cron runner。`_get_pool()`、`_add_ledger()`、`_task_id()` 可直接复用。
`tasks.py create_task` ~70% 可复用（需 poster='社区' 分支）。

## Chain D: cancelTask poster='社区' — ✅ 直接插入

`nt.py:531` 对 poster='社区' 碰巧正确处理：查 User 返回 None → else 分支退还给 pool.balance。

## Chain E: GET /api/tasks?mode=hall — ✅ 直接插入

纯 Python 过滤，不改 SQL。~12 行。

## Chain F: sync cron_active — ✅ 直接插入

sync 响应加 1 个 key，客户端 3 处 guard check。~11 行。
