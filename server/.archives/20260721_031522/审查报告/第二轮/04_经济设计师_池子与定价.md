# 第二轮审查 · 经济设计师 · 池子与定价

> 2026-07-21 · Agent: economy-designer
> 审查范围：COMMUNITY_POOL 定义、池操作路径、模板定价、TASK_ESCROW 变量、池耗尽影响

---

## A) COMMUNITY_POOL 定义 — 🔴 关键缺口

- 客户端 `nt-core.js:34`: `var COMMUNITY_POOL = 0`
- 服务端 `models.py:77`: `CommunityPool.balance` 默认 0
- **两端独立，互不同步。** 客户端 `_dailyPoolRefill` 和服务端 `daily_tick` 各自独立印钞。

## B) 池操作路径

现有 9 条路径。新增 2 条（cron create + cancel 退款）不破坏会计等式：
`pool.balance` 和 `pool.task_escrow` 之间零和转移。
`cancel_task` 对 poster='社区' 碰巧正确（回退到 else 分支）。

## C) 定价合理性

| 模板 | reward | 实际参照 | 参照价 | 比例 |
|------|:---:|------|:---:|:---:|
| daily_sweep | 3 | cleaning(clean) | 5 | 60% |
| weekly_deep_clean | 10 | cleaning(dirty) | 20 | 50% |
| weekly_inventory | 8 | stock_in+out | 3 | 267% |
| daily_compost | 2→**5** | 无直接参照 | — | —

住宿费最低 80 NT/周，全勤系统任务 53 NT/周→**交不起房租**。但手动任务+校核收入可补充。

## D) TASK_ESCROW

服务端 `CommunityPool.task_escrow`（`models.py:79`）已存在。
poster='社区' 的 escrow 在会计上等价于「社区池转账到托管」。

## E) 池耗尽影响

🔴 **无用户通知**。仅 WARN 日志。
建议：sync 响应加 `community_pool_status: "low"` + 客户端 UI 横幅。
