# 第二轮审查 · UI 程序员 · 代码匹配验证

> 2026-07-21 · Agent: ui-programmer
> 审查范围：设计文档附录 D/E 中的函数名、复用声明、行数估算、文件改动点是否与实际代码匹配

---

## A) 函数名与位置验证

| 设计文档声明 | 实际位置 | 判定 |
|---|---|---|
| `renderCardRoom()` at L371 | **L365**（差 6 行） | ⚠️ 行号偏移 -6 |
| `openDiscoveryDetail(discId)` at L893 | **L888**（差 5 行） | ⚠️ 行号偏移 -5 |
| `confirmDiscovery(discId)` at L1020 | **L1020** | ✅ 精确匹配 |
| `denyDiscovery(discId)` at L1076 | **L1071**（差 5 行） | ⚠️ 行号偏移 -5 |
| `_submitSelfReport()` at L842 | **L837**（差 5 行） | ⚠️ 行号偏移 -5 |
| `openSelfReport(opts)` at L512 | **L506**（差 6 行） | ⚠️ 行号偏移 -6 |
| `openQuestHallPage()` at L220 | **core.js:220** | ✅ 精确匹配 |
| `filterQuests()` at L245 | **core.js:245** | ✅ 精确匹配 |
| `renderTaskCard(t, opts)` at L283 | **core.js:283** | ✅ 精确匹配 |
| `openPublishTask()`（附录 E.4 提及） | **core.js:484** | ✅ 函数存在 |

## B) 复用声明验证

| 设计文档声称的复用 | 实际是否存在 | 判定 |
|---|---|---|
| `my-fchip` 筛选 chip 样式 | **存在** — CSS `main.css:151-152` | ✅ |
| `my-tab` tab 栏样式 | **存在** — CSS `main.css:134-137` | ✅ |
| `task-card` 任务卡片样式 | **存在** — `core.js:309` | ✅ |
| `overlayQuestHall` | **存在** — `index.html:354-377` | ✅ |
| `overlayCardRoom` | **存在** — `index.html:211-217` | ✅ |
| `cardDiscoveries[]` 数据源 | **存在** — `ui-cardroom.js:318` `_getDiscoveries()` | ✅ |
| `pendingVerifications` 数据源 | **存在** — `app-data.js:21` | ⚠️ `ui-cardroom.js` 不直接引用此数组，需跨文件读取 `AppData._data.pendingVerifications` |

## C) 需修正项

### D-1: tab HTML 位置错误
`renderCardRoom()` 用 `el.innerHTML = h` 全量覆写 cardRoomBody（L414），静态 HTML 会被清除。tab 栏必须在 `renderCardRoom()` JS 内生成。

### D-2: filterQuests 选择器污染
`filterQuests()` L246 用 `#questHallBody .my-fchip.on` 取激活 chip。引入二级 chip 行后此选择器会同时匹配一级和二级 chip。需限定作用域。
