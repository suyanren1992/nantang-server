# 执行方案校对报告

> 2026-07-21 · 3 个校对 Agent 并行交叉检查
> 问题来源 × 执行方案 → 覆盖 / 缺口 / 矛盾 / 依赖

---

## 一、总体统计

| 指标 | 数量 |
|------|:---:|
| Agent 调查发现 | 38 |
| 用户反馈意见 | 11 |
| **问题总计** | **49** |
| 执行方案覆盖 | 42 |
| **未覆盖** | **7** |
| 设计矛盾 | 4 |
| 依赖冲突 | 6 |
| 执行细节缺失 | 13 |

---

## 二、覆盖确认 ✓

### Phase A (9 项)
- A1-A6：原始 7 Agent 审查发现 → 全部对应
- A7（物品保存bug）→ Agent 调查 4.1
- A8（打扫保存bug）→ Agent 调查 4.2
- A9（登录排查）→ Agent 调查 4.4

### Phase B (10 项)
- B1-B9：原始 7 Agent 审查发现 → 全部对应
- B10（信箱延迟）→ Agent 调查 4.3

### Phase B+ (6 项)
- B+1-B+5：原始 7 Agent 审查发现 → 全部对应
- B+6（删过期注释）→ Agent 调查 1

### Phase C (4 阶段)
- 三条资金路径（createTask / verifyTask / transfer）→ 覆盖主要 NT 流
- sync 端点（C0）→ 覆盖客户端-服务端同步

### Phase D (13 项)
- 全部有具体执行方案
- D2（校核制双路径）→ 用户反馈 2/6
- D15（tx_hash）→ Agent 调查 2

### Phase E (11 项)
- E3.1-E3.3（入住系统）→ Agent 调查 5 全部 6 项
- E3.4（新手引导）→ Agent 调查 6 全部 6 项（含 me_3/checkin_5 hook 修复）
- E3.5-E3.6（社区发配任务）→ Agent 调查 7 部分覆盖
- E3.7（访客权限）→ 用户反馈 9
- E3.8（社区管理员）→ Agent 调查 7 + 用户反馈 5
- E3.9（周期性任务）→ Agent 调查 7 + 用户反馈 7
- E3.10（赏金池）→ Agent 调查 7 + 用户反馈 8
- E3.11（双栏布局）→ 用户反馈 6

---

## 三、未覆盖问题 ✗

### 🔴 严重（影响一致性）

| # | 问题 | 来源 | 影响 |
|---|------|------|------|
| **U1** | `pendingTransactions`（客户端）与 `DepositIntent`（服务端）两套充值记录，互不同步 | 调查2 | Phase C sync 端点不包含充值意图，双设备充值状态不一致 |
| **U2** | 服务端入住 API 不存在。E3 全是客户端改动。Phase C「服务端唯一真相源」原则在身份系统上被违反 | 调查5 | 双设备角色/入住状态不一致 |
| **U3** | 校核制 earn 是第四条资金路径，但 Phase C2 只硬化了三条（createTask/verifyTask/transfer） | 调查3 | 校核通过后 NT pool 变动只发生在客户端，服务端 pool 永远不同步 |

### 🟡 中等（功能缺口）

| # | 问题 | 来源 |
|---|------|------|
| **U4** | `newbie_quest_steps`（System B，app.js:68-77）死代码——8 个步骤、渲染进度条、无 completion hook。E3.4 建了全新 flow 但没删旧的 | 调查6 |
| **U5** | `NT.userTasks()` 存在但 UI 从未调用。E3.6 在 renderMyTasks 里自己写过滤逻辑，造了第二套并行过滤 | 调查7 |
| **U6** | `category` 字段 write-only——E3.5/E3.9 写入 category 但没有任何代码读它做过滤/分组 | 调查7 |
| **U7** | 三池模型（nt-three-pool-redesign.md）已设计但未迁移到任何阶段，也没有明确的「不做」决策 | 调查3 |

---

## 四、设计矛盾 ⚠

| # | 矛盾 | 文件内位置 |
|---|------|-----------|
| **C1** | 新手任务出现在哪里？用户说「校核室」→ E2 流程图说「工作台」→ E3.5 的 createTask 默认进「任务大厅」 | 三处不一致 |
| **C2** | 周期性任务出现在哪里？用户说「卡片室/校核室（社区内部）」→ E3.9 说「卡片式大厅，任何人可认领」 | E3.9 vs 反馈7 |
| **C3** | E4 checklist：「社区任务不在大厅公开展示」 vs E3.9：「任务出现在卡片式大厅，任何人可认领」 | Phase E 内部自相矛盾 |
| **C4** | 新手任务校核人=管理员，但 createTask 没有 reviewer 字段。poster='社区' 时 poster 就是 verifier，无法指定管理员 | E3.5 代码 vs 数据模型 |

---

## 五、依赖冲突 🔗

| # | 冲突 | 严重度 |
|---|------|:---:|
| **D1** | D10（INN_ROOMS 配置化）vs E3.1（删除 INN_ROOMS）→ 两者互斥，E3.1 应取代 D10 | 🔴 |
| **D2** | D13（_deductAccommodation 债务修复）的目标函数在 E3.1 中被重写 → D13 修复可能作废 | 🟡 |
| **D3** | D15 client-side _addLedger 加 txHash 参数 → Phase C3 删除所有写函数 → _addLedger 可能被删 | 🟡 |
| **D4** | B3（部分状态词汇修正）vs D1（统一状态词汇）→ B3 的临时修正会被 D1 覆盖 | 🟡 |
| **D5** | D2 依赖 C0 sync 端点，但 C0 的 payload 不含 verification 数据 | 🟡 |
| **D6** | E3.9 daily tick 客户端执行 vs Phase C 服务端权威 → 周期性任务由谁生成？双端生成会重复 | 🟡 |

---

## 六、执行细节缺失 📝

共 13 项（详见 Phase E 校对报告 Section 4），关键几项：

| # | 缺失 | 影响 |
|---|------|------|
| M1 | 已有用户的迁移路径——升级后 role 默认 visitor 还是自动检测 tenant？ | 升级后老用户身份丢失 |
| M2 | E3.2 公约 overlay 关闭不签署 → 床位是否回滚？ | 床位抢占 |
| M3 | COMMUNITY_POOL 不够 38 NT 时新手任务是否部分创建？ | 经济边界 |
| M4 | createTask poster='社区' 的服务端分支——E3.5 只提了客户端 nt-core.js | 服务端不同步 |
| M5 | 社区分配任务初始 status——pending 还是 accepted？ | 任务生命周期 |
| M6 | 卡片室/校核室/工作台/任务大厅四个空间的导航机制 | UI 架构 |

---

## 七、修正建议（优先级排序）

### 立刻修（改计划文件，不改代码）
1. **D10 标记被 E3.1 取代**（解 C1/D1）
2. **统一新手任务位置**：确认是校核室还是工作台（解 C1/C3）
3. **统一周期性任务位置**：确认是社区内部还是公开（解 C2/C3）

### 加入计划
4. **U1-U3**：作为新执行项加入 Phase C 或 D
5. **C4**：createTask 加 reviewer 参数 → 加入 E3.5
6. **U4-U6**：死代码清理 + NT.userTasks 使用 + category 读取 → 加入 B+ 或 E
7. **U7**：三池模型决策（做/不做/何时做）→ 加入 D

### 补充细节
8. 填补 13 项执行细节缺失（主要是 E3 各子项）

---

## 八、校对结论

**执行方案覆盖率：42/49 = 86%**

- 原始 7 Agent 审查的 93 项发现 → 全部覆盖（18 执行项 + 信任架构）
- Agent 补充调查的 38 项发现 → 31 项覆盖，7 项未覆盖
- 用户反馈 11 项 → 全部覆盖

**主要缺口**：充值双轨同步、服务端入住 API、校核制作为第四条资金路径。

**主要矛盾**：新手任务/周期性任务的渲染位置在三处文档中不一致。

**最大风险**：Phase E 全部客户端改动，没有对应服务端端点。Phase C 后身份系统成为信任架构的漏洞。
