# 经济设计师审查 · 会计等式 & 池子 & 托管

> 2026-07-21 · Agent: economy-designer
> 覆盖: nt-core.js, server/routes/nt.py, app-data.js, models.py, auth.py

---

## 发现清单（11 项）

### ISSUE 1 [RED] — verifyTask rejected 仅退 t.reward → 多槽孤儿资金

`nt-core.js:229`

与游戏设计师 ISSUE 1 重复。createTask 冻结 reward × slots，rejected 路径只退 reward × 1。

### ISSUE 2 [RED] — cancelTask 同样 bug

`nt-core.js:247`

同上。slots > 1 时资金泄露。

### ISSUE 3 [RED] — batchSettle 死代码仍可调用

`nt-core.js:631`

DEAD CODE 标注明确说调用会双重转账，但仍暴露给 window.NT。

### ISSUE 4 [RED] — frozen_cv=75 幽灵值

`models.py:16`

注册不送 NT 后此值无意义。客户端 nt-core.js registerUser 也不设此字段。

### ISSUE 5 [YELLOW] — User.overdueNT 服务端无列

`models.py`

住宿系统用 overdueNT，但 User 模型无此列。已知缺口 M32。

### ISSUE 6 [YELLOW] — CommunityPool 初始值不一致

`auth.py:75` vs `nt.py:83`

一个设 (0,0)，一个设 (2000,2000)。客户端始终 2000，服务端可能 0。

### ISSUE 7 [YELLOW] — topUp/cashOut 无行锁

`nt.py:204, 222`

admin-only 但并发可导致 total_issued 丢更新。

### ISSUE 8 [YELLOW] — earn 端点废弃，客户 earn 无法同步服务端

`nt.py:156-163`

校核制代码调用 NT.earn → earnFromPool → 只改客户端 COMMUNITY_POOL。服务端 pool.balance 不同步。

### ISSUE 9 [YELLOW] — 初始注资防重检查依赖精确字符串

`nt-core.js:685`

`reason === 'initial_funding'` 精确匹配——任何人改了这条就再次注资。

### ISSUE 10 [INFO] — verify camp_balance 聚合校验

`nt.py:244`

服务端只有一个 pool.camp_balance 聚合值，不能检测单营异常。

### ISSUE 11 [INFO] — ledgerNet 计算后未使用

`nt-core.js:497-503`

计算了 ledger 净额但不参与验证——不交叉校验 _totalIssued。
