# QA 测试审查 · 输入验证 & 权限 & 竞态 & 空指针

> 2026-07-21 · Agent: qa-tester
> 覆盖: 全量服务端端点和客户端写操作

---

## 发现清单（6 项）[不完整——Agent 超时]

注：此 Agent 在 99,719 tokens 后返回，部分输出丢失。以下为已捕获的发现。

### 验证缺口

- 部分端点缺少输入验证（负数金额、空字符串、过长输入）
- 部分端点缺少认证检查
- nt.py/auth.py/tasks.py/data.py 端点需逐项检查

### 客户端空指针风险

- `.value`、`.trim()` 调用前未 null-check
- setTimeout/setInterval 内状态修改的竞态条件

### _processedTxIds 防重放

- 有防重放机制但无限增长（已在方案中记录）

### deposit/withdraw stub

- `nt-core.js:103-105` 已改为 stub + console.warn，无调用方会崩溃

注：完整 QA 报告因 agent 超时未能捕获。建议后续单独补跑一次 QA 审查。
