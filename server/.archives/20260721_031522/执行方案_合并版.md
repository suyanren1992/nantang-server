# 南塘云村 · 统一执行方案

> 2026-07-21 · 合并 [28 个 🔴](执行方案_28bugs.md) + [56 个 🟡🟢](执行方案_55个🟡🟢.md)
> 去重：P1-9=A1-6, P1-5=B1-4, P4-2=B2-6, P4-3=🟢2, P2-11=B2-1
> 交叉引用：P1-1/P1-2↔A1-1/A1-2(CV对齐), P1-8/P1-9(读写耦合), P4-3↔🟢2

## 执行约定

**回滚策略：** 每轮一个 git commit。某轮出问题 → `git revert` 该 commit，不影响其他轮。

---

## 待办总计

| 轮次 | 内容 | 数量 | 来源 |
|------|------|:--:|------|
| 第1轮 | 🔴 崩溃/安全 | 6 | P0 |
| 第2轮 | 🔴+🟡 经济一致性（server+client 配对） | 19 | P1 + A1 + A2 |
| 第3轮 | 🔴+🟡 任务生命周期 + UX | 18 | P2 + A3 |
| 第4轮 | 🔴+🟡 客户端健壮性 + 刷新 | 17 | P3 + A4 |
| 第5轮 | 🟡 服务端完善 + 功能缺口 | 23 | P4 + B1 + B2 + B3（5-9 已去重） |
| 第6轮 | 🟢 改善项 | 14 | C1×7 + C2×6 + 🟢1 |
| 暂缓 | — | 2 | Q1 + Q2 |
| **合计** | | **99** | |

> 5 个重复已去除：P1-9=A1-6, P1-5=B1-4, P4-2=B2-6, P4-3=🟢2, P2-11=B2-1。
> 97 可执行 + 2 暂缓 = 99。

---

## 第1轮 · 崩溃/安全（6 个 · 先修，否则 inbox 不可用 + 用户泄露）

> **验证：** `python -c "from main import app"` 通过 + 浏览器打开 inbox 面板不崩溃 + `node -c js/*.js` 全通过

### 1-1. index.html — _inboxReadIds 未声明 🔴
**源：P0-1**
**修复：** 在 `_markInboxRead` 上方添加：
```javascript
var _inboxReadIds = {};
try { _inboxReadIds = JSON.parse(localStorage.getItem('nt_inbox_read') || '{}'); } catch(e) {}
```

### 1-2. index.html — _inboxCategory 未声明 🔴
**源：P0-2**
**修复：** 同位置添加：
```javascript
var _inboxCategory = '全部';
```

### 1-3. auth.py — /api/auth/users 无认证 🔴
**源：P0-3** · 文件：`server/routes/auth.py:107-110`
**修复：**
```python
@router.get("/users")
async def list_users(user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    if not user: raise HTTPException(status_code=401)
    ...
```

### 1-4. auth.py — register 不验证用户名 🔴
**源：P0-4** · 文件：`server/routes/auth.py:50-51`
**修复：** register 函数开头加：
```python
if not req.name or len(req.name) > 64:
    return JSONResponse({"ok": False, "error": "用户名需为1-64字符"})
```

### 1-5. data.py — sync_shared 内层缺类型校验 🟡
**源：P0-5** · 文件：`server/routes/data.py:297-320`
**修复：**
```python
if isinstance(req.get("camps"), dict):
    for camp_id, camp_data in req["camps"].items():
        ...
```

### 1-6. data.py + camps.py — json.loads 无 try/except 🟡
**源：P0-6** · 文件：`server/routes/camps.py:30` `data.py:193-194,235`
**修复：** 每个 `json.loads` 加 try/except，降级返回 `[]` 或 `{}`

---

## 第2轮 · 经济一致性（19 个 · server/client 配对修）

> **验证：** `NT.verify()` 返回 `pass: true` + server/client 双端 earn/spend 后 CV 同值 + camp 创建后 `earnFromPool('camp:xxx')` 不返回 null
> **闸门：** 本轮完成后执行 `NT.verify()`——`pass: false` 则阻塞后续轮次。
> **建议：** 拆为 2a(server 2-1~2-7) → 2b(client 2-8~2-19) 两次 commit，减少回溯范围。3-18（_mergeSyncData）建议在本轮尽早执行。

### 2a · Server 端（nt.py）

#### 2-1. earn 不更新 CV 🔴
**源：P1-1** · `server/routes/nt.py:147-148` · 关联：A1-2
```python
user.nt_balance += req.amount
user.experience_value += req.amount
user.contribution_value += req.amount  # 新增
```

#### 2-2. spend 不减 CV（与 v2 决策对齐） 🔴
**源：P1-2 + A1-1** · `server/routes/nt.py:167-168`
**决策：** spend 不减 CV（花钱不影响历史贡献）。server spend 当前无 CV 操作，此条为确认——不添加 CV 修改代码。客户端 A1-1(2-8) 删除现有减 CV 行。
> 原 28bugs P1-2 曾建议"新增 CV 扣减以对齐客户端"。v2 联合审查后决策反转：spend 不应减 CV，两端同步删除而非同步新增。

#### 2-3. transfer 无 CV 转移规则 🔴
**源：P1-3** · `server/routes/nt.py:109-131`
```python
cv_amount = req.amount
user.contribution_value = max(0, user.contribution_value - cv_amount)
to_user.contribution_value = (to_user.contribution_value or 0) + int(cv_amount * 0.75)
pool.contribution_pool = (pool.contribution_pool or 0) + int(cv_amount * 0.25)
```

#### 2-4. spend scope→ledger 映射不一致 🔴
**源：P1-4** · `server/routes/nt.py:175`
```python
lid = _ledger_id()
await _add_ledger(db, lid, user.id, "community_pool", req.amount, f"{req.scope}_spend", req.reason)
```

#### 2-5. 缺 cashout 端点 🔴
**源：P1-5** · `server/routes/nt.py`（新增）
```python
@router.post("/cashout")
async def cashout(req: TopUpRequest, admin: User = Depends(require_admin), db: AsyncSession = Depends(get_db)):
    if req.amount <= 0: raise HTTPException(status_code=400, detail="金额必须大于0")
    target = (await db.execute(select(User).where(User.id == req.user))).scalar_one_or_none()
    if not target: raise HTTPException(status_code=404, detail="用户不存在")
    if target.nt_balance < req.amount:
        raise HTTPException(status_code=400, detail=f"余额不足（当前 {target.nt_balance} NT）")
    target.nt_balance -= req.amount; target.updated_at = datetime.utcnow().isoformat()
    pool = await _get_pool(db); pool.total_issued -= req.amount
    if pool.total_issued < 0: raise HTTPException(status_code=400, detail="系统发行量不足")
    lid = _ledger_id()
    await _add_ledger(db, lid, target.id, None, req.amount, "cashout", req.reason)
    await db.commit()
    return {"ok": True, "entry_id": lid, "balance": target.nt_balance}
```

#### 2-6. CAMP_POOLS 注资（服务端） 🔴
**源：P1-7(server)** · models.py + camps.py + nt.py
```python
# models.py CommunityPool 加：
camp_balance = Column(Integer, default=0)

# camps.py create_camp（约87行）：
pool.camp_balance += camp_total

# nt.py earn（约141行）：
if req.scope == "camp" or (hasattr(req, 'pool') and req.pool and req.pool.startswith('camp:')):
    if pool.camp_balance < req.amount: raise HTTPException(400, "营队池余额不足")
    pool.camp_balance -= req.amount
else:
    if pool.balance < req.amount: raise HTTPException(400, "社区池余额不足")
    pool.balance -= req.amount
```

#### 2-7. reject 缺 poster 时缺 ledger 记录 🟡
**源：P1-11** · `server/routes/nt.py:331-332`
```python
await _add_ledger(db, lid, task.poster or "escrow", "community_pool", task.reward, "escrow_absorbed", "发布者不存在,NT 回收", task_id)
```

### 2b · Client 端（nt-core.js + app-data.js）

#### 2-8. spendToPool 减 CV → 删除（与 server 对齐） 🟡
**源：A1-1** · `nt-core.js:428` · 关联：2-1
```javascript
// 删除此行：u.contributionValue = Math.max(0, (u.contributionValue||0) - amount);
```

#### 2-9. verifyTask 加 CV/XP 🟡
**源：A1-2** · `nt-core.js:213-216` · 关联：2-2
```javascript
assignee.contributionValue = (assignee.contributionValue||0) + t.reward;
assignee.experienceValue = (assignee.experienceValue||0) + t.reward;
```

#### 2-10. cashOut 混入 totalPaid 🟡
**源：A1-3** · `nt-core.js:383`
**修复：** 删除 `u.totalPaid += amount;`

#### 2-11. earn/spend wrappers 签名误导 🟡
**源：A1-4** · `nt-core.js:354-356`
```javascript
// ponytail: 三池支持前，earn/spend wrapper 固定走 community pool，scope 仅影响 ledger type 标签
```

#### 2-12. PUBLIC_CV_POOL 无 sink 🟡
**源：A1-5** · `nt-core.js:460-464`
```javascript
// ponytail: PUBLIC_CV_POOL 单向积累无出口。当 >10000 时需实现分配机制（如按周活跃度分发给成员）
```

#### 2-13. CAMP_POOLS 注资（客户端） 🔴
**源：P1-7(client)** · `nt-core.js`
```javascript
function depositToCampPool(campId, amount, reason) {
  if (!CAMP_POOLS[campId]) CAMP_POOLS[campId] = 0;
  CAMP_POOLS[campId] += amount; _totalIssued += amount;
  _addLedger('system', 'camp:' + campId, amount, 'deposit', reason || '营队注资');
  _saveState(true); return CAMP_POOLS[campId];
}
```

#### 2-14. spend() scope 参数误导 🟡
**源：P1-6** · `nt-core.js:358-360`
```javascript
// ponytail: CAMP_POOLS 端到端实现前，spend() 固定走 community pool。scope 仅影响 ledger type 标签。
function spend(userId, amount, reason, scope) {
  return spendToPool(userId, amount, reason, 'community', scope);
}
```

#### 2-15. overdueNT 永不追缴 + 存用户 🔴🔴（配对修复）
**源：P1-8 + P1-9** · `app-data.js:417-431`
```javascript
// 修复插入位置：原代码第 420 行已有 var user = NT.getUser(room.tenant); 复用此变量
// 读：同时覆盖 room.overdueNT（旧数据）和 user.overdueNT（P1-9 后新数据）
var userOverdue = user ? (user.overdueNT || 0) : 0;
var totalDue = roomPrice * daysPassed + (room.overdueNT || 0) + userOverdue;
// ...原有 affordable 计算保持不变...
// 写：欠费存用户对象而非房间（在原 overdueNT 赋值处替换）
if (unpaid > 0 && user) {
  user.overdueNT = (user.overdueNT || 0) + unpaid;
}
```
> ⚠️ P1-8 和 P1-9 有读写耦合：P1-9 生效前旧欠费在 room.overdueNT，生���后在 ntUser.overdueNT。P1-8 读两处确保不遗漏。

#### 2-16. _loadState 旧 sp→cmp 迁移 🔴
**源：P1-10** · `nt-core.js:47-48`
```javascript
if (s.cmp == null && s.sp != null) {
  console.warn('[NT] 检测到旧 sp 字段，已迁移到 cmp，请验证会计等式');
  COMMUNITY_POOL = s.sp; _saveState(true);
} else {
  COMMUNITY_POOL = s.cmp != null ? s.cmp : 0;
}
```

#### 2-17. 初始注资检查 fragile 🟡
**源：A2-1** · `nt-core.js:694-698`
```javascript
var _initialFunded = localStorage.getItem('nt_initial_funded');
if (!_initialFunded) {
  COMMUNITY_POOL += 2000; _totalIssued += 2000;
  _addLedger('system', '__community_pool__', 2000, 'deposit', 'initial_funding');
  localStorage.setItem('nt_initial_funded', '1'); _saveState(true);
}
```

#### 2-18. sync payload inventory 字段值错误 🟡
**源：A2-2** · `app-data.js:223`
```javascript
// line 223: inventory: this._data.items → inventory: this._data.inventory
```

#### 2-19. ntBalance 从本地读（server 变更不感知） 🟡
**源：A2-3** · `app-data.js:88-91`
```javascript
if (srv && !srv.detail && !srv._offline) {
  var ntUser = NT.getUser(CURRENT_USER);
  if (ntUser) { ntUser.ntBalance = srv.balance; ntUser.contributionValue = srv.cv; ntUser.experienceValue = srv.xp; }
}
```

---

## 第3轮 · 任务生命周期 + UX（18 个）

> **验证：** 3 人任务全提交→全部可审核→全部可结算 + reviewer 为空时非 admin 验证被拒 + 搜索"筹备会"能命中对应任务

#### 3-1. doSubmit 多人任务阻断 🔴
**源：P2-1** · `data.js:322`
```javascript
if ((t.slots || 1) > 1) {
  var allSubmitted = (t.claimants || []).every(function(c) { return c.submission; });
  if (!allSubmitted) { AppData._saveShared(); filterQuests(); renderMyTasks(); return; }
}
t.status = '待审核';
```

#### 3-2. claimTask 不检查 slots 上限 🔴
**源：P2-2** · `data.js:333-338`
```javascript
if ((t.claimants || []).length >= (t.slots || 1)) { showToast('名额已满', 'error'); return; }
```

#### 3-3. slots 无下限 🔴
**源：P2-3** · `server/routes/tasks.py:21`
```python
slots: int = Field(1, ge=1, le=100)
```

#### 3-4. verify_task 空 reviewer 无权限 🔴
**源：P2-4** · `server/routes/nt.py:297-299`
```python
if user.role != "admin" and (not task.reviewer or task.reviewer != user.id):
    raise HTTPException(status_code=403, detail="只有指定的审核人可以验证此任务")
```

#### 3-5. reviewer 字段零校验 🔴
**源：P2-5** · `server/routes/tasks.py:76,98`
```python
if req.reviewer and req.reviewer.strip():
    rv = (await db.execute(select(User).where(User.id == req.reviewer.strip()))).scalar_one_or_none()
    if not rv: raise HTTPException(status_code=400, detail="审核人不存在")
```

#### 3-6. accept_task 覆盖已有 assignee 🔴
**源：P2-6** · `server/routes/nt.py:281-282`
```python
if task.assignee: raise HTTPException(status_code=409, detail="任务已被接取")
```

#### 3-7. 缺 unclaimTask 🔴
**源：P2-7** · `nt-core.js`
```javascript
function unclaimTask(taskId, assigneeId) {
  var t = _getTask(taskId); if (!t) return null;
  if (t.assignee !== assigneeId) return _err('你不是此任务的认领者');
  if (t.status !== 'active' && t.status !== 'pending') return _err('任务状态不允许放弃');
  TASK_ESCROW -= t.reward;
  var poster = _getUser(t.poster);
  if (poster) poster.ntBalance += t.reward;
  t.assignee = null; t.status = 'pending'; t.acceptedAt = null;
  _saveState(true); return t;
}
```

#### 3-8. 缺 resolveDispute 🔴
**源：P2-8** · `nt-core.js`
```javascript
function resolveDispute(taskId, resolution) {
  var t = _getTask(taskId); if (!t) return null;
  if (t.status !== 'disputed') return _err('任务不在争议状态');
  if (resolution === 'approve') {
    TASK_ESCROW -= t.reward;
    var a = _getUser(t.assignee); if (a) { a.ntBalance += t.reward; a.totalEarned += t.reward; }
    t.status = 'settled'; t.settledAt = new Date().toISOString();
    _addLedger('escrow', t.assignee, t.reward, 'dispute_resolve', '争议裁决:判给执行者', taskId);
  } else if (resolution === 'refund') {
    TASK_ESCROW -= t.reward;
    var p = _getUser(t.poster); if (p) p.ntBalance += t.reward;
    t.status = 'cancelled';
    _addLedger('escrow', t.poster, t.reward, 'dispute_resolve', '争议裁决:退还发布者', taskId);
  } else { return _err('无效裁决类型，需为 approve/refund'); }
  _saveState(true); return t;
}
```

#### 3-9. 缺 cancel/dispute/submit 端点 🔴
**源：P2-9** · `server/routes/nt.py`
```python
@router.post("/tasks/{task_id}/cancel")
async def cancel_task(task_id: str, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    if not user: raise HTTPException(status_code=401)
    result = await db.execute(select(NTTask).where(NTTask.id == task_id))
    task = result.scalar_one_or_none()
    if not task: raise HTTPException(status_code=404)
    if task.poster != user.id and user.role != "admin":
        raise HTTPException(status_code=403)
    if task.status in ("已结算", "待结算"):
        raise HTTPException(status_code=400, detail="不可取消已结算任务")
    if task.escrow_amount > 0:
        pool = await _get_pool(db)
        poster = (await db.execute(select(User).where(User.id == task.poster))).scalar_one_or_none()
        if poster: poster.nt_balance += task.escrow_amount
        else: pool.balance += task.escrow_amount
        pool.task_escrow -= task.escrow_amount
    task.status = "已取消"
    await db.commit()
    return {"ok": True}

@router.post("/tasks/{task_id}/submit")
async def submit_task(task_id: str, evidence: str = "", user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    if not user: raise HTTPException(status_code=401)
    result = await db.execute(select(NTTask).where(NTTask.id == task_id))
    task = result.scalar_one_or_none()
    if not task: raise HTTPException(status_code=404)
    if task.assignee != user.id: raise HTTPException(status_code=403, detail="只有认领者可以提交")
    if task.status not in ("进行中", "待提交", "退回修改"):
        raise HTTPException(status_code=400, detail=f"任务状态不可提交: {task.status}")
    task.evidence = evidence
    task.status = "待审核"
    task.completed_at = datetime.utcnow().isoformat()
    await db.commit()
    return {"ok": True}

@router.post("/tasks/{task_id}/dispute")
async def dispute_task(task_id: str, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)):
    if not user: raise HTTPException(status_code=401)
    result = await db.execute(select(NTTask).where(NTTask.id == task_id))
    task = result.scalar_one_or_none()
    if not task: raise HTTPException(status_code=404)
    if task.poster != user.id and task.assignee != user.id:
        raise HTTPException(status_code=403, detail="只有任务参与者可以发起争议")
    task.status = "已争议"
    await db.commit()
    return {"ok": True}
```

#### 3-10. "已完成" 死状态 🔴
**源：P2-10** · `core.js:259,979`
**修复：** 从过滤器中移除"已完成"，统一"待结算"→"已结算"

#### 3-11. completed_at 打回不清除 🟡
**源：P2-11** · `server/routes/tasks.py:115-118`
**修复：** 打回时 `task.completed_at = None`

### 🟡 UX 缺陷

#### 3-12. 注册失败仅报告第一个错误 🟡
**源：A3-1** · `core.js:733`
```javascript
var errors = [];
if (!n) errors.push('请输入名字');
if (p.length < 6) errors.push('密码至少6位');
if (errors.length) { showToast(errors.join('；'), 'error'); return; }
```

#### 3-13. silentRefresh 错误不分级 🟡
**源：A3-2** · index.html autoLogin 块
API.silentRefresh 区分网络断/token过期/服务器错误，autoLogin 据此显示不同提示。

#### 3-14. autoLogin 无 loading 反馈 🟡
**源：A3-3** · index.html autoLogin 块
显示"正在恢复会话…"，超时5秒降级登录页。

#### 3-15. deleteDraft 无确认 🟡
**源：A3-4** · `core.js:581`
```javascript
showConfirm('确定删除草稿「' + name + '」？', function() { AppData.deleteTask(name); ... });
```

#### 3-16. "发布委托"不可点击 🟡
**源：A3-5** · `core.js:271`
```javascript
h += '<span style="color:var(--green-primary);cursor:pointer;text-decoration:underline" onclick="openPublishTask()">+ 发布委托</span>';
```

#### 3-17. closeMyPage 不设 hash 🟡
**源：A3-6** · `core.js:1206`
```javascript
window.location.hash = '';
```

#### 3-18. _mergeSyncData 丢弃 items/newbie 🟡
**源：A2-4** · `core.js:819`
```javascript
if (data.items && Array.isArray(data.items)) {
  if (!AppData._data.items[AppData._currentUser]) AppData._data.items[AppData._currentUser] = [];
  var existing = AppData._data.items[AppData._currentUser];
  data.items.forEach(function(it) {
    if (!existing.find(function(e) { return e.id === it.id; })) existing.push(it);
  });
}
if (data.newbie && Array.isArray(data.newbie)) {
  AppData._data.newbieQuests = {};
  data.newbie.forEach(function(q) { AppData._data.newbieQuests[q.quest_id] = q; });
}
```

---

## 第4轮 · 客户端健壮性 + 刷新（17 个）

> **验证：** 快速双击发布按钮不重复冻结 NT + `NT.spend(g.name, 0.5, ...)` 被 amount>0 校验拒绝 + 卡片室猜错扣 1 NT（非 0.1）

#### 4-1. doPublish 双击双重冻结 🔴
**源：P3-1** · `core.js:543-562`
```javascript
var _publishing = false;
function doPublish() { if (_publishing) return; _publishing = true; /* ... 原有逻辑 ... */ _publishing = false; }
```

#### 4-2. publishDraft 无防重入 🔴
**源：P3-2** · `core.js:567-577` · 同 4-1 加锁

#### 4-3. syncTaskUpdate 用错 ID 🔴
**源：P3-3** · `api.js:101-105`
```javascript
var srvId = task._srvId || name;
xhr.open('PUT', '/api/tasks/' + encodeURIComponent(srvId), true);
```

#### 4-4. NT.spend 浮点数 0.1（2处） 🔴
**源：P3-4** · `ui-cardroom.js:1031,1081`
```javascript
NT.spend(g.name, 1, '猜错: ' + (d.actionLabel||d.description), 'personal');
```

#### 4-5. filterQuests 搜索用 ID 而非 title 🔴
**源：P3-5** · `core.js:244-255`
```javascript
if (keyword) items = items.filter(function(t) {
  return (t.name||'').toLowerCase().indexOf(keyword)!==-1
      || (t.title||'').toLowerCase().indexOf(keyword)!==-1
      || (t.note||'').toLowerCase().indexOf(keyword)!==-1;
});
```

#### 4-6. sync 丢弃 location_id 🔴
**源：P3-6** · `core.js:204-206`
```javascript
locationId: t.location_id || ''
```

#### 4-7. _avatarSeedPool 重复定义 🔴
**源：P3-7** · `core.js:1002` + `index.html:1221`
**修复：** 删除 index.html 中的重复定义

#### 4-8. _secFold 重复定义 🔴
**源：P3-8** · `core.js:197` + `index.html:1218`
**修复：** 删除 index.html 中的重复定义

#### 4-9. earnFromPool/spendToPool 不校验 amount>0 🔴
**源：P3-9** · `nt-core.js:392,418`
```javascript
if (amount <= 0) { console.error('[NT] amount must be > 0, got', amount); return null; }
```

#### 4-10. batchSettle 死代码 🔴
**源：P3-10** · `nt-core.js:274-347`
```javascript
function batchSettle() {
  console.error('[NT] batchSettle() is disabled. Use verifyTask() instead.');
  return { settled: 0, error: 'disabled' };
}
```

#### 4-11. deposit/withdraw 静默 null 仍导出 🔴
**源：P3-11** · `nt-core.js:102-124`
**修复：** 从 `window.NT` 导出对象中移除

#### 4-12. campRmb 未初始化 🔴
**源：P3-12** · `app-data.js:init`
```javascript
if (this._data.campRmb === undefined) this._data.campRmb = 0;
```

#### 4-13. tickDirtiness/poolRefill 未用 Clock 🔴
**源：P3-13** · `app-data.js:547,560`
```javascript
var today = (typeof Clock !== 'undefined' ? Clock.today() : new Date().toISOString().slice(0,10));
```

#### 4-14. myItems 死数据 🟡
**源：P3-14** · `app-data.js:38`
**修复：** 从 `_savePrivate` 移除 `myItems`

### 🟡 刷新缺口

#### 4-15. polling token 过期持续 401 🟡
**源：A4-1** · `core.js:198-214`
```javascript
API.fetchTasks(function(tasks) {
  if (tasks && tasks._offline) return;
  if (tasks && tasks.detail === 'unauthorized') { _stopPolling(); return; }
  // ... 正常处理
});
```

#### 4-16. 网络错误分型 + _offline 处理 🟡
**源：A4-2** · `api.js:23`
```javascript
catch(e) {
  if (e.name === 'TypeError') return {ok:false, error:'网络不通', _offline:true};
  if (e.name === 'AbortError') return {ok:false, error:'请求超时', _offline:true};
  return {ok:false, error:'网络异常', _offline:true};
}
// 调用方统一约定：_offline 时不弹 toast
```

#### 4-17. closeOverlay 无条件显示 villagePage 🟡
**源：A4-4** · `data.js:500`
```javascript
function closeOverlay(id, showVillage) {
  if (showVillage !== false) document.getElementById('villagePage').classList.remove('hidden');
}
```

---

## 第5轮 · 服务端完善 + 功能缺口（23 个）

> **验证：** `python -c "from main import app"` + task_id 含随机后缀无碰撞 + `/api/auth/users` 需登录才能访问 + sync_all 只返回自己任务

### B1. NT 系统完善（8 个，B1-4 已合并到 2-5）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 5-1 | nt.py:213 | camp_pool_balance 从 ledger 动态算 | CommunityPool 加 camp_balance 持久化列 |
| 5-2 | nt.py:141 | earn 并发 pool 超支 | `with_for_update()` 行锁 |
| 5-3 | nt.py:134 | earn 无 amount 上限 | 10000 NT 上限（来源：client bridge 已有，server 对齐） |
| 5-4 | nt-core.js:472 | _processedTxIds 无过期 | setInterval 清理 7 天前 txId |
| 5-5 | nt-core.js:71 | registerUser 不设 role | 加可选 role 参数 |
| 5-6 | nt-core.js:450 | transfer 权限依赖全局 CURRENT_USER | 加显式 caller 参数 |
| 5-7 | nt-core.js:53 | _saveState 静默失败 | 失败时 showToast |
| 5-8 | core.js:881 | 营队NT从TASKS算 | 从 NT Ledger 汇总（与 5-1 同批确保 client-server 一致） |

### B2. 服务端完整化（5 个，B2-1=P2-11→3-11 已修，B2-6=P4-2→5-23 已修）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 5-10 | tasks.py:53 | list_tasks 缺时间字段 | 返回 accepted_at/completed_at |
| 5-11 | data.py:108 | get_verifications 返回不全 | 补全所有模型字段 |
| 5-12 | data.py:39 | get_activity_log 无认证 | `Depends(get_current_user)` |
| 5-13 | data.py:105 | get_verifications 无范围限制 | 非 admin 仅返回自己的 |
| 5-14 | auth.py:63 | pool 创建非原子 | init_db 预创建 CommunityPool row |

### B3. 功能缺口（7 个）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 5-15 | core.js:489 | reviewer 默认自己 | 默认空，提示选择审核人 |
| 5-16 | core.js:376 | 审核打回可无限循环 | rejectCount，超3次自动关闭 |
| 5-17 | core.js:7 | isTaskOverdue 不触发变更 | 页面加载时处理逾期 |
| 5-18 | core.js:204 | type↔category 值域不兼容 | 统一枚举值 |
| 5-19 | core.js:204 | sync 不映射 assignee/evidence | 同步时加字段 |
| 5-20 | data.js:348 | unclaimTask orphan | 加发布者审批流程 |
| 5-21 | api.js:90 | syncTask 缺 req_photo/req_file | 补发字段 |

### 🔴 服务端健壮性（含 P4 剩余）

#### 5-22. task_id 碰撞风险 🟡
**源：P4-1** · `nt.py:252` + `tasks.py:38-39`
```python
import secrets
task_id = f"T{datetime.utcnow().strftime('%y%m%d%H%M%S')}-{secrets.token_hex(3)}"
```

#### 5-23. sync_all 返回全量任务 🟡
**源：P4-2** · `server/routes/data.py:324-354`
```python
tasks_r = await db.execute(
    select(NTTask).where(
        (NTTask.poster == user.id) | (NTTask.assignee == user.id)
    ).order_by(NTTask.created_at.desc())
)
```

#### 5-24. Camp 缺 season 字段 🟢
**源：P4-3** · `server/models.py:81-103`
```python
season = Column(String, nullable=True)
```

---

## 第6轮 · 改善项（14 个 · 有空修）

> **验证：** iOS Safari 点击无 300ms 延迟 + inbox 面板不被 notch 遮挡 + 登录页底部按钮不被 home indicator 遮挡

### C1. 轻量改进（7 个）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 6-1 | index.html:633 | inbox 分类重复计算 ×6 | 缓存一次结果 |
| 6-2 | index.html:27-63 | 登录页无 safe-area | `padding-bottom: env(safe-area-inset-bottom)` |
| 6-3 | index.html:686 | inbox top:50px 硬编码 | `calc(50px + env(safe-area-inset-top))` |
| 6-4 | core.js:673 | avatar picker 滚动偏离 | `position:fixed` + viewport 定位 |
| 6-5 | 全局 CSS | onclick 无 touch 优化 | `[onclick] { touch-action: manipulation }` |
| 6-6 | index.html:711 | 旧营队种子孤行 | 删除残留对象 |
| 6-7 | index.html:505 | TASKS 重复声明 | 删除 index.html 中的声明 |

### C2. 设计债（6 个）

| # | 文件 | 问题 | 说明 |
|---|------|------|------|
| 6-8 | app-data.js:148 | addItem 无 deleteItem | 有出库需求再补 |
| 6-9 | app-data.js:20 | pendingTransactions 原则违反 | 改注释或迁移 |
| 6-10 | ui-wizard.js:52 | 创营向导验证不完整 | 补 Step 2-4,6 |
| 6-11 | ui-wizard.js:557 | NT 总池漏 extraExpense | 纳入 NT 标记支出 |
| 6-12 | ui-archive.js:176 | 两套 inbox 消息源 | 统一 computeInboxMessages |
| 6-13 | nt.js:18 | ROLE_TITLES 重复 | 删除 nt.js 版，统一 core.js |

### 🟢 改善建议（1 个，🟢2 已合并到 5-24）

| # | 文件 | 建议 |
|---|------|------|
| 6-14 | camps.py:35 | Camp 创建用 Pydantic 模型替代裸 dict |

---

## 已知暂缓

| # | 发现 | 理由 |
|---|------|------|
| Q1 | nt.py:115 — transfer 并发绕过余额检查 | MVP 并发 <5 用户，行级锁不值得。并发量上来加 `with_for_update()` |
| Q2 | topUp 不补 pool.balance | 2-17 初始注资已覆盖；持续运营补充机制需单独设计 |

## 架构债

三套 NT 并行模型不在此轮修复——选择修症状（CV/ camp_pool / transfer）而非重构架构。

触发重构：`NT.verify()` HTTP 模式 diff>10 持续 3 天，或跨设备不一致反馈 ≥3 次。

---

## 完整性校验清单

- [ ] 第1轮 6 项
- [ ] 第2轮 19 项
- [ ] 第3轮 18 项
- [ ] 第4轮 17 项
- [ ] 第5轮 23 项（5-1~5-24，5-9 空号/已去重）
- [ ] 第6轮 14 项
- [ ] 暂缓 Q1+Q2
- [ ] 架构债标注

**来源覆盖：**
- [ ] 28bugs.md P0×6 P1×11 P2×11 P3×14 P4×3 = 45 全部映射
- [ ] 55🟡🟢.md A×19 B×23 C×12 🟢×2 = 56 全部映射
- [ ] 5 个重复已标注：P1-9↔A1-6, P1-5↔B1-4, P4-2↔B2-6, P4-3↔🟢2, P2-11↔B2-1

**97 个可执行修复 + 2 个暂缓 + 1 个架构债 = 100 项全覆盖。**
