# 身份系统与数据流审查报告

> 2026-07-19 · Agent 自动审查

---

## 1. upgradeRole 函数 — 已实现但有多处缺陷

**位置**: nantang-mobile.html 第1120行

| 问题 | 严重度 |
|------|:--:|
| 不同步 `data.members`（getIdentity()依赖） | 高 |
| 不同步 `AppData._data.users[name].role` | 中 |
| 不同步 `currentUser.role`（权限检查依赖） | 高 |
| 不刷新 UI（refreshUserUI 未调用） | 中 |
| `season_history` 记录语义与 `changeUserRole` 相反 | 中 |
| 不检查 builder 升级前提（需先有冒险者经历） | 低 |
| `inviteCode` 写入逻辑偏差 | 低 |

## 2. registerUser — 基本正确

- ✅ 首个用户=admin，后续=visitor
- ✅ 新用户无 bio/wallet/location（等价空白资料）
- ⚠️ 首个用户自动 admin 与方案"仅砚仁"矛盾
- ⚠️ 注册后未设置 `currentUser` 对象

## 3. 打包任务自动升级 — 已实现但范围过宽

- ✅ confirmSettle() 中有升级逻辑
- ⚠️ 触发条件是 `t.scope === '营队'`（所有营队任务），而非方案要求的"打包任务"
- ⚠️ 继承 upgradeRole 所有同步问题
- ⚠️ 缺少用户通知

## 4. 资料卡编辑 — 可编辑可持久化，同步不全

- ✅ bio/wallet/location 可编辑
- ✅ 写入 users[name] + AppData._data.users[name]
- ⚠️ 不同步 `data.members`（getIdentity().profile 返回旧值）
- ⚠️ `renderProfile('view')` 和 `getIdentity()` 读取不同数据源

## 5. showInvitePrompt — 参数匹配，绕过标准流程

- ✅ validateInviteCode/consumeInviteCode 参数正确
- ⚠️ 绕过 `changeUserRole`，导致 season_history/currentUser/AppData 全部未更新
- ⚠️ 两套并行的邀请码系统（nt_world_codes vs AppData.inviteCodes）

## 6. enterVillage 注册 — 基本正确

- ✅ 首个用户判定逻辑正确
- ⚠️ getUsers 未定义时的 fallback 风险
- ⚠️ 注册后未设置 currentUser 对象

## 7. 三套用户变量同步 — 不一致 🔴

| 变量 | 类型 | 用途 |
|------|------|------|
| `CURRENT_USER` | string | 主应用内联代码 |
| `currentUser` | object | auth.js 权限检查 |
| `AppData._currentUser` | string | AppData 内部 |

| 操作 | CURRENT_USER | currentUser | AppData._currentUser |
|------|:--:|:--:|:--:|
| 注册 | ✅ 设置 | ❌ 未设置 | ✅ 设置 |
| 登录 | ✅ | ✅ | ✅ |
| 邀请码升级 | — | ❌ 未更新 | ❌ 未更新 |
| changeUserRole | — | ✅ | ❌ |
| 登出 | ❌ 未清理 | ✅ | ❌ 未清理 |

**关键风险**: 注册后权限检查全部返回 false；升级后角色过期；登出后 CURRENT_USER 残留。
