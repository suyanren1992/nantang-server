# Phase 3 执行提示词 — 单窗口顺序执行

> 把以下内容复制到一个 Claude Code 窗口中，按顺序逐项执行。每项做完验证再继续下一项。
> 前置：票 0-1 已直接应用（`_saveShared`/`_savePrivate` 已支持 `immediate` 参数）。

---

# 第 1 轮：致命 + 安全（5 项）

---

## 1/20 · pendingTransactions 初始化

文件：`js/app-data.js`

`init()` 中，`inventory` 初始化行后加：
```javascript
if (!this._data.pendingTransactions) this._data.pendingTransactions = [];
```

验证：管理员审批充值 → F5 → `AppData._data.pendingTransactions` 为数组且数据保持。

---

## 2/20 · _saveKey 静默失败加警告

文件：`js/app-data.js` `_saveKey`

```javascript
// 改前：catch(e) {}
// 改后：
catch(e) {
  console.error('[AppData] 保存失败: '+key, e);
  if (typeof showToast === 'function') showToast('⚠️ 存储空间不足，数据可能丢失', 'error');
}
```

验证：`for(let i=0;i<10000;i++) localStorage.setItem('fill_'+i,'x'.repeat(1000));` 后操作应用 → toast。

---

## 3/20 · renderProfile XSS

文件：`nantang-mobile.html` `renderProfile()`

所有 `getUsers()[name].xxx` 字段拼入 innerHTML 前用 `esc()` 包裹。

验证：注册 `<script>alert(1)</script>` → 查看资料不弹窗。

---

## 4/20 · NT.transfer 权限

文件：`js/nt-core.js` `transfer()`

函数开头加：
```javascript
var caller = (typeof CURRENT_USER !== 'undefined') ? CURRENT_USER : '';
var callerRole = ((typeof getUsers === 'function' ? getUsers() : {})[caller] || {}).role || '';
if (caller !== fromId && callerRole !== 'admin') return _err('无权转账');
```

验证：`CURRENT_USER='userA'; NT.transfer('userB','userA',100,'hack')` → 错误。

---

## 5/20 · 地图物品同步保存顺序

文件：`../实景地图移动端代码/app.js` `_syncItemToAppData`

把 `NT.earn()` 移到 `AppData._save(true)` 之后。

验证：放入物品 → F5 → 物品和 NT 均保持。

---

# 第 2 轮：工作台 + 数据层（5 项 · 顺序）

---

## 6/20 · Game.refresh 加持久化

文件：`nantang-mobile.html` `Game.refresh()`

```javascript
refresh: function() {
  if (window.AppData) AppData._saveShared();
  if (typeof refreshUserUI === 'function') refreshUserUI();
},
```

验证：地图操作后调 `Game.refresh()` → localStorage 中 `nt_app_v2_shared` 更新。

---

## 7/20 · doPublish 顺序修复

文件：`nantang-mobile.html` `doPublish()` 和 `publishDraft()`

改为：`AppData.addTask()` 先校验 → 失败 return → 成功才 `NT.createTask()`。

验证：同名任务 → 冲突提示 → NT 未扣。

---

## 8/20 · 角色双向同步

文件：`js/auth.js` + `js/app-data.js`

① `auth.js` `changeUserRole()` 中 `saveUsers(users)` 后加：`if (window.AppData) AppData.setMe('role', newRole);`

② `app-data.js` `setMe()` 中 role 变更时：
```javascript
if (key === 'role') {
  var users = (typeof getUsers === 'function') ? getUsers() : {};
  if (users[this._currentUser]) { users[this._currentUser].role = val;
    if (typeof saveUsers === 'function') saveUsers(users); }
}
```

验证：改角色 → F5 → `getUsers()` 与 `AppData._data.users` 角色一致。

---

## 9/20 · 跨标签同步 + 退出清理

文件：`js/app-data.js` + `nantang-mobile.html`

① app-data.js 底部加 storage 监听（深度合并，非 Object.assign）：
```javascript
window.addEventListener('storage', function(e) {
  if (e.key === 'nt_app_v2_shared' && e.newValue) {
    try {
      var d = JSON.parse(e.newValue);
      if (d) {
        Object.keys(d).forEach(function(k) {
          if (typeof d[k] === 'object' && !Array.isArray(d[k]) && typeof AppData._data[k] === 'object') {
            Object.assign(AppData._data[k], d[k]);
          } else { AppData._data[k] = d[k]; }
        });
      }
    } catch(ex) {}
    if (typeof refreshUserUI === 'function') refreshUserUI();
  }
});
```

② `logout()` 中 `localStorage.removeItem('nt_session')` 后加：`safeStorage.removeItem('nt_remembered_user');`

验证：两标签页 → A 改数据 → B 自动看到，本地未保存字段不被覆盖。退出后 remembered_user 清除。

---

## 10/20 · migration 补全 + _save 误调用

文件：`js/app-data.js`

① 迁移 IIFE 共享保存对象加：`map_locations: old.map_locations || {}, member_locations: old.member_locations || {}`

② `_saveItems()` 的 `AppData._save()` → `AppData._savePrivate()`；`setCanteenMenu()`/`launchCamp()` 的 `AppData._save()` → `AppData._saveShared()`

验证：迁移数据完整；物品操作仅触发私有保存。

---

# 第 3 轮：地图模块（4 项 · 顺序）

全部操作 `../实景地图移动端代码/app.js`。

---

## 11/20 · DIRTINESS_RATES 改为函数

```javascript
// 删除 var DIRTINESS_RATES = _mlConfig()...;
// 改为：
function _dirtinessRates() {
  var cfg = _mlConfig();
  if (cfg.dirtiness_rates && Object.keys(cfg.dirtiness_rates).length) return cfg.dirtiness_rates;
  if (window.AppData && AppData._data.map_locations && AppData._data.map_locations.config && AppData._data.map_locations.config.dirtiness_rates) {
    return AppData._data.map_locations.config.dirtiness_rates;
  }
  return { bathroom:15, kitchen:10, hallway:8, studio:8, bedroom:5, laundry:5, storage:3, outdoor:2, field:0 };
}
// 所有 DIRTINESS_RATES[ → _dirtinessRates()[
```

验证：管理员改脏污率 → 地图脏污增速立即变化。

---

## 12/20 · 清洁定价统一

```javascript
function _cleaningPricing() {
  return (_mlConfig().cleaning_pricing) || MGMT_DATA.cleaning.pricing;
}
// _submitMyCleaning() 和 renderCleaningPanel() 两处改用 _cleaningPricing()
```

验证：管理员改清洁定价 → 打扫 NT 按新价格。

---

## 13/20 · mySelections + _todayStr + _editRoomItem + 删死代码

① `_toggleMyRoom()` 末尾加 `MGMT_DATA._save();`；`_saveMySelections()` 的 `Game.toast` 前加 `MGMT_DATA._save();`

② `_todayStr()` 改为：`var ct = Clock.today?Clock.today():null; return ct?ct.slice(5,10):new Date().toISOString().slice(5,10);`

③ `_editRoomItem` 解析：
```javascript
if (parts.length >= 3) { text = parts.slice(1,-1).join(' '); status = parts[parts.length-1]; }
else if (parts.length === 2) { text = parts[1]; status = 'clean'; }
else { text = '新物品'; status = 'clean'; }
```

④ 删除 `_roomItemsHtml()` 函数。

验证：选房间 F5 保持；日期 MM-DD；`+🍎 苹果` 正常；grep `_roomItemsHtml` 无匹配。

---

## 14/20 · 防御守卫

```javascript
// _growDirtiness(): cl.spaces = cl.spaces || {}; cl.log = cl.log || [];
// _doCleaning(): (cl.log = cl.log || []).push({...});
// buildRoomDetail(): var inv = AppData._data.inventory || {}; // 后续 inv[spaceId]
```

验证：清除 cleaning 子键或 inventory → 地图不崩溃。

---

# 第 4 轮：轻量收尾（6 项）

---

## 15/20 · addJournal 统一

文件：`nantang-mobile.html` + `js/app-data.js`

全局 `addJournal` 委托给 `AppData.addJournal(user, type, content, opts)`。删 HTML 重复实现。

验证：日记条目正常显示。

---

## 16/20 · cleaning 迁移类型检查

文件：`js/app-data.js` 迁移 IIFE

```javascript
if (old.cleaning && typeof old.cleaning.lastCheckDate === 'string') { /* 扁平跳过 */ }
else if (old.cleaning) { /* 按用户拆分 */ }
```

验证：扁平 cleaning 不被错误拆分。

---

## 17/20 · getUsers 用 safeStorage + inviteKeys 常量

文件：`js/auth.js`

① `getUsers()` 中 `localStorage.getItem` → `safeStorage.getItem`。

② 加 `var INVITES_KEY = 'nt_invites';`，引用处用常量。

验证：safeStorage 降级时 `getUsers` 返回 `{}`。

---

## 18/20 · 注册记住我 + 地图错误提示

① `nantang-mobile.html` 注册成功后：`safeStorage.setItem('nt_remembered_user', JSON.stringify({name:n}));`

② `../实景地图移动端代码/app.js` `_initMap()` catch：`if(window.Game&&Game.toast) Game.toast('地图加载失败，请刷新');`

验证：注册 → F5 → 自动登录；地图加载失败 → toast。

---

## 19/20 · Game.getData 默认值补全

文件：`nantang-mobile.html` `Game.getData()`

map_locations 回退对象补全：`|| { buildings:[], plots:[], accommodations:{}, people_on_site:[], state:{}, config:{} }`

验证：无 map_locations 时 getData() 返回完整默认结构。

---

## 20/20 · approveTx 返回值检查

文件：`js/nt-core.js`

调 `approveTx()` 处检查返回值，失败 toast。验证：`NT._loadState=function(){return null;}` 后审批 → toast。

---

**全部 20 项完成。Phase 3 结束。**
