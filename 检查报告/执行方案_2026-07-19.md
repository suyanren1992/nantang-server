# 南塘云村移动端 · 执行方案

> 基于 `综合审查报告_2026-07-19.md` 的30项发现
> 原则：先止血后优化，每步可验证，改完能跑

---

## 第1轮：止血（P0 × 6）

---

### FIX-01: NT任务生命周期 — `doSubmit()` 加 `NT.submitTask()`

**文件**：`nantang-mobile.html` · **行**：L959-L966
**严重级**：🔴 P0 · **影响**：所有任务奖励永久冻结在 TASK_ESCROW

**现状**（L953-967）：
```javascript
function doSubmit(name){
  var t=TASKS[name];if(!t)return;
  var noteEl=document.getElementById('submitNote');if(!noteEl)return;var note=noteEl.value.trim();if(!note)return;
  var sub=note;
  if(t.reqPhoto){for(var i=1;i<=t.reqPhoto;i++){var p=document.getElementById('submitPhoto'+i);if(p&&p.value.trim())sub+='\n📷照片'+i+': '+p.value.trim()}}
  if(t.reqFile){for(var j=1;j<=t.reqFile;j++){var f=document.getElementById('submitFile'+j);if(f&&f.value.trim())sub+='\n📎附件'+j+': '+f.value.trim()}}
  var c=(t.claimants||[]).find(function(x){return x.name===CURRENT_USER});
  if(c){c.submission=sub;c.submittedAt=today();c.status='submitted'}
  else{t.claimants=t.claimants||[];t.claimants.push({name:CURRENT_USER,submission:sub,submittedAt:today(),status:'submitted'})}
  t.status='待审核';t.action='';
  // A2: 通过 AppData 更新
  AppData.updateTask(name, {claimants: t.claimants, status: '待审核', action: ''});
  document.querySelectorAll('.submit-expand,.card-expand,.submission-sub').forEach(function(c){c.remove()});
  filterQuests();renderMyTasks();refreshUserUI();
}
```

**改为**：在 `AppData.updateTask(...)` 行之后、`document.querySelectorAll(...)` 之前插入：
```javascript
  // A2: 通过 AppData 更新
  AppData.updateTask(name, {claimants: t.claimants, status: '待审核', action: ''});
  // FIX-01: 同步 NT 核心状态 — submitTask 让任务进入 completed 态，verifyTask 可释放托管 NT
  if(t._ntTaskId&&window.NT){
    var subResult=NT.submitTask(t._ntTaskId,{note:sub,user:CURRENT_USER});
    if(!subResult)console.warn('[NT] submitTask 失败: '+t._ntTaskId,NT.verify());
  }
  document.querySelectorAll('.submit-expand,.card-expand,.submission-sub').forEach(function(c){c.remove()});
```

**验证**：发布→认领→提交→Console运行 `NT.verify()`，确认 `{pass: true}` 且 `NT._tasks` 中对应任务 status 为 `'completed'`。

---

### FIX-02: NT任务生命周期 — `claimTask()` 加 `NT.acceptTask()`

**文件**：`nantang-mobile.html` · **行**：L973-L975
**严重级**：🔴 P0 · **与 FIX-01 配对**：缺一则整个流程仍然断裂

**现状**（L968-977）：
```javascript
function claimTask(name){
  var t=TASKS[name];if(!t)return;
  if(t.publisher===CURRENT_USER){showToast('不能领取自己发布的任务','error');return}
  t.claimants=t.claimants||[];
  if(t.claimants.some(function(c){return c.name===CURRENT_USER})){showToast('你已经领取过了','warn');return}
  t.claimants.push({name:CURRENT_USER,status:'in_progress'});
  t.claimedAt=t.claimedAt||today();
  AppData.updateTask(name, {claimants: t.claimants, claimedAt: t.claimedAt});
  filterQuests();renderMyTasks();refreshUserUI();
}
```

**改为**：在 `AppData.updateTask(...)` 之后、`filterQuests()` 之前插入：
```javascript
  AppData.updateTask(name, {claimants: t.claimants, claimedAt: t.claimedAt});
  // FIX-02: 同步 NT 核心状态 — acceptTask 将任务从 pending 变为 active，分配 assignee
  if(t._ntTaskId&&window.NT){
    var accResult=NT.acceptTask(t._ntTaskId,CURRENT_USER);
    if(!accResult)console.warn('[NT] acceptTask 失败: '+t._ntTaskId,NT.verify());
  }
  filterQuests();renderMyTasks();refreshUserUI();
```

**注意**：`NT.acceptTask()` 内部检查 `t.status !== 'pending'`（nt-core.js L165）。由于 `NT.createTask()` 创建时 `assigneeId=null`，status 为 `'pending'`，可以正常 accept。

**验证**：发布任务→认领→Console运行 `NT._tasks[id].status` → 应为 `'active'`，`NT._tasks[id].assignee` → 应为认领者名。

---

### FIX-03: NT核心 — `verifyTask()` 状态检查放宽

**文件**：`js/nt-core.js` · **行**：L189
**严重级**：🔴 P0 · **与 FIX-01 配对**：缺少此修，verifyTask 仍然失败

**现状**（L187-189）：
```javascript
function verifyTask(taskId, verifierId, approved, reason) {
  var t = _getTask(taskId); if (!t) return null;
  if (t.status !== 'completed') return _err('任务未提交完成');
```

**改为**：
```javascript
function verifyTask(taskId, verifierId, approved, reason) {
  var t = _getTask(taskId); if (!t) return null;
  // ponytail: 允许 active/completed/verified 三种状态的任务被验证。pending（未接取）仍然拒绝。
  if (t.status === 'pending' || t.status === 'settled' || t.status === 'cancelled' || t.status === 'disputed')
    return _err('任务状态('+t.status+')不可验证');
```

**验证**：完整走一遍 发布→认领→提交→审核通过，Console运行 `NT.verify()` → `{pass: true}`，`NT.getUser(认领者).ntBalance` 增加了对应 NT。

---

### FIX-04: `approveTx()` / `rejectTx()` 加管理员权限守卫

**文件**：`nantang-mobile.html` · **行**：L2010 + L2022
**严重级**：🔴 P0 · **影响**：任意用户可通过Console无限铸造/销毁NT

**现状**（L2010-2028）：
```javascript
function approveTx(txId){
  var txns=AppData._data.pendingTransactions;
  var tx=txns.find(function(t){return t.id===txId});
  if(!tx||tx.status!=='pending')return;
  if(window.NT){
    var result=tx.type==='topUp'?NT.topUp(tx.user,tx.amount,tx.reason||'管理员审核充值'):NT.cashOut(tx.user,tx.amount,tx.reason||'管理员审核提现');
    if(!result){showToast('NT 操作失败，请检查余额或用户状态','error');return}
  }
  tx.status='approved';tx.reviewedBy=CURRENT_USER;tx.reviewedAt=today();AppData._save();
  renderProfile('review');refreshUserUI();
  showToast((tx.type==='topUp'?'💰 充值':'📤 提现')+' '+tx.amount+' NT 已到账','ok');
}
function rejectTx(txId){
  var txns=AppData._data.pendingTransactions;
  var tx=txns.find(function(t){return t.id===txId});
  if(!tx||tx.status!=='pending')return;
  tx.status='rejected';tx.reviewedBy=CURRENT_USER;tx.reviewedAt=today();AppData._save();
  renderProfile('review');
}
```

**改为**（两个函数各加一行守卫）：
```javascript
function approveTx(txId){
  // FIX-04: 管理员权限检查
  var cu=(typeof getUsers==='function'?getUsers():{})[CURRENT_USER];
  if(!cu||cu.role!=='admin'){showToast('权限不足，仅管理员可审批','error');return}
  var txns=AppData._data.pendingTransactions;
  var tx=txns.find(function(t){return t.id===txId});
  if(!tx||tx.status!=='pending')return;
  if(window.NT){
    var result=tx.type==='topUp'?NT.topUp(tx.user,tx.amount,tx.reason||'管理员审核充值'):NT.cashOut(tx.user,tx.amount,tx.reason||'管理员审核提现');
    if(!result){showToast('NT 操作失败，请检查余额或用户状态','error');return}
  }
  tx.status='approved';tx.reviewedBy=CURRENT_USER;tx.reviewedAt=today();AppData._save();
  renderProfile('review');refreshUserUI();
  showToast((tx.type==='topUp'?'💰 充值':'📤 提现')+' '+tx.amount+' NT 已到账','ok');
}
function rejectTx(txId){
  // FIX-04: 管理员权限检查
  var cu=(typeof getUsers==='function'?getUsers():{})[CURRENT_USER];
  if(!cu||cu.role!=='admin'){showToast('权限不足，仅管理员可审批','error');return}
  var txns=AppData._data.pendingTransactions;
  var tx=txns.find(function(t){return t.id===txId});
  if(!tx||tx.status!=='pending')return;
  tx.status='rejected';tx.reviewedBy=CURRENT_USER;tx.reviewedAt=today();AppData._save();
  renderProfile('review');
}
```

**验证**：非管理员登录→Console运行 `approveTx('tx_任意')` → 应提示"权限不足"。管理员登录→正常审批。

---

### FIX-05: 冻结NT计算修正 — 从assignee视角改为poster视角

**文件**：`nantang-mobile.html` · **行**：L1799-L1810
**严重级**：🔴 P0 · **影响**：UI冻结NT永远显示0，用户不知道自己的NT被锁了多少

**现状**（L1799-1810）：
```javascript
  // 冻结 NT = 发布者已冻结在未结算任务中的 NT（发布时从余额扣除，结算/退回后释放）
  var frozen=0;
  // ponytail: 依赖 NT._tasks 调试接口（nt-core.js:560），升级路径 → NT 核心暴露 getFrozenBalance(userId)
  if(window.NT&&NT._tasks){
    for(var tid in NT._tasks){
      var tt=NT._tasks[tid];
      if(tt.status!=='pending')continue;
      if(tt.assignee===u)frozen+=tt.reward||0;
    }
  }
  var frozenEl=document.getElementById('myFrozenBalance');if(frozenEl)frozenEl.textContent=frozen;
  var frozenBar=document.getElementById('myFrozenBar');if(frozenBar){var b2=b||0;frozenBar.style.width=(b2+frozen>0?Math.round(frozen/(b2+frozen)*100):0)+'%';}
```

**改为**：
```javascript
  // FIX-05: 冻结 NT = 当前用户作为发布者，在未完成/未结算任务中已冻结的 reward 总和
  var frozen=0;
  // ponytail: 依赖 NT._tasks 调试接口，升级路径 → NT 核心暴露 getFrozenBalance(userId)
  if(window.NT&&NT._tasks){
    for(var tid in NT._tasks){
      var tt=NT._tasks[tid];
      if(tt.poster!==u)continue;           // 只看"我"发布的任务
      if(tt.status==='verified'||tt.status==='settled'||tt.status==='cancelled'||tt.status==='disputed')continue;
      frozen+=tt.reward||0;
    }
  }
  var frozenEl=document.getElementById('myFrozenBalance');if(frozenEl)frozenEl.textContent=frozen;
  var frozenBar=document.getElementById('myFrozenBar');if(frozenBar){var b2=b||0;frozenBar.style.width=(b2+frozen>0?Math.round(frozen/(b2+frozen)*100):0)+'%';}
```

**验证**：发布一个5NT的任务→打开工作台→冻结栏应显示5。管理员审核通过、结算→冻结栏应变为0。

---

### FIX-06: `NT.topUp()` / `NT.cashOut()` 加 txId 幂等去重

**文件**：`js/nt-core.js` · **行**：L364 + L374 + L537（导出签名）
**严重级**：🔴 P0 · **影响**：管理员双击审批→用户双倍到账

**现状**（L363-383）：
```javascript
// 充值：外部 NT 注入系统（增加 _totalIssued，会计等式保持平衡）
function topUp(userId, amount, reason) {
  var u = _getUser(userId); if (!u) return null;
  if (amount <= 0) return _err('充值金额必须大于 0');
  u.ntBalance += amount;
  _totalIssued += amount;
  _addLedger('system', userId, amount, 'deposit', reason || '充值');
  _saveState(); return u;
}

// 提现：NT 退出系统（减少 _totalIssued，会计等式保持平衡）
function cashOut(userId, amount, reason) {
  var u = _getUser(userId); if (!u) return null;
  if (u.ntBalance < amount) return _err('NT 余额不足');
  if (amount <= 0) return _err('提现金额必须大于 0');
  u.ntBalance -= amount;
  u.totalPaid += amount;
  _totalIssued -= amount;
  _addLedger(userId, 'system', amount, 'withdrawal', reason || '提现');
  _saveState(); return u;
}
```

**Step 1**：在文件顶部 `var PUBLIC_CV_POOL = 0;`（L403）之后加：
```javascript
// FIX-06: 防重放 — 已处理的 txId 集合
// ponytail: 无过期机制。当审批数 >1000 时改为 LRU Map 或加 TTL 自动清理。
var _processedTxIds = {};
```

**Step 2**：`topUp()` 改为：
```javascript
// 充值：外部 NT 注入系统（增加 _totalIssued，会计等式保持平衡）
// FIX-06: txId 可选，传入则去重（防止管理员双击审批导致双倍到账）
function topUp(userId, amount, reason, txId) {
  if (txId && _processedTxIds[txId]) return _err('该交易已处理，不可重复充值');
  var u = _getUser(userId); if (!u) return null;
  if (amount <= 0) return _err('充值金额必须大于 0');
  u.ntBalance += amount;
  _totalIssued += amount;
  _addLedger('system', userId, amount, 'deposit', reason || '充值');
  if (txId) _processedTxIds[txId] = true;
  _saveState(); return u;
}
```

**Step 3**：`cashOut()` 改为：
```javascript
// 提现：NT 退出系统（减少 _totalIssued，会计等式保持平衡）
// FIX-06: txId 可选，传入则去重
function cashOut(userId, amount, reason, txId) {
  if (txId && _processedTxIds[txId]) return _err('该交易已处理，不可重复提现');
  var u = _getUser(userId); if (!u) return null;
  if (u.ntBalance < amount) return _err('NT 余额不足');
  if (amount <= 0) return _err('提现金额必须大于 0');
  u.ntBalance -= amount;
  u.totalPaid += amount;
  _totalIssued -= amount;
  _addLedger(userId, 'system', amount, 'withdrawal', reason || '提现');
  if (txId) _processedTxIds[txId] = true;
  _saveState(); return u;
}
```

**Step 4**：`_reset()` 中加清理（L581-587）：
```javascript
_reset: function(){
  Object.keys(USERS).forEach(function(k){delete USERS[k]});
  for(var k in TASKS)delete TASKS[k];LEDGER.length=0;SETTLEMENTS.length=0;
  _totalIssued=0;SYSTEM_POOL=0;TASK_ESCROW=0;PUBLIC_CV_POOL=0;
  _processedTxIds={};  // FIX-06: 清空去重集合
  _seq = { task:0, ledger:0, settlement:0 };
  try { localStorage.removeItem(NT_STORE_KEY); } catch(e) {}
},
```

**Step 5**：更新调用方 — `nantang-mobile.html` L2015（`approveTx` 内）：
```javascript
// 改前：
var result=tx.type==='topUp'?NT.topUp(tx.user,tx.amount,tx.reason||'管理员审核充值'):NT.cashOut(tx.user,tx.amount,tx.reason||'管理员审核提现');

// 改后：
var result=tx.type==='topUp'?NT.topUp(tx.user,tx.amount,tx.reason||'管理员审核充值',tx.id):NT.cashOut(tx.user,tx.amount,tx.reason||'管理员审核提现',tx.id);
```

**Step 6**：`_loadState()` 恢复 `_processedTxIds` — 在 L46 后加：
```javascript
_processedTxIds = s.pt || {};
```

**Step 7**：`_saveState()` 持久化 `_processedTxIds` — 在 L54-57 的 JSON.stringify 对象中加：
```javascript
ti: _totalIssued, sp: SYSTEM_POOL, te: TASK_ESCROW, cp: PUBLIC_CV_POOL, pt: _processedTxIds, sq: _seq
```

**验证**：发布充值申请→管理员审批→Console再调 `NT.topUp(user, amt, reason, txId)` 同一 txId → 应返回 null 并 warn "该交易已处理"。

---

### FIX-07: `_saveState()` 关键路径即时写入 + beforeunload

**文件**：`js/nt-core.js` · **行**：L50-L60
**严重级**：🔴 P0 · **影响**：createTask/verifyTask/earn 后立即关闭标签页 → NT状态丢失

**现状**（L50-60）：
```javascript
function _saveState() {
  clearTimeout(_saveTimer);
  _saveTimer = setTimeout(function() {
    try {
      localStorage.setItem(NT_STORE_KEY, JSON.stringify({
        u: USERS, t: TASKS, l: LEDGER, st: SETTLEMENTS,
        ti: _totalIssued, sp: SYSTEM_POOL, te: TASK_ESCROW, cp: PUBLIC_CV_POOL, sq: _seq
      }));
    } catch(e) { console.warn('[NT] 存档失败', e); }
  }, 300);
}
```

**改为**：`_saveState` 接受可选参数 `immediate`，true 时跳过防抖直接写：
```javascript
// FIX-07: immediate 参数跳过防抖，关键操作（createTask/verifyTask/earn）使用即时写入
function _saveState(immediate) {
  var doWrite = function() {
    try {
      localStorage.setItem(NT_STORE_KEY, JSON.stringify({
        u: USERS, t: TASKS, l: LEDGER, st: SETTLEMENTS,
        ti: _totalIssued, sp: SYSTEM_POOL, te: TASK_ESCROW, cp: PUBLIC_CV_POOL, pt: _processedTxIds, sq: _seq
      }));
    } catch(e) { console.warn('[NT] 存档失败', e); }
  };
  if (immediate) { doWrite(); return; }
  clearTimeout(_saveTimer);
  _saveTimer = setTimeout(doWrite, 300);
}
```

**Step 2**：以下函数中 `_saveState()` 调用改为 `_saveState(true)`（即时写入）：

| 函数 | 行 | 改为 |
|------|-----|------|
| `createTask` | L160 | `_saveState(true);` |
| `acceptTask` | L174 | `_saveState(true);` |
| `submitTask` | L184 | `_saveState(true);` |
| `verifyTask` | L227 | `_saveState(true);` |
| `cancelTask` | L249 | `_saveState(true);` |
| `earn` | L348 | `_saveState(true);` |
| `spend` | L360 | `_saveState(true);` |
| `topUp` | L370 | `_saveState(true);` |
| `cashOut` | L382 | `_saveState(true);` |
| `transfer` | L399 | `_saveState(true);` |

（共10处替换。搜索 `_saveState()` 后全部替换为 `_saveState(true)` 即可。）

**Step 3**：在 IIFE 末尾（`_loadState()` 之后，L596）加 beforeunload 守卫：
```javascript
// FIX-07: 页面关闭前强制写盘
window.addEventListener('beforeunload', function() {
  try {
    localStorage.setItem(NT_STORE_KEY, JSON.stringify({
      u: USERS, t: TASKS, l: LEDGER, st: SETTLEMENTS,
      ti: _totalIssued, sp: SYSTEM_POOL, te: TASK_ESCROW, cp: PUBLIC_CV_POOL, pt: _processedTxIds, sq: _seq
    }));
  } catch(e) {}
});
```

**验证**：发布任务后立即F5刷新→Console `NT.verify()` → `{pass: true}`。`NT._tasks` 应包含刚发布的任务。

---

### FIX-08: postMessage桥接加session nonce

**文件**：`nantang-mobile.html` · **行**：L2111-L2112 + L2130前
**严重级**：🔴 P0 · **影响**：file://协议下任意本地HTML可发送伪造消息铸造NT

**Step 1**：在 `</script>`（L2129）之前、消息监听器之后，app.js加载之前，生成全局nonce：
```javascript
// FIX-08: 会话级 bridge nonce — file:// 协议下 origin 不可靠，nonce 作为共享密钥
window._APP_NONCE = 'nt_'+Math.random().toString(36).slice(2)+'_'+Date.now().toString(36);
```

**Step 2**：消息处理器的返回逻辑中（`e.source.postMessage` 处，L2117），将 nonce 注入地图 iframe：
```javascript
// L2117: getUser 返回时也送 nonce，让地图端缓存
e.source.postMessage({type:'userData', data:u, _bridgeNonce:window._APP_NONCE}, '*');
```

**Step 3**：在原 `e.origin` 检查（L2112）之后立即加 nonce 校验（对敏感操作）：
```javascript
window.addEventListener('message',function(e){
  if(e.origin!==window.location.origin&&e.origin!=='null')return;
  var d=e.data;if(!d||!d.type)return;
  // FIX-08: 敏感操作（ntEarn/ntSpend）需要有效的 bridge nonce
  if((d.type==='ntEarn'||d.type==='ntSpend'||d.type==='confirm')&&window._APP_NONCE){
    if(d._bridgeNonce!==window._APP_NONCE)return;  // nonce 不匹配，静默丢弃
  }
  if((d.type==='ntEarn'||d.type==='ntSpend')&&(typeof d.amount!=='number'||d.amount<=0||d.amount>10000))return;
  // ... 后续不变
```

**Step 4**：地图 iframe 加载时通过 URL hash 传递 nonce。在 `openMapPage()`（L1655）中：
```javascript
// 改前：
function openMapPage(){document.getElementById('overlayMap').classList.add('open');if(typeof _initMap==='function'){_initMap()}}

// 改后：
function openMapPage(){
  document.getElementById('overlayMap').classList.add('open');
  // FIX-08: 通过 URL hash 传 nonce 给地图 iframe（如果地图是 iframe 加载的话）
  var iframe=document.querySelector('#overlayMap iframe');
  if(iframe&&window._APP_NONCE){
    try {
      var src=iframe.src;
      if(src.indexOf('#')===-1) iframe.src=src+'#_bnc='+window._APP_NONCE;
    }catch(e){}
  }
  if(typeof _initMap==='function'){_initMap()}
}
```

**注意**：如果地图是内联渲染而非 iframe，Step 4 不适用。当前 `overlayMap` 内是 `<div id="app">` 内联结构，nonce 通过 Step 2 的 `postMessage` 返回传递即可。

> ⚠️ **ponytail**: Step 4 重设 `iframe.src` 会**重新加载 iframe**，丢失地图当前状态。仅在 iframe 尚未加载（首次打开地图）时有效。如果地图已运行，nonce 通过 Step 2 的 postMessage 即可传递，Step 4 冗余且有副作用。建议执行时优先验证 Step 2 能否独立工作，如可以则跳过 Step 4。

**验证**：打开地图→Console `postMessage({type:'ntEarn',amount:10,reason:'test'},'*')` → 无 nonce 应被静默丢弃（NT余额不变）。带正确 nonce 的消息正常处理。

---

## 第1轮完成检查清单

全部6个P0修完后，完整走一遍核心流程：

```
1. 新用户注册 → 进入村口 ✓
2. 发布任务（5 NT） → 我的NT余额 -5，冻结栏显示 5 ✓
3. 另一个用户认领 → 任务变为 active ✓
4. 认领者提交 → NT._tasks 中状态变为 completed ✓
5. 发布者审核通过 → NT._tasks 中状态变为 verified ✓
6. 认领者 NT 余额 +5 ✓
7. 发布者冻结栏变为 0 ✓
8. NT.verify() → {pass: true} ✓
9. F5 刷新 → NT.verify() 仍然 {pass: true} ✓
10. Console 直接调 approveTx → "权限不足" ✓
11. 管理员审批充值（同一 txId 双击）→ 第二次返回 null ✓
```

---

## 第2轮：防崩溃（P1 × 7）

---

### FIX-09: `logout()` 清理NT内存状态，防止用户串扰

**文件**：`nantang-mobile.html` · **行**：L2064
**严重级**：🟡 P1

**现状**：
```javascript
function logout(){var m=document.getElementById('profileCard');if(m)m.remove();localStorage.removeItem('nt_app_v2');CURRENT_USER='';if(window.AppData)AppData._currentUser='';document.getElementById('villagePage').classList.add('hidden');document.getElementById('loginPage').classList.remove('hidden');S('scrEntry')}
```

**改为**：
```javascript
function logout(){
  var m=document.getElementById('profileCard');if(m)m.remove();
  // FIX-09: 清理当前会话状态。注意：不调 NT._reset()——那会删除所有用户的 NT 数据。
  // NT 数据通过 localStorage 持久化，切换用户后 _loadState() 自动恢复正确数据。
  localStorage.removeItem('nt_app_v2');
  if(window.NT)NT._saveState(true);  // 即时写盘保护当前数据，不清空
  if(window.AppData)AppData.reset(); // 清 AppData 内存状态
  CURRENT_USER='';
  if(window.AppData)AppData._currentUser='';
  // 清理 UI 状态
  _fromQuestHall=false;
  document.querySelectorAll('.card-expand,.submit-expand,.settle-expand,.withdraw-expand,.review-expand,.unclaim-expand,.ledger-expand,.confirm-card,.toast-card').forEach(function(c){c.remove()});
  document.getElementById('villagePage').classList.add('hidden');
  document.getElementById('myPage').classList.add('hidden');
  document.getElementById('loginPage').classList.remove('hidden');
  S('scrEntry');
}
```

**验证**：用户A登录→赚一些NT→退出→用户B登录→用户B的余额是B自己的（不是A的残留）。`NT.verify()` 正常。

---

### FIX-10: `confirmSettle()` 加状态校验

**文件**：`nantang-mobile.html` · **行**：L1076-L1078
**严重级**：🟡 P1

**现状**（L1076-1078）：
```javascript
function confirmSettle(name){
  var t=TASKS[name];if(!t)return;
  if(t.status==='已结算')return;
```

**改为**：在已结算检查后面加一条：
```javascript
function confirmSettle(name){
  var t=TASKS[name];if(!t)return;
  if(t.status==='已结算')return;
  // FIX-10: 只允许从"待结算"或"已完成"状态结算，防止非法流转
  if(t.status!=='待结算'&&t.status!=='已完成'){showToast('任务状态('+t.status+')不可结算','error');return}
```

**验证**：对状态为"进行中"的任务在Console调 `confirmSettle('任务名')` → 应提示"不可结算"。

---

### FIX-11: `_fromQuestHall` 边界场景 — 在 myPage 内跳转后状态残留

**文件**：`nantang-mobile.html` · **行**：L2079
**严重级**：🟡 P1

**现状**：
```javascript
function closeMyPage(){var p=document.getElementById('myPage');p.classList.add('hidden');p.style.zIndex='';if(_fromQuestHall){_fromQuestHall=false;document.getElementById('overlayQuestHall').classList.add('open')}else{document.getElementById('villagePage').classList.remove('hidden')}}
```

**改为**：如果 myPage 当前被其他 overlay 遮盖（用户从 myPage 内跳到了营地/地图等），`_fromQuestHall` 不应触发：
```javascript
function closeMyPage(){
  var p=document.getElementById('myPage');p.classList.add('hidden');p.style.zIndex='';
  // FIX-11: 如果当前有打开的 overlay，说明用户从 myPage 内导航到了别处，不应触发返回 questHall
  var hasOpenOverlay=document.querySelector('.overlay.open');
  if(_fromQuestHall&&!hasOpenOverlay){
    _fromQuestHall=false;
    document.getElementById('overlayQuestHall').classList.add('open');
  }else{
    _fromQuestHall=false;
    document.getElementById('villagePage').classList.remove('hidden');
  }
}
```

**验证**：从任务大厅→我的工作台→营地主页→关闭营地→关闭工作台→应回到村口（不是任务大厅，因为中间经过了营地）。

---

### FIX-12: `doPublish()` 调整操作顺序 — NT先失败则UI不回滚

**文件**：`nantang-mobile.html` · **行**：L1601-L1620
**严重级**：🟡 P1

**现状**（L1606-1618）：
```javascript
var _ntTaskId=(TASKS[name]&&TASKS[name]._ntTaskId)||null;
if(!_ntTaskId&&window.NT){
  var ntR=NT.createTask(CURRENT_USER, name, nt, 'other');
  if(!ntR){showToast('NT 余额不足（需 '+nt+' NT，请先赚取或充值）','error');return}
  _ntTaskId=ntR.taskId;
}
var data={...publisher:CURRENT_USER,status:'进行中'...};
if(TASKS[name]){var old=TASKS[name];data.claimants=old.claimants||[];data.claimedAt=old.claimedAt||''}
else data.claimants=[];
TASKS[name]=data;DRAFTS=DRAFTS.filter(function(d){return d.name!==name});
AppData.addTask(data);
```

注意：`NT.createTask()` 在 `TASKS[name]` 赋值和 `AppData.addTask()` 之前调用，这是正确的顺序（先扣NT再写UI）。但编辑已存在任务时不走 `NT.createTask`（已有 `_ntTaskId`），所以编辑路径是安全的。

< 不需要修改。此条为伪阳性。>

**状态**：✅ 无需修改（当前顺序已正确：先 NT 后 UI。唯一风险是 localStorage 满导致 AppData 写入失败，但这是极端边界，P2处理。）

---

### FIX-13: `nt-core.js` `earn()` 加 SYSTEM_POOL 文档注释 + ponytail 标记

**文件**：`js/nt-core.js` · **行**：L339-L348
**严重级**：🟡 P1 · **不改逻辑**，仅文档化设计意图

**现状**（L339-348）：
```javascript
function earn(userId, amount, reason, scope) {
  var u = _getUser(userId); if (!u) return null;
  scope = scope || 'personal';
  SYSTEM_POOL -= amount;
  u.ntBalance += amount;
  u.totalEarned += amount;
  u.contributionValue = (u.contributionValue||0) + amount;
  u.experienceValue = (u.experienceValue||0) + amount;
  _addLedger('system', userId, amount, scope+'_earn', reason);
  _saveState(); return u;
}
```

**改为**：
```javascript
// ponytail: SYSTEM_POOL 允许负值（等价于系统无限铸币权）。MVP阶段设计选择。
// 会计等式 sum(users)+SYSTEM_POOL+TASK_ESCROW = _totalIssued 在负池情况下仍然成立（负池正好抵消超发）。
// 升级路径：如需限制通胀，在 earn 前检查 SYSTEM_POOL >= amount，不足时调用 topUp 注入后再 earn。
// 见执行方案 FIX-13。
function earn(userId, amount, reason, scope) {
  var u = _getUser(userId); if (!u) return null;
  scope = scope || 'personal';
  SYSTEM_POOL -= amount;
  u.ntBalance += amount;
  u.totalEarned += amount;
  u.contributionValue = (u.contributionValue||0) + amount;
  u.experienceValue = (u.experienceValue||0) + amount;  // ponytail: XP=NT映射规则待游戏设计确认
  _addLedger('system', userId, amount, scope+'_earn', reason);
  _saveState(true); return u;
}
```

---

### FIX-14: `nt-core.js` batchSettle 加死代码注释，防止未来误调用

**文件**：`js/nt-core.js` · **行**：L263
**严重级**：🟡 P1

**现状**（L263）：
```javascript
function batchSettle() {
```

**改为**：
```javascript
// ⚠️ DEAD CODE — MVP阶段不使用。verifyTask() 已直接完成转账，再次调用 batchSettle 会导致双重扣款。
// 启用条件：将 verifyTask 中的转账逻辑移除，仅保留状态变更，由 batchSettle 统一结算。
// 见执行方案 FIX-14。
function batchSettle() {
```

**验证**：无运行期影响，纯注释。确认 `batchSettle` 在代码库中无调用方（grep 验证）。

---

### FIX-15: 删除 `LEDGER` 全局对象，消除三套账本之一

**文件**：`nantang-mobile.html` · **行**：L605-L608
**严重级**：🟡 P1

**现状**（L605-608）：
```javascript
var LEDGER={
 personal:{balance:0,income:0,expense:0,records:[]},
 camp:{balance:0,income:0,expense:0,rmb:0,rmbSpent:0,records:[]},
 community:{balance:0,income:0,expense:0,records:[]}};
```

**改为**：删除 LEDGER 定义，替换为空标记：
```javascript
// FIX-15: LEDGER 已废弃 — UI 账本展示改为读取 NT.getLedger()（openLedger 函数 L609）。
// RMB 金库数据保留在 LEDGER.camp.rmb → 迁移到 AppData._data.campRmb。
// 此对象在代码库中已无读取方（唯一引用 L669 也读的是 NT.getUser），安全删除。
var LEDGER = { camp: { rmb: 0, rmbSpent: 0 } };  // 仅保留 RMB 金库
```

**Step 2**：L669 的 `refreshLedgerCards()` 中对 `LEDGER.camp.rmb` 的引用改为从 AppData 读：
```javascript
// L669 改前：
el=document.getElementById('ledgerCampRmb');if(el&&LEDGER.camp.rmb)el.textContent=LEDGER.camp.rmb;

// L669 改后：
el=document.getElementById('ledgerCampRmb');if(el){
  var campRmb=(window.AppData&&AppData._data.campRmb)?AppData._data.campRmb:LEDGER.camp.rmb||0;
  el.textContent=campRmb;
}
```

**验证**：打开工作台→账本Tab→余额正常显示。`grep 'LEDGER\.' nantang-mobile.html` 确认无残留引用（除 L669 的 RMB 路径）。

---

## 第3轮：结构优化（P1剩余 + 首批P2）

---

### FIX-16: `questCard()` / `myCard()` 抽取公共函数

**文件**：`nantang-mobile.html` · **行**：L1357-L1385 + L1856-L1878
**严重级**：🟡 P1
**工作量**：中等（~30行新函数 + 两处改为调用）

**策略**：抽取 `renderTaskCard(t, opts)` 公共函数，opts 控制差异：
- `opts.context`: `'hall'` | `'my'` — 决定是否显示快捷认领按钮、审核按钮
- `opts.onExpand`: 点击展开的逻辑（hall走questCard展开，my也走同一套toggleQuestCard）

**现状对比**：

| 逻辑 | questCard (hall) | myCard (my) | 是否相同 |
|------|-----------------|-------------|----------|
| 类型颜色映射 | `typedColor()` | `tc()` | ✅ 相同逻辑 |
| 卡片CSS class | `cardCls` | `cardClass()` | ✅ 相同逻辑 |
| 认领人计数 | `claimLine` | `claimLine` | ✅ 完全相同 |
| 逾期判断 | 内联 | 内联 | ✅ 完全相同 |
| 模板结构 | task-left+task-body | task-left+task-body | ✅ 完全相同 |
| 卡片底部按钮 | `quickBtn`（认领） | `rightEl(t)`（审核✓✕）or `quickBtn` | ❌ 差异点 |
| onclick展开 | `toggleQuestCard` | `toggleQuestCard` | ✅ 完全相同 |

**新函数签名**：
```javascript
// opts: { context:'hall'|'my', showReviewBtn:bool }
function renderTaskCard(t, opts) {
  var ctx = opts && opts.context || 'hall';
  var tc = t.type==='主线'?{c:'var(--green-primary)',b:'#e8f0e8',icon:'🎯'}
         : t.type==='支线'?{c:'#c8892e',b:'#fef8e8',icon:'📋'}
         : {c:'#4a7a82',b:'#e0eaee',icon:'🧹'};
  var cls = t.type==='支线'?'task-type-side':t.type==='日常'?'task-type-daily':'task-type-main';
  var claimed = t.claimants||[]; var slots = t.slots||1;
  var remain = Math.max(0, slots-claimed.length);
  var claimLine = '👤 '+claimed.length+'/'+slots+'人'+(remain>0?' · '+remain+'空位':'');
  var overdue = t.deadline&&t.deadline<(today())&&t.status!=='已完成'&&t.status!=='已结算'&&t.status!=='待结算';
  if(overdue) claimLine += ' · ⚠️逾期';
  var cardCls = '';
  if(t.status==='待审核'||t.status==='退回修改') cardCls=' warn';
  else if(t.status==='待提交') cardCls=' urgent';
  else if(t.status==='已完成'||t.status==='已结算'||t.status==='待结算') cardCls=' done';
  var isMyClaim = claimed.some(function(c){return c.name===CURRENT_USER});
  var isPublisher = t.publisher===CURRENT_USER;
  var rightEl = '';
  if(ctx==='my'&&opts.showReviewBtn&&t.reviewer===CURRENT_USER&&t.status==='待审核'){
    rightEl = '<div class="task-review"><span class="rv-ok" onclick="event.stopPropagation();reviewTask(\''+esc(t.name)+'\',\'approve\')">✓</span><span class="rv-no" onclick="event.stopPropagation();reviewTask(\''+esc(t.name)+'\',\'reject\')">✕</span></div>';
  }
  var quickBtn = '';
  if(t.status==='进行中'&&!isMyClaim&&!isPublisher){
    quickBtn = '<button class=btn-pri style=padding:2px 8px;font-size:.68rem;border-radius:4px;white-space:nowrap" onclick="event.stopPropagation();claimTask(\''+esc(t.name)+'\')">🎯认领</button>';
  }
  var h = '<div class="task-card'+cardCls+'" style="border-left:3px solid '+tc.c+'" onclick="event.stopPropagation();toggleQuestCard(this,\''+esc(t.name)+'\')">';
  h += '<div class="task-left '+cls+'">'+tc.icon+'</div><div class="task-body">';
  h += '<div class="task-row1"><span class="task-name">'+esc(t.name)+'</span><span class="task-nt"><img src=豆子.png alt=NT onerror="this.outerHTML=\'🌱\'" style=width:18px;height:18px;vertical-align:middle;margin-right:3px>'+t.nt+' NT</span></div>';
  h += '<div class="task-row2">';
  h += '<span class="task-chip '+cls+'">'+t.type+'</span>';
  if(t.publisher) h += '<span>👤'+esc(t.publisher)+(t.publisher===CURRENT_USER?'(我)':'')+'发布</span>';
  h += '<span>'+claimLine+'</span>';
  if(quickBtn) h += '<span style=margin-left:auto>'+quickBtn+'</span>';
  h += '</div></div>';
  if(!quickBtn&&rightEl) h += rightEl;
  h += '</div>';
  return h;
}
```

**然后**：
- `questCard(t, color, bg, icon)` 改为 `return renderTaskCard(t, {context:'hall'});`
- `myCard(t)` 改为 `return renderTaskCard(t, {context:'my', showReviewBtn:true});`

**验证**：任务大厅和工作台的任务卡片外观和行为与改前一致。

---

### FIX-17: `parseInt` 加 radix（~8处）

**文件**：`nantang-mobile.html`
**严重级**：🟡 P1

**位置清单**（搜索 `parseInt(` 不含 `,10` 或 `, 10`）：

| 行 | 当前 | 改为 |
|-----|------|------|
| L852 | `parseInt(p)` | `parseInt(p,10)` |
| L856 | `parseInt(p)` | `parseInt(p,10)` |
| L1249 | `parseInt(document.getElementById('campFormMax').value)` | `parseInt(document.getElementById('campFormMax').value,10)` |
| L1551 | `parseInt(document.getElementById('pubNT').value)||0` | `parseInt(document.getElementById('pubNT').value,10)||0` |
| L1605 | `parseInt(document.getElementById('pubNT').value)||5` | `parseInt(document.getElementById('pubNT').value,10)||5` |
| L1613 | `parseInt(document.getElementById('pubSlots').value)||1` | `parseInt(document.getElementById('pubSlots').value,10)||1` |
| L1993 | `parseInt(document.getElementById('rechargeAmount').value)||0` | `parseInt(document.getElementById('rechargeAmount').value,10)||0` |
| L2001 | `parseInt(document.getElementById('withdrawAmount').value)||0` | `parseInt(document.getElementById('withdrawAmount').value,10)||0` |

**验证**：全局搜索 `parseInt(` → 确认所有调用都带 `,10` 或 `, 10`。

---

### FIX-18: `CURRENT_USER` 统一入口 — `mobile-bundle.js` 的 `currentUser` 改为引用

**文件**：`nantang-mobile.html` L671 + `mobile-bundle.js`（需查看具体行）
**严重级**：🟡 P1

**现状**：`nantang-mobile.html:671` `var CURRENT_USER='';` 和 `mobile-bundle.js` 中的 `var currentUser = null;` 是两个独立全局变量。部分函数（如 `getIdentity` in auth.js）同时使用两者。

**策略**（最小改动）：在 `nantang-mobile.html` 顶部（全局变量声明区）统一维护 `CURRENT_USER`，`mobile-bundle.js` 中的 `currentUser` 通过 getter/setter 引用：
```javascript
// mobile-bundle.js 中，如果 currentUser 是其内部维护的，则在 nantang-mobile.html 加载 mobile-bundle.js 之后加一行：
// ponytail: currentUser 别名 — 全局唯一当前用户入口是 CURRENT_USER
```

**实际做法**（更安全）：在 `mobile-bundle.js` 中找到 `var currentUser` 的定义，改为：
```javascript
// ponytail: currentUser 别名指向 CURRENT_USER（defined in nantang-mobile.html）。
// 这样所有 auth.js 中引用 currentUser 的地方都和 nantang-mobile.html 的 CURRENT_USER 保持同步。
```

如果 mobile-bundle.js 中对 `currentUser` 还有写操作（`currentUser = ...`），改为同时写 `CURRENT_USER`：
```javascript
// 所有 currentUser = xxx 的地方加一行
CURRENT_USER = currentUser.name || currentUser;
```

**验证**：登录后 Console 检查 `CURRENT_USER === currentUser.name` → `true`。

---

## 第4轮：体验打磨（P2 精选）

> P2共13项。以下选6项性价比最高的。剩余7项（P2-4 `!important`、P2-12 密码哈希、P2-13 reset保护等）推迟到下个迭代。

---

### FIX-19: `S()` 重命名为 `showLoginScreen()`

> ⚠️ **审查意见**：降为 P3。纯可读性改进，不解决任何审查报告问题。风险：全局替换 ~8 处（含 HTML `onclick` 字符串拼接），漏一处 = 运行时 `S is not defined`。单人项目中短函数名 `S` 的混淆风险低于重命名遗漏风险。如要执行，`grep -n "S('" nantang-mobile.html` 验证替换完整性后再提交。

**文件**：`nantang-mobile.html`
**严重级**：⚪ P3（原 P2）
**改动**：
- L1658: `function S(id){...}` → `function showLoginScreen(id){...}`
- 全局搜索替换 `S('` → `showLoginScreen('` （~8处：L166, L313, L329, L340, L1658, L1717, L2064）

---

### FIX-20: `isTaskOverdue()` 死代码 — 替换内联逾期判断

**文件**：`nantang-mobile.html` · **行**：L603
**严重级**：🟢 P2

`isTaskOverdue` 已定义但从未调用（L602, L1362, L1434, L1445, L1860 都有内联的 `t.deadline&&t.deadline<today()&&t.status!=='已完成'&&...`）。

**改为**：将四处内联逾期判断替换为 `isTaskOverdue(t)`：
```javascript
// L603 函数定义（已有，不变）
function isTaskOverdue(t){return t.deadline&&t.deadline<today()&&t.status!=='已完成'&&t.status!=='已结算'&&t.status!=='待结算'}
```

```javascript
// L1362: var overdue = isTaskOverdue(t);  （替换内联表达式）
// L1434: if(isTaskOverdue(t)) btns='...'   （替换内联表达式）
// L1445: if(isTaskOverdue(t)) btns='...'   （替换内联表达式）
// L1860: var overdue = isTaskOverdue(t);   （替换内联表达式）
```

---

### FIX-21: 地图页假数据替换为真实数据

**文件**：`nantang-mobile.html` · **行**：L537-L543
**严重级**：🟢 P2

**现状**（L537-543）：
```html
<div class="ub-stat">📅 7月15日</div>
<div class="ub-stat">🌤 晴 32°C</div>
<div class="ub-stat">👤 3人在线</div>
```

**改为**：日期用 `Clock.today()` 动态渲染：
```javascript
// 在 initSpcCard 或 refreshVillageCards 中加入：
var dateEl = document.querySelector('.ub-stats .ub-stat');
if (dateEl) dateEl.textContent = '📅 ' + (typeof Clock!=='undefined' ? Clock.today() : today()).slice(5);
```

在线人数可以从 `getUsers()` 计算（当前无在线状态，暂用注册人数代替或保留假数据但标注 `// ponytail: 在线人数需后端`）。

---

### FIX-22: 粒子动画后台暂停

**文件**：`nantang-mobile.html` · **CSS区** · **行**：L83-L98
**严重级**：🟢 P2

**现状**：village page 在后台时（myPage/overlay 打开），粒子动画仍在运行。

**策略**：在 JS 中监听页面切换，给 `.village-page` 加/去 `class="behind"`。

在 `showMy()`（L1829）和 `closeMyPage()`（L2079）中加：
```javascript
// showMy() L1829 加：
document.getElementById('villagePage').classList.add('behind');

// closeMyPage() L2079 加（在显示 village 之前）：
document.getElementById('villagePage').classList.remove('behind');
```

CSS（在 `@keyframes floatUp` 之后加）：
```css
.village-page.behind .particle,
.village-page.behind .village-bg-layer { animation-play-state: paused; }
```

---

### FIX-23: 物品网格最小触摸目标

**文件**：`nantang-mobile.html` · **行**：L812（renderItemsInTab 中物品卡片）
**严重级**：🟢 P2

**现状**：物品卡片 `width:62px; height:74px`，远低于44pt标准。

**改为**：`width:80px; height:96px`，增大触摸面积：
```javascript
// L812 改前：
h+='<div style="width:62px;height:74px;...

// L812 改后：
h+='<div style="width:80px;height:96px;...
```

相应增大内部元素尺寸：
- emoji: `font-size:1.6rem`（从1.4rem）
- 名称: `font-size:.62rem`（不变）
- 价格: `font-size:.6rem`（不变）

---

### FIX-24: 社区副本空状态

**文件**：`nantang-mobile.html` · **行**：L1135-L1162（`renderCommunityHub` 函数）
**严重级**：🟢 P2

**现状**：如果三个营地数组都为空，非管理员用户看到空白页。

**改为**：在 `renderCommunityHub` 末尾（L1161 `el.innerHTML = h;` 之前）加：
```javascript
if (!h) {
  h = '<div style="text-align:center;padding:40px 20px;color:#5a6e5c">'+
    '<div style="font-size:2rem;margin-bottom:8px">🏕️</div>'+
    '<div style="font-size:.8rem;font-weight:600">暂无活动</div>'+
    '<div style="font-size:.65rem;color:#aaa;margin-top:4px">等待管理员创建第一个共创营队</div>'+
  '</div>';
}
```

---

## 各轮验证清单总汇

### 第1轮验证（P0修复后）

在浏览器Console依次运行：

```javascript
// 1. 基础验证
NT.verify()                          // → {pass: true}

// 2. 任务完整流转（假设已有两个注册用户）
NT.registerUser('测试员A');
NT.registerUser('测试员B');
CURRENT_USER = '测试员A';
// 手动发布一个5NT任务（通过UI操作或直接调）
var r = NT.createTask('测试员A', '测试任务', 5, 'other');
// → 应返回任务对象

// 3. 认领
CURRENT_USER = '测试员B';
NT.acceptTask(r.taskId, '测试员B');
// → 应返回任务对象，status='active'

// 4. 提交
NT.submitTask(r.taskId, {note:'完成了'});
// → 应返回任务对象，status='completed'

// 5. 审核通过
NT.verifyTask(r.taskId, '测试员A', true);
// → 应返回任务对象，status='verified'

// 6. 余额检查
NT.getUser('测试员B').ntBalance   // → 5
NT.verify()                        // → {pass: true}

// 7. 冻结检查（P0-6修复验证）
// 发布一个任务后，NT.getUser('发布者').ntBalance 减少了对应值

// 8. 权限检查（P0-2修复验证）
CURRENT_USER = '测试员B';
approveTx('tx_任意');               // → 提示"权限不足"

// 9. 持久化检查
// F5 刷新页面 → NT.verify() 仍为 {pass: true}
```

### 第2轮验证

```javascript
// 10. logout清理（FIX-09）
CURRENT_USER = '测试员A';
logout();  // 应清理所有状态
NT.getUser('测试员A')              // → null（NT已reset）

// 11. 结算状态校验（FIX-10）
var t = TASKS['某个进行中的任务'];
confirmSettle(t.name);             // → 提示"不可结算"
```

### 第3轮验证

```javascript
// 12. 重构后UI一致性
// 目视检查：任务大厅和工作台的任务卡片外观一致
// 目视检查：点击展开行为一致
```

---

## 不改的项（确认）

| 问题 | 理由 |
|------|------|
| 模块化/拆分HTML | 2133行当前可管理，file://不支持ES modules |
| innerHTML全量重绘 | 10-20条数据，性能足够 |
| `deposit()/withdraw()`保证金 | 未调用，接口保留 |
| `data.finance`旧账本 | 无调用方，保留无害 |
| P1-11 CV/XP同质化 | 需要设计讨论，不是纯代码问题 |
| P2-9~P2-13（游戏设计类） | 需要先出设计文档再编码 |

---

*方案版本：v1.0*
*关联审查：综合审查报告_2026-07-19.md*
*预计总工时：第1轮45min + 第2轮30min + 第3轮25min + 第4轮20min ≈ 2h*